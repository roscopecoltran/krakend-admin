[[backends]]
  group = "refuge-restrooms-v1"
  url_pattern = "/api/v1/restrooms"
  method = "JSON"
  encoding = "xml"
  host = ["https://www.refugerestrooms.org"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "refugerestrooms.org"
    description = "REFUGE is a web application that seeks to provide safe restroom access for transgender, intersex, and gender nonconforming individuals."
    version = "v1"
    slug = "refuge-restrooms-v1-api-v1-restrooms"
    schemes = ["https"]
    produces = ["xml", "json", "vnd.api+json", "plain"]
    hosts = ["https://www.refugerestrooms.org"]
    topics = ["open-data"]

[[backends]]
  group = "refuge-restrooms-v1"
  url_pattern = "/api/v1/restrooms/by_date"
  method = "JSON"
  encoding = "xml"
  host = ["https://www.refugerestrooms.org"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "refugerestrooms.org"
    description = "REFUGE is a web application that seeks to provide safe restroom access for transgender, intersex, and gender nonconforming individuals."
    version = "v1"
    slug = "refuge-restrooms-v1-api-v1-restrooms-by-date"
    schemes = ["https"]
    produces = ["xml", "json", "vnd.api+json", "plain"]
    hosts = ["https://www.refugerestrooms.org"]
    topics = ["open-data"]

[[backends]]
  group = "refuge-restrooms-v1"
  url_pattern = "/api/v1/restrooms/by_location"
  method = "JSON"
  encoding = "xml"
  host = ["https://www.refugerestrooms.org"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "refugerestrooms.org"
    description = "REFUGE is a web application that seeks to provide safe restroom access for transgender, intersex, and gender nonconforming individuals."
    version = "v1"
    slug = "refuge-restrooms-v1-api-v1-restrooms-by-location"
    schemes = ["https"]
    produces = ["xml", "json", "vnd.api+json", "plain"]
    hosts = ["https://www.refugerestrooms.org"]
    topics = ["open-data"]

[[backends]]
  group = "refuge-restrooms-v1"
  url_pattern = "/api/v1/restrooms/search"
  method = "JSON"
  encoding = "xml"
  host = ["https://www.refugerestrooms.org"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "refugerestrooms.org"
    description = "REFUGE is a web application that seeks to provide safe restroom access for transgender, intersex, and gender nonconforming individuals."
    version = "v1"
    slug = "refuge-restrooms-v1-api-v1-restrooms-search"
    schemes = ["https"]
    produces = ["xml", "json", "vnd.api+json", "plain"]
    hosts = ["https://www.refugerestrooms.org"]
    topics = ["open-data"]
