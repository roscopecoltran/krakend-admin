[[backends]]
  group = "scrape-website-email-api-0-1"
  slug = "scrape-website-email-api-0-1-v1-ping"
  url_pattern = "/v1/ping"
  method = "JSON"
  encoding = "json"
  host = ["http://scrapewebsite.email"]
  concurrent_calls = 1
  [backends.extra_config]
    produces = ["json"]
    hosts = ["http://scrapewebsite.email"]
    tags = ["email", "tools"]
    schemes = ["http"]

[[backends]]
  group = "scrape-website-email-api-0-1"
  slug = "scrape-website-email-api-0-1-v1-scrape-emails"
  url_pattern = "/v1/scrape_emails"
  method = "JSON"
  encoding = "json"
  host = ["http://scrapewebsite.email"]
  concurrent_calls = 1
  [backends.extra_config]
    produces = ["json"]
    hosts = ["http://scrapewebsite.email"]
    tags = ["email", "tools"]
    schemes = ["http"]

[[backends]]
  group = "scrape-website-email-api-0-1"
  slug = "scrape-website-email-api-0-1-v1-scrape-store-links"
  url_pattern = "/v1/scrape_store_links"
  method = "JSON"
  encoding = "json"
  host = ["http://scrapewebsite.email"]
  concurrent_calls = 1
  [backends.extra_config]
    produces = ["json"]
    hosts = ["http://scrapewebsite.email"]
    tags = ["email", "tools"]
    schemes = ["http"]
