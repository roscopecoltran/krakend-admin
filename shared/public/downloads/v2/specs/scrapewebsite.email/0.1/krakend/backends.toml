[[backends]]
  group = "scrape-website-email-api"
  slug = "scrape-website-email-api-0-1"
  url_pattern = "/v1/ping"
  method = "JSON"
  encoding = "json"
  host = ["http://scrapewebsite.email"]
  [backends.extra_config]
    produces = ["json"]
    hosts = ["http://scrapewebsite.email"]
    schemes = ["http"]

[[backends]]
  group = "scrape-website-email-api"
  slug = "scrape-website-email-api-0-1"
  url_pattern = "/v1/scrape_emails"
  method = "JSON"
  encoding = "json"
  host = ["http://scrapewebsite.email"]
  [backends.extra_config]
    produces = ["json"]
    hosts = ["http://scrapewebsite.email"]
    schemes = ["http"]

[[backends]]
  group = "scrape-website-email-api"
  slug = "scrape-website-email-api-0-1"
  url_pattern = "/v1/scrape_store_links"
  method = "JSON"
  encoding = "json"
  host = ["http://scrapewebsite.email"]
  [backends.extra_config]
    produces = ["json"]
    hosts = ["http://scrapewebsite.email"]
    schemes = ["http"]
