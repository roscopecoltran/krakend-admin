[[backends]]
  group = "scrape-website-email-api-v1-ping"
  slug = "scrape-website-email-api-0-1-v1-ping"
  url_pattern = "/v1/ping"
  method = "JSON"
  encoding = "json"
  host = ["http://scrapewebsite.email"]
  concurrent_calls = 0
  [backends.extra_config]
    produces = ["json"]
    hosts = ["http://scrapewebsite.email"]
    schemes = ["http"]

[[backends]]
  group = "scrape-website-email-api-v1-scrape-emails"
  slug = "scrape-website-email-api-0-1-v1-scrape-emails"
  url_pattern = "/v1/scrape_emails"
  method = "JSON"
  encoding = "json"
  host = ["http://scrapewebsite.email"]
  concurrent_calls = 0
  [backends.extra_config]
    produces = ["json"]
    hosts = ["http://scrapewebsite.email"]
    schemes = ["http"]

[[backends]]
  group = "scrape-website-email-api-v1-scrape-store-links"
  slug = "scrape-website-email-api-0-1-v1-scrape-store-links"
  url_pattern = "/v1/scrape_store_links"
  method = "JSON"
  encoding = "json"
  host = ["http://scrapewebsite.email"]
  concurrent_calls = 0
  [backends.extra_config]
    produces = ["json"]
    hosts = ["http://scrapewebsite.email"]
    schemes = ["http"]
