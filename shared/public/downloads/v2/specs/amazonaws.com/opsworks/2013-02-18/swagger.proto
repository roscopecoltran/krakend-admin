syntax = "proto3";

import "google/protobuf/empty.proto";

import "google/api/annotations.proto";

package awsopsworks;

message PostRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    AssignInstanceRequest body = 10;
}

message Post#AssignVolumeRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    AssignVolumeRequest body = 10;
}

message Post#AssociateElasticIpRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    AssociateElasticIpRequest body = 10;
}

message Post#AttachElasticLoadBalancerRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    AttachElasticLoadBalancerRequest body = 10;
}

message Post#CloneStackRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    CloneStackRequest body = 10;
}

message Post#CreateAppRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    CreateAppRequest body = 10;
}

message Post#CreateDeploymentRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    CreateDeploymentRequest body = 10;
}

message Post#CreateInstanceRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    CreateInstanceRequest body = 10;
}

message Post#CreateLayerRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    CreateLayerRequest body = 10;
}

message Post#CreateStackRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    CreateStackRequest body = 10;
}

message Post#CreateUserProfileRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    CreateUserProfileRequest body = 10;
}

message Post#DeleteAppRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DeleteAppRequest body = 10;
}

message Post#DeleteInstanceRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DeleteInstanceRequest body = 10;
}

message Post#DeleteLayerRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DeleteLayerRequest body = 10;
}

message Post#DeleteStackRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DeleteStackRequest body = 10;
}

message Post#DeleteUserProfileRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DeleteUserProfileRequest body = 10;
}

message Post#DeregisterEcsClusterRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DeregisterEcsClusterRequest body = 10;
}

message Post#DeregisterElasticIpRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DeregisterElasticIpRequest body = 10;
}

message Post#DeregisterInstanceRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DeregisterInstanceRequest body = 10;
}

message Post#DeregisterRdsDbInstanceRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DeregisterRdsDbInstanceRequest body = 10;
}

message Post#DeregisterVolumeRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DeregisterVolumeRequest body = 10;
}

message Post#DescribeAgentVersionsRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DescribeAgentVersionsRequest body = 10;
}

message Post#DescribeAppsRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DescribeAppsRequest body = 10;
}

message Post#DescribeCommandsRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DescribeCommandsRequest body = 10;
}

message Post#DescribeDeploymentsRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DescribeDeploymentsRequest body = 10;
}

message Post#DescribeEcsClustersRequest {
    string Action = 1;
    // Pagination limit
    string MaxResults = 2;
    // Pagination token
    string NextToken = 3;
    string Version = 4;
    string X_Amz_Algorithm = 5;
    string X_Amz_Content_Sha256 = 6;
    string X_Amz_Credential = 7;
    string X_Amz_Date = 8;
    string X_Amz_Security_Token = 9;
    string X_Amz_Signature = 10;
    string X_Amz_SignedHeaders = 11;
    DescribeEcsClustersRequest body = 12;
}

message Post#DescribeElasticIpsRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DescribeElasticIpsRequest body = 10;
}

message Post#DescribeElasticLoadBalancersRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DescribeElasticLoadBalancersRequest body = 10;
}

message Post#DescribeInstancesRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DescribeInstancesRequest body = 10;
}

message Post#DescribeLayersRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DescribeLayersRequest body = 10;
}

message Post#DescribeLoadBasedAutoScalingRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DescribeLoadBasedAutoScalingRequest body = 10;
}

message Post#DescribeMyUserProfileRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
}

message Post#DescribePermissionsRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DescribePermissionsRequest body = 10;
}

message Post#DescribeRaidArraysRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DescribeRaidArraysRequest body = 10;
}

message Post#DescribeRdsDbInstancesRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DescribeRdsDbInstancesRequest body = 10;
}

message Post#DescribeServiceErrorsRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DescribeServiceErrorsRequest body = 10;
}

message Post#DescribeStackProvisioningParametersRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DescribeStackProvisioningParametersRequest body = 10;
}

message Post#DescribeStackSummaryRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DescribeStackSummaryRequest body = 10;
}

message Post#DescribeStacksRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DescribeStacksRequest body = 10;
}

message Post#DescribeTimeBasedAutoScalingRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DescribeTimeBasedAutoScalingRequest body = 10;
}

message Post#DescribeUserProfilesRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DescribeUserProfilesRequest body = 10;
}

message Post#DescribeVolumesRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DescribeVolumesRequest body = 10;
}

message Post#DetachElasticLoadBalancerRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DetachElasticLoadBalancerRequest body = 10;
}

message Post#DisassociateElasticIpRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DisassociateElasticIpRequest body = 10;
}

message Post#GetHostnameSuggestionRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    GetHostnameSuggestionRequest body = 10;
}

message Post#GrantAccessRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    GrantAccessRequest body = 10;
}

message Post#ListTagsRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    ListTagsRequest body = 10;
}

message Post#RebootInstanceRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    RebootInstanceRequest body = 10;
}

message Post#RegisterEcsClusterRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    RegisterEcsClusterRequest body = 10;
}

message Post#RegisterElasticIpRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    RegisterElasticIpRequest body = 10;
}

message Post#RegisterInstanceRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    RegisterInstanceRequest body = 10;
}

message Post#RegisterRdsDbInstanceRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    RegisterRdsDbInstanceRequest body = 10;
}

message Post#RegisterVolumeRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    RegisterVolumeRequest body = 10;
}

message Post#SetLoadBasedAutoScalingRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    SetLoadBasedAutoScalingRequest body = 10;
}

message Post#SetPermissionRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    SetPermissionRequest body = 10;
}

message Post#SetTimeBasedAutoScalingRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    SetTimeBasedAutoScalingRequest body = 10;
}

message Post#StartInstanceRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    StartInstanceRequest body = 10;
}

message Post#StartStackRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    StartStackRequest body = 10;
}

message Post#StopInstanceRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    StopInstanceRequest body = 10;
}

message Post#StopStackRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    StopStackRequest body = 10;
}

message Post#TagResourceRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    TagResourceRequest body = 10;
}

message Post#UnassignInstanceRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    UnassignInstanceRequest body = 10;
}

message Post#UnassignVolumeRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    UnassignVolumeRequest body = 10;
}

message Post#UntagResourceRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    UntagResourceRequest body = 10;
}

message Post#UpdateAppRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    UpdateAppRequest body = 10;
}

message Post#UpdateElasticIpRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    UpdateElasticIpRequest body = 10;
}

message Post#UpdateInstanceRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    UpdateInstanceRequest body = 10;
}

message Post#UpdateLayerRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    UpdateLayerRequest body = 10;
}

message Post#UpdateMyUserProfileRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    UpdateMyUserProfileRequest body = 10;
}

message Post#UpdateRdsDbInstanceRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    UpdateRdsDbInstanceRequest body = 10;
}

message Post#UpdateStackRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    UpdateStackRequest body = 10;
}

message Post#UpdateUserProfileRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    UpdateUserProfileRequest body = 10;
}

message Post#UpdateVolumeRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    UpdateVolumeRequest body = 10;
}

message AgentVersion {
    // The configuration manager.
    StackConfigurationManager ConfigurationManager = 1;
    // The agent version.
    string Version = 2;
}

repeated AgentVersion AgentVersions = 1

message App {
    // The app ID.
    string AppId = 1;
    // A <code>Source</code> object that describes the app repository.
    Source AppSource = 2;
    // The stack attributes.
    array Attributes = 3;
    // When the app was created.
    string CreatedAt = 4;
    // The app's data sources.
    array DataSources = 5;
    // A description of the app.
    string Description = 6;
    // The app vhost settings with multiple domains separated by commas. For example: <code>'www.example.com, example.com'</code>
    array Domains = 7;
    // Whether to enable SSL for the app.
    boolean EnableSsl = 8;
    // <p>An array of <code>EnvironmentVariable</code> objects that specify environment variables to be associated with the app. After you deploy the app, these variables are defined on the associated app server instances. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workingapps-creating.html#workingapps-creating-environment"> Environment Variables</a>. </p> <note> <p>There is no specific limit on the number of environment variables. However, the size of the associated data structure - which includes the variable names, values, and protected flag values - cannot exceed 10 KB (10240 Bytes). This limit should accommodate most if not all use cases, but if you do exceed it, you will cause an exception (API) with an "Environment: is too large (maximum is 10KB)" message.</p> </note>
    array Environment = 9;
    // The app name.
    string Name = 10;
    // The app's short name.
    string Shortname = 11;
    // An <code>SslConfiguration</code> object with the SSL configuration.
    SslConfiguration SslConfiguration = 12;
    // The app stack ID.
    string StackId = 13;
    // The app type.
    AppType Type = 14;
}

message AppAttribute {
    AppAttributesKeys key = 1;
    string value = 2;
}
repeated AppAttribute AppAttributes = 1

enum AppAttributesKey {
    DOCUMENTROOT = 0;
    RAILSENV = 1;
    AUTOBUNDLEONDEPLOY = 2;
    AWSFLOWRUBYSETTINGS = 3;
}

enum AppType {
    AWSFLOWRUBY = 0;
    JAVA = 1;
    RAILS = 2;
    PHP = 3;
    NODEJS = 4;
    STATIC = 5;
    OTHER = 6;
}

repeated App Apps = 1

enum Architecture {
    X86_64 = 0;
    I386 = 1;
}

message AssignInstanceRequest {
    // The instance ID.
    string InstanceId = 1;
    // The layer ID, which must correspond to a custom layer. You cannot assign a registered instance to a built-in layer.
    array LayerIds = 2;
}

message AssignVolumeRequest {
    // The instance ID.
    string InstanceId = 1;
    // The volume ID.
    string VolumeId = 2;
}

message AssociateElasticIpRequest {
    // The Elastic IP address.
    string ElasticIp = 1;
    // The instance ID.
    string InstanceId = 2;
}

message AttachElasticLoadBalancerRequest {
    // The Elastic Load Balancing instance's name.
    string ElasticLoadBalancerName = 1;
    // The ID of the layer that the Elastic Load Balancing instance is to be attached to.
    string LayerId = 2;
}

message AutoScalingThresholds {
    // <p>Custom Cloudwatch auto scaling alarms, to be used as thresholds. This parameter takes a list of up to five alarm names, which are case sensitive and must be in the same region as the stack.</p> <note> <p>To use custom alarms, you must update your service role to allow <code>cloudwatch:DescribeAlarms</code>. You can either have AWS OpsWorks Stacks update the role for you when you first use this feature or you can edit the role manually. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-servicerole.html">Allowing AWS OpsWorks Stacks to Act on Your Behalf</a>.</p> </note>
    array Alarms = 1;
    // The CPU utilization threshold, as a percent of the available CPU. A value of -1 disables the threshold.
    number CpuThreshold = 2;
    // The amount of time (in minutes) after a scaling event occurs that AWS OpsWorks Stacks should ignore metrics and suppress additional scaling events. For example, AWS OpsWorks Stacks adds new instances following an upscaling event but the instances won't start reducing the load until they have been booted and configured. There is no point in raising additional scaling events during that operation, which typically takes several minutes. <code>IgnoreMetricsTime</code> allows you to direct AWS OpsWorks Stacks to suppress scaling events long enough to get the new instances online.
    integer IgnoreMetricsTime = 3;
    // The number of instances to add or remove when the load exceeds a threshold.
    integer InstanceCount = 4;
    // The load threshold. A value of -1 disables the threshold. For more information about how load is computed, see <a href="http://en.wikipedia.org/wiki/Load_%28computing%29">Load (computing)</a>.
    number LoadThreshold = 5;
    // The memory utilization threshold, as a percent of the available memory. A value of -1 disables the threshold.
    number MemoryThreshold = 6;
    // The amount of time, in minutes, that the load must exceed a threshold before more instances are added or removed.
    integer ThresholdsWaitTime = 7;
}

enum AutoScalingType {
    LOAD = 0;
    TIMER = 1;
}

message BlockDeviceMapping {
    // The device name that is exposed to the instance, such as <code>/dev/sdh</code>. For the root device, you can use the explicit device name or you can set this parameter to <code>ROOT_DEVICE</code> and AWS OpsWorks Stacks will provide the correct device name.
    string DeviceName = 1;
    // An <code>EBSBlockDevice</code> that defines how to configure an Amazon EBS volume when the instance is launched.
    EbsBlockDevice Ebs = 2;
    // Suppresses the specified device included in the AMI's block device mapping.
    string NoDevice = 3;
    // The virtual device name. For more information, see <a href="http://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_BlockDeviceMapping.html">BlockDeviceMapping</a>.
    string VirtualName = 4;
}

repeated BlockDeviceMapping BlockDeviceMappings = 1



message ChefConfiguration {
    // The Berkshelf version.
    string BerkshelfVersion = 1;
    // Whether to enable Berkshelf.
    boolean ManageBerkshelf = 2;
}

message CloneStackRequest {
    // <p>The default AWS OpsWorks Stacks agent version. You have the following options:</p> <ul> <li> <p>Auto-update - Set this parameter to <code>LATEST</code>. AWS OpsWorks Stacks automatically installs new agent versions on the stack's instances as soon as they are available.</p> </li> <li> <p>Fixed version - Set this parameter to your preferred agent version. To update the agent version, you must edit the stack configuration and specify a new version. AWS OpsWorks Stacks then automatically installs that version on the stack's instances.</p> </li> </ul> <p>The default setting is <code>LATEST</code>. To specify an agent version, you must use the complete version number, not the abbreviated number shown on the console. For a list of available agent version numbers, call <a>DescribeAgentVersions</a>. AgentVersion cannot be set to Chef 12.2.</p> <note> <p>You can also specify an agent version when you create or update an instance, which overrides the stack's default setting.</p> </note>
    string AgentVersion = 1;
    // A list of stack attributes and values as key/value pairs to be added to the cloned stack.
    array Attributes = 2;
    // A <code>ChefConfiguration</code> object that specifies whether to enable Berkshelf and the Berkshelf version on Chef 11.10 stacks. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workingstacks-creating.html">Create a New Stack</a>.
    ChefConfiguration ChefConfiguration = 3;
    // A list of source stack app IDs to be included in the cloned stack.
    array CloneAppIds = 4;
    // Whether to clone the source stack's permissions.
    boolean ClonePermissions = 5;
    // The configuration manager. When you clone a stack we recommend that you use the configuration manager to specify the Chef version: 12, 11.10, or 11.4 for Linux stacks, or 12.2 for Windows stacks. The default value for Linux stacks is currently 12.
    StackConfigurationManager ConfigurationManager = 6;
    Source CustomCookbooksSource = 7;
    // <p>A string that contains user-defined, custom JSON. It is used to override the corresponding default stack configuration JSON values. The string should be in the following format:</p> <p> <code>"{\"key1\": \"value1\", \"key2\": \"value2\",...}"</code> </p> <p>For more information on custom JSON, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workingstacks-json.html">Use Custom JSON to Modify the Stack Configuration Attributes</a> </p>
    string CustomJson = 8;
    // The cloned stack's default Availability Zone, which must be in the specified region. For more information, see <a href="http://docs.aws.amazon.com/general/latest/gr/rande.html">Regions and Endpoints</a>. If you also specify a value for <code>DefaultSubnetId</code>, the subnet must be in the same zone. For more information, see the <code>VpcId</code> parameter description.
    string DefaultAvailabilityZone = 9;
    // The Amazon Resource Name (ARN) of an IAM profile that is the default profile for all of the stack's EC2 instances. For more information about IAM ARNs, see <a href="http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_Identifiers.html">Using Identifiers</a>.
    string DefaultInstanceProfileArn = 10;
    // <p>The stack's operating system, which must be set to one of the following.</p> <ul> <li> <p>A supported Linux operating system: An Amazon Linux version, such as <code>Amazon Linux 2017.03</code>, <code>Amazon Linux 2016.09</code>, <code>Amazon Linux 2016.03</code>, <code>Amazon Linux 2015.09</code>, or <code>Amazon Linux 2015.03</code>.</p> </li> <li> <p>A supported Ubuntu operating system, such as <code>Ubuntu 16.04 LTS</code>, <code>Ubuntu 14.04 LTS</code>, or <code>Ubuntu 12.04 LTS</code>.</p> </li> <li> <p> <code>CentOS Linux 7</code> </p> </li> <li> <p> <code>Red Hat Enterprise Linux 7</code> </p> </li> <li> <p> <code>Microsoft Windows Server 2012 R2 Base</code>, <code>Microsoft Windows Server 2012 R2 with SQL Server Express</code>, <code>Microsoft Windows Server 2012 R2 with SQL Server Standard</code>, or <code>Microsoft Windows Server 2012 R2 with SQL Server Web</code>.</p> </li> <li> <p>A custom AMI: <code>Custom</code>. You specify the custom AMI you want to use when you create instances. For more information on how to use custom AMIs with OpsWorks, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinginstances-custom-ami.html">Using Custom AMIs</a>.</p> </li> </ul> <p>The default option is the parent stack's operating system. For more information on the supported operating systems, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinginstances-os.html">AWS OpsWorks Stacks Operating Systems</a>.</p> <note> <p>You can specify a different Linux operating system for the cloned stack, but you cannot change from Linux to Windows or Windows to Linux.</p> </note>
    string DefaultOs = 11;
    // The default root device type. This value is used by default for all instances in the cloned stack, but you can override it when you create an instance. For more information, see <a href="http://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ComponentsAMIs.html#storage-for-the-root-device">Storage for the Root Device</a>.
    RootDeviceType DefaultRootDeviceType = 12;
    // A default Amazon EC2 key pair name. The default value is none. If you specify a key pair name, AWS OpsWorks installs the public key on the instance and you can use the private key with an SSH client to log in to the instance. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinginstances-ssh.html"> Using SSH to Communicate with an Instance</a> and <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/security-ssh-access.html"> Managing SSH Access</a>. You can override this setting by specifying a different key pair, or no key pair, when you <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinginstances-add.html"> create an instance</a>.
    string DefaultSshKeyName = 13;
    // The stack's default VPC subnet ID. This parameter is required if you specify a value for the <code>VpcId</code> parameter. All instances are launched into this subnet unless you specify otherwise when you create the instance. If you also specify a value for <code>DefaultAvailabilityZone</code>, the subnet must be in that zone. For information on default values and when this parameter is required, see the <code>VpcId</code> parameter description.
    string DefaultSubnetId = 14;
    // <p>The stack's host name theme, with spaces are replaced by underscores. The theme is used to generate host names for the stack's instances. By default, <code>HostnameTheme</code> is set to <code>Layer_Dependent</code>, which creates host names by appending integers to the layer's short name. The other themes are:</p> <ul> <li> <p> <code>Baked_Goods</code> </p> </li> <li> <p> <code>Clouds</code> </p> </li> <li> <p> <code>Europe_Cities</code> </p> </li> <li> <p> <code>Fruits</code> </p> </li> <li> <p> <code>Greek_Deities</code> </p> </li> <li> <p> <code>Legendary_creatures_from_Japan</code> </p> </li> <li> <p> <code>Planets_and_Moons</code> </p> </li> <li> <p> <code>Roman_Deities</code> </p> </li> <li> <p> <code>Scottish_Islands</code> </p> </li> <li> <p> <code>US_Cities</code> </p> </li> <li> <p> <code>Wild_Cats</code> </p> </li> </ul> <p>To obtain a generated host name, call <code>GetHostNameSuggestion</code>, which returns a host name based on the current theme.</p>
    string HostnameTheme = 15;
    // The cloned stack name.
    string Name = 16;
    // The cloned stack AWS region, such as "ap-northeast-2". For more information about AWS regions, see <a href="http://docs.aws.amazon.com/general/latest/gr/rande.html">Regions and Endpoints</a>.
    string Region = 17;
    // <p>The stack AWS Identity and Access Management (IAM) role, which allows AWS OpsWorks Stacks to work with AWS resources on your behalf. You must set this parameter to the Amazon Resource Name (ARN) for an existing IAM role. If you create a stack by using the AWS OpsWorks Stacks console, it creates the role for you. You can obtain an existing stack's IAM ARN programmatically by calling <a>DescribePermissions</a>. For more information about IAM ARNs, see <a href="http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_Identifiers.html">Using Identifiers</a>.</p> <note> <p>You must set this parameter to a valid service role ARN or the action will fail; there is no default value. You can specify the source stack's service role ARN, if you prefer, but you must do so explicitly.</p> </note>
    string ServiceRoleArn = 18;
    // The source stack ID.
    string SourceStackId = 19;
    // Whether to use custom cookbooks.
    boolean UseCustomCookbooks = 20;
    // <p>Whether to associate the AWS OpsWorks Stacks built-in security groups with the stack's layers.</p> <p>AWS OpsWorks Stacks provides a standard set of built-in security groups, one for each layer, which are associated with layers by default. With <code>UseOpsworksSecurityGroups</code> you can instead provide your own custom security groups. <code>UseOpsworksSecurityGroups</code> has the following settings: </p> <ul> <li> <p>True - AWS OpsWorks Stacks automatically associates the appropriate built-in security group with each layer (default setting). You can associate additional security groups with a layer after you create it but you cannot delete the built-in security group.</p> </li> <li> <p>False - AWS OpsWorks Stacks does not associate built-in security groups with layers. You must create appropriate Amazon Elastic Compute Cloud (Amazon EC2) security groups and associate a security group with each layer that you create. However, you can still manually associate a built-in security group with a layer on creation; custom security groups are required only for those layers that need custom settings.</p> </li> </ul> <p>For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workingstacks-creating.html">Create a New Stack</a>.</p>
    boolean UseOpsworksSecurityGroups = 21;
    // <p>The ID of the VPC that the cloned stack is to be launched into. It must be in the specified region. All instances are launched into this VPC, and you cannot change the ID later.</p> <ul> <li> <p>If your account supports EC2 Classic, the default value is no VPC.</p> </li> <li> <p>If your account does not support EC2 Classic, the default value is the default VPC for the specified region.</p> </li> </ul> <p>If the VPC ID corresponds to a default VPC and you have specified either the <code>DefaultAvailabilityZone</code> or the <code>DefaultSubnetId</code> parameter only, AWS OpsWorks Stacks infers the value of the other parameter. If you specify neither parameter, AWS OpsWorks Stacks sets these parameters to the first valid Availability Zone for the specified region and the corresponding default VPC subnet ID, respectively. </p> <p>If you specify a nondefault VPC ID, note the following:</p> <ul> <li> <p>It must belong to a VPC in your account that is in the specified region.</p> </li> <li> <p>You must specify a value for <code>DefaultSubnetId</code>.</p> </li> </ul> <p>For more information on how to use AWS OpsWorks Stacks with a VPC, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workingstacks-vpc.html">Running a Stack in a VPC</a>. For more information on default VPC and EC2 Classic, see <a href="http://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-supported-platforms.html">Supported Platforms</a>. </p>
    string VpcId = 22;
}

message CloneStackResult {
    // The cloned stack ID.
    string StackId = 1;
}

message CloudWatchLogsConfiguration {
    // Whether CloudWatch Logs is enabled for a layer.
    boolean Enabled = 1;
    // A list of configuration options for CloudWatch Logs.
    array LogStreams = 2;
}

enum CloudWatchLogsEncoding {
    ASCII = 0;
    BIG5 = 1;
    BIG5HKSCS = 2;
    CP037 = 3;
    CP424 = 4;
    CP437 = 5;
    CP500 = 6;
    CP720 = 7;
    CP737 = 8;
    CP775 = 9;
    CP850 = 10;
    CP852 = 11;
    CP855 = 12;
    CP856 = 13;
    CP857 = 14;
    CP858 = 15;
    CP860 = 16;
    CP861 = 17;
    CP862 = 18;
    CP863 = 19;
    CP864 = 20;
    CP865 = 21;
    CP866 = 22;
    CP869 = 23;
    CP874 = 24;
    CP875 = 25;
    CP932 = 26;
    CP949 = 27;
    CP950 = 28;
    CP1006 = 29;
    CP1026 = 30;
    CP1140 = 31;
    CP1250 = 32;
    CP1251 = 33;
    CP1252 = 34;
    CP1253 = 35;
    CP1254 = 36;
    CP1255 = 37;
    CP1256 = 38;
    CP1257 = 39;
    CP1258 = 40;
    EUC_JP = 41;
    EUC_JIS_2004 = 42;
    EUC_JISX0213 = 43;
    EUC_KR = 44;
    GB2312 = 45;
    GBK = 46;
    GB18030 = 47;
    HZ = 48;
    ISO2022_JP = 49;
    ISO2022_JP_1 = 50;
    ISO2022_JP_2 = 51;
    ISO2022_JP_2004 = 52;
    ISO2022_JP_3 = 53;
    ISO2022_JP_EXT = 54;
    ISO2022_KR = 55;
    LATIN_1 = 56;
    ISO8859_2 = 57;
    ISO8859_3 = 58;
    ISO8859_4 = 59;
    ISO8859_5 = 60;
    ISO8859_6 = 61;
    ISO8859_7 = 62;
    ISO8859_8 = 63;
    ISO8859_9 = 64;
    ISO8859_10 = 65;
    ISO8859_13 = 66;
    ISO8859_14 = 67;
    ISO8859_15 = 68;
    ISO8859_16 = 69;
    JOHAB = 70;
    KOI8_R = 71;
    KOI8_U = 72;
    MAC_CYRILLIC = 73;
    MAC_GREEK = 74;
    MAC_ICELAND = 75;
    MAC_LATIN2 = 76;
    MAC_ROMAN = 77;
    MAC_TURKISH = 78;
    PTCP154 = 79;
    SHIFT_JIS = 80;
    SHIFT_JIS_2004 = 81;
    SHIFT_JISX0213 = 82;
    UTF_32 = 83;
    UTF_32_BE = 84;
    UTF_32_LE = 85;
    UTF_16 = 86;
    UTF_16_BE = 87;
    UTF_16_LE = 88;
    UTF_7 = 89;
    UTF_8 = 90;
    UTF_8_SIG = 91;
}

enum CloudWatchLogsInitialPosition {
    START_OF_FILE = 0;
    END_OF_FILE = 1;
}

message CloudWatchLogsLogStream {
    // Specifies the max number of log events in a batch, up to 10000. The default value is 1000.
    integer BatchCount = 1;
    // Specifies the maximum size of log events in a batch, in bytes, up to 1048576 bytes. The default value is 32768 bytes. This size is calculated as the sum of all event messages in UTF-8, plus 26 bytes for each log event.
    integer BatchSize = 2;
    // Specifies the time duration for the batching of log events. The minimum value is 5000ms and default value is 5000ms.
    integer BufferDuration = 3;
    // Specifies how the time stamp is extracted from logs. For more information, see the <a href="http://docs.aws.amazon.com/AmazonCloudWatch/latest/logs/AgentReference.html">CloudWatch Logs Agent Reference</a>.
    string DatetimeFormat = 4;
    // Specifies the encoding of the log file so that the file can be read correctly. The default is <code>utf_8</code>. Encodings supported by Python <code>codecs.decode()</code> can be used here.
    CloudWatchLogsEncoding Encoding = 5;
    // <p>Specifies log files that you want to push to CloudWatch Logs.</p> <p> <code>File</code> can point to a specific file or multiple files (by using wild card characters such as <code>/var/log/system.log*</code>). Only the latest file is pushed to CloudWatch Logs, based on file modification time. We recommend that you use wild card characters to specify a series of files of the same type, such as <code>access_log.2014-06-01-01</code>, <code>access_log.2014-06-01-02</code>, and so on by using a pattern like <code>access_log.*</code>. Don't use a wildcard to match multiple file types, such as <code>access_log_80</code> and <code>access_log_443</code>. To specify multiple, different file types, add another log stream entry to the configuration file, so that each log file type is stored in a different log group.</p> <p>Zipped files are not supported.</p>
    string File = 6;
    // Specifies the range of lines for identifying a file. The valid values are one number, or two dash-delimited numbers, such as '1', '2-5'. The default value is '1', meaning the first line is used to calculate the fingerprint. Fingerprint lines are not sent to CloudWatch Logs unless all specified lines are available.
    string FileFingerprintLines = 7;
    // Specifies where to start to read data (start_of_file or end_of_file). The default is start_of_file. This setting is only used if there is no state persisted for that log stream.
    CloudWatchLogsInitialPosition InitialPosition = 8;
    // Specifies the destination log group. A log group is created automatically if it doesn't already exist. Log group names can be between 1 and 512 characters long. Allowed characters include a-z, A-Z, 0-9, '_' (underscore), '-' (hyphen), '/' (forward slash), and '.' (period).
    string LogGroupName = 9;
    // Specifies the pattern for identifying the start of a log message.
    string MultiLineStartPattern = 10;
    // Specifies the time zone of log event time stamps.
    CloudWatchLogsTimeZone TimeZone = 11;
}

repeated CloudWatchLogsLogStream CloudWatchLogsLogStreams = 1

enum CloudWatchLogsTimeZone {
    LOCAL = 0;
    UTC = 1;
}

message Command {
    // Date and time when the command was acknowledged.
    string AcknowledgedAt = 1;
    // The command ID.
    string CommandId = 2;
    // Date when the command completed.
    string CompletedAt = 3;
    // Date and time when the command was run.
    string CreatedAt = 4;
    // The command deployment ID.
    string DeploymentId = 5;
    // The command exit code.
    integer ExitCode = 6;
    // The ID of the instance where the command was executed.
    string InstanceId = 7;
    // The URL of the command log.
    string LogUrl = 8;
    // <p>The command status:</p> <ul> <li> <p>failed</p> </li> <li> <p>successful</p> </li> <li> <p>skipped</p> </li> <li> <p>pending</p> </li> </ul>
    string Status = 9;
    // <p>The command type:</p> <ul> <li> <p> <code>configure</code> </p> </li> <li> <p> <code>deploy</code> </p> </li> <li> <p> <code>execute_recipes</code> </p> </li> <li> <p> <code>install_dependencies</code> </p> </li> <li> <p> <code>restart</code> </p> </li> <li> <p> <code>rollback</code> </p> </li> <li> <p> <code>setup</code> </p> </li> <li> <p> <code>start</code> </p> </li> <li> <p> <code>stop</code> </p> </li> <li> <p> <code>undeploy</code> </p> </li> <li> <p> <code>update_custom_cookbooks</code> </p> </li> <li> <p> <code>update_dependencies</code> </p> </li> </ul>
    string Type = 10;
}

repeated Command Commands = 1

message CreateAppRequest {
    // A <code>Source</code> object that specifies the app repository.
    Source AppSource = 1;
    // One or more user-defined key/value pairs to be added to the stack attributes.
    array Attributes = 2;
    // The app's data source.
    array DataSources = 3;
    // A description of the app.
    string Description = 4;
    // The app virtual host settings, with multiple domains separated by commas. For example: <code>'www.example.com, example.com'</code>
    array Domains = 5;
    // Whether to enable SSL for the app.
    boolean EnableSsl = 6;
    // <p>An array of <code>EnvironmentVariable</code> objects that specify environment variables to be associated with the app. After you deploy the app, these variables are defined on the associated app server instance. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workingapps-creating.html#workingapps-creating-environment"> Environment Variables</a>.</p> <p>There is no specific limit on the number of environment variables. However, the size of the associated data structure - which includes the variables' names, values, and protected flag values - cannot exceed 10 KB (10240 Bytes). This limit should accommodate most if not all use cases. Exceeding it will cause an exception with the message, "Environment: is too large (maximum is 10KB)."</p> <note> <p>This parameter is supported only by Chef 11.10 stacks. If you have specified one or more environment variables, you cannot modify the stack's Chef version.</p> </note>
    array Environment = 7;
    // The app name.
    string Name = 8;
    // The app's short name.
    string Shortname = 9;
    // An <code>SslConfiguration</code> object with the SSL configuration.
    SslConfiguration SslConfiguration = 10;
    // The stack ID.
    string StackId = 11;
    // The app type. Each supported type is associated with a particular layer. For example, PHP applications are associated with a PHP layer. AWS OpsWorks Stacks deploys an application to those instances that are members of the corresponding layer. If your app isn't one of the standard types, or you prefer to implement your own Deploy recipes, specify <code>other</code>.
    AppType Type = 12;
}

message CreateAppResult {
    // The app ID.
    string AppId = 1;
}

message CreateDeploymentRequest {
    // The app ID. This parameter is required for app deployments, but not for other deployment commands.
    string AppId = 1;
    // A <code>DeploymentCommand</code> object that specifies the deployment command and any associated arguments.
    DeploymentCommand Command = 2;
    // A user-defined comment.
    string Comment = 3;
    // <p>A string that contains user-defined, custom JSON. It is used to override the corresponding default stack configuration JSON values. The string should be in the following format:</p> <p> <code>"{\"key1\": \"value1\", \"key2\": \"value2\",...}"</code> </p> <p>For more information on custom JSON, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workingstacks-json.html">Use Custom JSON to Modify the Stack Configuration Attributes</a>.</p>
    string CustomJson = 4;
    // The instance IDs for the deployment targets.
    array InstanceIds = 5;
    // The layer IDs for the deployment targets.
    array LayerIds = 6;
    // The stack ID.
    string StackId = 7;
}

message CreateDeploymentResult {
    // The deployment ID, which can be used with other requests to identify the deployment.
    string DeploymentId = 1;
}

message CreateInstanceRequest {
    // <p>The default AWS OpsWorks Stacks agent version. You have the following options:</p> <ul> <li> <p> <code>INHERIT</code> - Use the stack's default agent version setting.</p> </li> <li> <p> <i>version_number</i> - Use the specified agent version. This value overrides the stack's default setting. To update the agent version, edit the instance configuration and specify a new version. AWS OpsWorks Stacks then automatically installs that version on the instance.</p> </li> </ul> <p>The default setting is <code>INHERIT</code>. To specify an agent version, you must use the complete version number, not the abbreviated number shown on the console. For a list of available agent version numbers, call <a>DescribeAgentVersions</a>. AgentVersion cannot be set to Chef 12.2.</p>
    string AgentVersion = 1;
    // <p>A custom AMI ID to be used to create the instance. The AMI should be based on one of the supported operating systems. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinginstances-custom-ami.html">Using Custom AMIs</a>.</p> <note> <p>If you specify a custom AMI, you must set <code>Os</code> to <code>Custom</code>.</p> </note>
    string AmiId = 2;
    // The instance architecture. The default option is <code>x86_64</code>. Instance types do not necessarily support both architectures. For a list of the architectures that are supported by the different instance types, see <a href="http://docs.aws.amazon.com/AWSEC2/latest/UserGuide/instance-types.html">Instance Families and Types</a>.
    Architecture Architecture = 3;
    // For load-based or time-based instances, the type. Windows stacks can use only time-based instances.
    AutoScalingType AutoScalingType = 4;
    // The instance Availability Zone. For more information, see <a href="http://docs.aws.amazon.com/general/latest/gr/rande.html">Regions and Endpoints</a>.
    string AvailabilityZone = 5;
    // An array of <code>BlockDeviceMapping</code> objects that specify the instance's block devices. For more information, see <a href="http://docs.aws.amazon.com/AWSEC2/latest/UserGuide/block-device-mapping-concepts.html">Block Device Mapping</a>. Note that block device mappings are not supported for custom AMIs.
    array BlockDeviceMappings = 6;
    // Whether to create an Amazon EBS-optimized instance.
    boolean EbsOptimized = 7;
    // The instance host name.
    string Hostname = 8;
    // <p>Whether to install operating system and package updates when the instance boots. The default value is <code>true</code>. To control when updates are installed, set this value to <code>false</code>. You must then update your instances manually by using <a>CreateDeployment</a> to run the <code>update_dependencies</code> stack command or by manually running <code>yum</code> (Amazon Linux) or <code>apt-get</code> (Ubuntu) on the instances. </p> <note> <p>We strongly recommend using the default value of <code>true</code> to ensure that your instances have the latest security updates.</p> </note>
    boolean InstallUpdatesOnBoot = 9;
    // The instance type, such as <code>t2.micro</code>. For a list of supported instance types, open the stack in the console, choose <b>Instances</b>, and choose <b>+ Instance</b>. The <b>Size</b> list contains the currently supported types. For more information, see <a href="http://docs.aws.amazon.com/AWSEC2/latest/UserGuide/instance-types.html">Instance Families and Types</a>. The parameter values that you use to specify the various types are in the <b>API Name</b> column of the <b>Available Instance Types</b> table.
    string InstanceType = 10;
    // An array that contains the instance's layer IDs.
    array LayerIds = 11;
    // <p>The instance's operating system, which must be set to one of the following.</p> <ul> <li> <p>A supported Linux operating system: An Amazon Linux version, such as <code>Amazon Linux 2017.03</code>, <code>Amazon Linux 2016.09</code>, <code>Amazon Linux 2016.03</code>, <code>Amazon Linux 2015.09</code>, or <code>Amazon Linux 2015.03</code>.</p> </li> <li> <p>A supported Ubuntu operating system, such as <code>Ubuntu 16.04 LTS</code>, <code>Ubuntu 14.04 LTS</code>, or <code>Ubuntu 12.04 LTS</code>.</p> </li> <li> <p> <code>CentOS Linux 7</code> </p> </li> <li> <p> <code>Red Hat Enterprise Linux 7</code> </p> </li> <li> <p>A supported Windows operating system, such as <code>Microsoft Windows Server 2012 R2 Base</code>, <code>Microsoft Windows Server 2012 R2 with SQL Server Express</code>, <code>Microsoft Windows Server 2012 R2 with SQL Server Standard</code>, or <code>Microsoft Windows Server 2012 R2 with SQL Server Web</code>.</p> </li> <li> <p>A custom AMI: <code>Custom</code>.</p> </li> </ul> <p>For more information on the supported operating systems, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinginstances-os.html">AWS OpsWorks Stacks Operating Systems</a>.</p> <p>The default option is the current Amazon Linux version. If you set this parameter to <code>Custom</code>, you must use the <a>CreateInstance</a> action's AmiId parameter to specify the custom AMI that you want to use. Block device mappings are not supported if the value is <code>Custom</code>. For more information on the supported operating systems, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinginstances-os.html">Operating Systems</a>For more information on how to use custom AMIs with AWS OpsWorks Stacks, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinginstances-custom-ami.html">Using Custom AMIs</a>.</p>
    string Os = 12;
    // The instance root device type. For more information, see <a href="http://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ComponentsAMIs.html#storage-for-the-root-device">Storage for the Root Device</a>.
    RootDeviceType RootDeviceType = 13;
    // The instance's Amazon EC2 key-pair name.
    string SshKeyName = 14;
    // The stack ID.
    string StackId = 15;
    // The ID of the instance's subnet. If the stack is running in a VPC, you can use this parameter to override the stack's default subnet ID value and direct AWS OpsWorks Stacks to launch the instance in a different subnet.
    string SubnetId = 16;
    // The instance's tenancy option. The default option is no tenancy, or if the instance is running in a VPC, inherit tenancy settings from the VPC. The following are valid values for this parameter: <code>dedicated</code>, <code>default</code>, or <code>host</code>. Because there are costs associated with changes in tenancy options, we recommend that you research tenancy options before choosing them for your instances. For more information about dedicated hosts, see <a href="http://aws.amazon.com/ec2/dedicated-hosts/">Dedicated Hosts Overview</a> and <a href="http://aws.amazon.com/ec2/dedicated-hosts/">Amazon EC2 Dedicated Hosts</a>. For more information about dedicated instances, see <a href="http://docs.aws.amazon.com/AmazonVPC/latest/UserGuide/dedicated-instance.html">Dedicated Instances</a> and <a href="http://aws.amazon.com/ec2/purchasing-options/dedicated-instances/">Amazon EC2 Dedicated Instances</a>.
    string Tenancy = 17;
    // The instance's virtualization type, <code>paravirtual</code> or <code>hvm</code>.
    string VirtualizationType = 18;
}

message CreateInstanceResult {
    // The instance ID.
    string InstanceId = 1;
}

message CreateLayerRequest {
    // <p>One or more user-defined key-value pairs to be added to the stack attributes.</p> <p>To create a cluster layer, set the <code>EcsClusterArn</code> attribute to the cluster's ARN.</p>
    array Attributes = 1;
    // Whether to automatically assign an <a href="http://docs.aws.amazon.com/AWSEC2/latest/UserGuide/elastic-ip-addresses-eip.html">Elastic IP address</a> to the layer's instances. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinglayers-basics-edit.html">How to Edit a Layer</a>.
    boolean AutoAssignElasticIps = 2;
    // For stacks that are running in a VPC, whether to automatically assign a public IP address to the layer's instances. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinglayers-basics-edit.html">How to Edit a Layer</a>.
    boolean AutoAssignPublicIps = 3;
    // Specifies CloudWatch Logs configuration options for the layer. For more information, see <a>CloudWatchLogsLogStream</a>.
    CloudWatchLogsConfiguration CloudWatchLogsConfiguration = 4;
    // The ARN of an IAM profile to be used for the layer's EC2 instances. For more information about IAM ARNs, see <a href="http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_Identifiers.html">Using Identifiers</a>.
    string CustomInstanceProfileArn = 5;
    // A JSON-formatted string containing custom stack configuration and deployment attributes to be installed on the layer's instances. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workingcookbook-json-override.html"> Using Custom JSON</a>. This feature is supported as of version 1.7.42 of the AWS CLI.
    string CustomJson = 6;
    // A <code>LayerCustomRecipes</code> object that specifies the layer custom recipes.
    Recipes CustomRecipes = 7;
    // An array containing the layer custom security group IDs.
    array CustomSecurityGroupIds = 8;
    // Whether to disable auto healing for the layer.
    boolean EnableAutoHealing = 9;
    // <p>Whether to install operating system and package updates when the instance boots. The default value is <code>true</code>. To control when updates are installed, set this value to <code>false</code>. You must then update your instances manually by using <a>CreateDeployment</a> to run the <code>update_dependencies</code> stack command or by manually running <code>yum</code> (Amazon Linux) or <code>apt-get</code> (Ubuntu) on the instances. </p> <note> <p>To ensure that your instances have the latest security updates, we strongly recommend using the default value of <code>true</code>.</p> </note>
    boolean InstallUpdatesOnBoot = 10;
    // A <code>LifeCycleEventConfiguration</code> object that you can use to configure the Shutdown event to specify an execution timeout and enable or disable Elastic Load Balancer connection draining.
    LifecycleEventConfiguration LifecycleEventConfiguration = 11;
    // The layer name, which is used by the console.
    string Name = 12;
    // An array of <code>Package</code> objects that describes the layer packages.
    array Packages = 13;
    // <p>For custom layers only, use this parameter to specify the layer's short name, which is used internally by AWS OpsWorks Stacks and by Chef recipes. The short name is also used as the name for the directory where your app files are installed. It can have a maximum of 200 characters, which are limited to the alphanumeric characters, '-', '_', and '.'.</p> <p>The built-in layers' short names are defined by AWS OpsWorks Stacks. For more information, see the <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/layers.html">Layer Reference</a>.</p>
    string Shortname = 14;
    // The layer stack ID.
    string StackId = 15;
    // The layer type. A stack cannot have more than one built-in layer of the same type. It can have any number of custom layers. Built-in layers are not available in Chef 12 stacks.
    LayerType Type = 16;
    // Whether to use Amazon EBS-optimized instances.
    boolean UseEbsOptimizedInstances = 17;
    // A <code>VolumeConfigurations</code> object that describes the layer's Amazon EBS volumes.
    array VolumeConfigurations = 18;
}

message CreateLayerResult {
    // The layer ID.
    string LayerId = 1;
}

message CreateStackRequest {
    // <p>The default AWS OpsWorks Stacks agent version. You have the following options:</p> <ul> <li> <p>Auto-update - Set this parameter to <code>LATEST</code>. AWS OpsWorks Stacks automatically installs new agent versions on the stack's instances as soon as they are available.</p> </li> <li> <p>Fixed version - Set this parameter to your preferred agent version. To update the agent version, you must edit the stack configuration and specify a new version. AWS OpsWorks Stacks then automatically installs that version on the stack's instances.</p> </li> </ul> <p>The default setting is the most recent release of the agent. To specify an agent version, you must use the complete version number, not the abbreviated number shown on the console. For a list of available agent version numbers, call <a>DescribeAgentVersions</a>. AgentVersion cannot be set to Chef 12.2.</p> <note> <p>You can also specify an agent version when you create or update an instance, which overrides the stack's default setting.</p> </note>
    string AgentVersion = 1;
    // One or more user-defined key-value pairs to be added to the stack attributes.
    array Attributes = 2;
    // A <code>ChefConfiguration</code> object that specifies whether to enable Berkshelf and the Berkshelf version on Chef 11.10 stacks. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workingstacks-creating.html">Create a New Stack</a>.
    ChefConfiguration ChefConfiguration = 3;
    // The configuration manager. When you create a stack we recommend that you use the configuration manager to specify the Chef version: 12, 11.10, or 11.4 for Linux stacks, or 12.2 for Windows stacks. The default value for Linux stacks is currently 11.4.
    StackConfigurationManager ConfigurationManager = 4;
    Source CustomCookbooksSource = 5;
    // <p>A string that contains user-defined, custom JSON. It can be used to override the corresponding default stack configuration attribute values or to pass data to recipes. The string should be in the following format:</p> <p> <code>"{\"key1\": \"value1\", \"key2\": \"value2\",...}"</code> </p> <p>For more information on custom JSON, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workingstacks-json.html">Use Custom JSON to Modify the Stack Configuration Attributes</a>.</p>
    string CustomJson = 6;
    // The stack's default Availability Zone, which must be in the specified region. For more information, see <a href="http://docs.aws.amazon.com/general/latest/gr/rande.html">Regions and Endpoints</a>. If you also specify a value for <code>DefaultSubnetId</code>, the subnet must be in the same zone. For more information, see the <code>VpcId</code> parameter description.
    string DefaultAvailabilityZone = 7;
    // The Amazon Resource Name (ARN) of an IAM profile that is the default profile for all of the stack's EC2 instances. For more information about IAM ARNs, see <a href="http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_Identifiers.html">Using Identifiers</a>.
    string DefaultInstanceProfileArn = 8;
    // <p>The stack's default operating system, which is installed on every instance unless you specify a different operating system when you create the instance. You can specify one of the following.</p> <ul> <li> <p>A supported Linux operating system: An Amazon Linux version, such as <code>Amazon Linux 2017.03</code>, <code>Amazon Linux 2016.09</code>, <code>Amazon Linux 2016.03</code>, <code>Amazon Linux 2015.09</code>, or <code>Amazon Linux 2015.03</code>.</p> </li> <li> <p>A supported Ubuntu operating system, such as <code>Ubuntu 16.04 LTS</code>, <code>Ubuntu 14.04 LTS</code>, or <code>Ubuntu 12.04 LTS</code>.</p> </li> <li> <p> <code>CentOS Linux 7</code> </p> </li> <li> <p> <code>Red Hat Enterprise Linux 7</code> </p> </li> <li> <p>A supported Windows operating system, such as <code>Microsoft Windows Server 2012 R2 Base</code>, <code>Microsoft Windows Server 2012 R2 with SQL Server Express</code>, <code>Microsoft Windows Server 2012 R2 with SQL Server Standard</code>, or <code>Microsoft Windows Server 2012 R2 with SQL Server Web</code>.</p> </li> <li> <p>A custom AMI: <code>Custom</code>. You specify the custom AMI you want to use when you create instances. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinginstances-custom-ami.html"> Using Custom AMIs</a>.</p> </li> </ul> <p>The default option is the current Amazon Linux version. For more information on the supported operating systems, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinginstances-os.html">AWS OpsWorks Stacks Operating Systems</a>.</p>
    string DefaultOs = 9;
    // The default root device type. This value is the default for all instances in the stack, but you can override it when you create an instance. The default option is <code>instance-store</code>. For more information, see <a href="http://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ComponentsAMIs.html#storage-for-the-root-device">Storage for the Root Device</a>.
    RootDeviceType DefaultRootDeviceType = 10;
    // A default Amazon EC2 key pair name. The default value is none. If you specify a key pair name, AWS OpsWorks installs the public key on the instance and you can use the private key with an SSH client to log in to the instance. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinginstances-ssh.html"> Using SSH to Communicate with an Instance</a> and <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/security-ssh-access.html"> Managing SSH Access</a>. You can override this setting by specifying a different key pair, or no key pair, when you <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinginstances-add.html"> create an instance</a>.
    string DefaultSshKeyName = 11;
    // The stack's default VPC subnet ID. This parameter is required if you specify a value for the <code>VpcId</code> parameter. All instances are launched into this subnet unless you specify otherwise when you create the instance. If you also specify a value for <code>DefaultAvailabilityZone</code>, the subnet must be in that zone. For information on default values and when this parameter is required, see the <code>VpcId</code> parameter description.
    string DefaultSubnetId = 12;
    // <p>The stack's host name theme, with spaces replaced by underscores. The theme is used to generate host names for the stack's instances. By default, <code>HostnameTheme</code> is set to <code>Layer_Dependent</code>, which creates host names by appending integers to the layer's short name. The other themes are:</p> <ul> <li> <p> <code>Baked_Goods</code> </p> </li> <li> <p> <code>Clouds</code> </p> </li> <li> <p> <code>Europe_Cities</code> </p> </li> <li> <p> <code>Fruits</code> </p> </li> <li> <p> <code>Greek_Deities</code> </p> </li> <li> <p> <code>Legendary_creatures_from_Japan</code> </p> </li> <li> <p> <code>Planets_and_Moons</code> </p> </li> <li> <p> <code>Roman_Deities</code> </p> </li> <li> <p> <code>Scottish_Islands</code> </p> </li> <li> <p> <code>US_Cities</code> </p> </li> <li> <p> <code>Wild_Cats</code> </p> </li> </ul> <p>To obtain a generated host name, call <code>GetHostNameSuggestion</code>, which returns a host name based on the current theme.</p>
    string HostnameTheme = 13;
    // The stack name.
    string Name = 14;
    // The stack's AWS region, such as "ap-south-1". For more information about Amazon regions, see <a href="http://docs.aws.amazon.com/general/latest/gr/rande.html">Regions and Endpoints</a>.
    string Region = 15;
    // The stack's AWS Identity and Access Management (IAM) role, which allows AWS OpsWorks Stacks to work with AWS resources on your behalf. You must set this parameter to the Amazon Resource Name (ARN) for an existing IAM role. For more information about IAM ARNs, see <a href="http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_Identifiers.html">Using Identifiers</a>.
    string ServiceRoleArn = 16;
    // Whether the stack uses custom cookbooks.
    boolean UseCustomCookbooks = 17;
    // <p>Whether to associate the AWS OpsWorks Stacks built-in security groups with the stack's layers.</p> <p>AWS OpsWorks Stacks provides a standard set of built-in security groups, one for each layer, which are associated with layers by default. With <code>UseOpsworksSecurityGroups</code> you can instead provide your own custom security groups. <code>UseOpsworksSecurityGroups</code> has the following settings: </p> <ul> <li> <p>True - AWS OpsWorks Stacks automatically associates the appropriate built-in security group with each layer (default setting). You can associate additional security groups with a layer after you create it, but you cannot delete the built-in security group.</p> </li> <li> <p>False - AWS OpsWorks Stacks does not associate built-in security groups with layers. You must create appropriate EC2 security groups and associate a security group with each layer that you create. However, you can still manually associate a built-in security group with a layer on creation; custom security groups are required only for those layers that need custom settings.</p> </li> </ul> <p>For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workingstacks-creating.html">Create a New Stack</a>.</p>
    boolean UseOpsworksSecurityGroups = 18;
    // <p>The ID of the VPC that the stack is to be launched into. The VPC must be in the stack's region. All instances are launched into this VPC. You cannot change the ID later.</p> <ul> <li> <p>If your account supports EC2-Classic, the default value is <code>no VPC</code>.</p> </li> <li> <p>If your account does not support EC2-Classic, the default value is the default VPC for the specified region.</p> </li> </ul> <p>If the VPC ID corresponds to a default VPC and you have specified either the <code>DefaultAvailabilityZone</code> or the <code>DefaultSubnetId</code> parameter only, AWS OpsWorks Stacks infers the value of the other parameter. If you specify neither parameter, AWS OpsWorks Stacks sets these parameters to the first valid Availability Zone for the specified region and the corresponding default VPC subnet ID, respectively.</p> <p>If you specify a nondefault VPC ID, note the following:</p> <ul> <li> <p>It must belong to a VPC in your account that is in the specified region.</p> </li> <li> <p>You must specify a value for <code>DefaultSubnetId</code>.</p> </li> </ul> <p>For more information on how to use AWS OpsWorks Stacks with a VPC, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workingstacks-vpc.html">Running a Stack in a VPC</a>. For more information on default VPC and EC2-Classic, see <a href="http://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-supported-platforms.html">Supported Platforms</a>. </p>
    string VpcId = 19;
}

message CreateStackResult {
    // The stack ID, which is an opaque string that you use to identify the stack when performing actions such as <code>DescribeStacks</code>.
    string StackId = 1;
}

message CreateUserProfileRequest {
    // Whether users can specify their own SSH public key through the My Settings page. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/security-settingsshkey.html">Setting an IAM User's Public SSH Key</a>.
    boolean AllowSelfManagement = 1;
    // The user's IAM ARN; this can also be a federated user's ARN.
    string IamUserArn = 2;
    // The user's public SSH key.
    string SshPublicKey = 3;
    // The user's SSH user name. The allowable characters are [a-z], [A-Z], [0-9], '-', and '_'. If the specified name includes other punctuation marks, AWS OpsWorks Stacks removes them. For example, <code>my.name</code> will be changed to <code>myname</code>. If you do not specify an SSH user name, AWS OpsWorks Stacks generates one from the IAM user name.
    string SshUsername = 4;
}

message CreateUserProfileResult {
    // The user's IAM ARN.
    string IamUserArn = 1;
}

message DailyAutoScalingSchedule {
    string key = 1;
    string value = 2;
}
repeated DailyAutoScalingSchedule DailyAutoScalingSchedule = 1

message DataSource {
    // The data source's ARN.
    string Arn = 1;
    // The database name.
    string DatabaseName = 2;
    // The data source's type, <code>AutoSelectOpsworksMysqlInstance</code>, <code>OpsworksMysqlInstance</code>, or <code>RdsDbInstance</code>.
    string Type = 3;
}

repeated DataSource DataSources = 1



message DeleteAppRequest {
    // The app ID.
    string AppId = 1;
}

message DeleteInstanceRequest {
    // Whether to delete the instance Elastic IP address.
    boolean DeleteElasticIp = 1;
    // Whether to delete the instance's Amazon EBS volumes.
    boolean DeleteVolumes = 2;
    // The instance ID.
    string InstanceId = 3;
}

message DeleteLayerRequest {
    // The layer ID.
    string LayerId = 1;
}

message DeleteStackRequest {
    // The stack ID.
    string StackId = 1;
}

message DeleteUserProfileRequest {
    // The user's IAM ARN. This can also be a federated user's ARN.
    string IamUserArn = 1;
}

message Deployment {
    // The app ID.
    string AppId = 1;
    DeploymentCommand Command = 2;
    // A user-defined comment.
    string Comment = 3;
    // Date when the deployment completed.
    string CompletedAt = 4;
    // Date when the deployment was created.
    string CreatedAt = 5;
    // <p>A string that contains user-defined custom JSON. It can be used to override the corresponding default stack configuration attribute values for stack or to pass data to recipes. The string should be in the following format:</p> <p> <code>"{\"key1\": \"value1\", \"key2\": \"value2\",...}"</code> </p> <p>For more information on custom JSON, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workingstacks-json.html">Use Custom JSON to Modify the Stack Configuration Attributes</a>.</p>
    string CustomJson = 6;
    // The deployment ID.
    string DeploymentId = 7;
    // The deployment duration.
    integer Duration = 8;
    // The user's IAM ARN.
    string IamUserArn = 9;
    // The IDs of the target instances.
    array InstanceIds = 10;
    // The stack ID.
    string StackId = 11;
    // <p>The deployment status:</p> <ul> <li> <p>running</p> </li> <li> <p>successful</p> </li> <li> <p>failed</p> </li> </ul>
    string Status = 12;
}

message DeploymentCommand {
    // <p>The arguments of those commands that take arguments. It should be set to a JSON object with the following format:</p> <p> <code>{"arg_name1" : ["value1", "value2", ...], "arg_name2" : ["value1", "value2", ...], ...}</code> </p> <p>The <code>update_dependencies</code> command takes two arguments:</p> <ul> <li> <p> <code>upgrade_os_to</code> - Specifies the desired Amazon Linux version for instances whose OS you want to upgrade, such as <code>Amazon Linux 2016.09</code>. You must also set the <code>allow_reboot</code> argument to true.</p> </li> <li> <p> <code>allow_reboot</code> - Specifies whether to allow AWS OpsWorks Stacks to reboot the instances if necessary, after installing the updates. This argument can be set to either <code>true</code> or <code>false</code>. The default value is <code>false</code>.</p> </li> </ul> <p>For example, to upgrade an instance to Amazon Linux 2016.09, set <code>Args</code> to the following.</p> <p> <code> { "upgrade_os_to":["Amazon Linux 2016.09"], "allow_reboot":["true"] } </code> </p>
    array Args = 1;
    // <p>Specifies the operation. You can specify only one command.</p> <p>For stacks, the following commands are available:</p> <ul> <li> <p> <code>execute_recipes</code>: Execute one or more recipes. To specify the recipes, set an <code>Args</code> parameter named <code>recipes</code> to the list of recipes to be executed. For example, to execute <code>phpapp::appsetup</code>, set <code>Args</code> to <code>{"recipes":["phpapp::appsetup"]}</code>.</p> </li> <li> <p> <code>install_dependencies</code>: Install the stack's dependencies.</p> </li> <li> <p> <code>update_custom_cookbooks</code>: Update the stack's custom cookbooks.</p> </li> <li> <p> <code>update_dependencies</code>: Update the stack's dependencies.</p> </li> </ul> <note> <p>The update_dependencies and install_dependencies commands are supported only for Linux instances. You can run the commands successfully on Windows instances, but they do nothing.</p> </note> <p>For apps, the following commands are available:</p> <ul> <li> <p> <code>deploy</code>: Deploy an app. Ruby on Rails apps have an optional <code>Args</code> parameter named <code>migrate</code>. Set <code>Args</code> to {"migrate":["true"]} to migrate the database. The default setting is {"migrate":["false"]}.</p> </li> <li> <p> <code>rollback</code> Roll the app back to the previous version. When you update an app, AWS OpsWorks Stacks stores the previous version, up to a maximum of five versions. You can use this command to roll an app back as many as four versions.</p> </li> <li> <p> <code>start</code>: Start the app's web or application server.</p> </li> <li> <p> <code>stop</code>: Stop the app's web or application server.</p> </li> <li> <p> <code>restart</code>: Restart the app's web or application server.</p> </li> <li> <p> <code>undeploy</code>: Undeploy the app.</p> </li> </ul>
    DeploymentCommandName Name = 2;
}

message DeploymentCommandArg {
    string key = 1;
    array value = 2;
}
repeated DeploymentCommandArg DeploymentCommandArgs = 1

enum DeploymentCommandName {
    INSTALL_DEPENDENCIES = 0;
    UPDATE_DEPENDENCIES = 1;
    UPDATE_CUSTOM_COOKBOOKS = 2;
    EXECUTE_RECIPES = 3;
    CONFIGURE = 4;
    SETUP = 5;
    DEPLOY = 6;
    ROLLBACK = 7;
    START = 8;
    STOP = 9;
    RESTART = 10;
    UNDEPLOY = 11;
}

repeated Deployment Deployments = 1

message DeregisterEcsClusterRequest {
    // The cluster's ARN.
    string EcsClusterArn = 1;
}

message DeregisterElasticIpRequest {
    // The Elastic IP address.
    string ElasticIp = 1;
}

message DeregisterInstanceRequest {
    // The instance ID.
    string InstanceId = 1;
}

message DeregisterRdsDbInstanceRequest {
    // The Amazon RDS instance's ARN.
    string RdsDbInstanceArn = 1;
}

message DeregisterVolumeRequest {
    // The AWS OpsWorks Stacks volume ID, which is the GUID that AWS OpsWorks Stacks assigned to the instance when you registered the volume with the stack, not the Amazon EC2 volume ID.
    string VolumeId = 1;
}

message DescribeAgentVersionsRequest {
    // The configuration manager.
    StackConfigurationManager ConfigurationManager = 1;
    // The stack ID.
    string StackId = 2;
}

message DescribeAgentVersionsResult {
    // The agent versions for the specified stack or configuration manager. Note that this value is the complete version number, not the abbreviated number used by the console.
    array AgentVersions = 1;
}

message DescribeAppsRequest {
    // An array of app IDs for the apps to be described. If you use this parameter, <code>DescribeApps</code> returns a description of the specified apps. Otherwise, it returns a description of every app.
    array AppIds = 1;
    // The app stack ID. If you use this parameter, <code>DescribeApps</code> returns a description of the apps in the specified stack.
    string StackId = 2;
}

message DescribeAppsResult {
    // An array of <code>App</code> objects that describe the specified apps.
    array Apps = 1;
}

message DescribeCommandsRequest {
    // An array of command IDs. If you include this parameter, <code>DescribeCommands</code> returns a description of the specified commands. Otherwise, it returns a description of every command.
    array CommandIds = 1;
    // The deployment ID. If you include this parameter, <code>DescribeCommands</code> returns a description of the commands associated with the specified deployment.
    string DeploymentId = 2;
    // The instance ID. If you include this parameter, <code>DescribeCommands</code> returns a description of the commands associated with the specified instance.
    string InstanceId = 3;
}

message DescribeCommandsResult {
    // An array of <code>Command</code> objects that describe each of the specified commands.
    array Commands = 1;
}

message DescribeDeploymentsRequest {
    // The app ID. If you include this parameter, <code>DescribeDeployments</code> returns a description of the commands associated with the specified app.
    string AppId = 1;
    // An array of deployment IDs to be described. If you include this parameter, <code>DescribeDeployments</code> returns a description of the specified deployments. Otherwise, it returns a description of every deployment.
    array DeploymentIds = 2;
    // The stack ID. If you include this parameter, <code>DescribeDeployments</code> returns a description of the commands associated with the specified stack.
    string StackId = 3;
}

message DescribeDeploymentsResult {
    // An array of <code>Deployment</code> objects that describe the deployments.
    array Deployments = 1;
}

message DescribeEcsClustersRequest {
    // A list of ARNs, one for each cluster to be described.
    array EcsClusterArns = 1;
    // To receive a paginated response, use this parameter to specify the maximum number of results to be returned with a single call. If the number of available results exceeds this maximum, the response includes a <code>NextToken</code> value that you can assign to the <code>NextToken</code> request parameter to get the next set of results.
    integer MaxResults = 2;
    // If the previous paginated request did not return all of the remaining results, the response object's<code>NextToken</code> parameter value is set to a token. To retrieve the next set of results, call <code>DescribeEcsClusters</code> again and assign that token to the request object's <code>NextToken</code> parameter. If there are no remaining results, the previous response object's <code>NextToken</code> parameter is set to <code>null</code>.
    string NextToken = 3;
    // A stack ID. <code>DescribeEcsClusters</code> returns a description of the cluster that is registered with the stack.
    string StackId = 4;
}

message DescribeEcsClustersResult {
    // A list of <code>EcsCluster</code> objects containing the cluster descriptions.
    array EcsClusters = 1;
    // If a paginated request does not return all of the remaining results, this parameter is set to a token that you can assign to the request object's <code>NextToken</code> parameter to retrieve the next set of results. If the previous paginated request returned all of the remaining results, this parameter is set to <code>null</code>.
    string NextToken = 2;
}

message DescribeElasticIpsRequest {
    // The instance ID. If you include this parameter, <code>DescribeElasticIps</code> returns a description of the Elastic IP addresses associated with the specified instance.
    string InstanceId = 1;
    // An array of Elastic IP addresses to be described. If you include this parameter, <code>DescribeElasticIps</code> returns a description of the specified Elastic IP addresses. Otherwise, it returns a description of every Elastic IP address.
    array Ips = 2;
    // A stack ID. If you include this parameter, <code>DescribeElasticIps</code> returns a description of the Elastic IP addresses that are registered with the specified stack.
    string StackId = 3;
}

message DescribeElasticIpsResult {
    // An <code>ElasticIps</code> object that describes the specified Elastic IP addresses.
    array ElasticIps = 1;
}

message DescribeElasticLoadBalancersRequest {
    // A list of layer IDs. The action describes the Elastic Load Balancing instances for the specified layers.
    array LayerIds = 1;
    // A stack ID. The action describes the stack's Elastic Load Balancing instances.
    string StackId = 2;
}

message DescribeElasticLoadBalancersResult {
    // A list of <code>ElasticLoadBalancer</code> objects that describe the specified Elastic Load Balancing instances.
    array ElasticLoadBalancers = 1;
}

message DescribeInstancesRequest {
    // An array of instance IDs to be described. If you use this parameter, <code>DescribeInstances</code> returns a description of the specified instances. Otherwise, it returns a description of every instance.
    array InstanceIds = 1;
    // A layer ID. If you use this parameter, <code>DescribeInstances</code> returns descriptions of the instances associated with the specified layer.
    string LayerId = 2;
    // A stack ID. If you use this parameter, <code>DescribeInstances</code> returns descriptions of the instances associated with the specified stack.
    string StackId = 3;
}

message DescribeInstancesResult {
    // An array of <code>Instance</code> objects that describe the instances.
    array Instances = 1;
}

message DescribeLayersRequest {
    // An array of layer IDs that specify the layers to be described. If you omit this parameter, <code>DescribeLayers</code> returns a description of every layer in the specified stack.
    array LayerIds = 1;
    // The stack ID.
    string StackId = 2;
}

message DescribeLayersResult {
    // An array of <code>Layer</code> objects that describe the layers.
    array Layers = 1;
}

message DescribeLoadBasedAutoScalingRequest {
    // An array of layer IDs.
    array LayerIds = 1;
}

message DescribeLoadBasedAutoScalingResult {
    // An array of <code>LoadBasedAutoScalingConfiguration</code> objects that describe each layer's configuration.
    array LoadBasedAutoScalingConfigurations = 1;
}

message DescribeMyUserProfileResult {
    // A <code>UserProfile</code> object that describes the user's SSH information.
    SelfUserProfile UserProfile = 1;
}

message DescribePermissionsRequest {
    // The user's IAM ARN. This can also be a federated user's ARN. For more information about IAM ARNs, see <a href="http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_Identifiers.html">Using Identifiers</a>.
    string IamUserArn = 1;
    // The stack ID.
    string StackId = 2;
}

message DescribePermissionsResult {
    // <p>An array of <code>Permission</code> objects that describe the stack permissions.</p> <ul> <li> <p>If the request object contains only a stack ID, the array contains a <code>Permission</code> object with permissions for each of the stack IAM ARNs.</p> </li> <li> <p>If the request object contains only an IAM ARN, the array contains a <code>Permission</code> object with permissions for each of the user's stack IDs.</p> </li> <li> <p>If the request contains a stack ID and an IAM ARN, the array contains a single <code>Permission</code> object with permissions for the specified stack and IAM ARN.</p> </li> </ul>
    array Permissions = 1;
}

message DescribeRaidArraysRequest {
    // The instance ID. If you use this parameter, <code>DescribeRaidArrays</code> returns descriptions of the RAID arrays associated with the specified instance.
    string InstanceId = 1;
    // An array of RAID array IDs. If you use this parameter, <code>DescribeRaidArrays</code> returns descriptions of the specified arrays. Otherwise, it returns a description of every array.
    array RaidArrayIds = 2;
    // The stack ID.
    string StackId = 3;
}

message DescribeRaidArraysResult {
    // A <code>RaidArrays</code> object that describes the specified RAID arrays.
    array RaidArrays = 1;
}

message DescribeRdsDbInstancesRequest {
    // An array containing the ARNs of the instances to be described.
    array RdsDbInstanceArns = 1;
    // The stack ID that the instances are registered with. The operation returns descriptions of all registered Amazon RDS instances.
    string StackId = 2;
}

message DescribeRdsDbInstancesResult {
    // An a array of <code>RdsDbInstance</code> objects that describe the instances.
    array RdsDbInstances = 1;
}

message DescribeServiceErrorsRequest {
    // The instance ID. If you use this parameter, <code>DescribeServiceErrors</code> returns descriptions of the errors associated with the specified instance.
    string InstanceId = 1;
    // An array of service error IDs. If you use this parameter, <code>DescribeServiceErrors</code> returns descriptions of the specified errors. Otherwise, it returns a description of every error.
    array ServiceErrorIds = 2;
    // The stack ID. If you use this parameter, <code>DescribeServiceErrors</code> returns descriptions of the errors associated with the specified stack.
    string StackId = 3;
}

message DescribeServiceErrorsResult {
    // An array of <code>ServiceError</code> objects that describe the specified service errors.
    array ServiceErrors = 1;
}

message DescribeStackProvisioningParametersRequest {
    // The stack ID
    string StackId = 1;
}

message DescribeStackProvisioningParametersResult {
    // The AWS OpsWorks Stacks agent installer's URL.
    string AgentInstallerUrl = 1;
    // An embedded object that contains the provisioning parameters.
    array Parameters = 2;
}

message DescribeStackSummaryRequest {
    // The stack ID.
    string StackId = 1;
}

message DescribeStackSummaryResult {
    // A <code>StackSummary</code> object that contains the results.
    StackSummary StackSummary = 1;
}

message DescribeStacksRequest {
    // An array of stack IDs that specify the stacks to be described. If you omit this parameter, <code>DescribeStacks</code> returns a description of every stack.
    array StackIds = 1;
}

message DescribeStacksResult {
    // An array of <code>Stack</code> objects that describe the stacks.
    array Stacks = 1;
}

message DescribeTimeBasedAutoScalingRequest {
    // An array of instance IDs.
    array InstanceIds = 1;
}

message DescribeTimeBasedAutoScalingResult {
    // An array of <code>TimeBasedAutoScalingConfiguration</code> objects that describe the configuration for the specified instances.
    array TimeBasedAutoScalingConfigurations = 1;
}

message DescribeUserProfilesRequest {
    // An array of IAM or federated user ARNs that identify the users to be described.
    array IamUserArns = 1;
}

message DescribeUserProfilesResult {
    // A <code>Users</code> object that describes the specified users.
    array UserProfiles = 1;
}

message DescribeVolumesRequest {
    // The instance ID. If you use this parameter, <code>DescribeVolumes</code> returns descriptions of the volumes associated with the specified instance.
    string InstanceId = 1;
    // The RAID array ID. If you use this parameter, <code>DescribeVolumes</code> returns descriptions of the volumes associated with the specified RAID array.
    string RaidArrayId = 2;
    // A stack ID. The action describes the stack's registered Amazon EBS volumes.
    string StackId = 3;
    // Am array of volume IDs. If you use this parameter, <code>DescribeVolumes</code> returns descriptions of the specified volumes. Otherwise, it returns a description of every volume.
    array VolumeIds = 4;
}

message DescribeVolumesResult {
    // An array of volume IDs.
    array Volumes = 1;
}

message DetachElasticLoadBalancerRequest {
    // The Elastic Load Balancing instance's name.
    string ElasticLoadBalancerName = 1;
    // The ID of the layer that the Elastic Load Balancing instance is attached to.
    string LayerId = 2;
}

message DisassociateElasticIpRequest {
    // The Elastic IP address.
    string ElasticIp = 1;
}

message EbsBlockDevice {
    // Whether the volume is deleted on instance termination.
    boolean DeleteOnTermination = 1;
    // The number of I/O operations per second (IOPS) that the volume supports. For more information, see <a href="http://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_EbsBlockDevice.html">EbsBlockDevice</a>.
    integer Iops = 2;
    // The snapshot ID.
    string SnapshotId = 3;
    // The volume size, in GiB. For more information, see <a href="http://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_EbsBlockDevice.html">EbsBlockDevice</a>.
    integer VolumeSize = 4;
    // The volume type. <code>gp2</code> for General Purpose (SSD) volumes, <code>io1</code> for Provisioned IOPS (SSD) volumes, and <code>standard</code> for Magnetic volumes.
    VolumeType VolumeType = 5;
}

message EcsCluster {
    // The cluster's ARN.
    string EcsClusterArn = 1;
    // The cluster name.
    string EcsClusterName = 2;
    // The time and date that the cluster was registered with the stack.
    string RegisteredAt = 3;
    // The stack ID.
    string StackId = 4;
}

repeated EcsCluster EcsClusters = 1

message ElasticIp {
    // The domain.
    string Domain = 1;
    // The ID of the instance that the address is attached to.
    string InstanceId = 2;
    // The IP address.
    string Ip = 3;
    // The name.
    string Name = 4;
    // The AWS region. For more information, see <a href="http://docs.aws.amazon.com/general/latest/gr/rande.html">Regions and Endpoints</a>.
    string Region = 5;
}

repeated ElasticIp ElasticIps = 1

message ElasticLoadBalancer {
    // A list of Availability Zones.
    array AvailabilityZones = 1;
    // The instance's public DNS name.
    string DnsName = 2;
    // A list of the EC2 instances that the Elastic Load Balancing instance is managing traffic for.
    array Ec2InstanceIds = 3;
    // The Elastic Load Balancing instance's name.
    string ElasticLoadBalancerName = 4;
    // The ID of the layer that the instance is attached to.
    string LayerId = 5;
    // The instance's AWS region.
    string Region = 6;
    // The ID of the stack that the instance is associated with.
    string StackId = 7;
    // A list of subnet IDs, if the stack is running in a VPC.
    array SubnetIds = 8;
    // The VPC ID.
    string VpcId = 9;
}

repeated ElasticLoadBalancer ElasticLoadBalancers = 1

message EnvironmentVariable {
    // (Required) The environment variable's name, which can consist of up to 64 characters and must be specified. The name can contain upper- and lowercase letters, numbers, and underscores (_), but it must start with a letter or underscore.
    string Key = 1;
    // (Optional) Whether the variable's value will be returned by the <a>DescribeApps</a> action. To conceal an environment variable's value, set <code>Secure</code> to <code>true</code>. <code>DescribeApps</code> then returns <code>*****FILTERED*****</code> instead of the actual value. The default value for <code>Secure</code> is <code>false</code>.
    boolean Secure = 2;
    // (Optional) The environment variable's value, which can be left empty. If you specify a value, it can contain up to 256 characters, which must all be printable.
    string Value = 3;
}

repeated EnvironmentVariable EnvironmentVariables = 1

message GetHostnameSuggestionRequest {
    // The layer ID.
    string LayerId = 1;
}

message GetHostnameSuggestionResult {
    // The generated host name.
    string Hostname = 1;
    // The layer ID.
    string LayerId = 2;
}

message GrantAccessRequest {
    // The instance's AWS OpsWorks Stacks ID.
    string InstanceId = 1;
    // The length of time (in minutes) that the grant is valid. When the grant expires at the end of this period, the user will no longer be able to use the credentials to log in. If the user is logged in at the time, he or she automatically will be logged out.
    integer ValidForInMinutes = 2;
}

message GrantAccessResult {
    // A <code>TemporaryCredential</code> object that contains the data needed to log in to the instance by RDP clients, such as the Microsoft Remote Desktop Connection.
    TemporaryCredential TemporaryCredential = 1;
}

message Instance {
    // The agent version. This parameter is set to <code>INHERIT</code> if the instance inherits the default stack setting or to a a version number for a fixed agent version.
    string AgentVersion = 1;
    // A custom AMI ID to be used to create the instance. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinginstances-custom-ami.html">Instances</a>
    string AmiId = 2;
    // The instance architecture: "i386" or "x86_64".
    Architecture Architecture = 3;
    string Arn = 4;
    // For load-based or time-based instances, the type.
    AutoScalingType AutoScalingType = 5;
    // The instance Availability Zone. For more information, see <a href="http://docs.aws.amazon.com/general/latest/gr/rande.html">Regions and Endpoints</a>.
    string AvailabilityZone = 6;
    // An array of <code>BlockDeviceMapping</code> objects that specify the instance's block device mappings.
    array BlockDeviceMappings = 7;
    // The time that the instance was created.
    string CreatedAt = 8;
    // Whether this is an Amazon EBS-optimized instance.
    boolean EbsOptimized = 9;
    // The ID of the associated Amazon EC2 instance.
    string Ec2InstanceId = 10;
    // For container instances, the Amazon ECS cluster's ARN.
    string EcsClusterArn = 11;
    // For container instances, the instance's ARN.
    string EcsContainerInstanceArn = 12;
    // The instance <a href="http://docs.aws.amazon.com/AWSEC2/latest/UserGuide/elastic-ip-addresses-eip.html">Elastic IP address </a>.
    string ElasticIp = 13;
    // The instance host name.
    string Hostname = 14;
    // For registered instances, the infrastructure class: <code>ec2</code> or <code>on-premises</code>.
    string InfrastructureClass = 15;
    // <p>Whether to install operating system and package updates when the instance boots. The default value is <code>true</code>. If this value is set to <code>false</code>, you must then update your instances manually by using <a>CreateDeployment</a> to run the <code>update_dependencies</code> stack command or by manually running <code>yum</code> (Amazon Linux) or <code>apt-get</code> (Ubuntu) on the instances. </p> <note> <p>We strongly recommend using the default value of <code>true</code>, to ensure that your instances have the latest security updates.</p> </note>
    boolean InstallUpdatesOnBoot = 16;
    // The instance ID.
    string InstanceId = 17;
    // The ARN of the instance's IAM profile. For more information about IAM ARNs, see <a href="http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_Identifiers.html">Using Identifiers</a>.
    string InstanceProfileArn = 18;
    // The instance type, such as <code>t2.micro</code>.
    string InstanceType = 19;
    // The ID of the last service error. For more information, call <a>DescribeServiceErrors</a>.
    string LastServiceErrorId = 20;
    // An array containing the instance layer IDs.
    array LayerIds = 21;
    // The instance's operating system.
    string Os = 22;
    // The instance's platform.
    string Platform = 23;
    // The instance's private DNS name.
    string PrivateDns = 24;
    // The instance's private IP address.
    string PrivateIp = 25;
    // The instance public DNS name.
    string PublicDns = 26;
    // The instance public IP address.
    string PublicIp = 27;
    // For registered instances, who performed the registration.
    string RegisteredBy = 28;
    // The instance's reported AWS OpsWorks Stacks agent version.
    string ReportedAgentVersion = 29;
    // For registered instances, the reported operating system.
    ReportedOs ReportedOs = 30;
    // The instance's root device type. For more information, see <a href="http://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ComponentsAMIs.html#storage-for-the-root-device">Storage for the Root Device</a>.
    RootDeviceType RootDeviceType = 31;
    // The root device volume ID.
    string RootDeviceVolumeId = 32;
    // An array containing the instance security group IDs.
    array SecurityGroupIds = 33;
    // The SSH key's Deep Security Agent (DSA) fingerprint.
    string SshHostDsaKeyFingerprint = 34;
    // The SSH key's RSA fingerprint.
    string SshHostRsaKeyFingerprint = 35;
    // The instance's Amazon EC2 key-pair name.
    string SshKeyName = 36;
    // The stack ID.
    string StackId = 37;
    // <p>The instance status:</p> <ul> <li> <p> <code>booting</code> </p> </li> <li> <p> <code>connection_lost</code> </p> </li> <li> <p> <code>online</code> </p> </li> <li> <p> <code>pending</code> </p> </li> <li> <p> <code>rebooting</code> </p> </li> <li> <p> <code>requested</code> </p> </li> <li> <p> <code>running_setup</code> </p> </li> <li> <p> <code>setup_failed</code> </p> </li> <li> <p> <code>shutting_down</code> </p> </li> <li> <p> <code>start_failed</code> </p> </li> <li> <p> <code>stop_failed</code> </p> </li> <li> <p> <code>stopped</code> </p> </li> <li> <p> <code>stopping</code> </p> </li> <li> <p> <code>terminated</code> </p> </li> <li> <p> <code>terminating</code> </p> </li> </ul>
    string Status = 38;
    // The instance's subnet ID; applicable only if the stack is running in a VPC.
    string SubnetId = 39;
    // The instance's tenancy option, such as <code>dedicated</code> or <code>host</code>.
    string Tenancy = 40;
    // The instance's virtualization type: <code>paravirtual</code> or <code>hvm</code>.
    VirtualizationType VirtualizationType = 41;
}

message InstanceIdentity {
    // A JSON document that contains the metadata.
    string Document = 1;
    // A signature that can be used to verify the document's accuracy and authenticity.
    string Signature = 2;
}

repeated Instance Instances = 1

message InstancesCount {
    // The number of instances in the Assigning state.
    integer Assigning = 1;
    // The number of instances with <code>booting</code> status.
    integer Booting = 2;
    // The number of instances with <code>connection_lost</code> status.
    integer ConnectionLost = 3;
    // The number of instances in the Deregistering state.
    integer Deregistering = 4;
    // The number of instances with <code>online</code> status.
    integer Online = 5;
    // The number of instances with <code>pending</code> status.
    integer Pending = 6;
    // The number of instances with <code>rebooting</code> status.
    integer Rebooting = 7;
    // The number of instances in the Registered state.
    integer Registered = 8;
    // The number of instances in the Registering state.
    integer Registering = 9;
    // The number of instances with <code>requested</code> status.
    integer Requested = 10;
    // The number of instances with <code>running_setup</code> status.
    integer RunningSetup = 11;
    // The number of instances with <code>setup_failed</code> status.
    integer SetupFailed = 12;
    // The number of instances with <code>shutting_down</code> status.
    integer ShuttingDown = 13;
    // The number of instances with <code>start_failed</code> status.
    integer StartFailed = 14;
    // The number of instances with <code>stopped</code> status.
    integer Stopped = 15;
    // The number of instances with <code>stopping</code> status.
    integer Stopping = 16;
    // The number of instances with <code>terminated</code> status.
    integer Terminated = 17;
    // The number of instances with <code>terminating</code> status.
    integer Terminating = 18;
    // The number of instances in the Unassigning state.
    integer Unassigning = 19;
}

message Layer {
    string Arn = 1;
    // <p>The layer attributes.</p> <p>For the <code>HaproxyStatsPassword</code>, <code>MysqlRootPassword</code>, and <code>GangliaPassword</code> attributes, AWS OpsWorks Stacks returns <code>*****FILTERED*****</code> instead of the actual value</p> <p>For an ECS Cluster layer, AWS OpsWorks Stacks the <code>EcsClusterArn</code> attribute is set to the cluster's ARN.</p>
    array Attributes = 2;
    // Whether to automatically assign an <a href="http://docs.aws.amazon.com/AWSEC2/latest/UserGuide/elastic-ip-addresses-eip.html">Elastic IP address</a> to the layer's instances. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinglayers-basics-edit.html">How to Edit a Layer</a>.
    boolean AutoAssignElasticIps = 3;
    // For stacks that are running in a VPC, whether to automatically assign a public IP address to the layer's instances. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinglayers-basics-edit.html">How to Edit a Layer</a>.
    boolean AutoAssignPublicIps = 4;
    // The Amazon CloudWatch Logs configuration settings for the layer.
    CloudWatchLogsConfiguration CloudWatchLogsConfiguration = 5;
    // Date when the layer was created.
    string CreatedAt = 6;
    // The ARN of the default IAM profile to be used for the layer's EC2 instances. For more information about IAM ARNs, see <a href="http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_Identifiers.html">Using Identifiers</a>.
    string CustomInstanceProfileArn = 7;
    // A JSON formatted string containing the layer's custom stack configuration and deployment attributes.
    string CustomJson = 8;
    // A <code>LayerCustomRecipes</code> object that specifies the layer's custom recipes.
    Recipes CustomRecipes = 9;
    // An array containing the layer's custom security group IDs.
    array CustomSecurityGroupIds = 10;
    Recipes DefaultRecipes = 11;
    // An array containing the layer's security group names.
    array DefaultSecurityGroupNames = 12;
    // Whether auto healing is disabled for the layer.
    boolean EnableAutoHealing = 13;
    // <p>Whether to install operating system and package updates when the instance boots. The default value is <code>true</code>. If this value is set to <code>false</code>, you must then update your instances manually by using <a>CreateDeployment</a> to run the <code>update_dependencies</code> stack command or manually running <code>yum</code> (Amazon Linux) or <code>apt-get</code> (Ubuntu) on the instances. </p> <note> <p>We strongly recommend using the default value of <code>true</code>, to ensure that your instances have the latest security updates.</p> </note>
    boolean InstallUpdatesOnBoot = 14;
    // The layer ID.
    string LayerId = 15;
    // A <code>LifeCycleEventConfiguration</code> object that specifies the Shutdown event configuration.
    LifecycleEventConfiguration LifecycleEventConfiguration = 16;
    // The layer name.
    string Name = 17;
    // An array of <code>Package</code> objects that describe the layer's packages.
    array Packages = 18;
    // The layer short name.
    string Shortname = 19;
    // The layer stack ID.
    string StackId = 20;
    // The layer type.
    LayerType Type = 21;
    // Whether the layer uses Amazon EBS-optimized instances.
    boolean UseEbsOptimizedInstances = 22;
    // A <code>VolumeConfigurations</code> object that describes the layer's Amazon EBS volumes.
    array VolumeConfigurations = 23;
}

message LayerAttribute {
    LayerAttributesKeys key = 1;
    string value = 2;
}
repeated LayerAttribute LayerAttributes = 1

enum LayerAttributesKey {
    ECSCLUSTERARN = 0;
    ENABLEHAPROXYSTATS = 1;
    HAPROXYSTATSURL = 2;
    HAPROXYSTATSUSER = 3;
    HAPROXYSTATSPASSWORD = 4;
    HAPROXYHEALTHCHECKURL = 5;
    HAPROXYHEALTHCHECKMETHOD = 6;
    MYSQLROOTPASSWORD = 7;
    MYSQLROOTPASSWORDUBIQUITOUS = 8;
    GANGLIAURL = 9;
    GANGLIAUSER = 10;
    GANGLIAPASSWORD = 11;
    MEMCACHEDMEMORY = 12;
    NODEJSVERSION = 13;
    RUBYVERSION = 14;
    RUBYGEMSVERSION = 15;
    MANAGEBUNDLER = 16;
    BUNDLERVERSION = 17;
    RAILSSTACK = 18;
    PASSENGERVERSION = 19;
    JVM = 20;
    JVMVERSION = 21;
    JVMOPTIONS = 22;
    JAVAAPPSERVER = 23;
    JAVAAPPSERVERVERSION = 24;
}

enum LayerType {
    AWSFLOWRUBY = 0;
    ECSCLUSTER = 1;
    JAVAAPP = 2;
    LB = 3;
    WEB = 4;
    PHPAPP = 5;
    RAILSAPP = 6;
    NODEJSAPP = 7;
    MEMCACHED = 8;
    DBMASTER = 9;
    MONITORINGMASTER = 10;
    CUSTOM = 11;
}

repeated Layer Layers = 1

message LifecycleEventConfiguration {
    // A <code>ShutdownEventConfiguration</code> object that specifies the Shutdown event configuration.
    ShutdownEventConfiguration Shutdown = 1;
}

message ListTagsRequest {
    // Do not use. A validation exception occurs if you add a <code>MaxResults</code> parameter to a <code>ListTagsRequest</code> call.
    integer MaxResults = 1;
    // Do not use. A validation exception occurs if you add a <code>NextToken</code> parameter to a <code>ListTagsRequest</code> call.
    string NextToken = 2;
    // The stack or layer's Amazon Resource Number (ARN).
    string ResourceArn = 3;
}

message ListTagsResult {
    // If a paginated request does not return all of the remaining results, this parameter is set to a token that you can assign to the request object's <code>NextToken</code> parameter to get the next set of results. If the previous paginated request returned all of the remaining results, this parameter is set to <code>null</code>.
    string NextToken = 1;
    // A set of key-value pairs that contain tag keys and tag values that are attached to a stack or layer.
    array Tags = 2;
}

message LoadBasedAutoScalingConfiguration {
    // An <code>AutoScalingThresholds</code> object that describes the downscaling configuration, which defines how and when AWS OpsWorks Stacks reduces the number of instances.
    AutoScalingThresholds DownScaling = 1;
    // Whether load-based auto scaling is enabled for the layer.
    boolean Enable = 2;
    // The layer ID.
    string LayerId = 3;
    // An <code>AutoScalingThresholds</code> object that describes the upscaling configuration, which defines how and when AWS OpsWorks Stacks increases the number of instances.
    AutoScalingThresholds UpScaling = 4;
}

repeated LoadBasedAutoScalingConfiguration LoadBasedAutoScalingConfigurations = 1







message Parameter {
    string key = 1;
    string value = 2;
}
repeated Parameter Parameters = 1

message Permission {
    // Whether the user can use SSH.
    boolean AllowSsh = 1;
    // Whether the user can use <b>sudo</b>.
    boolean AllowSudo = 2;
    // The Amazon Resource Name (ARN) for an AWS Identity and Access Management (IAM) role. For more information about IAM ARNs, see <a href="http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_Identifiers.html">Using Identifiers</a>.
    string IamUserArn = 3;
    // <p>The user's permission level, which must be the following:</p> <ul> <li> <p> <code>deny</code> </p> </li> <li> <p> <code>show</code> </p> </li> <li> <p> <code>deploy</code> </p> </li> <li> <p> <code>manage</code> </p> </li> <li> <p> <code>iam_only</code> </p> </li> </ul> <p>For more information on the permissions associated with these levels, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a> </p>
    string Level = 4;
    // A stack ID.
    string StackId = 5;
}

repeated Permission Permissions = 1

message RaidArray {
    // The array's Availability Zone. For more information, see <a href="http://docs.aws.amazon.com/general/latest/gr/rande.html">Regions and Endpoints</a>.
    string AvailabilityZone = 1;
    // When the RAID array was created.
    string CreatedAt = 2;
    // The array's Linux device. For example /dev/mdadm0.
    string Device = 3;
    // The instance ID.
    string InstanceId = 4;
    // For PIOPS volumes, the IOPS per disk.
    integer Iops = 5;
    // The array's mount point.
    string MountPoint = 6;
    // The array name.
    string Name = 7;
    // The number of disks in the array.
    integer NumberOfDisks = 8;
    // The array ID.
    string RaidArrayId = 9;
    // The <a href="http://en.wikipedia.org/wiki/Standard_RAID_levels">RAID level</a>.
    integer RaidLevel = 10;
    // The array's size.
    integer Size = 11;
    // The stack ID.
    string StackId = 12;
    // The volume type, standard or PIOPS.
    string VolumeType = 13;
}

repeated RaidArray RaidArrays = 1

message RdsDbInstance {
    // The instance's address.
    string Address = 1;
    // The DB instance identifier.
    string DbInstanceIdentifier = 2;
    // AWS OpsWorks Stacks returns <code>*****FILTERED*****</code> instead of the actual value.
    string DbPassword = 3;
    // The master user name.
    string DbUser = 4;
    // The instance's database engine.
    string Engine = 5;
    // Set to <code>true</code> if AWS OpsWorks Stacks is unable to discover the Amazon RDS instance. AWS OpsWorks Stacks attempts to discover the instance only once. If this value is set to <code>true</code>, you must deregister the instance, and then register it again.
    boolean MissingOnRds = 6;
    // The instance's ARN.
    string RdsDbInstanceArn = 7;
    // The instance's AWS region.
    string Region = 8;
    // The ID of the stack with which the instance is registered.
    string StackId = 9;
}

repeated RdsDbInstance RdsDbInstances = 1

message RebootInstanceRequest {
    // The instance ID.
    string InstanceId = 1;
}

message Recipes {
    // An array of custom recipe names to be run following a <code>configure</code> event.
    array Configure = 1;
    // An array of custom recipe names to be run following a <code>deploy</code> event.
    array Deploy = 2;
    // An array of custom recipe names to be run following a <code>setup</code> event.
    array Setup = 3;
    // An array of custom recipe names to be run following a <code>shutdown</code> event.
    array Shutdown = 4;
    // An array of custom recipe names to be run following a <code>undeploy</code> event.
    array Undeploy = 5;
}

message RegisterEcsClusterRequest {
    // The cluster's ARN.
    string EcsClusterArn = 1;
    // The stack ID.
    string StackId = 2;
}

message RegisterEcsClusterResult {
    // The cluster's ARN.
    string EcsClusterArn = 1;
}

message RegisterElasticIpRequest {
    // The Elastic IP address.
    string ElasticIp = 1;
    // The stack ID.
    string StackId = 2;
}

message RegisterElasticIpResult {
    // The Elastic IP address.
    string ElasticIp = 1;
}

message RegisterInstanceRequest {
    // The instance's hostname.
    string Hostname = 1;
    // An InstanceIdentity object that contains the instance's identity.
    InstanceIdentity InstanceIdentity = 2;
    // The instance's private IP address.
    string PrivateIp = 3;
    // The instance's public IP address.
    string PublicIp = 4;
    // The instances public RSA key. This key is used to encrypt communication between the instance and the service.
    string RsaPublicKey = 5;
    // The instances public RSA key fingerprint.
    string RsaPublicKeyFingerprint = 6;
    // The ID of the stack that the instance is to be registered with.
    string StackId = 7;
}

message RegisterInstanceResult {
    // The registered instance's AWS OpsWorks Stacks ID.
    string InstanceId = 1;
}

message RegisterRdsDbInstanceRequest {
    // The database password.
    string DbPassword = 1;
    // The database's master user name.
    string DbUser = 2;
    // The Amazon RDS instance's ARN.
    string RdsDbInstanceArn = 3;
    // The stack ID.
    string StackId = 4;
}

message RegisterVolumeRequest {
    // The Amazon EBS volume ID.
    string Ec2VolumeId = 1;
    // The stack ID.
    string StackId = 2;
}

message RegisterVolumeResult {
    // The volume ID.
    string VolumeId = 1;
}

message ReportedOs {
    // The operating system family.
    string Family = 1;
    // The operating system name.
    string Name = 2;
    // The operating system version.
    string Version = 3;
}

message ResourceNotFoundException {
    // The exception message.
    string message = 1;
}

enum RootDeviceType {
    EBS = 0;
    INSTANCESTORE = 1;
}

message SelfUserProfile {
    // The user's IAM ARN.
    string IamUserArn = 1;
    // The user's name.
    string Name = 2;
    // The user's SSH public key.
    string SshPublicKey = 3;
    // The user's SSH user name.
    string SshUsername = 4;
}

message ServiceError {
    // When the error occurred.
    string CreatedAt = 1;
    // The instance ID.
    string InstanceId = 2;
    // A message that describes the error.
    string Message = 3;
    // The error ID.
    string ServiceErrorId = 4;
    // The stack ID.
    string StackId = 5;
    // The error type.
    string Type = 6;
}

repeated ServiceError ServiceErrors = 1

message SetLoadBasedAutoScalingRequest {
    // An <code>AutoScalingThresholds</code> object with the downscaling threshold configuration. If the load falls below these thresholds for a specified amount of time, AWS OpsWorks Stacks stops a specified number of instances.
    AutoScalingThresholds DownScaling = 1;
    // Enables load-based auto scaling for the layer.
    boolean Enable = 2;
    // The layer ID.
    string LayerId = 3;
    // An <code>AutoScalingThresholds</code> object with the upscaling threshold configuration. If the load exceeds these thresholds for a specified amount of time, AWS OpsWorks Stacks starts a specified number of instances.
    AutoScalingThresholds UpScaling = 4;
}

message SetPermissionRequest {
    // The user is allowed to use SSH to communicate with the instance.
    boolean AllowSsh = 1;
    // The user is allowed to use <b>sudo</b> to elevate privileges.
    boolean AllowSudo = 2;
    // The user's IAM ARN. This can also be a federated user's ARN.
    string IamUserArn = 3;
    // <p>The user's permission level, which must be set to one of the following strings. You cannot set your own permissions level.</p> <ul> <li> <p> <code>deny</code> </p> </li> <li> <p> <code>show</code> </p> </li> <li> <p> <code>deploy</code> </p> </li> <li> <p> <code>manage</code> </p> </li> <li> <p> <code>iam_only</code> </p> </li> </ul> <p>For more information on the permissions associated with these levels, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    string Level = 4;
    // The stack ID.
    string StackId = 5;
}

message SetTimeBasedAutoScalingRequest {
    // An <code>AutoScalingSchedule</code> with the instance schedule.
    WeeklyAutoScalingSchedule AutoScalingSchedule = 1;
    // The instance ID.
    string InstanceId = 2;
}

message ShutdownEventConfiguration {
    // Whether to enable Elastic Load Balancing connection draining. For more information, see <a href="http://docs.aws.amazon.com/ElasticLoadBalancing/latest/DeveloperGuide/TerminologyandKeyConcepts.html#conn-drain">Connection Draining</a>
    boolean DelayUntilElbConnectionsDrained = 1;
    // The time, in seconds, that AWS OpsWorks Stacks will wait after triggering a Shutdown event before shutting down an instance.
    integer ExecutionTimeout = 2;
}

message Source {
    // <p>When included in a request, the parameter depends on the repository type.</p> <ul> <li> <p>For Amazon S3 bundles, set <code>Password</code> to the appropriate IAM secret access key.</p> </li> <li> <p>For HTTP bundles and Subversion repositories, set <code>Password</code> to the password.</p> </li> </ul> <p>For more information on how to safely handle IAM credentials, see <a href="http://docs.aws.amazon.com/general/latest/gr/aws-access-keys-best-practices.html">http://docs.aws.amazon.com/general/latest/gr/aws-access-keys-best-practices.html</a>.</p> <p>In responses, AWS OpsWorks Stacks returns <code>*****FILTERED*****</code> instead of the actual value.</p>
    string Password = 1;
    // The application's version. AWS OpsWorks Stacks enables you to easily deploy new versions of an application. One of the simplest approaches is to have branches or revisions in your repository that represent different versions that can potentially be deployed.
    string Revision = 2;
    // <p>In requests, the repository's SSH key.</p> <p>In responses, AWS OpsWorks Stacks returns <code>*****FILTERED*****</code> instead of the actual value.</p>
    string SshKey = 3;
    // The repository type.
    SourceType Type = 4;
    // The source URL. The following is an example of an Amazon S3 source URL: <code>https://s3.amazonaws.com/opsworks-demo-bucket/opsworks_cookbook_demo.tar.gz</code>.
    string Url = 5;
    // <p>This parameter depends on the repository type.</p> <ul> <li> <p>For Amazon S3 bundles, set <code>Username</code> to the appropriate IAM access key ID.</p> </li> <li> <p>For HTTP bundles, Git repositories, and Subversion repositories, set <code>Username</code> to the user name.</p> </li> </ul>
    string Username = 6;
}

enum SourceType {
    GIT = 0;
    SVN = 1;
    ARCHIVE = 2;
    S3 = 3;
}

message SslConfiguration {
    // The contents of the certificate's domain.crt file.
    string Certificate = 1;
    // Optional. Can be used to specify an intermediate certificate authority key or client authentication.
    string Chain = 2;
    // The private key; the contents of the certificate's domain.kex file.
    string PrivateKey = 3;
}

message Stack {
    // The agent version. This parameter is set to <code>LATEST</code> for auto-update. or a version number for a fixed agent version.
    string AgentVersion = 1;
    // The stack's ARN.
    string Arn = 2;
    // The stack's attributes.
    array Attributes = 3;
    // A <code>ChefConfiguration</code> object that specifies whether to enable Berkshelf and the Berkshelf version. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workingstacks-creating.html">Create a New Stack</a>.
    ChefConfiguration ChefConfiguration = 4;
    // The configuration manager.
    StackConfigurationManager ConfigurationManager = 5;
    // The date when the stack was created.
    string CreatedAt = 6;
    Source CustomCookbooksSource = 7;
    // <p>A JSON object that contains user-defined attributes to be added to the stack configuration and deployment attributes. You can use custom JSON to override the corresponding default stack configuration attribute values or to pass data to recipes. The string should be in the following format:</p> <p> <code>"{\"key1\": \"value1\", \"key2\": \"value2\",...}"</code> </p> <p>For more information on custom JSON, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workingstacks-json.html">Use Custom JSON to Modify the Stack Configuration Attributes</a>.</p>
    string CustomJson = 8;
    // The stack's default Availability Zone. For more information, see <a href="http://docs.aws.amazon.com/general/latest/gr/rande.html">Regions and Endpoints</a>.
    string DefaultAvailabilityZone = 9;
    // The ARN of an IAM profile that is the default profile for all of the stack's EC2 instances. For more information about IAM ARNs, see <a href="http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_Identifiers.html">Using Identifiers</a>.
    string DefaultInstanceProfileArn = 10;
    // The stack's default operating system.
    string DefaultOs = 11;
    // The default root device type. This value is used by default for all instances in the stack, but you can override it when you create an instance. For more information, see <a href="http://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ComponentsAMIs.html#storage-for-the-root-device">Storage for the Root Device</a>.
    RootDeviceType DefaultRootDeviceType = 12;
    // A default Amazon EC2 key pair for the stack's instances. You can override this value when you create or update an instance.
    string DefaultSshKeyName = 13;
    // The default subnet ID; applicable only if the stack is running in a VPC.
    string DefaultSubnetId = 14;
    // The stack host name theme, with spaces replaced by underscores.
    string HostnameTheme = 15;
    // The stack name.
    string Name = 16;
    // The stack AWS region, such as "ap-northeast-2". For more information about AWS regions, see <a href="http://docs.aws.amazon.com/general/latest/gr/rande.html">Regions and Endpoints</a>.
    string Region = 17;
    // The stack AWS Identity and Access Management (IAM) role.
    string ServiceRoleArn = 18;
    // The stack ID.
    string StackId = 19;
    // Whether the stack uses custom cookbooks.
    boolean UseCustomCookbooks = 20;
    // Whether the stack automatically associates the AWS OpsWorks Stacks built-in security groups with the stack's layers.
    boolean UseOpsworksSecurityGroups = 21;
    // The VPC ID; applicable only if the stack is running in a VPC.
    string VpcId = 22;
}

message StackAttribute {
    StackAttributesKeys key = 1;
    string value = 2;
}
repeated StackAttribute StackAttributes = 1

enum StackAttributesKey {
    COLOR = 0;
}

message StackConfigurationManager {
    // The name. This parameter must be set to "Chef".
    string Name = 1;
    // The Chef version. This parameter must be set to 12, 11.10, or 11.4 for Linux stacks, and to 12.2 for Windows stacks. The default value for Linux stacks is 11.4.
    string Version = 2;
}

message StackSummary {
    // The number of apps.
    integer AppsCount = 1;
    // The stack's ARN.
    string Arn = 2;
    // An <code>InstancesCount</code> object with the number of instances in each status.
    InstancesCount InstancesCount = 3;
    // The number of layers.
    integer LayersCount = 4;
    // The stack name.
    string Name = 5;
    // The stack ID.
    string StackId = 6;
}

repeated Stack Stacks = 1

message StartInstanceRequest {
    // The instance ID.
    string InstanceId = 1;
}

message StartStackRequest {
    // The stack ID.
    string StackId = 1;
}

message StopInstanceRequest {
    // The instance ID.
    string InstanceId = 1;
}

message StopStackRequest {
    // The stack ID.
    string StackId = 1;
}



repeated string Strings = 1





repeated string TagKeys = 1

message TagResourceRequest {
    // The stack or layer's Amazon Resource Number (ARN).
    string ResourceArn = 1;
    // <p>A map that contains tag keys and tag values that are attached to a stack or layer.</p> <ul> <li> <p>The key cannot be empty.</p> </li> <li> <p>The key can be a maximum of 127 characters, and can contain only Unicode letters, numbers, or separators, or the following special characters: <code>+ - = . _ : /</code> </p> </li> <li> <p>The value can be a maximum 255 characters, and contain only Unicode letters, numbers, or separators, or the following special characters: <code>+ - = . _ : /</code> </p> </li> <li> <p>Leading and trailing white spaces are trimmed from both the key and value.</p> </li> <li> <p>A maximum of 40 tags is allowed for any resource.</p> </li> </ul>
    array Tags = 2;
}

message Tag {
    string key = 1;
    string value = 2;
}
repeated Tag Tags = 1

message TemporaryCredential {
    // The instance's AWS OpsWorks Stacks ID.
    string InstanceId = 1;
    // The password.
    string Password = 2;
    // The user name.
    string Username = 3;
    // The length of time (in minutes) that the grant is valid. When the grant expires, at the end of this period, the user will no longer be able to use the credentials to log in. If they are logged in at the time, they will be automatically logged out.
    integer ValidForInMinutes = 4;
}

message TimeBasedAutoScalingConfiguration {
    // A <code>WeeklyAutoScalingSchedule</code> object with the instance schedule.
    WeeklyAutoScalingSchedule AutoScalingSchedule = 1;
    // The instance ID.
    string InstanceId = 2;
}

repeated TimeBasedAutoScalingConfiguration TimeBasedAutoScalingConfigurations = 1

message UnassignInstanceRequest {
    // The instance ID.
    string InstanceId = 1;
}

message UnassignVolumeRequest {
    // The volume ID.
    string VolumeId = 1;
}

message UntagResourceRequest {
    // The stack or layer's Amazon Resource Number (ARN).
    string ResourceArn = 1;
    // A list of the keys of tags to be removed from a stack or layer.
    array TagKeys = 2;
}

message UpdateAppRequest {
    // The app ID.
    string AppId = 1;
    // A <code>Source</code> object that specifies the app repository.
    Source AppSource = 2;
    // One or more user-defined key/value pairs to be added to the stack attributes.
    array Attributes = 3;
    // The app's data sources.
    array DataSources = 4;
    // A description of the app.
    string Description = 5;
    // The app's virtual host settings, with multiple domains separated by commas. For example: <code>'www.example.com, example.com'</code>
    array Domains = 6;
    // Whether SSL is enabled for the app.
    boolean EnableSsl = 7;
    // <p>An array of <code>EnvironmentVariable</code> objects that specify environment variables to be associated with the app. After you deploy the app, these variables are defined on the associated app server instances.For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workingapps-creating.html#workingapps-creating-environment"> Environment Variables</a>.</p> <p>There is no specific limit on the number of environment variables. However, the size of the associated data structure - which includes the variables' names, values, and protected flag values - cannot exceed 10 KB (10240 Bytes). This limit should accommodate most if not all use cases. Exceeding it will cause an exception with the message, "Environment: is too large (maximum is 10KB)."</p> <note> <p>This parameter is supported only by Chef 11.10 stacks. If you have specified one or more environment variables, you cannot modify the stack's Chef version.</p> </note>
    array Environment = 8;
    // The app name.
    string Name = 9;
    // An <code>SslConfiguration</code> object with the SSL configuration.
    SslConfiguration SslConfiguration = 10;
    // The app type.
    AppType Type = 11;
}

message UpdateElasticIpRequest {
    // The address.
    string ElasticIp = 1;
    // The new name.
    string Name = 2;
}

message UpdateInstanceRequest {
    // <p>The default AWS OpsWorks Stacks agent version. You have the following options:</p> <ul> <li> <p> <code>INHERIT</code> - Use the stack's default agent version setting.</p> </li> <li> <p> <i>version_number</i> - Use the specified agent version. This value overrides the stack's default setting. To update the agent version, you must edit the instance configuration and specify a new version. AWS OpsWorks Stacks then automatically installs that version on the instance.</p> </li> </ul> <p>The default setting is <code>INHERIT</code>. To specify an agent version, you must use the complete version number, not the abbreviated number shown on the console. For a list of available agent version numbers, call <a>DescribeAgentVersions</a>.</p> <p>AgentVersion cannot be set to Chef 12.2.</p>
    string AgentVersion = 1;
    // The ID of the AMI that was used to create the instance. The value of this parameter must be the same AMI ID that the instance is already using. You cannot apply a new AMI to an instance by running UpdateInstance. UpdateInstance does not work on instances that are using custom AMIs.
    string AmiId = 2;
    // The instance architecture. Instance types do not necessarily support both architectures. For a list of the architectures that are supported by the different instance types, see <a href="http://docs.aws.amazon.com/AWSEC2/latest/UserGuide/instance-types.html">Instance Families and Types</a>.
    Architecture Architecture = 3;
    // For load-based or time-based instances, the type. Windows stacks can use only time-based instances.
    AutoScalingType AutoScalingType = 4;
    // This property cannot be updated.
    boolean EbsOptimized = 5;
    // The instance host name.
    string Hostname = 6;
    // <p>Whether to install operating system and package updates when the instance boots. The default value is <code>true</code>. To control when updates are installed, set this value to <code>false</code>. You must then update your instances manually by using <a>CreateDeployment</a> to run the <code>update_dependencies</code> stack command or by manually running <code>yum</code> (Amazon Linux) or <code>apt-get</code> (Ubuntu) on the instances. </p> <note> <p>We strongly recommend using the default value of <code>true</code>, to ensure that your instances have the latest security updates.</p> </note>
    boolean InstallUpdatesOnBoot = 7;
    // The instance ID.
    string InstanceId = 8;
    // The instance type, such as <code>t2.micro</code>. For a list of supported instance types, open the stack in the console, choose <b>Instances</b>, and choose <b>+ Instance</b>. The <b>Size</b> list contains the currently supported types. For more information, see <a href="http://docs.aws.amazon.com/AWSEC2/latest/UserGuide/instance-types.html">Instance Families and Types</a>. The parameter values that you use to specify the various types are in the <b>API Name</b> column of the <b>Available Instance Types</b> table.
    string InstanceType = 9;
    // The instance's layer IDs.
    array LayerIds = 10;
    // <p>The instance's operating system, which must be set to one of the following. You cannot update an instance that is using a custom AMI.</p> <ul> <li> <p>A supported Linux operating system: An Amazon Linux version, such as <code>Amazon Linux 2017.03</code>, <code>Amazon Linux 2016.09</code>, <code>Amazon Linux 2016.03</code>, <code>Amazon Linux 2015.09</code>, or <code>Amazon Linux 2015.03</code>.</p> </li> <li> <p>A supported Ubuntu operating system, such as <code>Ubuntu 16.04 LTS</code>, <code>Ubuntu 14.04 LTS</code>, or <code>Ubuntu 12.04 LTS</code>.</p> </li> <li> <p> <code>CentOS Linux 7</code> </p> </li> <li> <p> <code>Red Hat Enterprise Linux 7</code> </p> </li> <li> <p>A supported Windows operating system, such as <code>Microsoft Windows Server 2012 R2 Base</code>, <code>Microsoft Windows Server 2012 R2 with SQL Server Express</code>, <code>Microsoft Windows Server 2012 R2 with SQL Server Standard</code>, or <code>Microsoft Windows Server 2012 R2 with SQL Server Web</code>.</p> </li> </ul> <p>For more information on the supported operating systems, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinginstances-os.html">AWS OpsWorks Stacks Operating Systems</a>.</p> <p>The default option is the current Amazon Linux version. If you set this parameter to <code>Custom</code>, you must use the AmiId parameter to specify the custom AMI that you want to use. For more information on the supported operating systems, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinginstances-os.html">Operating Systems</a>. For more information on how to use custom AMIs with OpsWorks, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinginstances-custom-ami.html">Using Custom AMIs</a>.</p> <note> <p>You can specify a different Linux operating system for the updated stack, but you cannot change from Linux to Windows or Windows to Linux.</p> </note>
    string Os = 11;
    // The instance's Amazon EC2 key name.
    string SshKeyName = 12;
}

message UpdateLayerRequest {
    // One or more user-defined key/value pairs to be added to the stack attributes.
    array Attributes = 1;
    // Whether to automatically assign an <a href="http://docs.aws.amazon.com/AWSEC2/latest/UserGuide/elastic-ip-addresses-eip.html">Elastic IP address</a> to the layer's instances. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinglayers-basics-edit.html">How to Edit a Layer</a>.
    boolean AutoAssignElasticIps = 2;
    // For stacks that are running in a VPC, whether to automatically assign a public IP address to the layer's instances. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinglayers-basics-edit.html">How to Edit a Layer</a>.
    boolean AutoAssignPublicIps = 3;
    // Specifies CloudWatch Logs configuration options for the layer. For more information, see <a>CloudWatchLogsLogStream</a>.
    CloudWatchLogsConfiguration CloudWatchLogsConfiguration = 4;
    // The ARN of an IAM profile to be used for all of the layer's EC2 instances. For more information about IAM ARNs, see <a href="http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_Identifiers.html">Using Identifiers</a>.
    string CustomInstanceProfileArn = 5;
    // A JSON-formatted string containing custom stack configuration and deployment attributes to be installed on the layer's instances. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workingcookbook-json-override.html"> Using Custom JSON</a>.
    string CustomJson = 6;
    // A <code>LayerCustomRecipes</code> object that specifies the layer's custom recipes.
    Recipes CustomRecipes = 7;
    // An array containing the layer's custom security group IDs.
    array CustomSecurityGroupIds = 8;
    // Whether to disable auto healing for the layer.
    boolean EnableAutoHealing = 9;
    // <p>Whether to install operating system and package updates when the instance boots. The default value is <code>true</code>. To control when updates are installed, set this value to <code>false</code>. You must then update your instances manually by using <a>CreateDeployment</a> to run the <code>update_dependencies</code> stack command or manually running <code>yum</code> (Amazon Linux) or <code>apt-get</code> (Ubuntu) on the instances. </p> <note> <p>We strongly recommend using the default value of <code>true</code>, to ensure that your instances have the latest security updates.</p> </note>
    boolean InstallUpdatesOnBoot = 10;
    // The layer ID.
    string LayerId = 11;
    // <p/>
    LifecycleEventConfiguration LifecycleEventConfiguration = 12;
    // The layer name, which is used by the console.
    string Name = 13;
    // An array of <code>Package</code> objects that describe the layer's packages.
    array Packages = 14;
    // <p>For custom layers only, use this parameter to specify the layer's short name, which is used internally by AWS OpsWorks Stacks and by Chef. The short name is also used as the name for the directory where your app files are installed. It can have a maximum of 200 characters and must be in the following format: /\A[a-z0-9\-\_\.]+\Z/.</p> <p>The built-in layers' short names are defined by AWS OpsWorks Stacks. For more information, see the <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/layers.html">Layer Reference</a> </p>
    string Shortname = 15;
    // Whether to use Amazon EBS-optimized instances.
    boolean UseEbsOptimizedInstances = 16;
    // A <code>VolumeConfigurations</code> object that describes the layer's Amazon EBS volumes.
    array VolumeConfigurations = 17;
}

message UpdateMyUserProfileRequest {
    // The user's SSH public key.
    string SshPublicKey = 1;
}

message UpdateRdsDbInstanceRequest {
    // The database password.
    string DbPassword = 1;
    // The master user name.
    string DbUser = 2;
    // The Amazon RDS instance's ARN.
    string RdsDbInstanceArn = 3;
}

message UpdateStackRequest {
    // <p>The default AWS OpsWorks Stacks agent version. You have the following options:</p> <ul> <li> <p>Auto-update - Set this parameter to <code>LATEST</code>. AWS OpsWorks Stacks automatically installs new agent versions on the stack's instances as soon as they are available.</p> </li> <li> <p>Fixed version - Set this parameter to your preferred agent version. To update the agent version, you must edit the stack configuration and specify a new version. AWS OpsWorks Stacks then automatically installs that version on the stack's instances.</p> </li> </ul> <p>The default setting is <code>LATEST</code>. To specify an agent version, you must use the complete version number, not the abbreviated number shown on the console. For a list of available agent version numbers, call <a>DescribeAgentVersions</a>. AgentVersion cannot be set to Chef 12.2.</p> <note> <p>You can also specify an agent version when you create or update an instance, which overrides the stack's default setting.</p> </note>
    string AgentVersion = 1;
    // One or more user-defined key-value pairs to be added to the stack attributes.
    array Attributes = 2;
    // A <code>ChefConfiguration</code> object that specifies whether to enable Berkshelf and the Berkshelf version on Chef 11.10 stacks. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workingstacks-creating.html">Create a New Stack</a>.
    ChefConfiguration ChefConfiguration = 3;
    // The configuration manager. When you update a stack, we recommend that you use the configuration manager to specify the Chef version: 12, 11.10, or 11.4 for Linux stacks, or 12.2 for Windows stacks. The default value for Linux stacks is currently 11.4.
    StackConfigurationManager ConfigurationManager = 4;
    Source CustomCookbooksSource = 5;
    // <p>A string that contains user-defined, custom JSON. It can be used to override the corresponding default stack configuration JSON values or to pass data to recipes. The string should be in the following format:</p> <p> <code>"{\"key1\": \"value1\", \"key2\": \"value2\",...}"</code> </p> <p>For more information on custom JSON, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workingstacks-json.html">Use Custom JSON to Modify the Stack Configuration Attributes</a>.</p>
    string CustomJson = 6;
    // The stack's default Availability Zone, which must be in the stack's region. For more information, see <a href="http://docs.aws.amazon.com/general/latest/gr/rande.html">Regions and Endpoints</a>. If you also specify a value for <code>DefaultSubnetId</code>, the subnet must be in the same zone. For more information, see <a>CreateStack</a>.
    string DefaultAvailabilityZone = 7;
    // The ARN of an IAM profile that is the default profile for all of the stack's EC2 instances. For more information about IAM ARNs, see <a href="http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_Identifiers.html">Using Identifiers</a>.
    string DefaultInstanceProfileArn = 8;
    // <p>The stack's operating system, which must be set to one of the following:</p> <ul> <li> <p>A supported Linux operating system: An Amazon Linux version, such as <code>Amazon Linux 2017.03</code>, <code>Amazon Linux 2016.09</code>, <code>Amazon Linux 2016.03</code>, <code>Amazon Linux 2015.09</code>, or <code>Amazon Linux 2015.03</code>.</p> </li> <li> <p>A supported Ubuntu operating system, such as <code>Ubuntu 16.04 LTS</code>, <code>Ubuntu 14.04 LTS</code>, or <code>Ubuntu 12.04 LTS</code>.</p> </li> <li> <p> <code>CentOS Linux 7</code> </p> </li> <li> <p> <code>Red Hat Enterprise Linux 7</code> </p> </li> <li> <p>A supported Windows operating system, such as <code>Microsoft Windows Server 2012 R2 Base</code>, <code>Microsoft Windows Server 2012 R2 with SQL Server Express</code>, <code>Microsoft Windows Server 2012 R2 with SQL Server Standard</code>, or <code>Microsoft Windows Server 2012 R2 with SQL Server Web</code>.</p> </li> <li> <p>A custom AMI: <code>Custom</code>. You specify the custom AMI you want to use when you create instances. For more information on how to use custom AMIs with OpsWorks, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinginstances-custom-ami.html">Using Custom AMIs</a>.</p> </li> </ul> <p>The default option is the stack's current operating system. For more information on the supported operating systems, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinginstances-os.html">AWS OpsWorks Stacks Operating Systems</a>.</p>
    string DefaultOs = 9;
    // The default root device type. This value is used by default for all instances in the stack, but you can override it when you create an instance. For more information, see <a href="http://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ComponentsAMIs.html#storage-for-the-root-device">Storage for the Root Device</a>.
    RootDeviceType DefaultRootDeviceType = 10;
    // A default Amazon EC2 key-pair name. The default value is <code>none</code>. If you specify a key-pair name, AWS OpsWorks Stacks installs the public key on the instance and you can use the private key with an SSH client to log in to the instance. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinginstances-ssh.html"> Using SSH to Communicate with an Instance</a> and <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/security-ssh-access.html"> Managing SSH Access</a>. You can override this setting by specifying a different key pair, or no key pair, when you <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinginstances-add.html"> create an instance</a>.
    string DefaultSshKeyName = 11;
    // The stack's default VPC subnet ID. This parameter is required if you specify a value for the <code>VpcId</code> parameter. All instances are launched into this subnet unless you specify otherwise when you create the instance. If you also specify a value for <code>DefaultAvailabilityZone</code>, the subnet must be in that zone. For information on default values and when this parameter is required, see the <code>VpcId</code> parameter description.
    string DefaultSubnetId = 12;
    // <p>The stack's new host name theme, with spaces replaced by underscores. The theme is used to generate host names for the stack's instances. By default, <code>HostnameTheme</code> is set to <code>Layer_Dependent</code>, which creates host names by appending integers to the layer's short name. The other themes are:</p> <ul> <li> <p> <code>Baked_Goods</code> </p> </li> <li> <p> <code>Clouds</code> </p> </li> <li> <p> <code>Europe_Cities</code> </p> </li> <li> <p> <code>Fruits</code> </p> </li> <li> <p> <code>Greek_Deities</code> </p> </li> <li> <p> <code>Legendary_creatures_from_Japan</code> </p> </li> <li> <p> <code>Planets_and_Moons</code> </p> </li> <li> <p> <code>Roman_Deities</code> </p> </li> <li> <p> <code>Scottish_Islands</code> </p> </li> <li> <p> <code>US_Cities</code> </p> </li> <li> <p> <code>Wild_Cats</code> </p> </li> </ul> <p>To obtain a generated host name, call <code>GetHostNameSuggestion</code>, which returns a host name based on the current theme.</p>
    string HostnameTheme = 13;
    // The stack's new name.
    string Name = 14;
    // Do not use this parameter. You cannot update a stack's service role.
    string ServiceRoleArn = 15;
    // The stack ID.
    string StackId = 16;
    // Whether the stack uses custom cookbooks.
    boolean UseCustomCookbooks = 17;
    // <p>Whether to associate the AWS OpsWorks Stacks built-in security groups with the stack's layers.</p> <p>AWS OpsWorks Stacks provides a standard set of built-in security groups, one for each layer, which are associated with layers by default. <code>UseOpsworksSecurityGroups</code> allows you to provide your own custom security groups instead of using the built-in groups. <code>UseOpsworksSecurityGroups</code> has the following settings: </p> <ul> <li> <p>True - AWS OpsWorks Stacks automatically associates the appropriate built-in security group with each layer (default setting). You can associate additional security groups with a layer after you create it, but you cannot delete the built-in security group.</p> </li> <li> <p>False - AWS OpsWorks Stacks does not associate built-in security groups with layers. You must create appropriate EC2 security groups and associate a security group with each layer that you create. However, you can still manually associate a built-in security group with a layer on. Custom security groups are required only for those layers that need custom settings.</p> </li> </ul> <p>For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workingstacks-creating.html">Create a New Stack</a>.</p>
    boolean UseOpsworksSecurityGroups = 18;
}

message UpdateUserProfileRequest {
    // Whether users can specify their own SSH public key through the My Settings page. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/security-settingsshkey.html">Managing User Permissions</a>.
    boolean AllowSelfManagement = 1;
    // The user IAM ARN. This can also be a federated user's ARN.
    string IamUserArn = 2;
    // The user's new SSH public key.
    string SshPublicKey = 3;
    // The user's SSH user name. The allowable characters are [a-z], [A-Z], [0-9], '-', and '_'. If the specified name includes other punctuation marks, AWS OpsWorks Stacks removes them. For example, <code>my.name</code> will be changed to <code>myname</code>. If you do not specify an SSH user name, AWS OpsWorks Stacks generates one from the IAM user name.
    string SshUsername = 4;
}

message UpdateVolumeRequest {
    // The new mount point.
    string MountPoint = 1;
    // The new name.
    string Name = 2;
    // The volume ID.
    string VolumeId = 3;
}

message UserProfile {
    // Whether users can specify their own SSH public key through the My Settings page. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/security-settingsshkey.html">Managing User Permissions</a>.
    boolean AllowSelfManagement = 1;
    // The user's IAM ARN.
    string IamUserArn = 2;
    // The user's name.
    string Name = 3;
    // The user's SSH public key.
    string SshPublicKey = 4;
    // The user's SSH user name.
    string SshUsername = 5;
}

repeated UserProfile UserProfiles = 1



message ValidationException {
    // The exception message.
    string message = 1;
}

enum VirtualizationType {
    PARAVIRTUAL = 0;
    HVM = 1;
}

message Volume {
    // The volume Availability Zone. For more information, see <a href="http://docs.aws.amazon.com/general/latest/gr/rande.html">Regions and Endpoints</a>.
    string AvailabilityZone = 1;
    // The device name.
    string Device = 2;
    // The Amazon EC2 volume ID.
    string Ec2VolumeId = 3;
    // The instance ID.
    string InstanceId = 4;
    // For PIOPS volumes, the IOPS per disk.
    integer Iops = 5;
    // The volume mount point. For example, "/mnt/disk1".
    string MountPoint = 6;
    // The volume name.
    string Name = 7;
    // The RAID array ID.
    string RaidArrayId = 8;
    // The AWS region. For more information about AWS regions, see <a href="http://docs.aws.amazon.com/general/latest/gr/rande.html">Regions and Endpoints</a>.
    string Region = 9;
    // The volume size.
    integer Size = 10;
    // The value returned by <a href="http://docs.aws.amazon.com/AWSEC2/latest/APIReference/ApiReference-query-DescribeVolumes.html">DescribeVolumes</a>.
    string Status = 11;
    // The volume ID.
    string VolumeId = 12;
    // The volume type, standard or PIOPS.
    string VolumeType = 13;
}

message VolumeConfiguration {
    // For PIOPS volumes, the IOPS per disk.
    integer Iops = 1;
    // The volume mount point. For example "/dev/sdh".
    string MountPoint = 2;
    // The number of disks in the volume.
    integer NumberOfDisks = 3;
    // The volume <a href="http://en.wikipedia.org/wiki/Standard_RAID_levels">RAID level</a>.
    integer RaidLevel = 4;
    // The volume size.
    integer Size = 5;
    // <p>The volume type:</p> <ul> <li> <p> <code>standard</code> - Magnetic</p> </li> <li> <p> <code>io1</code> - Provisioned IOPS (SSD)</p> </li> <li> <p> <code>gp2</code> - General Purpose (SSD)</p> </li> </ul>
    string VolumeType = 6;
}

repeated VolumeConfiguration VolumeConfigurations = 1

enum VolumeType {
    GP2 = 0;
    IO1 = 1;
    STANDARD = 2;
}

repeated Volume Volumes = 1

message WeeklyAutoScalingSchedule {
    // The schedule for Friday.
    array Friday = 1;
    // The schedule for Monday.
    array Monday = 2;
    // The schedule for Saturday.
    array Saturday = 3;
    // The schedule for Sunday.
    array Sunday = 4;
    // The schedule for Thursday.
    array Thursday = 5;
    // The schedule for Tuesday.
    array Tuesday = 6;
    // The schedule for Wednesday.
    array Wednesday = 7;
}

service AWSOpsWorksService {
    // <p>Assign a registered instance to a layer.</p> <ul> <li> <p>You can assign registered on-premises instances to any layer type.</p> </li> <li> <p>You can assign registered Amazon EC2 instances only to custom layers.</p> </li> <li> <p>You cannot use this action with instances that were created with AWS OpsWorks Stacks.</p> </li> </ul> <p> <b>Required Permissions</b>: To use this action, an AWS Identity and Access Management (IAM) user must have a Manage permissions level for the stack or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post(PostRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//"
        body: "body"
      };
    }
    // <p>Assigns one of the stack's registered Amazon EBS volumes to a specified instance. The volume must first be registered with the stack by calling <a>RegisterVolume</a>. After you register the volume, you must call <a>UpdateVolume</a> to specify a mount point before calling <code>AssignVolume</code>. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/resources.html">Resource Management</a>.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#AssignVolume(Post#AssignVolumeRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#AssignVolume"
        body: "body"
      };
    }
    // <p>Associates one of the stack's registered Elastic IP addresses with a specified instance. The address must first be registered with the stack by calling <a>RegisterElasticIp</a>. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/resources.html">Resource Management</a>.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#AssociateElasticIp(Post#AssociateElasticIpRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#AssociateElasticIp"
        body: "body"
      };
    }
    // <p>Attaches an Elastic Load Balancing load balancer to a specified layer. AWS OpsWorks Stacks does not support Application Load Balancer. You can only use Classic Load Balancer with AWS OpsWorks Stacks. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/layers-elb.html">Elastic Load Balancing</a>.</p> <note> <p>You must create the Elastic Load Balancing instance separately, by using the Elastic Load Balancing console, API, or CLI. For more information, see <a href="http://docs.aws.amazon.com/ElasticLoadBalancing/latest/DeveloperGuide/Welcome.html"> Elastic Load Balancing Developer Guide</a>.</p> </note> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#AttachElasticLoadBalancer(Post#AttachElasticLoadBalancerRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#AttachElasticLoadBalancer"
        body: "body"
      };
    }
    // <p>Creates a clone of a specified stack. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workingstacks-cloning.html">Clone a Stack</a>. By default, all parameters are set to the values used by the parent stack.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#CloneStack(Post#CloneStackRequest) returns (CloneStackResult) {
      option (google.api.http) = {
        post: "//#CloneStack"
        body: "body"
      };
    }
    // <p>Creates an app for a specified stack. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workingapps-creating.html">Creating Apps</a>.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#CreateApp(Post#CreateAppRequest) returns (CreateAppResult) {
      option (google.api.http) = {
        post: "//#CreateApp"
        body: "body"
      };
    }
    // <p>Runs deployment or stack commands. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workingapps-deploying.html">Deploying Apps</a> and <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workingstacks-commands.html">Run Stack Commands</a>.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Deploy or Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#CreateDeployment(Post#CreateDeploymentRequest) returns (CreateDeploymentResult) {
      option (google.api.http) = {
        post: "//#CreateDeployment"
        body: "body"
      };
    }
    // <p>Creates an instance in a specified stack. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinginstances-add.html">Adding an Instance to a Layer</a>.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#CreateInstance(Post#CreateInstanceRequest) returns (CreateInstanceResult) {
      option (google.api.http) = {
        post: "//#CreateInstance"
        body: "body"
      };
    }
    // <p>Creates a layer. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinglayers-basics-create.html">How to Create a Layer</a>.</p> <note> <p>You should use <b>CreateLayer</b> for noncustom layer types such as PHP App Server only if the stack does not have an existing layer of that type. A stack can have at most one instance of each noncustom layer; if you attempt to create a second instance, <b>CreateLayer</b> fails. A stack can have an arbitrary number of custom layers, so you can call <b>CreateLayer</b> as many times as you like for that layer type.</p> </note> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#CreateLayer(Post#CreateLayerRequest) returns (CreateLayerResult) {
      option (google.api.http) = {
        post: "//#CreateLayer"
        body: "body"
      };
    }
    // <p>Creates a new stack. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workingstacks-edit.html">Create a New Stack</a>.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#CreateStack(Post#CreateStackRequest) returns (CreateStackResult) {
      option (google.api.http) = {
        post: "//#CreateStack"
        body: "body"
      };
    }
    // <p>Creates a new user profile.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#CreateUserProfile(Post#CreateUserProfileRequest) returns (CreateUserProfileResult) {
      option (google.api.http) = {
        post: "//#CreateUserProfile"
        body: "body"
      };
    }
    // <p>Deletes a specified app.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DeleteApp(Post#DeleteAppRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#DeleteApp"
        body: "body"
      };
    }
    // <p>Deletes a specified instance, which terminates the associated Amazon EC2 instance. You must stop an instance before you can delete it.</p> <p>For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinginstances-delete.html">Deleting Instances</a>.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DeleteInstance(Post#DeleteInstanceRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#DeleteInstance"
        body: "body"
      };
    }
    // <p>Deletes a specified layer. You must first stop and then delete all associated instances or unassign registered instances. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinglayers-basics-delete.html">How to Delete a Layer</a>.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DeleteLayer(Post#DeleteLayerRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#DeleteLayer"
        body: "body"
      };
    }
    // <p>Deletes a specified stack. You must first delete all instances, layers, and apps or deregister registered instances. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workingstacks-shutting.html">Shut Down a Stack</a>.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DeleteStack(Post#DeleteStackRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#DeleteStack"
        body: "body"
      };
    }
    // <p>Deletes a user profile.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DeleteUserProfile(Post#DeleteUserProfileRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#DeleteUserProfile"
        body: "body"
      };
    }
    // <p>Deregisters a specified Amazon ECS cluster from a stack. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinglayers-ecscluster.html#workinglayers-ecscluster-delete"> Resource Management</a>.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html</a>.</p>
    rpc Post#DeregisterEcsCluster(Post#DeregisterEcsClusterRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#DeregisterEcsCluster"
        body: "body"
      };
    }
    // <p>Deregisters a specified Elastic IP address. The address can then be registered by another stack. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/resources.html">Resource Management</a>.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DeregisterElasticIp(Post#DeregisterElasticIpRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#DeregisterElasticIp"
        body: "body"
      };
    }
    // <p>Deregister a registered Amazon EC2 or on-premises instance. This action removes the instance from the stack and returns it to your control. This action can not be used with instances that were created with AWS OpsWorks Stacks.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DeregisterInstance(Post#DeregisterInstanceRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#DeregisterInstance"
        body: "body"
      };
    }
    // <p>Deregisters an Amazon RDS instance.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DeregisterRdsDbInstance(Post#DeregisterRdsDbInstanceRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#DeregisterRdsDbInstance"
        body: "body"
      };
    }
    // <p>Deregisters an Amazon EBS volume. The volume can then be registered by another stack. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/resources.html">Resource Management</a>.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DeregisterVolume(Post#DeregisterVolumeRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#DeregisterVolume"
        body: "body"
      };
    }
    // Describes the available AWS OpsWorks Stacks agent versions. You must specify a stack ID or a configuration manager. <code>DescribeAgentVersions</code> returns a list of available agent versions for the specified stack or configuration manager.
    rpc Post#DescribeAgentVersions(Post#DescribeAgentVersionsRequest) returns (DescribeAgentVersionsResult) {
      option (google.api.http) = {
        post: "//#DescribeAgentVersions"
        body: "body"
      };
    }
    // <p>Requests a description of a specified set of apps.</p> <note> <p>This call accepts only one resource-identifying parameter.</p> </note> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Show, Deploy, or Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DescribeApps(Post#DescribeAppsRequest) returns (DescribeAppsResult) {
      option (google.api.http) = {
        post: "//#DescribeApps"
        body: "body"
      };
    }
    // <p>Describes the results of specified commands.</p> <note> <p>This call accepts only one resource-identifying parameter.</p> </note> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Show, Deploy, or Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DescribeCommands(Post#DescribeCommandsRequest) returns (DescribeCommandsResult) {
      option (google.api.http) = {
        post: "//#DescribeCommands"
        body: "body"
      };
    }
    // <p>Requests a description of a specified set of deployments.</p> <note> <p>This call accepts only one resource-identifying parameter.</p> </note> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Show, Deploy, or Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DescribeDeployments(Post#DescribeDeploymentsRequest) returns (DescribeDeploymentsResult) {
      option (google.api.http) = {
        post: "//#DescribeDeployments"
        body: "body"
      };
    }
    // <p>Describes Amazon ECS clusters that are registered with a stack. If you specify only a stack ID, you can use the <code>MaxResults</code> and <code>NextToken</code> parameters to paginate the response. However, AWS OpsWorks Stacks currently supports only one cluster per layer, so the result set has a maximum of one element.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Show, Deploy, or Manage permissions level for the stack or an attached policy that explicitly grants permission. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p> <p>This call accepts only one resource-identifying parameter.</p>
    rpc Post#DescribeEcsClusters(Post#DescribeEcsClustersRequest) returns (DescribeEcsClustersResult) {
      option (google.api.http) = {
        post: "//#DescribeEcsClusters"
        body: "body"
      };
    }
    // <p>Describes <a href="http://docs.aws.amazon.com/AWSEC2/latest/UserGuide/elastic-ip-addresses-eip.html">Elastic IP addresses</a>.</p> <note> <p>This call accepts only one resource-identifying parameter.</p> </note> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Show, Deploy, or Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DescribeElasticIps(Post#DescribeElasticIpsRequest) returns (DescribeElasticIpsResult) {
      option (google.api.http) = {
        post: "//#DescribeElasticIps"
        body: "body"
      };
    }
    // <p>Describes a stack's Elastic Load Balancing instances.</p> <note> <p>This call accepts only one resource-identifying parameter.</p> </note> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Show, Deploy, or Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DescribeElasticLoadBalancers(Post#DescribeElasticLoadBalancersRequest) returns (DescribeElasticLoadBalancersResult) {
      option (google.api.http) = {
        post: "//#DescribeElasticLoadBalancers"
        body: "body"
      };
    }
    // <p>Requests a description of a set of instances.</p> <note> <p>This call accepts only one resource-identifying parameter.</p> </note> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Show, Deploy, or Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DescribeInstances(Post#DescribeInstancesRequest) returns (DescribeInstancesResult) {
      option (google.api.http) = {
        post: "//#DescribeInstances"
        body: "body"
      };
    }
    // <p>Requests a description of one or more layers in a specified stack.</p> <note> <p>This call accepts only one resource-identifying parameter.</p> </note> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Show, Deploy, or Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DescribeLayers(Post#DescribeLayersRequest) returns (DescribeLayersResult) {
      option (google.api.http) = {
        post: "//#DescribeLayers"
        body: "body"
      };
    }
    // <p>Describes load-based auto scaling configurations for specified layers.</p> <note> <p>You must specify at least one of the parameters.</p> </note> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Show, Deploy, or Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DescribeLoadBasedAutoScaling(Post#DescribeLoadBasedAutoScalingRequest) returns (DescribeLoadBasedAutoScalingResult) {
      option (google.api.http) = {
        post: "//#DescribeLoadBasedAutoScaling"
        body: "body"
      };
    }
    // <p>Describes a user's SSH information.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have self-management enabled or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DescribeMyUserProfile(Post#DescribeMyUserProfileRequest) returns (DescribeMyUserProfileResult) {
      option (google.api.http) = {
        post: "//#DescribeMyUserProfile"
      };
    }
    // <p>Describes the permissions for a specified stack.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DescribePermissions(Post#DescribePermissionsRequest) returns (DescribePermissionsResult) {
      option (google.api.http) = {
        post: "//#DescribePermissions"
        body: "body"
      };
    }
    // <p>Describe an instance's RAID arrays.</p> <note> <p>This call accepts only one resource-identifying parameter.</p> </note> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Show, Deploy, or Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DescribeRaidArrays(Post#DescribeRaidArraysRequest) returns (DescribeRaidArraysResult) {
      option (google.api.http) = {
        post: "//#DescribeRaidArrays"
        body: "body"
      };
    }
    // <p>Describes Amazon RDS instances.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Show, Deploy, or Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p> <p>This call accepts only one resource-identifying parameter.</p>
    rpc Post#DescribeRdsDbInstances(Post#DescribeRdsDbInstancesRequest) returns (DescribeRdsDbInstancesResult) {
      option (google.api.http) = {
        post: "//#DescribeRdsDbInstances"
        body: "body"
      };
    }
    // <p>Describes AWS OpsWorks Stacks service errors.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Show, Deploy, or Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p> <p>This call accepts only one resource-identifying parameter.</p>
    rpc Post#DescribeServiceErrors(Post#DescribeServiceErrorsRequest) returns (DescribeServiceErrorsResult) {
      option (google.api.http) = {
        post: "//#DescribeServiceErrors"
        body: "body"
      };
    }
    // <p>Requests a description of a stack's provisioning parameters.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Show, Deploy, or Manage permissions level for the stack or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DescribeStackProvisioningParameters(Post#DescribeStackProvisioningParametersRequest) returns (DescribeStackProvisioningParametersResult) {
      option (google.api.http) = {
        post: "//#DescribeStackProvisioningParameters"
        body: "body"
      };
    }
    // <p>Describes the number of layers and apps in a specified stack, and the number of instances in each state, such as <code>running_setup</code> or <code>online</code>.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Show, Deploy, or Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DescribeStackSummary(Post#DescribeStackSummaryRequest) returns (DescribeStackSummaryResult) {
      option (google.api.http) = {
        post: "//#DescribeStackSummary"
        body: "body"
      };
    }
    // <p>Requests a description of one or more stacks.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Show, Deploy, or Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DescribeStacks(Post#DescribeStacksRequest) returns (DescribeStacksResult) {
      option (google.api.http) = {
        post: "//#DescribeStacks"
        body: "body"
      };
    }
    // <p>Describes time-based auto scaling configurations for specified instances.</p> <note> <p>You must specify at least one of the parameters.</p> </note> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Show, Deploy, or Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DescribeTimeBasedAutoScaling(Post#DescribeTimeBasedAutoScalingRequest) returns (DescribeTimeBasedAutoScalingResult) {
      option (google.api.http) = {
        post: "//#DescribeTimeBasedAutoScaling"
        body: "body"
      };
    }
    // <p>Describe specified users.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DescribeUserProfiles(Post#DescribeUserProfilesRequest) returns (DescribeUserProfilesResult) {
      option (google.api.http) = {
        post: "//#DescribeUserProfiles"
        body: "body"
      };
    }
    // <p>Describes an instance's Amazon EBS volumes.</p> <note> <p>This call accepts only one resource-identifying parameter.</p> </note> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Show, Deploy, or Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DescribeVolumes(Post#DescribeVolumesRequest) returns (DescribeVolumesResult) {
      option (google.api.http) = {
        post: "//#DescribeVolumes"
        body: "body"
      };
    }
    // <p>Detaches a specified Elastic Load Balancing instance from its layer.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DetachElasticLoadBalancer(Post#DetachElasticLoadBalancerRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#DetachElasticLoadBalancer"
        body: "body"
      };
    }
    // <p>Disassociates an Elastic IP address from its instance. The address remains registered with the stack. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/resources.html">Resource Management</a>.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#DisassociateElasticIp(Post#DisassociateElasticIpRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#DisassociateElasticIp"
        body: "body"
      };
    }
    // <p>Gets a generated host name for the specified layer, based on the current host name theme.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#GetHostnameSuggestion(Post#GetHostnameSuggestionRequest) returns (GetHostnameSuggestionResult) {
      option (google.api.http) = {
        post: "//#GetHostnameSuggestion"
        body: "body"
      };
    }
    // <note> <p>This action can be used only with Windows stacks.</p> </note> <p>Grants RDP access to a Windows instance for a specified time period.</p>
    rpc Post#GrantAccess(Post#GrantAccessRequest) returns (GrantAccessResult) {
      option (google.api.http) = {
        post: "//#GrantAccess"
        body: "body"
      };
    }
    // Returns a list of tags that are applied to the specified stack or layer.
    rpc Post#ListTags(Post#ListTagsRequest) returns (ListTagsResult) {
      option (google.api.http) = {
        post: "//#ListTags"
        body: "body"
      };
    }
    // <p>Reboots a specified instance. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinginstances-starting.html">Starting, Stopping, and Rebooting Instances</a>.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#RebootInstance(Post#RebootInstanceRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#RebootInstance"
        body: "body"
      };
    }
    // <p>Registers a specified Amazon ECS cluster with a stack. You can register only one cluster with a stack. A cluster can be registered with only one stack. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinglayers-ecscluster.html"> Resource Management</a>.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html"> Managing User Permissions</a>.</p>
    rpc Post#RegisterEcsCluster(Post#RegisterEcsClusterRequest) returns (RegisterEcsClusterResult) {
      option (google.api.http) = {
        post: "//#RegisterEcsCluster"
        body: "body"
      };
    }
    // <p>Registers an Elastic IP address with a specified stack. An address can be registered with only one stack at a time. If the address is already registered, you must first deregister it by calling <a>DeregisterElasticIp</a>. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/resources.html">Resource Management</a>.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#RegisterElasticIp(Post#RegisterElasticIpRequest) returns (RegisterElasticIpResult) {
      option (google.api.http) = {
        post: "//#RegisterElasticIp"
        body: "body"
      };
    }
    // <p>Registers instances that were created outside of AWS OpsWorks Stacks with a specified stack.</p> <note> <p>We do not recommend using this action to register instances. The complete registration operation includes two tasks: installing the AWS OpsWorks Stacks agent on the instance, and registering the instance with the stack. <code>RegisterInstance</code> handles only the second step. You should instead use the AWS CLI <code>register</code> command, which performs the entire registration operation. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/registered-instances-register.html"> Registering an Instance with an AWS OpsWorks Stacks Stack</a>.</p> </note> <p>Registered instances have the same requirements as instances that are created by using the <a>CreateInstance</a> API. For example, registered instances must be running a supported Linux-based operating system, and they must have a supported instance type. For more information about requirements for instances that you want to register, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/registered-instances-register-registering-preparer.html"> Preparing the Instance</a>.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#RegisterInstance(Post#RegisterInstanceRequest) returns (RegisterInstanceResult) {
      option (google.api.http) = {
        post: "//#RegisterInstance"
        body: "body"
      };
    }
    // <p>Registers an Amazon RDS instance with a stack.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#RegisterRdsDbInstance(Post#RegisterRdsDbInstanceRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#RegisterRdsDbInstance"
        body: "body"
      };
    }
    // <p>Registers an Amazon EBS volume with a specified stack. A volume can be registered with only one stack at a time. If the volume is already registered, you must first deregister it by calling <a>DeregisterVolume</a>. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/resources.html">Resource Management</a>.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#RegisterVolume(Post#RegisterVolumeRequest) returns (RegisterVolumeResult) {
      option (google.api.http) = {
        post: "//#RegisterVolume"
        body: "body"
      };
    }
    // <p>Specify the load-based auto scaling configuration for a specified layer. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinginstances-autoscaling.html">Managing Load with Time-based and Load-based Instances</a>.</p> <note> <p>To use load-based auto scaling, you must create a set of load-based auto scaling instances. Load-based auto scaling operates only on the instances from that set, so you must ensure that you have created enough instances to handle the maximum anticipated load.</p> </note> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#SetLoadBasedAutoScaling(Post#SetLoadBasedAutoScalingRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#SetLoadBasedAutoScaling"
        body: "body"
      };
    }
    // <p>Specifies a user's permissions. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workingsecurity.html">Security and Permissions</a>.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#SetPermission(Post#SetPermissionRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#SetPermission"
        body: "body"
      };
    }
    // <p>Specify the time-based auto scaling configuration for a specified instance. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinginstances-autoscaling.html">Managing Load with Time-based and Load-based Instances</a>.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#SetTimeBasedAutoScaling(Post#SetTimeBasedAutoScalingRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#SetTimeBasedAutoScaling"
        body: "body"
      };
    }
    // <p>Starts a specified instance. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinginstances-starting.html">Starting, Stopping, and Rebooting Instances</a>.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#StartInstance(Post#StartInstanceRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#StartInstance"
        body: "body"
      };
    }
    // <p>Starts a stack's instances.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#StartStack(Post#StartStackRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#StartStack"
        body: "body"
      };
    }
    // <p>Stops a specified instance. When you stop a standard instance, the data disappears and must be reinstalled when you restart the instance. You can stop an Amazon EBS-backed instance without losing data. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/workinginstances-starting.html">Starting, Stopping, and Rebooting Instances</a>.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#StopInstance(Post#StopInstanceRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#StopInstance"
        body: "body"
      };
    }
    // <p>Stops a specified stack.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#StopStack(Post#StopStackRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#StopStack"
        body: "body"
      };
    }
    // Apply cost-allocation tags to a specified stack or layer in AWS OpsWorks Stacks. For more information about how tagging works, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/tagging.html">Tags</a> in the AWS OpsWorks User Guide.
    rpc Post#TagResource(Post#TagResourceRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#TagResource"
        body: "body"
      };
    }
    // <p>Unassigns a registered instance from all of it's layers. The instance remains in the stack as an unassigned instance and can be assigned to another layer, as needed. You cannot use this action with instances that were created with AWS OpsWorks Stacks.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#UnassignInstance(Post#UnassignInstanceRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#UnassignInstance"
        body: "body"
      };
    }
    // <p>Unassigns an assigned Amazon EBS volume. The volume remains registered with the stack. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/resources.html">Resource Management</a>.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#UnassignVolume(Post#UnassignVolumeRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#UnassignVolume"
        body: "body"
      };
    }
    // Removes tags from a specified stack or layer.
    rpc Post#UntagResource(Post#UntagResourceRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#UntagResource"
        body: "body"
      };
    }
    // <p>Updates a specified app.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Deploy or Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#UpdateApp(Post#UpdateAppRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#UpdateApp"
        body: "body"
      };
    }
    // <p>Updates a registered Elastic IP address's name. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/resources.html">Resource Management</a>.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#UpdateElasticIp(Post#UpdateElasticIpRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#UpdateElasticIp"
        body: "body"
      };
    }
    // <p>Updates a specified instance.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#UpdateInstance(Post#UpdateInstanceRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#UpdateInstance"
        body: "body"
      };
    }
    // <p>Updates a specified layer.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#UpdateLayer(Post#UpdateLayerRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#UpdateLayer"
        body: "body"
      };
    }
    // <p>Updates a user's SSH public key.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have self-management enabled or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#UpdateMyUserProfile(Post#UpdateMyUserProfileRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#UpdateMyUserProfile"
        body: "body"
      };
    }
    // <p>Updates an Amazon RDS instance.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#UpdateRdsDbInstance(Post#UpdateRdsDbInstanceRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#UpdateRdsDbInstance"
        body: "body"
      };
    }
    // <p>Updates a specified stack.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#UpdateStack(Post#UpdateStackRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#UpdateStack"
        body: "body"
      };
    }
    // <p>Updates a specified user profile.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#UpdateUserProfile(Post#UpdateUserProfileRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#UpdateUserProfile"
        body: "body"
      };
    }
    // <p>Updates an Amazon EBS volume's name or mount point. For more information, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/resources.html">Resource Management</a>.</p> <p> <b>Required Permissions</b>: To use this action, an IAM user must have a Manage permissions level for the stack, or an attached policy that explicitly grants permissions. For more information on user permissions, see <a href="http://docs.aws.amazon.com/opsworks/latest/userguide/opsworks-security-users.html">Managing User Permissions</a>.</p>
    rpc Post#UpdateVolume(Post#UpdateVolumeRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#UpdateVolume"
        body: "body"
      };
    }
}
