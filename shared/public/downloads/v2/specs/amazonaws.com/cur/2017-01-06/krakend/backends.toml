[[backends]]
  group = "aws-cost-and-usage-report-service"
  slug = "aws-cost-and-usage-report-service-2017-01-06"
  url_pattern = "/#DescribeReportDefinitions"
  method = "POST"
  encoding = "json"
  host = ["http://cur.amazonaws.com"]
  query_string_params = ["MaxResults", "NextToken"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://cur.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "aws-cost-and-usage-report-service"
  slug = "aws-cost-and-usage-report-service-2017-01-06"
  url_pattern = "/#PutReportDefinition"
  method = "POST"
  encoding = "json"
  host = ["http://cur.amazonaws.com"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://cur.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "aws-cost-and-usage-report-service"
  slug = "aws-cost-and-usage-report-service-2017-01-06"
  url_pattern = "/"
  method = "POST"
  encoding = "json"
  host = ["http://cur.amazonaws.com"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://cur.amazonaws.com"]
    schemes = ["http"]
