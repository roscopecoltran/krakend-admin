syntax = "proto3";

import "google/protobuf/empty.proto";

import "google/api/annotations.proto";

package amazoncloudwatch;

message PostRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DeleteAlarmsInput body = 10;
}

message Post#DeleteDashboardsRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DeleteDashboardsInput body = 10;
}

message Post#DescribeAlarmHistoryRequest {
    string Action = 1;
    // Pagination limit
    string MaxRecords = 2;
    // Pagination token
    string NextToken = 3;
    string Version = 4;
    string X_Amz_Algorithm = 5;
    string X_Amz_Content_Sha256 = 6;
    string X_Amz_Credential = 7;
    string X_Amz_Date = 8;
    string X_Amz_Security_Token = 9;
    string X_Amz_Signature = 10;
    string X_Amz_SignedHeaders = 11;
    DescribeAlarmHistoryInput body = 12;
}

message Post#DescribeAlarmsRequest {
    string Action = 1;
    // Pagination limit
    string MaxRecords = 2;
    // Pagination token
    string NextToken = 3;
    string Version = 4;
    string X_Amz_Algorithm = 5;
    string X_Amz_Content_Sha256 = 6;
    string X_Amz_Credential = 7;
    string X_Amz_Date = 8;
    string X_Amz_Security_Token = 9;
    string X_Amz_Signature = 10;
    string X_Amz_SignedHeaders = 11;
    DescribeAlarmsInput body = 12;
}

message Post#DescribeAlarmsForMetricRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DescribeAlarmsForMetricInput body = 10;
}

message Post#DisableAlarmActionsRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DisableAlarmActionsInput body = 10;
}

message Post#EnableAlarmActionsRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    EnableAlarmActionsInput body = 10;
}

message Post#GetDashboardRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    GetDashboardInput body = 10;
}

message Post#GetMetricStatisticsRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    GetMetricStatisticsInput body = 10;
}

message Post#ListDashboardsRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    ListDashboardsInput body = 10;
}

message Post#ListMetricsRequest {
    string Action = 1;
    // Pagination token
    string NextToken = 2;
    string Version = 3;
    string X_Amz_Algorithm = 4;
    string X_Amz_Content_Sha256 = 5;
    string X_Amz_Credential = 6;
    string X_Amz_Date = 7;
    string X_Amz_Security_Token = 8;
    string X_Amz_Signature = 9;
    string X_Amz_SignedHeaders = 10;
    ListMetricsInput body = 11;
}

message Post#PutDashboardRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    PutDashboardInput body = 10;
}

message Post#PutMetricAlarmRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    PutMetricAlarmInput body = 10;
}

message Post#PutMetricDataRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    PutMetricDataInput body = 10;
}

message Post#SetAlarmStateRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    SetAlarmStateInput body = 10;
}

message AlarmHistoryItem {
    // The descriptive name for the alarm.
    string AlarmName = 1;
    // Data about the alarm, in JSON format.
    string HistoryData = 2;
    // The type of alarm history item.
    HistoryItemType HistoryItemType = 3;
    // A summary of the alarm history, in text format.
    string HistorySummary = 4;
    // The time stamp for the alarm history item.
    string Timestamp = 5;
}

repeated AlarmHistoryItem AlarmHistoryItems = 1





repeated string AlarmNames = 1



enum ComparisonOperator {
    GREATERTHANOREQUALTOTHRESHOLD = 0;
    GREATERTHANTHRESHOLD = 1;
    LESSTHANTHRESHOLD = 2;
    LESSTHANOREQUALTOTHRESHOLD = 3;
}





repeated DashboardEntry DashboardEntries = 1

message DashboardEntry {
    // The Amazon Resource Name (ARN) of the dashboard.
    string DashboardArn = 1;
    // The name of the dashboard.
    string DashboardName = 2;
    // The time stamp of when the dashboard was last modified, either by an API call or through the console. This number is expressed as the number of milliseconds since Jan 1, 1970 00:00:00 UTC.
    string LastModified = 3;
    // The size of the dashboard, in bytes.
    integer Size = 4;
}

message DashboardInvalidInputError {
    array dashboardValidationMessages = 1;
    string message = 2;
}





repeated string DashboardNames = 1

message DashboardNotFoundError {
    string message = 1;
}

message DashboardValidationMessage {
    // The data path related to the message.
    string DataPath = 1;
    // A message describing the error or warning.
    string Message = 2;
}

repeated DashboardValidationMessage DashboardValidationMessages = 1



message Datapoint {
    // The average of the metric values that correspond to the data point.
    number Average = 1;
    // The percentile statistic for the data point.
    array ExtendedStatistics = 2;
    // The maximum metric value for the data point.
    number Maximum = 3;
    // The minimum metric value for the data point.
    number Minimum = 4;
    // The number of metric values that contributed to the aggregate value of this data point.
    number SampleCount = 5;
    // The sum of the metric values for the data point.
    number Sum = 6;
    // The time stamp used for the data point.
    string Timestamp = 7;
    // The standard unit for the data point.
    StandardUnit Unit = 8;
}

message DatapointValueMap {
    string key = 1;
    number value = 2;
}
repeated DatapointValueMap DatapointValueMap = 1

repeated Datapoint Datapoints = 1

message DeleteAlarmsInput {
    // The alarms to be deleted.
    array AlarmNames = 1;
}

message DeleteDashboardsInput {
    // The dashboards to be deleted.
    array DashboardNames = 1;
}

message DeleteDashboardsOutput {
}

message DescribeAlarmHistoryInput {
    // The name of the alarm.
    string AlarmName = 1;
    // The ending date to retrieve alarm history.
    string EndDate = 2;
    // The type of alarm histories to retrieve.
    HistoryItemType HistoryItemType = 3;
    // The maximum number of alarm history records to retrieve.
    integer MaxRecords = 4;
    // The token returned by a previous call to indicate that there is more data available.
    string NextToken = 5;
    // The starting date to retrieve alarm history.
    string StartDate = 6;
}

message DescribeAlarmHistoryOutput {
    // The alarm histories, in JSON format.
    array AlarmHistoryItems = 1;
    // The token that marks the start of the next batch of returned results.
    string NextToken = 2;
}

message DescribeAlarmsForMetricInput {
    // The dimensions associated with the metric. If the metric has any associated dimensions, you must specify them in order for the call to succeed.
    array Dimensions = 1;
    // The percentile statistic for the metric. Specify a value between p0.0 and p100.
    string ExtendedStatistic = 2;
    // The name of the metric.
    string MetricName = 3;
    // The namespace of the metric.
    string Namespace = 4;
    // The period, in seconds, over which the statistic is applied.
    integer Period = 5;
    // The statistic for the metric, other than percentiles. For percentile statistics, use <code>ExtendedStatistics</code>.
    Statistic Statistic = 6;
    // The unit for the metric.
    StandardUnit Unit = 7;
}

message DescribeAlarmsForMetricOutput {
    // The information for each alarm with the specified metric.
    array MetricAlarms = 1;
}

message DescribeAlarmsInput {
    // The action name prefix.
    string ActionPrefix = 1;
    // The alarm name prefix. If this parameter is specified, you cannot specify <code>AlarmNames</code>.
    string AlarmNamePrefix = 2;
    // The names of the alarms.
    array AlarmNames = 3;
    // The maximum number of alarm descriptions to retrieve.
    integer MaxRecords = 4;
    // The token returned by a previous call to indicate that there is more data available.
    string NextToken = 5;
    // The state value to be used in matching alarms.
    StateValue StateValue = 6;
}

message DescribeAlarmsOutput {
    // The information for the specified alarms.
    array MetricAlarms = 1;
    // The token that marks the start of the next batch of returned results.
    string NextToken = 2;
}

message Dimension {
    // The name of the dimension.
    string Name = 1;
    // The value representing the dimension measurement.
    string Value = 2;
}

message DimensionFilter {
    // The dimension name to be matched.
    string Name = 1;
    // The value of the dimension to be matched.
    string Value = 2;
}

repeated DimensionFilter DimensionFilters = 1





repeated Dimension Dimensions = 1

message DisableAlarmActionsInput {
    // The names of the alarms.
    array AlarmNames = 1;
}

message EnableAlarmActionsInput {
    // The names of the alarms.
    array AlarmNames = 1;
}









repeated string ExtendedStatistics = 1



message GetDashboardInput {
    // The name of the dashboard to be described.
    string DashboardName = 1;
}

message GetDashboardOutput {
    // The Amazon Resource Name (ARN) of the dashboard.
    string DashboardArn = 1;
    // The detailed information about the dashboard, including what widgets are included and their location on the dashboard. For more information about the <code>DashboardBody</code> syntax, see <a>CloudWatch-Dashboard-Body-Structure</a>.
    string DashboardBody = 2;
    // The name of the dashboard.
    string DashboardName = 3;
}

message GetMetricStatisticsInput {
    // The dimensions. If the metric contains multiple dimensions, you must include a value for each dimension. CloudWatch treats each unique combination of dimensions as a separate metric. If a specific combination of dimensions was not published, you can't retrieve statistics for it. You must specify the same dimensions that were used when the metrics were created. For an example, see <a href="http://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/cloudwatch_concepts.html#dimension-combinations">Dimension Combinations</a> in the <i>Amazon CloudWatch User Guide</i>. For more information about specifying dimensions, see <a href="http://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/publishingMetrics.html">Publishing Metrics</a> in the <i>Amazon CloudWatch User Guide</i>.
    array Dimensions = 1;
    // <p>The time stamp that determines the last data point to return.</p> <p>The value specified is exclusive; results include data points up to the specified time stamp. The time stamp must be in ISO 8601 UTC format (for example, 2016-10-10T23:00:00Z).</p>
    string EndTime = 2;
    // The percentile statistics. Specify values between p0.0 and p100. When calling <code>GetMetricStatistics</code>, you must specify either <code>Statistics</code> or <code>ExtendedStatistics</code>, but not both.
    array ExtendedStatistics = 3;
    // The name of the metric, with or without spaces.
    string MetricName = 4;
    // The namespace of the metric, with or without spaces.
    string Namespace = 5;
    // <p>The granularity, in seconds, of the returned data points. For metrics with regular resolution, a period can be as short as one minute (60 seconds) and must be a multiple of 60. For high-resolution metrics that are collected at intervals of less than one minute, the period can be 1, 5, 10, 30, 60, or any multiple of 60. High-resolution metrics are those metrics stored by a <code>PutMetricData</code> call that includes a <code>StorageResolution</code> of 1 second.</p> <p>If the <code>StartTime</code> parameter specifies a time stamp that is greater than 3 hours ago, you must specify the period as follows or no data points in that time range is returned:</p> <ul> <li> <p>Start time between 3 hours and 15 days ago - Use a multiple of 60 seconds (1 minute).</p> </li> <li> <p>Start time between 15 and 63 days ago - Use a multiple of 300 seconds (5 minutes).</p> </li> <li> <p>Start time greater than 63 days ago - Use a multiple of 3600 seconds (1 hour).</p> </li> </ul>
    integer Period = 6;
    // <p>The time stamp that determines the first data point to return. Start times are evaluated relative to the time that CloudWatch receives the request.</p> <p>The value specified is inclusive; results include data points with the specified time stamp. The time stamp must be in ISO 8601 UTC format (for example, 2016-10-03T23:00:00Z).</p> <p>CloudWatch rounds the specified time stamp as follows:</p> <ul> <li> <p>Start time less than 15 days ago - Round down to the nearest whole minute. For example, 12:32:34 is rounded down to 12:32:00.</p> </li> <li> <p>Start time between 15 and 63 days ago - Round down to the nearest 5-minute clock interval. For example, 12:32:34 is rounded down to 12:30:00.</p> </li> <li> <p>Start time greater than 63 days ago - Round down to the nearest 1-hour clock interval. For example, 12:32:34 is rounded down to 12:00:00.</p> </li> </ul> <p>If you set <code>Period</code> to 5, 10, or 30, the start time of your request is rounded down to the nearest time that corresponds to even 5-, 10-, or 30-second divisions of a minute. For example, if you make a query at (HH:mm:ss) 01:05:23 for the previous 10-second period, the start time of your request is rounded down and you receive data from 01:05:10 to 01:05:20. If you make a query at 15:07:17 for the previous 5 minutes of data, using a period of 5 seconds, you receive data timestamped between 15:02:15 and 15:07:15. </p>
    string StartTime = 7;
    // The metric statistics, other than percentile. For percentile statistics, use <code>ExtendedStatistics</code>. When calling <code>GetMetricStatistics</code>, you must specify either <code>Statistics</code> or <code>ExtendedStatistics</code>, but not both.
    array Statistics = 8;
    // The unit for a given metric. Metrics may be reported in multiple units. Not supplying a unit results in all units being returned. If the metric only ever reports one unit, specifying a unit has no effect.
    StandardUnit Unit = 9;
}

message GetMetricStatisticsOutput {
    // The data points for the specified metric.
    array Datapoints = 1;
    // A label for the specified metric.
    string Label = 2;
}

enum HistoryItemType {
    CONFIGURATIONUPDATE = 0;
    STATEUPDATE = 1;
    ACTION = 2;
}

message InternalServiceFault {
    // <p/>
    string Message = 1;
}

message InvalidFormatFault {
    // <p/>
    string message = 1;
}

message InvalidNextToken {
    // <p/>
    string message = 1;
}

message InvalidParameterCombinationException {
    // <p/>
    string message = 1;
}

message InvalidParameterValueException {
    // <p/>
    string message = 1;
}

message LimitExceededFault {
    // <p/>
    string message = 1;
}

message ListDashboardsInput {
    // If you specify this parameter, only the dashboards with names starting with the specified string are listed. The maximum length is 255, and valid characters are A-Z, a-z, 0-9, ".", "-", and "_".
    string DashboardNamePrefix = 1;
    // The token returned by a previous call to indicate that there is more data available.
    string NextToken = 2;
}

message ListDashboardsOutput {
    // The list of matching dashboards.
    array DashboardEntries = 1;
    // The token that marks the start of the next batch of returned results.
    string NextToken = 2;
}

message ListMetricsInput {
    // The dimensions to filter against.
    array Dimensions = 1;
    // The name of the metric to filter against.
    string MetricName = 2;
    // The namespace to filter against.
    string Namespace = 3;
    // The token returned by a previous call to indicate that there is more data available.
    string NextToken = 4;
}

message ListMetricsOutput {
    // The metrics.
    array Metrics = 1;
    // The token that marks the start of the next batch of returned results.
    string NextToken = 2;
}

message Metric {
    // The dimensions for the metric.
    array Dimensions = 1;
    // The name of the metric.
    string MetricName = 2;
    // The namespace of the metric.
    string Namespace = 3;
}

message MetricAlarm {
    // Indicates whether actions should be executed during any changes to the alarm state.
    boolean ActionsEnabled = 1;
    // The actions to execute when this alarm transitions to the <code>ALARM</code> state from any other state. Each action is specified as an Amazon Resource Name (ARN).
    array AlarmActions = 2;
    // The Amazon Resource Name (ARN) of the alarm.
    string AlarmArn = 3;
    // The time stamp of the last update to the alarm configuration.
    string AlarmConfigurationUpdatedTimestamp = 4;
    // The description of the alarm.
    string AlarmDescription = 5;
    // The name of the alarm.
    string AlarmName = 6;
    // The arithmetic operation to use when comparing the specified statistic and threshold. The specified statistic value is used as the first operand.
    ComparisonOperator ComparisonOperator = 7;
    // The dimensions for the metric associated with the alarm.
    array Dimensions = 8;
    // Used only for alarms based on percentiles. If <code>ignore</code>, the alarm state does not change during periods with too few data points to be statistically significant. If <code>evaluate</code> or this parameter is not used, the alarm is always evaluated and possibly changes state no matter how many data points are available.
    string EvaluateLowSampleCountPercentile = 9;
    // The number of periods over which data is compared to the specified threshold.
    integer EvaluationPeriods = 10;
    // The percentile statistic for the metric associated with the alarm. Specify a value between p0.0 and p100.
    string ExtendedStatistic = 11;
    // The actions to execute when this alarm transitions to the <code>INSUFFICIENT_DATA</code> state from any other state. Each action is specified as an Amazon Resource Name (ARN).
    array InsufficientDataActions = 12;
    // The name of the metric associated with the alarm.
    string MetricName = 13;
    // The namespace of the metric associated with the alarm.
    string Namespace = 14;
    // The actions to execute when this alarm transitions to the <code>OK</code> state from any other state. Each action is specified as an Amazon Resource Name (ARN).
    array OKActions = 15;
    // The period, in seconds, over which the statistic is applied.
    integer Period = 16;
    // An explanation for the alarm state, in text format.
    string StateReason = 17;
    // An explanation for the alarm state, in JSON format.
    string StateReasonData = 18;
    // The time stamp of the last update to the alarm state.
    string StateUpdatedTimestamp = 19;
    // The state value for the alarm.
    StateValue StateValue = 20;
    // The statistic for the metric associated with the alarm, other than percentile. For percentile statistics, use <code>ExtendedStatistic</code>.
    Statistic Statistic = 21;
    // The value to compare with the specified statistic.
    number Threshold = 22;
    // Sets how this alarm is to handle missing data points. If this parameter is omitted, the default behavior of <code>missing</code> is used.
    string TreatMissingData = 23;
    // The unit of the metric associated with the alarm.
    StandardUnit Unit = 24;
}

repeated MetricAlarm MetricAlarms = 1

repeated MetricDatum MetricData = 1

message MetricDatum {
    // The dimensions associated with the metric.
    array Dimensions = 1;
    // The name of the metric.
    string MetricName = 2;
    // The statistical values for the metric.
    StatisticSet StatisticValues = 3;
    // <p>Valid values are 1 and 60. Setting this to 1 specifies this metric as a high-resolution metric, so that CloudWatch stores the metric with sub-minute resolution down to one second. Setting this to 60 specifies this metric as a regular-resolution metric, which CloudWatch stores at 1-minute resolution. Currently, high resolution is available only for custom metrics. For more information about high-resolution metrics, see <a href="http://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/publishingMetrics.html#high-resolution-metrics">High-Resolution Metrics</a> in the <i>Amazon CloudWatch User Guide</i>. </p> <p>This field is optional, if you do not specify it the default of 60 is used.</p>
    integer StorageResolution = 4;
    // The time the metric data was received, expressed as the number of milliseconds since Jan 1, 1970 00:00:00 UTC.
    string Timestamp = 5;
    // The unit of the metric.
    StandardUnit Unit = 6;
    // <p>The value for the metric.</p> <p>Although the parameter accepts numbers of type Double, CloudWatch rejects values that are either too small or too large. Values must be in the range of 8.515920e-109 to 1.174271e+108 (Base 10) or 2e-360 to 2e360 (Base 2). In addition, special values (for example, NaN, +Infinity, -Infinity) are not supported.</p>
    number Value = 7;
}





repeated Metric Metrics = 1

message MissingRequiredParameterException {
    // <p/>
    string message = 1;
}

message PutDashboardInput {
    // <p>The detailed information about the dashboard in JSON format, including the widgets to include and their location on the dashboard.</p> <p>For more information about the syntax, see <a>CloudWatch-Dashboard-Body-Structure</a>.</p>
    string DashboardBody = 1;
    // The name of the dashboard. If a dashboard with this name already exists, this call modifies that dashboard, replacing its current contents. Otherwise, a new dashboard is created. The maximum length is 255, and valid characters are A-Z, a-z, 0-9, "-", and "_".
    string DashboardName = 2;
}

message PutDashboardOutput {
    // <p>If the input for <code>PutDashboard</code> was correct and the dashboard was successfully created or modified, this result is empty.</p> <p>If this result includes only warning messages, then the input was valid enough for the dashboard to be created or modified, but some elements of the dashboard may not render.</p> <p>If this result includes error messages, the input was not valid and the operation failed.</p>
    array DashboardValidationMessages = 1;
}

message PutMetricAlarmInput {
    // Indicates whether actions should be executed during any changes to the alarm state.
    boolean ActionsEnabled = 1;
    // <p>The actions to execute when this alarm transitions to the <code>ALARM</code> state from any other state. Each action is specified as an Amazon Resource Name (ARN).</p> <p>Valid Values: arn:aws:automate:<i>region</i>:ec2:stop | arn:aws:automate:<i>region</i>:ec2:terminate | arn:aws:automate:<i>region</i>:ec2:recover</p> <p>Valid Values (for use with IAM roles): arn:aws:swf:us-east-1:{<i>customer-account</i>}:action/actions/AWS_EC2.InstanceId.Stop/1.0 | arn:aws:swf:us-east-1:{<i>customer-account</i>}:action/actions/AWS_EC2.InstanceId.Terminate/1.0 | arn:aws:swf:us-east-1:{<i>customer-account</i>}:action/actions/AWS_EC2.InstanceId.Reboot/1.0</p>
    array AlarmActions = 2;
    // The description for the alarm.
    string AlarmDescription = 3;
    // The name for the alarm. This name must be unique within the AWS account.
    string AlarmName = 4;
    //  The arithmetic operation to use when comparing the specified statistic and threshold. The specified statistic value is used as the first operand.
    ComparisonOperator ComparisonOperator = 5;
    // The dimensions for the metric associated with the alarm.
    array Dimensions = 6;
    // <p> Used only for alarms based on percentiles. If you specify <code>ignore</code>, the alarm state does not change during periods with too few data points to be statistically significant. If you specify <code>evaluate</code> or omit this parameter, the alarm is always evaluated and possibly changes state no matter how many data points are available. For more information, see <a href="http://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/AlarmThatSendsEmail.html#percentiles-with-low-samples">Percentile-Based CloudWatch Alarms and Low Data Samples</a>.</p> <p>Valid Values: <code>evaluate | ignore</code> </p>
    string EvaluateLowSampleCountPercentile = 7;
    // The number of periods over which data is compared to the specified threshold. An alarm's total current evaluation period can be no longer than one day, so this number multiplied by <code>Period</code> cannot be more than 86,400 seconds.
    integer EvaluationPeriods = 8;
    // The percentile statistic for the metric associated with the alarm. Specify a value between p0.0 and p100.
    string ExtendedStatistic = 9;
    // <p>The actions to execute when this alarm transitions to the <code>INSUFFICIENT_DATA</code> state from any other state. Each action is specified as an Amazon Resource Name (ARN).</p> <p>Valid Values: arn:aws:automate:<i>region</i>:ec2:stop | arn:aws:automate:<i>region</i>:ec2:terminate | arn:aws:automate:<i>region</i>:ec2:recover</p> <p>Valid Values (for use with IAM roles): arn:aws:swf:us-east-1:{<i>customer-account</i>}:action/actions/AWS_EC2.InstanceId.Stop/1.0 | arn:aws:swf:us-east-1:{<i>customer-account</i>}:action/actions/AWS_EC2.InstanceId.Terminate/1.0 | arn:aws:swf:us-east-1:{<i>customer-account</i>}:action/actions/AWS_EC2.InstanceId.Reboot/1.0</p>
    array InsufficientDataActions = 10;
    // The name for the metric associated with the alarm.
    string MetricName = 11;
    // The namespace for the metric associated with the alarm.
    string Namespace = 12;
    // <p>The actions to execute when this alarm transitions to an <code>OK</code> state from any other state. Each action is specified as an Amazon Resource Name (ARN).</p> <p>Valid Values: arn:aws:automate:<i>region</i>:ec2:stop | arn:aws:automate:<i>region</i>:ec2:terminate | arn:aws:automate:<i>region</i>:ec2:recover</p> <p>Valid Values (for use with IAM roles): arn:aws:swf:us-east-1:{<i>customer-account</i>}:action/actions/AWS_EC2.InstanceId.Stop/1.0 | arn:aws:swf:us-east-1:{<i>customer-account</i>}:action/actions/AWS_EC2.InstanceId.Terminate/1.0 | arn:aws:swf:us-east-1:{<i>customer-account</i>}:action/actions/AWS_EC2.InstanceId.Reboot/1.0</p>
    array OKActions = 13;
    // <p>The period, in seconds, over which the specified statistic is applied. Valid values are 10, 30, and any multiple of 60.</p> <p>Be sure to specify 10 or 30 only for metrics that are stored by a <code>PutMetricData</code> call with a <code>StorageResolution</code> of 1. If you specify a Period of 10 or 30 for a metric that does not have sub-minute resolution, the alarm still attempts to gather data at the period rate that you specify. In this case, it does not receive data for the attempts that do not correspond to a one-minute data resolution, and the alarm may often lapse into INSUFFICENT_DATA status. Specifying 10 or 30 also sets this alarm as a high-resolution alarm, which has a higher charge than other alarms. For more information about pricing, see <a href="https://aws.amazon.com/cloudwatch/pricing/">Amazon CloudWatch Pricing</a>.</p> <p>An alarm's total current evaluation period can be no longer than one day, so <code>Period</code> multiplied by <code>EvaluationPeriods</code> cannot be more than 86,400 seconds.</p>
    integer Period = 14;
    // The statistic for the metric associated with the alarm, other than percentile. For percentile statistics, use <code>ExtendedStatistic</code>.
    Statistic Statistic = 15;
    // The value against which the specified statistic is compared.
    number Threshold = 16;
    // <p> Sets how this alarm is to handle missing data points. If <code>TreatMissingData</code> is omitted, the default behavior of <code>missing</code> is used. For more information, see <a href="http://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/AlarmThatSendsEmail.html#alarms-and-missing-data">Configuring How CloudWatch Alarms Treats Missing Data</a>.</p> <p>Valid Values: <code>breaching | notBreaching | ignore | missing</code> </p>
    string TreatMissingData = 17;
    // <p>The unit of measure for the statistic. For example, the units for the Amazon EC2 NetworkIn metric are Bytes because NetworkIn tracks the number of bytes that an instance receives on all network interfaces. You can also specify a unit when you create a custom metric. Units help provide conceptual meaning to your data. Metric data points that specify a unit of measure, such as Percent, are aggregated separately.</p> <p>If you specify a unit, you must use a unit that is appropriate for the metric. Otherwise, the CloudWatch alarm can get stuck in the <code>INSUFFICIENT DATA</code> state. </p>
    StandardUnit Unit = 18;
}

message PutMetricDataInput {
    // The data for the metric.
    array MetricData = 1;
    // <p>The namespace for the metric data.</p> <p>You cannot specify a namespace that begins with "AWS/". Namespaces that begin with "AWS/" are reserved for use by Amazon Web Services products.</p>
    string Namespace = 2;
}

repeated string ResourceList = 1



message ResourceNotFound {
    // <p/>
    string message = 1;
}

message SetAlarmStateInput {
    // The name for the alarm. This name must be unique within the AWS account. The maximum length is 255 characters.
    string AlarmName = 1;
    // The reason that this alarm is set to this specific state, in text format.
    string StateReason = 2;
    // The reason that this alarm is set to this specific state, in JSON format.
    string StateReasonData = 3;
    // The value of the state.
    StateValue StateValue = 4;
}

enum StandardUnit {
    SECONDS = 0;
    MICROSECONDS = 1;
    MILLISECONDS = 2;
    BYTES = 3;
    KILOBYTES = 4;
    MEGABYTES = 5;
    GIGABYTES = 6;
    TERABYTES = 7;
    BITS = 8;
    KILOBITS = 9;
    MEGABITS = 10;
    GIGABITS = 11;
    TERABITS = 12;
    PERCENT = 13;
    COUNT = 14;
    BYTESSECOND = 15;
    KILOBYTESSECOND = 16;
    MEGABYTESSECOND = 17;
    GIGABYTESSECOND = 18;
    TERABYTESSECOND = 19;
    BITSSECOND = 20;
    KILOBITSSECOND = 21;
    MEGABITSSECOND = 22;
    GIGABITSSECOND = 23;
    TERABITSSECOND = 24;
    COUNTSECOND = 25;
    NONE = 26;
}

enum StateValue {
    OK = 0;
    ALARM = 1;
    INSUFFICIENT_DATA = 2;
}

enum Statistic {
    SAMPLECOUNT = 0;
    AVERAGE = 1;
    SUM = 2;
    MINIMUM = 3;
    MAXIMUM = 4;
}

message StatisticSet {
    // The maximum value of the sample set.
    number Maximum = 1;
    // The minimum value of the sample set.
    number Minimum = 2;
    // The number of samples used for the statistic set.
    number SampleCount = 3;
    // The sum of values for the sample set.
    number Sum = 4;
}

repeated Statistic Statistics = 1

























service AmazonCloudWatchService {
    // Deletes the specified alarms. In the event of an error, no alarms are deleted.
    rpc Post(PostRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//"
        body: "body"
      };
    }
    // Deletes all dashboards that you specify. You may specify up to 100 dashboards to delete. If there is an error during this call, no dashboards are deleted.
    rpc Post#DeleteDashboards(Post#DeleteDashboardsRequest) returns (DeleteDashboardsOutput) {
      option (google.api.http) = {
        post: "//#DeleteDashboards"
        body: "body"
      };
    }
    // <p>Retrieves the history for the specified alarm. You can filter the results by date range or item type. If an alarm name is not specified, the histories for all alarms are returned.</p> <p>CloudWatch retains the history of an alarm even if you delete the alarm.</p>
    rpc Post#DescribeAlarmHistory(Post#DescribeAlarmHistoryRequest) returns (DescribeAlarmHistoryOutput) {
      option (google.api.http) = {
        post: "//#DescribeAlarmHistory"
        body: "body"
      };
    }
    // Retrieves the specified alarms. If no alarms are specified, all alarms are returned. Alarms can be retrieved by using only a prefix for the alarm name, the alarm state, or a prefix for any action.
    rpc Post#DescribeAlarms(Post#DescribeAlarmsRequest) returns (DescribeAlarmsOutput) {
      option (google.api.http) = {
        post: "//#DescribeAlarms"
        body: "body"
      };
    }
    // Retrieves the alarms for the specified metric. To filter the results, specify a statistic, period, or unit.
    rpc Post#DescribeAlarmsForMetric(Post#DescribeAlarmsForMetricRequest) returns (DescribeAlarmsForMetricOutput) {
      option (google.api.http) = {
        post: "//#DescribeAlarmsForMetric"
        body: "body"
      };
    }
    // Disables the actions for the specified alarms. When an alarm's actions are disabled, the alarm actions do not execute when the alarm state changes.
    rpc Post#DisableAlarmActions(Post#DisableAlarmActionsRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#DisableAlarmActions"
        body: "body"
      };
    }
    // Enables the actions for the specified alarms.
    rpc Post#EnableAlarmActions(Post#EnableAlarmActionsRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#EnableAlarmActions"
        body: "body"
      };
    }
    // <p>Displays the details of the dashboard that you specify.</p> <p>To copy an existing dashboard, use <code>GetDashboard</code>, and then use the data returned within <code>DashboardBody</code> as the template for the new dashboard when you call <code>PutDashboard</code> to create the copy.</p>
    rpc Post#GetDashboard(Post#GetDashboardRequest) returns (GetDashboardOutput) {
      option (google.api.http) = {
        post: "//#GetDashboard"
        body: "body"
      };
    }
    // <p>Gets statistics for the specified metric.</p> <p>The maximum number of data points returned from a single call is 1,440. If you request more than 1,440 data points, CloudWatch returns an error. To reduce the number of data points, you can narrow the specified time range and make multiple requests across adjacent time ranges, or you can increase the specified period. Data points are not returned in chronological order.</p> <p>CloudWatch aggregates data points based on the length of the period that you specify. For example, if you request statistics with a one-hour period, CloudWatch aggregates all data points with time stamps that fall within each one-hour period. Therefore, the number of values aggregated by CloudWatch is larger than the number of data points returned.</p> <p>CloudWatch needs raw data points to calculate percentile statistics. If you publish data using a statistic set instead, you can only retrieve percentile statistics for this data if one of the following conditions is true:</p> <ul> <li> <p>The SampleCount value of the statistic set is 1.</p> </li> <li> <p>The Min and the Max values of the statistic set are equal.</p> </li> </ul> <p>Amazon CloudWatch retains metric data as follows:</p> <ul> <li> <p>Data points with a period of less than 60 seconds are available for 3 hours. These data points are high-resolution metrics and are available only for custom metrics that have been defined with a <code>StorageResolution</code> of 1.</p> </li> <li> <p>Data points with a period of 60 seconds (1-minute) are available for 15 days.</p> </li> <li> <p>Data points with a period of 300 seconds (5-minute) are available for 63 days.</p> </li> <li> <p>Data points with a period of 3600 seconds (1 hour) are available for 455 days (15 months).</p> </li> </ul> <p>Data points that are initially published with a shorter period are aggregated together for long-term storage. For example, if you collect data using a period of 1 minute, the data remains available for 15 days with 1-minute resolution. After 15 days, this data is still available, but is aggregated and retrievable only with a resolution of 5 minutes. After 63 days, the data is further aggregated and is available with a resolution of 1 hour.</p> <p>CloudWatch started retaining 5-minute and 1-hour metric data as of July 9, 2016.</p> <p>For information about metrics and dimensions supported by AWS services, see the <a href="http://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/CW_Support_For_AWS.html">Amazon CloudWatch Metrics and Dimensions Reference</a> in the <i>Amazon CloudWatch User Guide</i>.</p>
    rpc Post#GetMetricStatistics(Post#GetMetricStatisticsRequest) returns (GetMetricStatisticsOutput) {
      option (google.api.http) = {
        post: "//#GetMetricStatistics"
        body: "body"
      };
    }
    // Returns a list of the dashboards for your account. If you include <code>DashboardNamePrefix</code>, only those dashboards with names starting with the prefix are listed. Otherwise, all dashboards in your account are listed.
    rpc Post#ListDashboards(Post#ListDashboardsRequest) returns (ListDashboardsOutput) {
      option (google.api.http) = {
        post: "//#ListDashboards"
        body: "body"
      };
    }
    // <p>List the specified metrics. You can use the returned metrics with <a>GetMetricStatistics</a> to obtain statistical data.</p> <p>Up to 500 results are returned for any one call. To retrieve additional results, use the returned token with subsequent calls.</p> <p>After you create a metric, allow up to fifteen minutes before the metric appears. Statistics about the metric, however, are available sooner using <a>GetMetricStatistics</a>.</p>
    rpc Post#ListMetrics(Post#ListMetricsRequest) returns (ListMetricsOutput) {
      option (google.api.http) = {
        post: "//#ListMetrics"
        body: "body"
      };
    }
    // <p>Creates a dashboard if it does not already exist, or updates an existing dashboard. If you update a dashboard, the entire contents are replaced with what you specify here.</p> <p>You can have up to 500 dashboards per account. All dashboards in your account are global, not region-specific.</p> <p>A simple way to create a dashboard using <code>PutDashboard</code> is to copy an existing dashboard. To copy an existing dashboard using the console, you can load the dashboard and then use the View/edit source command in the Actions menu to display the JSON block for that dashboard. Another way to copy a dashboard is to use <code>GetDashboard</code>, and then use the data returned within <code>DashboardBody</code> as the template for the new dashboard when you call <code>PutDashboard</code>.</p> <p>When you create a dashboard with <code>PutDashboard</code>, a good practice is to add a text widget at the top of the dashboard with a message that the dashboard was created by script and should not be changed in the console. This message could also point console users to the location of the <code>DashboardBody</code> script or the CloudFormation template used to create the dashboard.</p>
    rpc Post#PutDashboard(Post#PutDashboardRequest) returns (PutDashboardOutput) {
      option (google.api.http) = {
        post: "//#PutDashboard"
        body: "body"
      };
    }
    // <p>Creates or updates an alarm and associates it with the specified metric. Optionally, this operation can associate one or more Amazon SNS resources with the alarm.</p> <p>When this operation creates an alarm, the alarm state is immediately set to <code>INSUFFICIENT_DATA</code>. The alarm is evaluated and its state is set appropriately. Any actions associated with the state are then executed.</p> <p>When you update an existing alarm, its state is left unchanged, but the update completely overwrites the previous configuration of the alarm.</p> <p>If you are an IAM user, you must have Amazon EC2 permissions for some operations:</p> <ul> <li> <p> <code>ec2:DescribeInstanceStatus</code> and <code>ec2:DescribeInstances</code> for all alarms on EC2 instance status metrics</p> </li> <li> <p> <code>ec2:StopInstances</code> for alarms with stop actions</p> </li> <li> <p> <code>ec2:TerminateInstances</code> for alarms with terminate actions</p> </li> <li> <p> <code>ec2:DescribeInstanceRecoveryAttribute</code> and <code>ec2:RecoverInstances</code> for alarms with recover actions</p> </li> </ul> <p>If you have read/write permissions for Amazon CloudWatch but not for Amazon EC2, you can still create an alarm, but the stop or terminate actions are not performed. However, if you are later granted the required permissions, the alarm actions that you created earlier are performed.</p> <p>If you are using an IAM role (for example, an EC2 instance profile), you cannot stop or terminate the instance using alarm actions. However, you can still see the alarm state and perform any other actions such as Amazon SNS notifications or Auto Scaling policies.</p> <p>If you are using temporary security credentials granted using AWS STS, you cannot stop or terminate an EC2 instance using alarm actions.</p> <p>You must create at least one stop, terminate, or reboot alarm using either the Amazon EC2 or CloudWatch consoles to create the <b>EC2ActionsAccess</b> IAM role. After this IAM role is created, you can create stop, terminate, or reboot alarms using a command-line interface or API.</p>
    rpc Post#PutMetricAlarm(Post#PutMetricAlarmRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#PutMetricAlarm"
        body: "body"
      };
    }
    // <p>Publishes metric data points to Amazon CloudWatch. CloudWatch associates the data points with the specified metric. If the specified metric does not exist, CloudWatch creates the metric. When CloudWatch creates a metric, it can take up to fifteen minutes for the metric to appear in calls to <a>ListMetrics</a>.</p> <p>Each <code>PutMetricData</code> request is limited to 40 KB in size for HTTP POST requests.</p> <p>Although the <code>Value</code> parameter accepts numbers of type <code>Double</code>, CloudWatch rejects values that are either too small or too large. Values must be in the range of 8.515920e-109 to 1.174271e+108 (Base 10) or 2e-360 to 2e360 (Base 2). In addition, special values (for example, NaN, +Infinity, -Infinity) are not supported.</p> <p>You can use up to 10 dimensions per metric to further clarify what data the metric collects. For more information about specifying dimensions, see <a href="http://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/publishingMetrics.html">Publishing Metrics</a> in the <i>Amazon CloudWatch User Guide</i>.</p> <p>Data points with time stamps from 24 hours ago or longer can take at least 48 hours to become available for <a>GetMetricStatistics</a> from the time they are submitted.</p> <p>CloudWatch needs raw data points to calculate percentile statistics. If you publish data using a statistic set instead, you can only retrieve percentile statistics for this data if one of the following conditions is true:</p> <ul> <li> <p>The SampleCount value of the statistic set is 1</p> </li> <li> <p>The Min and the Max values of the statistic set are equal</p> </li> </ul>
    rpc Post#PutMetricData(Post#PutMetricDataRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#PutMetricData"
        body: "body"
      };
    }
    // Temporarily sets the state of an alarm for testing purposes. When the updated state differs from the previous value, the action configured for the appropriate state is invoked. For example, if your alarm is configured to send an Amazon SNS message when an alarm is triggered, temporarily changing the alarm state to <code>ALARM</code> sends an SNS message. The alarm returns to its actual state (often within seconds). Because the alarm state change happens quickly, it is typically only visible in the alarm's <b>History</b> tab in the Amazon CloudWatch console or through <a>DescribeAlarmHistory</a>.
    rpc Post#SetAlarmState(Post#SetAlarmStateRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#SetAlarmState"
        body: "body"
      };
    }
}
