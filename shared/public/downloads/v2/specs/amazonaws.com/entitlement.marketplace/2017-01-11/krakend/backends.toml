[[backends]]
  group = "aws-marketplace-entitlement-service-2017-01-11"
  url_pattern = "/"
  method = "POST"
  encoding = "json"
  host = ["http://entitlement.marketplace.amazonaws.com"]
  concurrent_calls = 1
  [backends.extra_config]
    slug = "aws-marketplace-entitlement-service-2017-01-11"
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://entitlement.marketplace.amazonaws.com"]
    topics = ["cloud"]
    schemes = ["http"]
