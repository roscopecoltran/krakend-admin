[[backends]]
  group = "amazon-dynamodb-streams"
  slug = "amazon-dynamodb-streams-2012-08-10"
  url_pattern = "/#GetRecords"
  method = "POST"
  encoding = "json"
  host = ["http://streams.dynamodb.amazonaws.com"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://streams.dynamodb.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "amazon-dynamodb-streams"
  slug = "amazon-dynamodb-streams-2012-08-10"
  url_pattern = "/#GetShardIterator"
  method = "POST"
  encoding = "json"
  host = ["http://streams.dynamodb.amazonaws.com"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://streams.dynamodb.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "amazon-dynamodb-streams"
  slug = "amazon-dynamodb-streams-2012-08-10"
  url_pattern = "/#ListStreams"
  method = "POST"
  encoding = "json"
  host = ["http://streams.dynamodb.amazonaws.com"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://streams.dynamodb.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "amazon-dynamodb-streams"
  slug = "amazon-dynamodb-streams-2012-08-10"
  url_pattern = "/"
  method = "POST"
  encoding = "json"
  host = ["http://streams.dynamodb.amazonaws.com"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://streams.dynamodb.amazonaws.com"]
    schemes = ["http"]
