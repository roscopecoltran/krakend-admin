[[backends]]
  group = "aws-cloudtrail"
  slug = "aws-cloudtrail-2013-11-01"
  url_pattern = "/#CreateTrail"
  method = "POST"
  encoding = "json"
  host = ["http://cloudtrail.amazonaws.com"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://cloudtrail.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "aws-cloudtrail"
  slug = "aws-cloudtrail-2013-11-01"
  url_pattern = "/#DeleteTrail"
  method = "POST"
  encoding = "json"
  host = ["http://cloudtrail.amazonaws.com"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://cloudtrail.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "aws-cloudtrail"
  slug = "aws-cloudtrail-2013-11-01"
  url_pattern = "/#DescribeTrails"
  method = "POST"
  encoding = "json"
  host = ["http://cloudtrail.amazonaws.com"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://cloudtrail.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "aws-cloudtrail"
  slug = "aws-cloudtrail-2013-11-01"
  url_pattern = "/#GetEventSelectors"
  method = "POST"
  encoding = "json"
  host = ["http://cloudtrail.amazonaws.com"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://cloudtrail.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "aws-cloudtrail"
  slug = "aws-cloudtrail-2013-11-01"
  url_pattern = "/#GetTrailStatus"
  method = "POST"
  encoding = "json"
  host = ["http://cloudtrail.amazonaws.com"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://cloudtrail.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "aws-cloudtrail"
  slug = "aws-cloudtrail-2013-11-01"
  url_pattern = "/#ListPublicKeys"
  method = "POST"
  encoding = "json"
  host = ["http://cloudtrail.amazonaws.com"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://cloudtrail.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "aws-cloudtrail"
  slug = "aws-cloudtrail-2013-11-01"
  url_pattern = "/#ListTags"
  method = "POST"
  encoding = "json"
  host = ["http://cloudtrail.amazonaws.com"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://cloudtrail.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "aws-cloudtrail"
  slug = "aws-cloudtrail-2013-11-01"
  url_pattern = "/#LookupEvents"
  method = "POST"
  encoding = "json"
  host = ["http://cloudtrail.amazonaws.com"]
  query_string_params = ["MaxResults", "NextToken"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://cloudtrail.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "aws-cloudtrail"
  slug = "aws-cloudtrail-2013-11-01"
  url_pattern = "/#PutEventSelectors"
  method = "POST"
  encoding = "json"
  host = ["http://cloudtrail.amazonaws.com"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://cloudtrail.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "aws-cloudtrail"
  slug = "aws-cloudtrail-2013-11-01"
  url_pattern = "/#RemoveTags"
  method = "POST"
  encoding = "json"
  host = ["http://cloudtrail.amazonaws.com"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://cloudtrail.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "aws-cloudtrail"
  slug = "aws-cloudtrail-2013-11-01"
  url_pattern = "/#StartLogging"
  method = "POST"
  encoding = "json"
  host = ["http://cloudtrail.amazonaws.com"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://cloudtrail.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "aws-cloudtrail"
  slug = "aws-cloudtrail-2013-11-01"
  url_pattern = "/#StopLogging"
  method = "POST"
  encoding = "json"
  host = ["http://cloudtrail.amazonaws.com"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://cloudtrail.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "aws-cloudtrail"
  slug = "aws-cloudtrail-2013-11-01"
  url_pattern = "/#UpdateTrail"
  method = "POST"
  encoding = "json"
  host = ["http://cloudtrail.amazonaws.com"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://cloudtrail.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "aws-cloudtrail"
  slug = "aws-cloudtrail-2013-11-01"
  url_pattern = "/"
  method = "POST"
  encoding = "json"
  host = ["http://cloudtrail.amazonaws.com"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://cloudtrail.amazonaws.com"]
    schemes = ["http"]
