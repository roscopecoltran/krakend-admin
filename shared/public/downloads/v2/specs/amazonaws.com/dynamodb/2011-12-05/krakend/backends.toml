[[backends]]
  group = "amazon-dynamodb-batchwriteitem"
  slug = "amazon-dynamodb-2011-12-05-batchwriteitem"
  url_pattern = "/#BatchWriteItem"
  method = "POST"
  encoding = "json"
  host = ["http://dynamodb.amazonaws.com"]
  concurrent_calls = 0
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://dynamodb.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "amazon-dynamodb-createtable"
  slug = "amazon-dynamodb-2011-12-05-createtable"
  url_pattern = "/#CreateTable"
  method = "POST"
  encoding = "json"
  host = ["http://dynamodb.amazonaws.com"]
  concurrent_calls = 0
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://dynamodb.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "amazon-dynamodb-deleteitem"
  slug = "amazon-dynamodb-2011-12-05-deleteitem"
  url_pattern = "/#DeleteItem"
  method = "POST"
  encoding = "json"
  host = ["http://dynamodb.amazonaws.com"]
  concurrent_calls = 0
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://dynamodb.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "amazon-dynamodb-deletetable"
  slug = "amazon-dynamodb-2011-12-05-deletetable"
  url_pattern = "/#DeleteTable"
  method = "POST"
  encoding = "json"
  host = ["http://dynamodb.amazonaws.com"]
  concurrent_calls = 0
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://dynamodb.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "amazon-dynamodb-describetable"
  slug = "amazon-dynamodb-2011-12-05-describetable"
  url_pattern = "/#DescribeTable"
  method = "POST"
  encoding = "json"
  host = ["http://dynamodb.amazonaws.com"]
  concurrent_calls = 0
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://dynamodb.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "amazon-dynamodb-getitem"
  slug = "amazon-dynamodb-2011-12-05-getitem"
  url_pattern = "/#GetItem"
  method = "POST"
  encoding = "json"
  host = ["http://dynamodb.amazonaws.com"]
  concurrent_calls = 0
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://dynamodb.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "amazon-dynamodb-listtables"
  slug = "amazon-dynamodb-2011-12-05-listtables"
  url_pattern = "/#ListTables"
  method = "POST"
  encoding = "json"
  host = ["http://dynamodb.amazonaws.com"]
  query_string_params = ["ExclusiveStartTableName", "Limit"]
  concurrent_calls = 0
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://dynamodb.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "amazon-dynamodb-putitem"
  slug = "amazon-dynamodb-2011-12-05-putitem"
  url_pattern = "/#PutItem"
  method = "POST"
  encoding = "json"
  host = ["http://dynamodb.amazonaws.com"]
  concurrent_calls = 0
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://dynamodb.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "amazon-dynamodb-query"
  slug = "amazon-dynamodb-2011-12-05-query"
  url_pattern = "/#Query"
  method = "POST"
  encoding = "json"
  host = ["http://dynamodb.amazonaws.com"]
  query_string_params = ["ExclusiveStartKey", "Limit"]
  concurrent_calls = 0
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://dynamodb.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "amazon-dynamodb-scan"
  slug = "amazon-dynamodb-2011-12-05-scan"
  url_pattern = "/#Scan"
  method = "POST"
  encoding = "json"
  host = ["http://dynamodb.amazonaws.com"]
  query_string_params = ["ExclusiveStartKey", "Limit"]
  concurrent_calls = 0
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://dynamodb.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "amazon-dynamodb-updateitem"
  slug = "amazon-dynamodb-2011-12-05-updateitem"
  url_pattern = "/#UpdateItem"
  method = "POST"
  encoding = "json"
  host = ["http://dynamodb.amazonaws.com"]
  concurrent_calls = 0
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://dynamodb.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "amazon-dynamodb-updatetable"
  slug = "amazon-dynamodb-2011-12-05-updatetable"
  url_pattern = "/#UpdateTable"
  method = "POST"
  encoding = "json"
  host = ["http://dynamodb.amazonaws.com"]
  concurrent_calls = 0
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://dynamodb.amazonaws.com"]
    schemes = ["http"]

[[backends]]
  group = "amazon-dynamodb"
  slug = "amazon-dynamodb-2011-12-05"
  url_pattern = "/"
  method = "POST"
  encoding = "json"
  host = ["http://dynamodb.amazonaws.com"]
  query_string_params = ["RequestItems"]
  concurrent_calls = 0
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["http://dynamodb.amazonaws.com"]
    schemes = ["http"]
