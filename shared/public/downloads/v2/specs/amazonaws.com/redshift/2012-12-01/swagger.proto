syntax = "proto3";

import "google/protobuf/empty.proto";

import "google/api/annotations.proto";

package amazonredshift;

message PostRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    AuthorizeClusterSecurityGroupIngressMessage body = 10;
}

message Post#AuthorizeSnapshotAccessRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    AuthorizeSnapshotAccessMessage body = 10;
}

message Post#CopyClusterSnapshotRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    CopyClusterSnapshotMessage body = 10;
}

message Post#CreateClusterRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    CreateClusterMessage body = 10;
}

message Post#CreateClusterParameterGroupRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    CreateClusterParameterGroupMessage body = 10;
}

message Post#CreateClusterSecurityGroupRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    CreateClusterSecurityGroupMessage body = 10;
}

message Post#CreateClusterSnapshotRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    CreateClusterSnapshotMessage body = 10;
}

message Post#CreateClusterSubnetGroupRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    CreateClusterSubnetGroupMessage body = 10;
}

message Post#CreateEventSubscriptionRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    CreateEventSubscriptionMessage body = 10;
}

message Post#CreateHsmClientCertificateRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    CreateHsmClientCertificateMessage body = 10;
}

message Post#CreateHsmConfigurationRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    CreateHsmConfigurationMessage body = 10;
}

message Post#CreateSnapshotCopyGrantRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    CreateSnapshotCopyGrantMessage body = 10;
}

message Post#CreateTagsRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    CreateTagsMessage body = 10;
}

message Post#DeleteClusterRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DeleteClusterMessage body = 10;
}

message Post#DeleteClusterParameterGroupRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DeleteClusterParameterGroupMessage body = 10;
}

message Post#DeleteClusterSecurityGroupRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DeleteClusterSecurityGroupMessage body = 10;
}

message Post#DeleteClusterSnapshotRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DeleteClusterSnapshotMessage body = 10;
}

message Post#DeleteClusterSubnetGroupRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DeleteClusterSubnetGroupMessage body = 10;
}

message Post#DeleteEventSubscriptionRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DeleteEventSubscriptionMessage body = 10;
}

message Post#DeleteHsmClientCertificateRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DeleteHsmClientCertificateMessage body = 10;
}

message Post#DeleteHsmConfigurationRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DeleteHsmConfigurationMessage body = 10;
}

message Post#DeleteSnapshotCopyGrantRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DeleteSnapshotCopyGrantMessage body = 10;
}

message Post#DeleteTagsRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DeleteTagsMessage body = 10;
}

message Post#DescribeClusterParameterGroupsRequest {
    string Action = 1;
    // Pagination token
    string Marker = 2;
    // Pagination limit
    string MaxRecords = 3;
    string Version = 4;
    string X_Amz_Algorithm = 5;
    string X_Amz_Content_Sha256 = 6;
    string X_Amz_Credential = 7;
    string X_Amz_Date = 8;
    string X_Amz_Security_Token = 9;
    string X_Amz_Signature = 10;
    string X_Amz_SignedHeaders = 11;
    DescribeClusterParameterGroupsMessage body = 12;
}

message Post#DescribeClusterParametersRequest {
    string Action = 1;
    // Pagination token
    string Marker = 2;
    // Pagination limit
    string MaxRecords = 3;
    string Version = 4;
    string X_Amz_Algorithm = 5;
    string X_Amz_Content_Sha256 = 6;
    string X_Amz_Credential = 7;
    string X_Amz_Date = 8;
    string X_Amz_Security_Token = 9;
    string X_Amz_Signature = 10;
    string X_Amz_SignedHeaders = 11;
    DescribeClusterParametersMessage body = 12;
}

message Post#DescribeClusterSecurityGroupsRequest {
    string Action = 1;
    // Pagination token
    string Marker = 2;
    // Pagination limit
    string MaxRecords = 3;
    string Version = 4;
    string X_Amz_Algorithm = 5;
    string X_Amz_Content_Sha256 = 6;
    string X_Amz_Credential = 7;
    string X_Amz_Date = 8;
    string X_Amz_Security_Token = 9;
    string X_Amz_Signature = 10;
    string X_Amz_SignedHeaders = 11;
    DescribeClusterSecurityGroupsMessage body = 12;
}

message Post#DescribeClusterSnapshotsRequest {
    string Action = 1;
    // Pagination token
    string Marker = 2;
    // Pagination limit
    string MaxRecords = 3;
    string Version = 4;
    string X_Amz_Algorithm = 5;
    string X_Amz_Content_Sha256 = 6;
    string X_Amz_Credential = 7;
    string X_Amz_Date = 8;
    string X_Amz_Security_Token = 9;
    string X_Amz_Signature = 10;
    string X_Amz_SignedHeaders = 11;
    DescribeClusterSnapshotsMessage body = 12;
}

message Post#DescribeClusterSubnetGroupsRequest {
    string Action = 1;
    // Pagination token
    string Marker = 2;
    // Pagination limit
    string MaxRecords = 3;
    string Version = 4;
    string X_Amz_Algorithm = 5;
    string X_Amz_Content_Sha256 = 6;
    string X_Amz_Credential = 7;
    string X_Amz_Date = 8;
    string X_Amz_Security_Token = 9;
    string X_Amz_Signature = 10;
    string X_Amz_SignedHeaders = 11;
    DescribeClusterSubnetGroupsMessage body = 12;
}

message Post#DescribeClusterVersionsRequest {
    string Action = 1;
    // Pagination token
    string Marker = 2;
    // Pagination limit
    string MaxRecords = 3;
    string Version = 4;
    string X_Amz_Algorithm = 5;
    string X_Amz_Content_Sha256 = 6;
    string X_Amz_Credential = 7;
    string X_Amz_Date = 8;
    string X_Amz_Security_Token = 9;
    string X_Amz_Signature = 10;
    string X_Amz_SignedHeaders = 11;
    DescribeClusterVersionsMessage body = 12;
}

message Post#DescribeClustersRequest {
    string Action = 1;
    // Pagination token
    string Marker = 2;
    // Pagination limit
    string MaxRecords = 3;
    string Version = 4;
    string X_Amz_Algorithm = 5;
    string X_Amz_Content_Sha256 = 6;
    string X_Amz_Credential = 7;
    string X_Amz_Date = 8;
    string X_Amz_Security_Token = 9;
    string X_Amz_Signature = 10;
    string X_Amz_SignedHeaders = 11;
    DescribeClustersMessage body = 12;
}

message Post#DescribeDefaultClusterParametersRequest {
    string Action = 1;
    // Pagination token
    string Marker = 2;
    // Pagination limit
    string MaxRecords = 3;
    string Version = 4;
    string X_Amz_Algorithm = 5;
    string X_Amz_Content_Sha256 = 6;
    string X_Amz_Credential = 7;
    string X_Amz_Date = 8;
    string X_Amz_Security_Token = 9;
    string X_Amz_Signature = 10;
    string X_Amz_SignedHeaders = 11;
    DescribeDefaultClusterParametersMessage body = 12;
}

message Post#DescribeEventCategoriesRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DescribeEventCategoriesMessage body = 10;
}

message Post#DescribeEventSubscriptionsRequest {
    string Action = 1;
    // Pagination token
    string Marker = 2;
    // Pagination limit
    string MaxRecords = 3;
    string Version = 4;
    string X_Amz_Algorithm = 5;
    string X_Amz_Content_Sha256 = 6;
    string X_Amz_Credential = 7;
    string X_Amz_Date = 8;
    string X_Amz_Security_Token = 9;
    string X_Amz_Signature = 10;
    string X_Amz_SignedHeaders = 11;
    DescribeEventSubscriptionsMessage body = 12;
}

message Post#DescribeEventsRequest {
    string Action = 1;
    // Pagination token
    string Marker = 2;
    // Pagination limit
    string MaxRecords = 3;
    string Version = 4;
    string X_Amz_Algorithm = 5;
    string X_Amz_Content_Sha256 = 6;
    string X_Amz_Credential = 7;
    string X_Amz_Date = 8;
    string X_Amz_Security_Token = 9;
    string X_Amz_Signature = 10;
    string X_Amz_SignedHeaders = 11;
    DescribeEventsMessage body = 12;
}

message Post#DescribeHsmClientCertificatesRequest {
    string Action = 1;
    // Pagination token
    string Marker = 2;
    // Pagination limit
    string MaxRecords = 3;
    string Version = 4;
    string X_Amz_Algorithm = 5;
    string X_Amz_Content_Sha256 = 6;
    string X_Amz_Credential = 7;
    string X_Amz_Date = 8;
    string X_Amz_Security_Token = 9;
    string X_Amz_Signature = 10;
    string X_Amz_SignedHeaders = 11;
    DescribeHsmClientCertificatesMessage body = 12;
}

message Post#DescribeHsmConfigurationsRequest {
    string Action = 1;
    // Pagination token
    string Marker = 2;
    // Pagination limit
    string MaxRecords = 3;
    string Version = 4;
    string X_Amz_Algorithm = 5;
    string X_Amz_Content_Sha256 = 6;
    string X_Amz_Credential = 7;
    string X_Amz_Date = 8;
    string X_Amz_Security_Token = 9;
    string X_Amz_Signature = 10;
    string X_Amz_SignedHeaders = 11;
    DescribeHsmConfigurationsMessage body = 12;
}

message Post#DescribeLoggingStatusRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DescribeLoggingStatusMessage body = 10;
}

message Post#DescribeOrderableClusterOptionsRequest {
    string Action = 1;
    // Pagination token
    string Marker = 2;
    // Pagination limit
    string MaxRecords = 3;
    string Version = 4;
    string X_Amz_Algorithm = 5;
    string X_Amz_Content_Sha256 = 6;
    string X_Amz_Credential = 7;
    string X_Amz_Date = 8;
    string X_Amz_Security_Token = 9;
    string X_Amz_Signature = 10;
    string X_Amz_SignedHeaders = 11;
    DescribeOrderableClusterOptionsMessage body = 12;
}

message Post#DescribeReservedNodeOfferingsRequest {
    string Action = 1;
    // Pagination token
    string Marker = 2;
    // Pagination limit
    string MaxRecords = 3;
    string Version = 4;
    string X_Amz_Algorithm = 5;
    string X_Amz_Content_Sha256 = 6;
    string X_Amz_Credential = 7;
    string X_Amz_Date = 8;
    string X_Amz_Security_Token = 9;
    string X_Amz_Signature = 10;
    string X_Amz_SignedHeaders = 11;
    DescribeReservedNodeOfferingsMessage body = 12;
}

message Post#DescribeReservedNodesRequest {
    string Action = 1;
    // Pagination token
    string Marker = 2;
    // Pagination limit
    string MaxRecords = 3;
    string Version = 4;
    string X_Amz_Algorithm = 5;
    string X_Amz_Content_Sha256 = 6;
    string X_Amz_Credential = 7;
    string X_Amz_Date = 8;
    string X_Amz_Security_Token = 9;
    string X_Amz_Signature = 10;
    string X_Amz_SignedHeaders = 11;
    DescribeReservedNodesMessage body = 12;
}

message Post#DescribeResizeRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DescribeResizeMessage body = 10;
}

message Post#DescribeSnapshotCopyGrantsRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DescribeSnapshotCopyGrantsMessage body = 10;
}

message Post#DescribeTableRestoreStatusRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DescribeTableRestoreStatusMessage body = 10;
}

message Post#DescribeTagsRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DescribeTagsMessage body = 10;
}

message Post#DisableLoggingRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DisableLoggingMessage body = 10;
}

message Post#DisableSnapshotCopyRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    DisableSnapshotCopyMessage body = 10;
}

message Post#EnableLoggingRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    EnableLoggingMessage body = 10;
}

message Post#EnableSnapshotCopyRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    EnableSnapshotCopyMessage body = 10;
}

message Post#GetClusterCredentialsRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    GetClusterCredentialsMessage body = 10;
}

message Post#ModifyClusterRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    ModifyClusterMessage body = 10;
}

message Post#ModifyClusterIamRolesRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    ModifyClusterIamRolesMessage body = 10;
}

message Post#ModifyClusterParameterGroupRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    ModifyClusterParameterGroupMessage body = 10;
}

message Post#ModifyClusterSubnetGroupRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    ModifyClusterSubnetGroupMessage body = 10;
}

message Post#ModifyEventSubscriptionRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    ModifyEventSubscriptionMessage body = 10;
}

message Post#ModifySnapshotCopyRetentionPeriodRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    ModifySnapshotCopyRetentionPeriodMessage body = 10;
}

message Post#PurchaseReservedNodeOfferingRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    PurchaseReservedNodeOfferingMessage body = 10;
}

message Post#RebootClusterRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    RebootClusterMessage body = 10;
}

message Post#ResetClusterParameterGroupRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    ResetClusterParameterGroupMessage body = 10;
}

message Post#RestoreFromClusterSnapshotRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    RestoreFromClusterSnapshotMessage body = 10;
}

message Post#RestoreTableFromClusterSnapshotRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    RestoreTableFromClusterSnapshotMessage body = 10;
}

message Post#RevokeClusterSecurityGroupIngressRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    RevokeClusterSecurityGroupIngressMessage body = 10;
}

message Post#RevokeSnapshotAccessRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    RevokeSnapshotAccessMessage body = 10;
}

message Post#RotateEncryptionKeyRequest {
    string Action = 1;
    string Version = 2;
    string X_Amz_Algorithm = 3;
    string X_Amz_Content_Sha256 = 4;
    string X_Amz_Credential = 5;
    string X_Amz_Date = 6;
    string X_Amz_Security_Token = 7;
    string X_Amz_Signature = 8;
    string X_Amz_SignedHeaders = 9;
    RotateEncryptionKeyMessage body = 10;
}

message AccessToSnapshotDeniedFault {
}

message AccountWithRestoreAccess {
    // The identifier of an AWS support account authorized to restore a snapshot. For AWS support, the identifier is <code>amazon-redshift-support</code>.
    string AccountAlias = 1;
    // The identifier of an AWS customer account authorized to restore a snapshot.
    string AccountId = 2;
}

repeated AccountWithRestoreAccess AccountsWithRestoreAccessList = 1



message AuthorizationAlreadyExistsFault {
}

message AuthorizationNotFoundFault {
}

message AuthorizationQuotaExceededFault {
}

message AuthorizeClusterSecurityGroupIngressMessage {
    // The IP range to be added the Amazon Redshift security group.
    string CIDRIP = 1;
    // The name of the security group to which the ingress rule is added.
    string ClusterSecurityGroupName = 2;
    // The EC2 security group to be added the Amazon Redshift security group.
    string EC2SecurityGroupName = 3;
    // <p>The AWS account number of the owner of the security group specified by the <i>EC2SecurityGroupName</i> parameter. The AWS Access Key ID is not an acceptable value. </p> <p>Example: <code>111122223333</code> </p>
    string EC2SecurityGroupOwnerId = 4;
}

message AuthorizeClusterSecurityGroupIngressResult {
    ClusterSecurityGroup ClusterSecurityGroup = 1;
}

message AuthorizeSnapshotAccessMessage {
    // <p>The identifier of the AWS customer account authorized to restore the specified snapshot.</p> <p>To share a snapshot with AWS support, specify amazon-redshift-support.</p>
    string AccountWithRestoreAccess = 1;
    // The identifier of the cluster the snapshot was created from. This parameter is required if your IAM user has a policy containing a snapshot resource element that specifies anything other than * for the cluster name.
    string SnapshotClusterIdentifier = 2;
    // The identifier of the snapshot the account is authorized to restore.
    string SnapshotIdentifier = 3;
}

message AuthorizeSnapshotAccessResult {
    Snapshot Snapshot = 1;
}

message AvailabilityZone {
    // The name of the availability zone.
    string Name = 1;
}

repeated AvailabilityZone AvailabilityZoneList = 1





message BucketNotFoundFault {
}

message Cluster {
    // A Boolean value that, if <code>true</code>, indicates that major version upgrades will be applied automatically to the cluster during the maintenance window.
    boolean AllowVersionUpgrade = 1;
    // The number of days that automatic cluster snapshots are retained.
    integer AutomatedSnapshotRetentionPeriod = 2;
    // The name of the Availability Zone in which the cluster is located.
    string AvailabilityZone = 3;
    // The date and time that the cluster was created.
    string ClusterCreateTime = 4;
    // The unique identifier of the cluster.
    string ClusterIdentifier = 5;
    // The nodes in the cluster.
    array ClusterNodes = 6;
    // The list of cluster parameter groups that are associated with this cluster. Each parameter group in the list is returned with its status.
    array ClusterParameterGroups = 7;
    // The public key for the cluster.
    string ClusterPublicKey = 8;
    // The specific revision number of the database in the cluster.
    string ClusterRevisionNumber = 9;
    // <p>A list of cluster security group that are associated with the cluster. Each security group is represented by an element that contains <code>ClusterSecurityGroup.Name</code> and <code>ClusterSecurityGroup.Status</code> subelements. </p> <p>Cluster security groups are used when the cluster is not created in an Amazon Virtual Private Cloud (VPC). Clusters that are created in a VPC use VPC security groups, which are listed by the <b>VpcSecurityGroups</b> parameter. </p>
    array ClusterSecurityGroups = 10;
    // A value that returns the destination region and retention period that are configured for cross-region snapshot copy.
    ClusterSnapshotCopyStatus ClusterSnapshotCopyStatus = 11;
    // <p> The current state of the cluster. Possible values are the following:</p> <ul> <li> <p> <code>available</code> </p> </li> <li> <p> <code>creating</code> </p> </li> <li> <p> <code>deleting</code> </p> </li> <li> <p> <code>final-snapshot</code> </p> </li> <li> <p> <code>hardware-failure</code> </p> </li> <li> <p> <code>incompatible-hsm</code> </p> </li> <li> <p> <code>incompatible-network</code> </p> </li> <li> <p> <code>incompatible-parameters</code> </p> </li> <li> <p> <code>incompatible-restore</code> </p> </li> <li> <p> <code>modifying</code> </p> </li> <li> <p> <code>rebooting</code> </p> </li> <li> <p> <code>renaming</code> </p> </li> <li> <p> <code>resizing</code> </p> </li> <li> <p> <code>rotating-keys</code> </p> </li> <li> <p> <code>storage-full</code> </p> </li> <li> <p> <code>updating-hsm</code> </p> </li> </ul>
    string ClusterStatus = 12;
    // The name of the subnet group that is associated with the cluster. This parameter is valid only when the cluster is in a VPC.
    string ClusterSubnetGroupName = 13;
    // The version ID of the Amazon Redshift engine that is running on the cluster.
    string ClusterVersion = 14;
    // The name of the initial database that was created when the cluster was created. This same name is returned for the life of the cluster. If an initial database was not specified, a database named <code>dev</code>dev was created by default.
    string DBName = 15;
    // The status of the elastic IP (EIP) address.
    ElasticIpStatus ElasticIpStatus = 16;
    // A Boolean value that, if <code>true</code>, indicates that data in the cluster is encrypted at rest.
    boolean Encrypted = 17;
    // The connection endpoint.
    Endpoint Endpoint = 18;
    // <p>An option that specifies whether to create the cluster with enhanced VPC routing enabled. To create a cluster that uses enhanced VPC routing, the cluster must be in a VPC. For more information, see <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/enhanced-vpc-routing.html">Enhanced VPC Routing</a> in the Amazon Redshift Cluster Management Guide.</p> <p>If this option is <code>true</code>, enhanced VPC routing is enabled. </p> <p>Default: false</p>
    boolean EnhancedVpcRouting = 19;
    // <p>A value that reports whether the Amazon Redshift cluster has finished applying any hardware security module (HSM) settings changes specified in a modify cluster command.</p> <p>Values: active, applying</p>
    HsmStatus HsmStatus = 20;
    // A list of AWS Identity and Access Management (IAM) roles that can be used by the cluster to access other AWS services.
    array IamRoles = 21;
    // The AWS Key Management Service (AWS KMS) key ID of the encryption key used to encrypt data in the cluster.
    string KmsKeyId = 22;
    // The master user name for the cluster. This name is used to connect to the database that is specified in the <b>DBName</b> parameter.
    string MasterUsername = 23;
    // The status of a modify operation, if any, initiated for the cluster.
    string ModifyStatus = 24;
    // The node type for the nodes in the cluster.
    string NodeType = 25;
    // The number of compute nodes in the cluster.
    integer NumberOfNodes = 26;
    // A value that, if present, indicates that changes to the cluster are pending. Specific pending changes are identified by subelements.
    PendingModifiedValues PendingModifiedValues = 27;
    // The weekly time range, in Universal Coordinated Time (UTC), during which system maintenance can occur.
    string PreferredMaintenanceWindow = 28;
    // A Boolean value that, if <code>true</code>, indicates that the cluster can be accessed from a public network.
    boolean PubliclyAccessible = 29;
    // A value that describes the status of a cluster restore action. This parameter returns null if the cluster was not created by restoring a snapshot.
    RestoreStatus RestoreStatus = 30;
    // The list of tags for the cluster.
    array Tags = 31;
    // The identifier of the VPC the cluster is in, if the cluster is in a VPC.
    string VpcId = 32;
    // A list of Amazon Virtual Private Cloud (Amazon VPC) security groups that are associated with the cluster. This parameter is returned only if the cluster is in a VPC.
    array VpcSecurityGroups = 33;
}

message ClusterAlreadyExistsFault {
}

message ClusterCredentials {
    // A temporary password that authorizes the user name returned by <code>DbUser</code> to log on to the database <code>DbName</code>.
    string DbPassword = 1;
    // A database user name that is authorized to log on to the database <code>DbName</code> using the password <code>DbPassword</code>. If the <code>DbGroups</code> parameter is specifed, <code>DbUser</code> is added to the listed groups for the current session. The user name is prefixed with <code>IAM:</code> for an existing user name or <code>IAMA:</code> if the user was auto-created.
    string DbUser = 2;
    // The date and time <code>DbPassword</code> expires.
    string Expiration = 3;
}

message ClusterIamRole {
    // <p>A value that describes the status of the IAM role's association with an Amazon Redshift cluster.</p> <p>The following are possible statuses and descriptions.</p> <ul> <li> <p> <code>in-sync</code>: The role is available for use by the cluster.</p> </li> <li> <p> <code>adding</code>: The role is in the process of being associated with the cluster.</p> </li> <li> <p> <code>removing</code>: The role is in the process of being disassociated with the cluster.</p> </li> </ul>
    string ApplyStatus = 1;
    // The Amazon Resource Name (ARN) of the IAM role, for example, <code>arn:aws:iam::123456789012:role/RedshiftCopyUnload</code>.
    string IamRoleArn = 2;
}

repeated ClusterIamRole ClusterIamRoleList = 1

repeated Cluster ClusterList = 1

message ClusterNode {
    // Whether the node is a leader node or a compute node.
    string NodeRole = 1;
    // The private IP address of a node within a cluster.
    string PrivateIPAddress = 2;
    // The public IP address of a node within a cluster.
    string PublicIPAddress = 3;
}

repeated ClusterNode ClusterNodesList = 1

message ClusterNotFoundFault {
}

message ClusterParameterGroup {
    // The description of the parameter group.
    string Description = 1;
    // The name of the cluster parameter group family that this cluster parameter group is compatible with.
    string ParameterGroupFamily = 2;
    // The name of the cluster parameter group.
    string ParameterGroupName = 3;
    // The list of tags for the cluster parameter group.
    array Tags = 4;
}

message ClusterParameterGroupAlreadyExistsFault {
}

message ClusterParameterGroupDetails {
    // A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request.
    string Marker = 1;
    // A list of <a>Parameter</a> instances. Each instance lists the parameters of one cluster parameter group.
    array Parameters = 2;
}

message ClusterParameterGroupNameMessage {
    // The name of the cluster parameter group.
    string ParameterGroupName = 1;
    // The status of the parameter group. For example, if you made a change to a parameter group name-value pair, then the change could be pending a reboot of an associated cluster.
    string ParameterGroupStatus = 2;
}

message ClusterParameterGroupNotFoundFault {
}

message ClusterParameterGroupQuotaExceededFault {
}

message ClusterParameterGroupStatus {
    // <p>The list of parameter statuses.</p> <p> For more information about parameters and parameter groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-parameter-groups.html">Amazon Redshift Parameter Groups</a> in the <i>Amazon Redshift Cluster Management Guide</i>.</p>
    array ClusterParameterStatusList = 1;
    // The status of parameter updates.
    string ParameterApplyStatus = 2;
    // The name of the cluster parameter group.
    string ParameterGroupName = 3;
}

repeated ClusterParameterGroupStatus ClusterParameterGroupStatusList = 1

message ClusterParameterGroupsMessage {
    // A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request.
    string Marker = 1;
    // A list of <a>ClusterParameterGroup</a> instances. Each instance describes one cluster parameter group.
    array ParameterGroups = 2;
}

message ClusterParameterStatus {
    // The error that prevented the parameter from being applied to the database.
    string ParameterApplyErrorDescription = 1;
    // <p>The status of the parameter that indicates whether the parameter is in sync with the database, waiting for a cluster reboot, or encountered an error when being applied.</p> <p>The following are possible statuses and descriptions.</p> <ul> <li> <p> <code>in-sync</code>: The parameter value is in sync with the database.</p> </li> <li> <p> <code>pending-reboot</code>: The parameter value will be applied after the cluster reboots.</p> </li> <li> <p> <code>applying</code>: The parameter value is being applied to the database.</p> </li> <li> <p> <code>invalid-parameter</code>: Cannot apply the parameter value because it has an invalid value or syntax.</p> </li> <li> <p> <code>apply-deferred</code>: The parameter contains static property changes. The changes are deferred until the cluster reboots.</p> </li> <li> <p> <code>apply-error</code>: Cannot connect to the cluster. The parameter change will be applied after the cluster reboots.</p> </li> <li> <p> <code>unknown-error</code>: Cannot apply the parameter change right now. The change will be applied after the cluster reboots.</p> </li> </ul>
    string ParameterApplyStatus = 2;
    // The name of the parameter.
    string ParameterName = 3;
}

repeated ClusterParameterStatus ClusterParameterStatusList = 1

message ClusterQuotaExceededFault {
}

message ClusterSecurityGroup {
    // The name of the cluster security group to which the operation was applied.
    string ClusterSecurityGroupName = 1;
    // A description of the security group.
    string Description = 2;
    // A list of EC2 security groups that are permitted to access clusters associated with this cluster security group.
    array EC2SecurityGroups = 3;
    // A list of IP ranges (CIDR blocks) that are permitted to access clusters associated with this cluster security group.
    array IPRanges = 4;
    // The list of tags for the cluster security group.
    array Tags = 5;
}

message ClusterSecurityGroupAlreadyExistsFault {
}

message ClusterSecurityGroupMembership {
    // The name of the cluster security group.
    string ClusterSecurityGroupName = 1;
    // The status of the cluster security group.
    string Status = 2;
}

repeated ClusterSecurityGroupMembership ClusterSecurityGroupMembershipList = 1

message ClusterSecurityGroupMessage {
    // A list of <a>ClusterSecurityGroup</a> instances.
    array ClusterSecurityGroups = 1;
    // A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request.
    string Marker = 2;
}

repeated string ClusterSecurityGroupNameList = 1

message ClusterSecurityGroupNotFoundFault {
}

message ClusterSecurityGroupQuotaExceededFault {
}

repeated ClusterSecurityGroup ClusterSecurityGroups = 1

message ClusterSnapshotAlreadyExistsFault {
}

message ClusterSnapshotCopyStatus {
    // The destination region that snapshots are automatically copied to when cross-region snapshot copy is enabled.
    string DestinationRegion = 1;
    // The number of days that automated snapshots are retained in the destination region after they are copied from a source region.
    integer RetentionPeriod = 2;
    // The name of the snapshot copy grant.
    string SnapshotCopyGrantName = 3;
}

message ClusterSnapshotNotFoundFault {
}

message ClusterSnapshotQuotaExceededFault {
}

message ClusterSubnetGroup {
    // The name of the cluster subnet group.
    string ClusterSubnetGroupName = 1;
    // The description of the cluster subnet group.
    string Description = 2;
    // The status of the cluster subnet group. Possible values are <code>Complete</code>, <code>Incomplete</code> and <code>Invalid</code>.
    string SubnetGroupStatus = 3;
    // A list of the VPC <a>Subnet</a> elements.
    array Subnets = 4;
    // The list of tags for the cluster subnet group.
    array Tags = 5;
    // The VPC ID of the cluster subnet group.
    string VpcId = 6;
}

message ClusterSubnetGroupAlreadyExistsFault {
}

message ClusterSubnetGroupMessage {
    // A list of <a>ClusterSubnetGroup</a> instances.
    array ClusterSubnetGroups = 1;
    // A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request.
    string Marker = 2;
}

message ClusterSubnetGroupNotFoundFault {
}

message ClusterSubnetGroupQuotaExceededFault {
}

repeated ClusterSubnetGroup ClusterSubnetGroups = 1

message ClusterSubnetQuotaExceededFault {
}

message ClusterVersion {
    // The name of the cluster parameter group family for the cluster.
    string ClusterParameterGroupFamily = 1;
    // The version number used by the cluster.
    string ClusterVersion = 2;
    // The description of the cluster version.
    string Description = 3;
}

repeated ClusterVersion ClusterVersionList = 1

message ClusterVersionsMessage {
    // A list of <code>Version</code> elements.
    array ClusterVersions = 1;
    // A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request.
    string Marker = 2;
}

message ClustersMessage {
    // A list of <code>Cluster</code> objects, where each object describes one cluster.
    array Clusters = 1;
    // A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request.
    string Marker = 2;
}

message CopyClusterSnapshotMessage {
    // <p>The identifier of the cluster the source snapshot was created from. This parameter is required if your IAM user has a policy containing a snapshot resource element that specifies anything other than * for the cluster name.</p> <p>Constraints:</p> <ul> <li> <p>Must be the identifier for a valid cluster.</p> </li> </ul>
    string SourceSnapshotClusterIdentifier = 1;
    // <p>The identifier for the source snapshot.</p> <p>Constraints:</p> <ul> <li> <p>Must be the identifier for a valid automated snapshot whose state is <code>available</code>.</p> </li> </ul>
    string SourceSnapshotIdentifier = 2;
    // <p>The identifier given to the new manual snapshot.</p> <p>Constraints:</p> <ul> <li> <p>Cannot be null, empty, or blank.</p> </li> <li> <p>Must contain from 1 to 255 alphanumeric characters or hyphens.</p> </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens.</p> </li> <li> <p>Must be unique for the AWS account that is making the request.</p> </li> </ul>
    string TargetSnapshotIdentifier = 3;
}

message CopyClusterSnapshotResult {
    Snapshot Snapshot = 1;
}

message CopyToRegionDisabledFault {
}

message CreateClusterMessage {
    // Reserved.
    string AdditionalInfo = 1;
    // <p>If <code>true</code>, major version upgrades can be applied during the maintenance window to the Amazon Redshift engine that is running on the cluster.</p> <p>When a new major version of the Amazon Redshift engine is released, you can request that the service automatically apply upgrades during the maintenance window to the Amazon Redshift engine that is running on your cluster.</p> <p>Default: <code>true</code> </p>
    boolean AllowVersionUpgrade = 2;
    // <p>The number of days that automated snapshots are retained. If the value is 0, automated snapshots are disabled. Even if automated snapshots are disabled, you can still create manual snapshots when you want with <a>CreateClusterSnapshot</a>. </p> <p>Default: <code>1</code> </p> <p>Constraints: Must be a value from 0 to 35.</p>
    integer AutomatedSnapshotRetentionPeriod = 3;
    // <p>The EC2 Availability Zone (AZ) in which you want Amazon Redshift to provision the cluster. For example, if you have several EC2 instances running in a specific Availability Zone, then you might want the cluster to be provisioned in the same zone in order to decrease network latency.</p> <p>Default: A random, system-chosen Availability Zone in the region that is specified by the endpoint.</p> <p>Example: <code>us-east-1d</code> </p> <p>Constraint: The specified Availability Zone must be in the same region as the current endpoint.</p>
    string AvailabilityZone = 4;
    // <p>A unique identifier for the cluster. You use this identifier to refer to the cluster for any subsequent cluster operations such as deleting or modifying. The identifier also appears in the Amazon Redshift console.</p> <p>Constraints:</p> <ul> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens.</p> </li> <li> <p>Alphabetic characters must be lowercase.</p> </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens.</p> </li> <li> <p>Must be unique for all clusters within an AWS account.</p> </li> </ul> <p>Example: <code>myexamplecluster</code> </p>
    string ClusterIdentifier = 5;
    // <p>The name of the parameter group to be associated with this cluster.</p> <p>Default: The default Amazon Redshift cluster parameter group. For information about the default parameter group, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-parameter-groups.html">Working with Amazon Redshift Parameter Groups</a> </p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters or hyphens.</p> </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens.</p> </li> </ul>
    string ClusterParameterGroupName = 6;
    // <p>A list of security groups to be associated with this cluster.</p> <p>Default: The default cluster security group for Amazon Redshift.</p>
    array ClusterSecurityGroups = 7;
    // <p>The name of a cluster subnet group to be associated with this cluster.</p> <p>If this parameter is not provided the resulting cluster will be deployed outside virtual private cloud (VPC).</p>
    string ClusterSubnetGroupName = 8;
    // <p>The type of the cluster. When cluster type is specified as</p> <ul> <li> <p> <code>single-node</code>, the <b>NumberOfNodes</b> parameter is not required.</p> </li> <li> <p> <code>multi-node</code>, the <b>NumberOfNodes</b> parameter is required.</p> </li> </ul> <p>Valid Values: <code>multi-node</code> | <code>single-node</code> </p> <p>Default: <code>multi-node</code> </p>
    string ClusterType = 9;
    // <p>The version of the Amazon Redshift engine software that you want to deploy on the cluster.</p> <p>The version selected runs on all the nodes in the cluster.</p> <p>Constraints: Only version 1.0 is currently available.</p> <p>Example: <code>1.0</code> </p>
    string ClusterVersion = 10;
    // <p>The name of the first database to be created when the cluster is created.</p> <p>To create additional databases after the cluster is created, connect to the cluster with a SQL client and use SQL commands to create a database. For more information, go to <a href="http://docs.aws.amazon.com/redshift/latest/dg/t_creating_database.html">Create a Database</a> in the Amazon Redshift Database Developer Guide. </p> <p>Default: <code>dev</code> </p> <p>Constraints:</p> <ul> <li> <p>Must contain 1 to 64 alphanumeric characters.</p> </li> <li> <p>Must contain only lowercase letters.</p> </li> <li> <p>Cannot be a word that is reserved by the service. A list of reserved words can be found in <a href="http://docs.aws.amazon.com/redshift/latest/dg/r_pg_keywords.html">Reserved Words</a> in the Amazon Redshift Database Developer Guide. </p> </li> </ul>
    string DBName = 11;
    // <p>The Elastic IP (EIP) address for the cluster.</p> <p>Constraints: The cluster must be provisioned in EC2-VPC and publicly-accessible through an Internet gateway. For more information about provisioning clusters in EC2-VPC, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html#cluster-platforms">Supported Platforms to Launch Your Cluster</a> in the Amazon Redshift Cluster Management Guide.</p>
    string ElasticIp = 12;
    // <p>If <code>true</code>, the data in the cluster is encrypted at rest. </p> <p>Default: false</p>
    boolean Encrypted = 13;
    // <p>An option that specifies whether to create the cluster with enhanced VPC routing enabled. To create a cluster that uses enhanced VPC routing, the cluster must be in a VPC. For more information, see <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/enhanced-vpc-routing.html">Enhanced VPC Routing</a> in the Amazon Redshift Cluster Management Guide.</p> <p>If this option is <code>true</code>, enhanced VPC routing is enabled. </p> <p>Default: false</p>
    boolean EnhancedVpcRouting = 14;
    // Specifies the name of the HSM client certificate the Amazon Redshift cluster uses to retrieve the data encryption keys stored in an HSM.
    string HsmClientCertificateIdentifier = 15;
    // Specifies the name of the HSM configuration that contains the information the Amazon Redshift cluster can use to retrieve and store keys in an HSM.
    string HsmConfigurationIdentifier = 16;
    // <p>A list of AWS Identity and Access Management (IAM) roles that can be used by the cluster to access other AWS services. You must supply the IAM roles in their Amazon Resource Name (ARN) format. You can supply up to 10 IAM roles in a single request.</p> <p>A cluster can have up to 10 IAM roles associated with it at any time.</p>
    array IamRoles = 17;
    // The AWS Key Management Service (KMS) key ID of the encryption key that you want to use to encrypt data in the cluster.
    string KmsKeyId = 18;
    // <p>The password associated with the master user account for the cluster that is being created.</p> <p>Constraints:</p> <ul> <li> <p>Must be between 8 and 64 characters in length.</p> </li> <li> <p>Must contain at least one uppercase letter.</p> </li> <li> <p>Must contain at least one lowercase letter.</p> </li> <li> <p>Must contain one number.</p> </li> <li> <p>Can be any printable ASCII character (ASCII code 33 to 126) except ' (single quote), " (double quote), \, /, @, or space.</p> </li> </ul>
    string MasterUserPassword = 19;
    // <p>The user name associated with the master user account for the cluster that is being created.</p> <p>Constraints:</p> <ul> <li> <p>Must be 1 - 128 alphanumeric characters.</p> </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot be a reserved word. A list of reserved words can be found in <a href="http://docs.aws.amazon.com/redshift/latest/dg/r_pg_keywords.html">Reserved Words</a> in the Amazon Redshift Database Developer Guide. </p> </li> </ul>
    string MasterUsername = 20;
    // <p>The node type to be provisioned for the cluster. For information about node types, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html#how-many-nodes"> Working with Clusters</a> in the <i>Amazon Redshift Cluster Management Guide</i>. </p> <p>Valid Values: <code>ds1.xlarge</code> | <code>ds1.8xlarge</code> | <code>ds2.xlarge</code> | <code>ds2.8xlarge</code> | <code>dc1.large</code> | <code>dc1.8xlarge</code>. </p>
    string NodeType = 21;
    // <p>The number of compute nodes in the cluster. This parameter is required when the <b>ClusterType</b> parameter is specified as <code>multi-node</code>. </p> <p>For information about determining how many nodes you need, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html#how-many-nodes"> Working with Clusters</a> in the <i>Amazon Redshift Cluster Management Guide</i>. </p> <p>If you don't specify this parameter, you get a single-node cluster. When requesting a multi-node cluster, you must specify the number of nodes that you want in the cluster.</p> <p>Default: <code>1</code> </p> <p>Constraints: Value must be at least 1 and no more than 100.</p>
    integer NumberOfNodes = 22;
    // <p>The port number on which the cluster accepts incoming connections.</p> <p>The cluster is accessible only via the JDBC and ODBC connection strings. Part of the connection string requires the port on which the cluster will listen for incoming connections.</p> <p>Default: <code>5439</code> </p> <p>Valid Values: <code>1150-65535</code> </p>
    integer Port = 23;
    // <p>The weekly time range (in UTC) during which automated cluster maintenance can occur.</p> <p> Format: <code>ddd:hh24:mi-ddd:hh24:mi</code> </p> <p> Default: A 30-minute window selected at random from an 8-hour block of time per region, occurring on a random day of the week. For more information about the time blocks for each region, see <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html#rs-maintenance-windows">Maintenance Windows</a> in Amazon Redshift Cluster Management Guide.</p> <p>Valid Days: Mon | Tue | Wed | Thu | Fri | Sat | Sun</p> <p>Constraints: Minimum 30-minute window.</p>
    string PreferredMaintenanceWindow = 24;
    // If <code>true</code>, the cluster can be accessed from a public network.
    boolean PubliclyAccessible = 25;
    // A list of tag instances.
    array Tags = 26;
    // <p>A list of Virtual Private Cloud (VPC) security groups to be associated with the cluster.</p> <p>Default: The default VPC security group is associated with the cluster.</p>
    array VpcSecurityGroupIds = 27;
}

message CreateClusterParameterGroupMessage {
    // A description of the parameter group.
    string Description = 1;
    // <p>The Amazon Redshift engine version to which the cluster parameter group applies. The cluster engine version determines the set of parameters.</p> <p>To get a list of valid parameter group family names, you can call <a>DescribeClusterParameterGroups</a>. By default, Amazon Redshift returns a list of all the parameter groups that are owned by your AWS account, including the default parameter groups for each Amazon Redshift engine version. The parameter group family names associated with the default parameter groups provide you the valid values. For example, a valid family name is "redshift-1.0". </p>
    string ParameterGroupFamily = 2;
    // <p>The name of the cluster parameter group.</p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters or hyphens</p> </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens.</p> </li> <li> <p>Must be unique withing your AWS account.</p> </li> </ul> <note> <p>This value is stored as a lower-case string.</p> </note>
    string ParameterGroupName = 3;
    // A list of tag instances.
    array Tags = 4;
}

message CreateClusterParameterGroupResult {
    ClusterParameterGroup ClusterParameterGroup = 1;
}

message CreateClusterResult {
    Cluster Cluster = 1;
}

message CreateClusterSecurityGroupMessage {
    // <p>The name for the security group. Amazon Redshift stores the value as a lowercase string.</p> <p>Constraints:</p> <ul> <li> <p>Must contain no more than 255 alphanumeric characters or hyphens.</p> </li> <li> <p>Must not be "Default".</p> </li> <li> <p>Must be unique for all security groups that are created by your AWS account.</p> </li> </ul> <p>Example: <code>examplesecuritygroup</code> </p>
    string ClusterSecurityGroupName = 1;
    // A description for the security group.
    string Description = 2;
    // A list of tag instances.
    array Tags = 3;
}

message CreateClusterSecurityGroupResult {
    ClusterSecurityGroup ClusterSecurityGroup = 1;
}

message CreateClusterSnapshotMessage {
    // The cluster identifier for which you want a snapshot.
    string ClusterIdentifier = 1;
    // <p>A unique identifier for the snapshot that you are requesting. This identifier must be unique for all snapshots within the AWS account.</p> <p>Constraints:</p> <ul> <li> <p>Cannot be null, empty, or blank</p> </li> <li> <p>Must contain from 1 to 255 alphanumeric characters or hyphens</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul> <p>Example: <code>my-snapshot-id</code> </p>
    string SnapshotIdentifier = 2;
    // A list of tag instances.
    array Tags = 3;
}

message CreateClusterSnapshotResult {
    Snapshot Snapshot = 1;
}

message CreateClusterSubnetGroupMessage {
    // <p>The name for the subnet group. Amazon Redshift stores the value as a lowercase string.</p> <p>Constraints:</p> <ul> <li> <p>Must contain no more than 255 alphanumeric characters or hyphens.</p> </li> <li> <p>Must not be "Default".</p> </li> <li> <p>Must be unique for all subnet groups that are created by your AWS account.</p> </li> </ul> <p>Example: <code>examplesubnetgroup</code> </p>
    string ClusterSubnetGroupName = 1;
    // A description for the subnet group.
    string Description = 2;
    // An array of VPC subnet IDs. A maximum of 20 subnets can be modified in a single request.
    array SubnetIds = 3;
    // A list of tag instances.
    array Tags = 4;
}

message CreateClusterSubnetGroupResult {
    ClusterSubnetGroup ClusterSubnetGroup = 1;
}

message CreateEventSubscriptionMessage {
    // A Boolean value; set to <code>true</code> to activate the subscription, set to <code>false</code> to create the subscription but not active it.
    boolean Enabled = 1;
    // <p>Specifies the Amazon Redshift event categories to be published by the event notification subscription.</p> <p>Values: Configuration, Management, Monitoring, Security</p>
    array EventCategories = 2;
    // <p>Specifies the Amazon Redshift event severity to be published by the event notification subscription.</p> <p>Values: ERROR, INFO</p>
    string Severity = 3;
    // The Amazon Resource Name (ARN) of the Amazon SNS topic used to transmit the event notifications. The ARN is created by Amazon SNS when you create a topic and subscribe to it.
    string SnsTopicArn = 4;
    // <p>A list of one or more identifiers of Amazon Redshift source objects. All of the objects must be of the same type as was specified in the source type parameter. The event subscription will return only events generated by the specified objects. If not specified, then events are returned for all objects within the source type specified.</p> <p>Example: my-cluster-1, my-cluster-2</p> <p>Example: my-snapshot-20131010</p>
    array SourceIds = 5;
    // <p>The type of source that will be generating the events. For example, if you want to be notified of events generated by a cluster, you would set this parameter to cluster. If this value is not specified, events are returned for all Amazon Redshift objects in your AWS account. You must specify a source type in order to specify source IDs.</p> <p>Valid values: cluster, cluster-parameter-group, cluster-security-group, and cluster-snapshot.</p>
    string SourceType = 6;
    // <p>The name of the event subscription to be created.</p> <p>Constraints:</p> <ul> <li> <p>Cannot be null, empty, or blank.</p> </li> <li> <p>Must contain from 1 to 255 alphanumeric characters or hyphens.</p> </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens.</p> </li> </ul>
    string SubscriptionName = 7;
    // A list of tag instances.
    array Tags = 8;
}

message CreateEventSubscriptionResult {
    EventSubscription EventSubscription = 1;
}

message CreateHsmClientCertificateMessage {
    // The identifier to be assigned to the new HSM client certificate that the cluster will use to connect to the HSM to use the database encryption keys.
    string HsmClientCertificateIdentifier = 1;
    // A list of tag instances.
    array Tags = 2;
}

message CreateHsmClientCertificateResult {
    HsmClientCertificate HsmClientCertificate = 1;
}

message CreateHsmConfigurationMessage {
    // A text description of the HSM configuration to be created.
    string Description = 1;
    // The identifier to be assigned to the new Amazon Redshift HSM configuration.
    string HsmConfigurationIdentifier = 2;
    // The IP address that the Amazon Redshift cluster must use to access the HSM.
    string HsmIpAddress = 3;
    // The name of the partition in the HSM where the Amazon Redshift clusters will store their database encryption keys.
    string HsmPartitionName = 4;
    // The password required to access the HSM partition.
    string HsmPartitionPassword = 5;
    // The HSMs public certificate file. When using Cloud HSM, the file name is server.pem.
    string HsmServerPublicCertificate = 6;
    // A list of tag instances.
    array Tags = 7;
}

message CreateHsmConfigurationResult {
    HsmConfiguration HsmConfiguration = 1;
}

message CreateSnapshotCopyGrantMessage {
    // The unique identifier of the customer master key (CMK) to which to grant Amazon Redshift permission. If no key is specified, the default key is used.
    string KmsKeyId = 1;
    // <p>The name of the snapshot copy grant. This name must be unique in the region for the AWS account.</p> <p>Constraints:</p> <ul> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens.</p> </li> <li> <p>Alphabetic characters must be lowercase.</p> </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens.</p> </li> <li> <p>Must be unique for all clusters within an AWS account.</p> </li> </ul>
    string SnapshotCopyGrantName = 2;
    // A list of tag instances.
    array Tags = 3;
}

message CreateSnapshotCopyGrantResult {
    SnapshotCopyGrant SnapshotCopyGrant = 1;
}

message CreateTagsMessage {
    // The Amazon Resource Name (ARN) to which you want to add the tag or tags. For example, <code>arn:aws:redshift:us-east-1:123456789:cluster:t1</code>.
    string ResourceName = 1;
    // One or more name/value pairs to add as tags to the specified resource. Each tag name is passed in with the parameter <code>Key</code> and the corresponding value is passed in with the parameter <code>Value</code>. The <code>Key</code> and <code>Value</code> parameters are separated by a comma (,). Separate multiple tags with a space. For example, <code>--tags "Key"="owner","Value"="admin" "Key"="environment","Value"="test" "Key"="version","Value"="1.0"</code>.
    array Tags = 2;
}

repeated string DbGroupList = 1

message DefaultClusterParameters {
    // A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request.
    string Marker = 1;
    // The name of the cluster parameter group family to which the engine default parameters apply.
    string ParameterGroupFamily = 2;
    // The list of cluster default parameters.
    array Parameters = 3;
}

message DeleteClusterMessage {
    // <p>The identifier of the cluster to be deleted.</p> <p>Constraints:</p> <ul> <li> <p>Must contain lowercase characters.</p> </li> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens.</p> </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens.</p> </li> </ul>
    string ClusterIdentifier = 1;
    // <p>The identifier of the final snapshot that is to be created immediately before deleting the cluster. If this parameter is provided, <i>SkipFinalClusterSnapshot</i> must be <code>false</code>. </p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters.</p> </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens.</p> </li> </ul>
    string FinalClusterSnapshotIdentifier = 2;
    // <p>Determines whether a final snapshot of the cluster is created before Amazon Redshift deletes the cluster. If <code>true</code>, a final cluster snapshot is not created. If <code>false</code>, a final cluster snapshot is created before the cluster is deleted. </p> <note> <p>The <i>FinalClusterSnapshotIdentifier</i> parameter must be specified if <i>SkipFinalClusterSnapshot</i> is <code>false</code>.</p> </note> <p>Default: <code>false</code> </p>
    boolean SkipFinalClusterSnapshot = 3;
}

message DeleteClusterParameterGroupMessage {
    // <p>The name of the parameter group to be deleted.</p> <p>Constraints:</p> <ul> <li> <p>Must be the name of an existing cluster parameter group.</p> </li> <li> <p>Cannot delete a default cluster parameter group.</p> </li> </ul>
    string ParameterGroupName = 1;
}

message DeleteClusterResult {
    Cluster Cluster = 1;
}

message DeleteClusterSecurityGroupMessage {
    // The name of the cluster security group to be deleted.
    string ClusterSecurityGroupName = 1;
}

message DeleteClusterSnapshotMessage {
    // <p>The unique identifier of the cluster the snapshot was created from. This parameter is required if your IAM user has a policy containing a snapshot resource element that specifies anything other than * for the cluster name.</p> <p>Constraints: Must be the name of valid cluster.</p>
    string SnapshotClusterIdentifier = 1;
    // <p>The unique identifier of the manual snapshot to be deleted.</p> <p>Constraints: Must be the name of an existing snapshot that is in the <code>available</code> state.</p>
    string SnapshotIdentifier = 2;
}

message DeleteClusterSnapshotResult {
    Snapshot Snapshot = 1;
}

message DeleteClusterSubnetGroupMessage {
    // The name of the cluster subnet group name to be deleted.
    string ClusterSubnetGroupName = 1;
}

message DeleteEventSubscriptionMessage {
    // The name of the Amazon Redshift event notification subscription to be deleted.
    string SubscriptionName = 1;
}

message DeleteHsmClientCertificateMessage {
    // The identifier of the HSM client certificate to be deleted.
    string HsmClientCertificateIdentifier = 1;
}

message DeleteHsmConfigurationMessage {
    // The identifier of the Amazon Redshift HSM configuration to be deleted.
    string HsmConfigurationIdentifier = 1;
}

message DeleteSnapshotCopyGrantMessage {
    // The name of the snapshot copy grant to delete.
    string SnapshotCopyGrantName = 1;
}

message DeleteTagsMessage {
    // The Amazon Resource Name (ARN) from which you want to remove the tag or tags. For example, <code>arn:aws:redshift:us-east-1:123456789:cluster:t1</code>.
    string ResourceName = 1;
    // The tag key that you want to delete.
    array TagKeys = 2;
}

message DependentServiceRequestThrottlingFault {
}

message DependentServiceUnavailableFault {
}

message DescribeClusterParameterGroupsMessage {
    // An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeClusterParameterGroups</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request.
    string Marker = 1;
    // <p>The maximum number of response records to return in each call. If the number of remaining response records exceeds the specified <code>MaxRecords</code> value, a value is returned in a <code>marker</code> field of the response. You can retrieve the next set of records by retrying the command with the returned marker value. </p> <p>Default: <code>100</code> </p> <p>Constraints: minimum 20, maximum 100.</p>
    integer MaxRecords = 2;
    // The name of a specific parameter group for which to return details. By default, details about all parameter groups and the default parameter group are returned.
    string ParameterGroupName = 3;
    // A tag key or keys for which you want to return all matching cluster parameter groups that are associated with the specified key or keys. For example, suppose that you have parameter groups that are tagged with keys called <code>owner</code> and <code>environment</code>. If you specify both of these tag keys in the request, Amazon Redshift returns a response with the parameter groups that have either or both of these tag keys associated with them.
    array TagKeys = 4;
    // A tag value or values for which you want to return all matching cluster parameter groups that are associated with the specified tag value or values. For example, suppose that you have parameter groups that are tagged with values called <code>admin</code> and <code>test</code>. If you specify both of these tag values in the request, Amazon Redshift returns a response with the parameter groups that have either or both of these tag values associated with them.
    array TagValues = 5;
}

message DescribeClusterParametersMessage {
    // An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeClusterParameters</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request.
    string Marker = 1;
    // <p>The maximum number of response records to return in each call. If the number of remaining response records exceeds the specified <code>MaxRecords</code> value, a value is returned in a <code>marker</code> field of the response. You can retrieve the next set of records by retrying the command with the returned marker value. </p> <p>Default: <code>100</code> </p> <p>Constraints: minimum 20, maximum 100.</p>
    integer MaxRecords = 2;
    // The name of a cluster parameter group for which to return details.
    string ParameterGroupName = 3;
    // <p>The parameter types to return. Specify <code>user</code> to show parameters that are different form the default. Similarly, specify <code>engine-default</code> to show parameters that are the same as the default parameter group. </p> <p>Default: All parameter types returned.</p> <p>Valid Values: <code>user</code> | <code>engine-default</code> </p>
    string Source = 4;
}

message DescribeClusterSecurityGroupsMessage {
    // <p>The name of a cluster security group for which you are requesting details. You can specify either the <b>Marker</b> parameter or a <b>ClusterSecurityGroupName</b> parameter, but not both. </p> <p> Example: <code>securitygroup1</code> </p>
    string ClusterSecurityGroupName = 1;
    // <p>An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeClusterSecurityGroups</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request. </p> <p>Constraints: You can specify either the <b>ClusterSecurityGroupName</b> parameter or the <b>Marker</b> parameter, but not both. </p>
    string Marker = 2;
    // <p>The maximum number of response records to return in each call. If the number of remaining response records exceeds the specified <code>MaxRecords</code> value, a value is returned in a <code>marker</code> field of the response. You can retrieve the next set of records by retrying the command with the returned marker value. </p> <p>Default: <code>100</code> </p> <p>Constraints: minimum 20, maximum 100.</p>
    integer MaxRecords = 3;
    // A tag key or keys for which you want to return all matching cluster security groups that are associated with the specified key or keys. For example, suppose that you have security groups that are tagged with keys called <code>owner</code> and <code>environment</code>. If you specify both of these tag keys in the request, Amazon Redshift returns a response with the security groups that have either or both of these tag keys associated with them.
    array TagKeys = 4;
    // A tag value or values for which you want to return all matching cluster security groups that are associated with the specified tag value or values. For example, suppose that you have security groups that are tagged with values called <code>admin</code> and <code>test</code>. If you specify both of these tag values in the request, Amazon Redshift returns a response with the security groups that have either or both of these tag values associated with them.
    array TagValues = 5;
}

message DescribeClusterSnapshotsMessage {
    // The identifier of the cluster for which information about snapshots is requested.
    string ClusterIdentifier = 1;
    // <p>A time value that requests only snapshots created at or before the specified time. The time value is specified in ISO 8601 format. For more information about ISO 8601, go to the <a href="http://en.wikipedia.org/wiki/ISO_8601">ISO8601 Wikipedia page.</a> </p> <p>Example: <code>2012-07-16T18:00:00Z</code> </p>
    string EndTime = 2;
    // An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeClusterSnapshots</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request.
    string Marker = 3;
    // <p>The maximum number of response records to return in each call. If the number of remaining response records exceeds the specified <code>MaxRecords</code> value, a value is returned in a <code>marker</code> field of the response. You can retrieve the next set of records by retrying the command with the returned marker value. </p> <p>Default: <code>100</code> </p> <p>Constraints: minimum 20, maximum 100.</p>
    integer MaxRecords = 4;
    // The AWS customer account used to create or copy the snapshot. Use this field to filter the results to snapshots owned by a particular account. To describe snapshots you own, either specify your AWS customer account, or do not specify the parameter.
    string OwnerAccount = 5;
    // The snapshot identifier of the snapshot about which to return information.
    string SnapshotIdentifier = 6;
    // <p>The type of snapshots for which you are requesting information. By default, snapshots of all types are returned.</p> <p>Valid Values: <code>automated</code> | <code>manual</code> </p>
    string SnapshotType = 7;
    // <p>A value that requests only snapshots created at or after the specified time. The time value is specified in ISO 8601 format. For more information about ISO 8601, go to the <a href="http://en.wikipedia.org/wiki/ISO_8601">ISO8601 Wikipedia page.</a> </p> <p>Example: <code>2012-07-16T18:00:00Z</code> </p>
    string StartTime = 8;
    // A tag key or keys for which you want to return all matching cluster snapshots that are associated with the specified key or keys. For example, suppose that you have snapshots that are tagged with keys called <code>owner</code> and <code>environment</code>. If you specify both of these tag keys in the request, Amazon Redshift returns a response with the snapshots that have either or both of these tag keys associated with them.
    array TagKeys = 9;
    // A tag value or values for which you want to return all matching cluster snapshots that are associated with the specified tag value or values. For example, suppose that you have snapshots that are tagged with values called <code>admin</code> and <code>test</code>. If you specify both of these tag values in the request, Amazon Redshift returns a response with the snapshots that have either or both of these tag values associated with them.
    array TagValues = 10;
}

message DescribeClusterSubnetGroupsMessage {
    // The name of the cluster subnet group for which information is requested.
    string ClusterSubnetGroupName = 1;
    // An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeClusterSubnetGroups</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request.
    string Marker = 2;
    // <p>The maximum number of response records to return in each call. If the number of remaining response records exceeds the specified <code>MaxRecords</code> value, a value is returned in a <code>marker</code> field of the response. You can retrieve the next set of records by retrying the command with the returned marker value. </p> <p>Default: <code>100</code> </p> <p>Constraints: minimum 20, maximum 100.</p>
    integer MaxRecords = 3;
    // A tag key or keys for which you want to return all matching cluster subnet groups that are associated with the specified key or keys. For example, suppose that you have subnet groups that are tagged with keys called <code>owner</code> and <code>environment</code>. If you specify both of these tag keys in the request, Amazon Redshift returns a response with the subnet groups that have either or both of these tag keys associated with them.
    array TagKeys = 4;
    // A tag value or values for which you want to return all matching cluster subnet groups that are associated with the specified tag value or values. For example, suppose that you have subnet groups that are tagged with values called <code>admin</code> and <code>test</code>. If you specify both of these tag values in the request, Amazon Redshift returns a response with the subnet groups that have either or both of these tag values associated with them.
    array TagValues = 5;
}

message DescribeClusterVersionsMessage {
    // <p>The name of a specific cluster parameter group family to return details for.</p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
    string ClusterParameterGroupFamily = 1;
    // <p>The specific cluster version to return.</p> <p>Example: <code>1.0</code> </p>
    string ClusterVersion = 2;
    // An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeClusterVersions</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request.
    string Marker = 3;
    // <p>The maximum number of response records to return in each call. If the number of remaining response records exceeds the specified <code>MaxRecords</code> value, a value is returned in a <code>marker</code> field of the response. You can retrieve the next set of records by retrying the command with the returned marker value. </p> <p>Default: <code>100</code> </p> <p>Constraints: minimum 20, maximum 100.</p>
    integer MaxRecords = 4;
}

message DescribeClustersMessage {
    // <p>The unique identifier of a cluster whose properties you are requesting. This parameter is case sensitive.</p> <p>The default is that all clusters defined for an account are returned.</p>
    string ClusterIdentifier = 1;
    // <p>An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeClusters</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request. </p> <p>Constraints: You can specify either the <b>ClusterIdentifier</b> parameter or the <b>Marker</b> parameter, but not both. </p>
    string Marker = 2;
    // <p>The maximum number of response records to return in each call. If the number of remaining response records exceeds the specified <code>MaxRecords</code> value, a value is returned in a <code>marker</code> field of the response. You can retrieve the next set of records by retrying the command with the returned marker value. </p> <p>Default: <code>100</code> </p> <p>Constraints: minimum 20, maximum 100.</p>
    integer MaxRecords = 3;
    // A tag key or keys for which you want to return all matching clusters that are associated with the specified key or keys. For example, suppose that you have clusters that are tagged with keys called <code>owner</code> and <code>environment</code>. If you specify both of these tag keys in the request, Amazon Redshift returns a response with the clusters that have either or both of these tag keys associated with them.
    array TagKeys = 4;
    // A tag value or values for which you want to return all matching clusters that are associated with the specified tag value or values. For example, suppose that you have clusters that are tagged with values called <code>admin</code> and <code>test</code>. If you specify both of these tag values in the request, Amazon Redshift returns a response with the clusters that have either or both of these tag values associated with them.
    array TagValues = 5;
}

message DescribeDefaultClusterParametersMessage {
    // An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeDefaultClusterParameters</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request.
    string Marker = 1;
    // <p>The maximum number of response records to return in each call. If the number of remaining response records exceeds the specified <code>MaxRecords</code> value, a value is returned in a <code>marker</code> field of the response. You can retrieve the next set of records by retrying the command with the returned marker value. </p> <p>Default: <code>100</code> </p> <p>Constraints: minimum 20, maximum 100.</p>
    integer MaxRecords = 2;
    // The name of the cluster parameter group family.
    string ParameterGroupFamily = 3;
}

message DescribeDefaultClusterParametersResult {
    DefaultClusterParameters DefaultClusterParameters = 1;
}

message DescribeEventCategoriesMessage {
    // <p>The source type, such as cluster or parameter group, to which the described event categories apply.</p> <p>Valid values: cluster, cluster-snapshot, cluster-parameter-group, and cluster-security-group.</p>
    string SourceType = 1;
}

message DescribeEventSubscriptionsMessage {
    // An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeEventSubscriptions</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request.
    string Marker = 1;
    // <p>The maximum number of response records to return in each call. If the number of remaining response records exceeds the specified <code>MaxRecords</code> value, a value is returned in a <code>marker</code> field of the response. You can retrieve the next set of records by retrying the command with the returned marker value. </p> <p>Default: <code>100</code> </p> <p>Constraints: minimum 20, maximum 100.</p>
    integer MaxRecords = 2;
    // The name of the Amazon Redshift event notification subscription to be described.
    string SubscriptionName = 3;
}

message DescribeEventsMessage {
    // <p>The number of minutes prior to the time of the request for which to retrieve events. For example, if the request is sent at 18:00 and you specify a duration of 60, then only events which have occurred after 17:00 will be returned.</p> <p>Default: <code>60</code> </p>
    integer Duration = 1;
    // <p>The end of the time interval for which to retrieve events, specified in ISO 8601 format. For more information about ISO 8601, go to the <a href="http://en.wikipedia.org/wiki/ISO_8601">ISO8601 Wikipedia page.</a> </p> <p>Example: <code>2009-07-08T18:00Z</code> </p>
    string EndTime = 2;
    // An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeEvents</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request.
    string Marker = 3;
    // <p>The maximum number of response records to return in each call. If the number of remaining response records exceeds the specified <code>MaxRecords</code> value, a value is returned in a <code>marker</code> field of the response. You can retrieve the next set of records by retrying the command with the returned marker value. </p> <p>Default: <code>100</code> </p> <p>Constraints: minimum 20, maximum 100.</p>
    integer MaxRecords = 4;
    // <p>The identifier of the event source for which events will be returned. If this parameter is not specified, then all sources are included in the response.</p> <p>Constraints:</p> <p>If <i>SourceIdentifier</i> is supplied, <i>SourceType</i> must also be provided.</p> <ul> <li> <p>Specify a cluster identifier when <i>SourceType</i> is <code>cluster</code>.</p> </li> <li> <p>Specify a cluster security group name when <i>SourceType</i> is <code>cluster-security-group</code>.</p> </li> <li> <p>Specify a cluster parameter group name when <i>SourceType</i> is <code>cluster-parameter-group</code>.</p> </li> <li> <p>Specify a cluster snapshot identifier when <i>SourceType</i> is <code>cluster-snapshot</code>.</p> </li> </ul>
    string SourceIdentifier = 5;
    // <p>The event source to retrieve events for. If no value is specified, all events are returned.</p> <p>Constraints:</p> <p>If <i>SourceType</i> is supplied, <i>SourceIdentifier</i> must also be provided.</p> <ul> <li> <p>Specify <code>cluster</code> when <i>SourceIdentifier</i> is a cluster identifier.</p> </li> <li> <p>Specify <code>cluster-security-group</code> when <i>SourceIdentifier</i> is a cluster security group name.</p> </li> <li> <p>Specify <code>cluster-parameter-group</code> when <i>SourceIdentifier</i> is a cluster parameter group name.</p> </li> <li> <p>Specify <code>cluster-snapshot</code> when <i>SourceIdentifier</i> is a cluster snapshot identifier.</p> </li> </ul>
    SourceType SourceType = 6;
    // <p>The beginning of the time interval to retrieve events for, specified in ISO 8601 format. For more information about ISO 8601, go to the <a href="http://en.wikipedia.org/wiki/ISO_8601">ISO8601 Wikipedia page.</a> </p> <p>Example: <code>2009-07-08T18:00Z</code> </p>
    string StartTime = 7;
}

message DescribeHsmClientCertificatesMessage {
    // The identifier of a specific HSM client certificate for which you want information. If no identifier is specified, information is returned for all HSM client certificates owned by your AWS customer account.
    string HsmClientCertificateIdentifier = 1;
    // An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeHsmClientCertificates</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request.
    string Marker = 2;
    // <p>The maximum number of response records to return in each call. If the number of remaining response records exceeds the specified <code>MaxRecords</code> value, a value is returned in a <code>marker</code> field of the response. You can retrieve the next set of records by retrying the command with the returned marker value. </p> <p>Default: <code>100</code> </p> <p>Constraints: minimum 20, maximum 100.</p>
    integer MaxRecords = 3;
    // A tag key or keys for which you want to return all matching HSM client certificates that are associated with the specified key or keys. For example, suppose that you have HSM client certificates that are tagged with keys called <code>owner</code> and <code>environment</code>. If you specify both of these tag keys in the request, Amazon Redshift returns a response with the HSM client certificates that have either or both of these tag keys associated with them.
    array TagKeys = 4;
    // A tag value or values for which you want to return all matching HSM client certificates that are associated with the specified tag value or values. For example, suppose that you have HSM client certificates that are tagged with values called <code>admin</code> and <code>test</code>. If you specify both of these tag values in the request, Amazon Redshift returns a response with the HSM client certificates that have either or both of these tag values associated with them.
    array TagValues = 5;
}

message DescribeHsmConfigurationsMessage {
    // The identifier of a specific Amazon Redshift HSM configuration to be described. If no identifier is specified, information is returned for all HSM configurations owned by your AWS customer account.
    string HsmConfigurationIdentifier = 1;
    // An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeHsmConfigurations</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request.
    string Marker = 2;
    // <p>The maximum number of response records to return in each call. If the number of remaining response records exceeds the specified <code>MaxRecords</code> value, a value is returned in a <code>marker</code> field of the response. You can retrieve the next set of records by retrying the command with the returned marker value. </p> <p>Default: <code>100</code> </p> <p>Constraints: minimum 20, maximum 100.</p>
    integer MaxRecords = 3;
    // A tag key or keys for which you want to return all matching HSM configurations that are associated with the specified key or keys. For example, suppose that you have HSM configurations that are tagged with keys called <code>owner</code> and <code>environment</code>. If you specify both of these tag keys in the request, Amazon Redshift returns a response with the HSM configurations that have either or both of these tag keys associated with them.
    array TagKeys = 4;
    // A tag value or values for which you want to return all matching HSM configurations that are associated with the specified tag value or values. For example, suppose that you have HSM configurations that are tagged with values called <code>admin</code> and <code>test</code>. If you specify both of these tag values in the request, Amazon Redshift returns a response with the HSM configurations that have either or both of these tag values associated with them.
    array TagValues = 5;
}

message DescribeLoggingStatusMessage {
    // <p>The identifier of the cluster from which to get the logging status.</p> <p>Example: <code>examplecluster</code> </p>
    string ClusterIdentifier = 1;
}

message DescribeOrderableClusterOptionsMessage {
    // <p>The version filter value. Specify this parameter to show only the available offerings matching the specified version.</p> <p>Default: All versions.</p> <p>Constraints: Must be one of the version returned from <a>DescribeClusterVersions</a>.</p>
    string ClusterVersion = 1;
    // An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeOrderableClusterOptions</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request.
    string Marker = 2;
    // <p>The maximum number of response records to return in each call. If the number of remaining response records exceeds the specified <code>MaxRecords</code> value, a value is returned in a <code>marker</code> field of the response. You can retrieve the next set of records by retrying the command with the returned marker value. </p> <p>Default: <code>100</code> </p> <p>Constraints: minimum 20, maximum 100.</p>
    integer MaxRecords = 3;
    // The node type filter value. Specify this parameter to show only the available offerings matching the specified node type.
    string NodeType = 4;
}

message DescribeReservedNodeOfferingsMessage {
    // An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeReservedNodeOfferings</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request.
    string Marker = 1;
    // <p>The maximum number of response records to return in each call. If the number of remaining response records exceeds the specified <code>MaxRecords</code> value, a value is returned in a <code>marker</code> field of the response. You can retrieve the next set of records by retrying the command with the returned marker value. </p> <p>Default: <code>100</code> </p> <p>Constraints: minimum 20, maximum 100.</p>
    integer MaxRecords = 2;
    // The unique identifier for the offering.
    string ReservedNodeOfferingId = 3;
}

message DescribeReservedNodesMessage {
    // An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeReservedNodes</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request.
    string Marker = 1;
    // <p>The maximum number of response records to return in each call. If the number of remaining response records exceeds the specified <code>MaxRecords</code> value, a value is returned in a <code>marker</code> field of the response. You can retrieve the next set of records by retrying the command with the returned marker value. </p> <p>Default: <code>100</code> </p> <p>Constraints: minimum 20, maximum 100.</p>
    integer MaxRecords = 2;
    // Identifier for the node reservation.
    string ReservedNodeId = 3;
}

message DescribeResizeMessage {
    // <p>The unique identifier of a cluster whose resize progress you are requesting. This parameter is case-sensitive.</p> <p>By default, resize operations for all clusters defined for an AWS account are returned.</p>
    string ClusterIdentifier = 1;
}

message DescribeSnapshotCopyGrantsMessage {
    // <p>An optional parameter that specifies the starting point to return a set of response records. When the results of a <code>DescribeSnapshotCopyGrant</code> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request. </p> <p>Constraints: You can specify either the <b>SnapshotCopyGrantName</b> parameter or the <b>Marker</b> parameter, but not both. </p>
    string Marker = 1;
    // <p>The maximum number of response records to return in each call. If the number of remaining response records exceeds the specified <code>MaxRecords</code> value, a value is returned in a <code>marker</code> field of the response. You can retrieve the next set of records by retrying the command with the returned marker value. </p> <p>Default: <code>100</code> </p> <p>Constraints: minimum 20, maximum 100.</p>
    integer MaxRecords = 2;
    // The name of the snapshot copy grant.
    string SnapshotCopyGrantName = 3;
    // A tag key or keys for which you want to return all matching resources that are associated with the specified key or keys. For example, suppose that you have resources tagged with keys called <code>owner</code> and <code>environment</code>. If you specify both of these tag keys in the request, Amazon Redshift returns a response with all resources that have either or both of these tag keys associated with them.
    array TagKeys = 4;
    // A tag value or values for which you want to return all matching resources that are associated with the specified value or values. For example, suppose that you have resources tagged with values called <code>admin</code> and <code>test</code>. If you specify both of these tag values in the request, Amazon Redshift returns a response with all resources that have either or both of these tag values associated with them.
    array TagValues = 5;
}

message DescribeTableRestoreStatusMessage {
    // The Amazon Redshift cluster that the table is being restored to.
    string ClusterIdentifier = 1;
    // An optional pagination token provided by a previous <code>DescribeTableRestoreStatus</code> request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by the <code>MaxRecords</code> parameter.
    string Marker = 2;
    // The maximum number of records to include in the response. If more records exist than the specified <code>MaxRecords</code> value, a pagination token called a marker is included in the response so that the remaining results can be retrieved.
    integer MaxRecords = 3;
    // The identifier of the table restore request to return status for. If you don't specify a <code>TableRestoreRequestId</code> value, then <code>DescribeTableRestoreStatus</code> returns the status of all in-progress table restore requests.
    string TableRestoreRequestId = 4;
}

message DescribeTagsMessage {
    // A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>marker</code> parameter and retrying the command. If the <code>marker</code> field is empty, all response records have been retrieved for the request.
    string Marker = 1;
    // The maximum number or response records to return in each call. If the number of remaining response records exceeds the specified <code>MaxRecords</code> value, a value is returned in a <code>marker</code> field of the response. You can retrieve the next set of records by retrying the command with the returned <code>marker</code> value.
    integer MaxRecords = 2;
    // The Amazon Resource Name (ARN) for which you want to describe the tag or tags. For example, <code>arn:aws:redshift:us-east-1:123456789:cluster:t1</code>.
    string ResourceName = 3;
    // <p>The type of resource with which you want to view tags. Valid resource types are: </p> <ul> <li> <p>Cluster</p> </li> <li> <p>CIDR/IP</p> </li> <li> <p>EC2 security group</p> </li> <li> <p>Snapshot</p> </li> <li> <p>Cluster security group</p> </li> <li> <p>Subnet group</p> </li> <li> <p>HSM connection</p> </li> <li> <p>HSM certificate</p> </li> <li> <p>Parameter group</p> </li> <li> <p>Snapshot copy grant</p> </li> </ul> <p>For more information about Amazon Redshift resource types and constructing ARNs, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/constructing-redshift-arn.html">Constructing an Amazon Redshift Amazon Resource Name (ARN)</a> in the Amazon Redshift Cluster Management Guide. </p>
    string ResourceType = 4;
    // A tag key or keys for which you want to return all matching resources that are associated with the specified key or keys. For example, suppose that you have resources tagged with keys called <code>owner</code> and <code>environment</code>. If you specify both of these tag keys in the request, Amazon Redshift returns a response with all resources that have either or both of these tag keys associated with them.
    array TagKeys = 5;
    // A tag value or values for which you want to return all matching resources that are associated with the specified value or values. For example, suppose that you have resources tagged with values called <code>admin</code> and <code>test</code>. If you specify both of these tag values in the request, Amazon Redshift returns a response with all resources that have either or both of these tag values associated with them.
    array TagValues = 6;
}

message DisableLoggingMessage {
    // <p>The identifier of the cluster on which logging is to be stopped.</p> <p>Example: <code>examplecluster</code> </p>
    string ClusterIdentifier = 1;
}

message DisableSnapshotCopyMessage {
    // <p>The unique identifier of the source cluster that you want to disable copying of snapshots to a destination region.</p> <p>Constraints: Must be the valid name of an existing cluster that has cross-region snapshot copy enabled.</p>
    string ClusterIdentifier = 1;
}

message DisableSnapshotCopyResult {
    Cluster Cluster = 1;
}

message EC2SecurityGroup {
    // The name of the EC2 Security Group.
    string EC2SecurityGroupName = 1;
    // The AWS ID of the owner of the EC2 security group specified in the <code>EC2SecurityGroupName</code> field.
    string EC2SecurityGroupOwnerId = 2;
    // The status of the EC2 security group.
    string Status = 3;
    // The list of tags for the EC2 security group.
    array Tags = 4;
}

repeated EC2SecurityGroup EC2SecurityGroupList = 1

message ElasticIpStatus {
    // The elastic IP (EIP) address for the cluster.
    string ElasticIp = 1;
    // The status of the elastic IP (EIP) address.
    string Status = 2;
}

message EnableLoggingMessage {
    // <p>The name of an existing S3 bucket where the log files are to be stored.</p> <p>Constraints:</p> <ul> <li> <p>Must be in the same region as the cluster</p> </li> <li> <p>The cluster must have read bucket and put object permissions</p> </li> </ul>
    string BucketName = 1;
    // <p>The identifier of the cluster on which logging is to be started.</p> <p>Example: <code>examplecluster</code> </p>
    string ClusterIdentifier = 2;
    // <p>The prefix applied to the log file names.</p> <p>Constraints:</p> <ul> <li> <p>Cannot exceed 512 characters</p> </li> <li> <p>Cannot contain spaces( ), double quotes ("), single quotes ('), a backslash (\), or control characters. The hexadecimal codes for invalid characters are: </p> <ul> <li> <p>x00 to x20</p> </li> <li> <p>x22</p> </li> <li> <p>x27</p> </li> <li> <p>x5c</p> </li> <li> <p>x7f or larger</p> </li> </ul> </li> </ul>
    string S3KeyPrefix = 3;
}

message EnableSnapshotCopyMessage {
    // <p>The unique identifier of the source cluster to copy snapshots from.</p> <p>Constraints: Must be the valid name of an existing cluster that does not already have cross-region snapshot copy enabled.</p>
    string ClusterIdentifier = 1;
    // <p>The destination region that you want to copy snapshots to.</p> <p>Constraints: Must be the name of a valid region. For more information, see <a href="http://docs.aws.amazon.com/general/latest/gr/rande.html#redshift_region">Regions and Endpoints</a> in the Amazon Web Services General Reference. </p>
    string DestinationRegion = 2;
    // <p>The number of days to retain automated snapshots in the destination region after they are copied from the source region.</p> <p>Default: 7.</p> <p>Constraints: Must be at least 1 and no more than 35.</p>
    integer RetentionPeriod = 3;
    // The name of the snapshot copy grant to use when snapshots of an AWS KMS-encrypted cluster are copied to the destination region.
    string SnapshotCopyGrantName = 4;
}

message EnableSnapshotCopyResult {
    Cluster Cluster = 1;
}

message Endpoint {
    // The DNS address of the Cluster.
    string Address = 1;
    // The port that the database engine is listening on.
    integer Port = 2;
}

message Event {
    // The date and time of the event.
    string Date = 1;
    // <p>A list of the event categories.</p> <p>Values: Configuration, Management, Monitoring, Security</p>
    array EventCategories = 2;
    // The identifier of the event.
    string EventId = 3;
    // The text of this event.
    string Message = 4;
    // <p>The severity of the event.</p> <p>Values: ERROR, INFO</p>
    string Severity = 5;
    // The identifier for the source of the event.
    string SourceIdentifier = 6;
    // The source type for this event.
    SourceType SourceType = 7;
}

repeated string EventCategoriesList = 1

message EventCategoriesMap {
    // The events in the event category.
    array Events = 1;
    // The source type, such as cluster or cluster-snapshot, that the returned categories belong to.
    string SourceType = 2;
}

repeated EventCategoriesMap EventCategoriesMapList = 1

message EventCategoriesMessage {
    // A list of event categories descriptions.
    array EventCategoriesMapList = 1;
}

message EventInfoMap {
    // The category of an Amazon Redshift event.
    array EventCategories = 1;
    // The description of an Amazon Redshift event.
    string EventDescription = 2;
    // The identifier of an Amazon Redshift event.
    string EventId = 3;
    // <p>The severity of the event.</p> <p>Values: ERROR, INFO</p>
    string Severity = 4;
}

repeated EventInfoMap EventInfoMapList = 1

repeated Event EventList = 1

message EventSubscription {
    // The name of the Amazon Redshift event notification subscription.
    string CustSubscriptionId = 1;
    // The AWS customer account associated with the Amazon Redshift event notification subscription.
    string CustomerAwsId = 2;
    // A Boolean value indicating whether the subscription is enabled. <code>true</code> indicates the subscription is enabled.
    boolean Enabled = 3;
    // <p>The list of Amazon Redshift event categories specified in the event notification subscription.</p> <p>Values: Configuration, Management, Monitoring, Security</p>
    array EventCategoriesList = 4;
    // <p>The event severity specified in the Amazon Redshift event notification subscription.</p> <p>Values: ERROR, INFO</p>
    string Severity = 5;
    // The Amazon Resource Name (ARN) of the Amazon SNS topic used by the event notification subscription.
    string SnsTopicArn = 6;
    // A list of the sources that publish events to the Amazon Redshift event notification subscription.
    array SourceIdsList = 7;
    // The source type of the events returned the Amazon Redshift event notification, such as cluster, or cluster-snapshot.
    string SourceType = 8;
    // <p>The status of the Amazon Redshift event notification subscription.</p> <p>Constraints:</p> <ul> <li> <p>Can be one of the following: active | no-permission | topic-not-exist</p> </li> <li> <p>The status "no-permission" indicates that Amazon Redshift no longer has permission to post to the Amazon SNS topic. The status "topic-not-exist" indicates that the topic was deleted after the subscription was created.</p> </li> </ul>
    string Status = 9;
    // The date and time the Amazon Redshift event notification subscription was created.
    string SubscriptionCreationTime = 10;
    // The list of tags for the event subscription.
    array Tags = 11;
}

message EventSubscriptionQuotaExceededFault {
}

repeated EventSubscription EventSubscriptionsList = 1

message EventSubscriptionsMessage {
    // A list of event subscriptions.
    array EventSubscriptionsList = 1;
    // A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request.
    string Marker = 2;
}

message EventsMessage {
    // A list of <code>Event</code> instances.
    array Events = 1;
    // A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request.
    string Marker = 2;
}

message GetClusterCredentialsMessage {
    // Create a database user with the name specified for <code>DbUser</code> if one does not exist.
    boolean AutoCreate = 1;
    // The unique identifier of the cluster that contains the database for which your are requesting credentials. This parameter is case sensitive.
    string ClusterIdentifier = 2;
    // A list of the names of existing database groups that <code>DbUser</code> will join for the current session. If not specified, the new user is added only to PUBLIC.
    array DbGroups = 3;
    // <p>The name of a database that <code>DbUser</code> is authorized to log on to. If <code>DbName</code> is not specified, <code>DbUser</code> can log in to any existing database.</p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 64 alphanumeric characters or hyphens</p> </li> <li> <p>Must contain only lowercase letters.</p> </li> <li> <p>Cannot be a reserved word. A list of reserved words can be found in <a href="http://docs.aws.amazon.com/redshift/latest/dg/r_pg_keywords.html">Reserved Words</a> in the Amazon Redshift Database Developer Guide.</p> </li> </ul>
    string DbName = 4;
    // <p>The name of a database user. If a user name matching <code>DbUser</code> exists in the database, the temporary user credentials have the same permissions as the existing user. If <code>DbUser</code> doesn't exist in the database and <code>Autocreate</code> is <code>True</code>, a new user is created using the value for <code>DbUser</code> with PUBLIC permissions. If a database user matching the value for <code>DbUser</code> doesn't exist and <code>Autocreate</code> is <code>False</code>, then the command succeeds but the connection attempt will fail because the user doesn't exist in the database.</p> <p>For more information, see <a href="http://docs.aws.amazon.com/http:/docs.aws.amazon.com/redshift/latest/dg/r_CREATE_USER.html">CREATE USER</a> in the Amazon Redshift Database Developer Guide. </p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 128 alphanumeric characters or hyphens</p> </li> <li> <p>Must contain only lowercase letters.</p> </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Must not contain a colon ( : ) or slash ( / ). </p> </li> <li> <p>Cannot be a reserved word. A list of reserved words can be found in <a href="http://docs.aws.amazon.com/redshift/latest/dg/r_pg_keywords.html">Reserved Words</a> in the Amazon Redshift Database Developer Guide.</p> </li> </ul>
    string DbUser = 5;
    // <p>The number of seconds until the returned temporary password expires.</p> <p>Constraint: minimum 900, maximum 3600.</p> <p>Default: 900</p>
    integer DurationSeconds = 6;
}

message HsmClientCertificate {
    // The identifier of the HSM client certificate.
    string HsmClientCertificateIdentifier = 1;
    // The public key that the Amazon Redshift cluster will use to connect to the HSM. You must register the public key in the HSM.
    string HsmClientCertificatePublicKey = 2;
    // The list of tags for the HSM client certificate.
    array Tags = 3;
}

message HsmClientCertificateAlreadyExistsFault {
}

repeated HsmClientCertificate HsmClientCertificateList = 1

message HsmClientCertificateMessage {
    // A list of the identifiers for one or more HSM client certificates used by Amazon Redshift clusters to store and retrieve database encryption keys in an HSM.
    array HsmClientCertificates = 1;
    // A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request.
    string Marker = 2;
}

message HsmClientCertificateNotFoundFault {
}

message HsmClientCertificateQuotaExceededFault {
}

message HsmConfiguration {
    // A text description of the HSM configuration.
    string Description = 1;
    // The name of the Amazon Redshift HSM configuration.
    string HsmConfigurationIdentifier = 2;
    // The IP address that the Amazon Redshift cluster must use to access the HSM.
    string HsmIpAddress = 3;
    // The name of the partition in the HSM where the Amazon Redshift clusters will store their database encryption keys.
    string HsmPartitionName = 4;
    // The list of tags for the HSM configuration.
    array Tags = 5;
}

message HsmConfigurationAlreadyExistsFault {
}

repeated HsmConfiguration HsmConfigurationList = 1

message HsmConfigurationMessage {
    // A list of <code>HsmConfiguration</code> objects.
    array HsmConfigurations = 1;
    // A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request.
    string Marker = 2;
}

message HsmConfigurationNotFoundFault {
}

message HsmConfigurationQuotaExceededFault {
}

message HsmStatus {
    // Specifies the name of the HSM client certificate the Amazon Redshift cluster uses to retrieve the data encryption keys stored in an HSM.
    string HsmClientCertificateIdentifier = 1;
    // Specifies the name of the HSM configuration that contains the information the Amazon Redshift cluster can use to retrieve and store keys in an HSM.
    string HsmConfigurationIdentifier = 2;
    // <p>Reports whether the Amazon Redshift cluster has finished applying any HSM settings changes specified in a modify cluster command.</p> <p>Values: active, applying</p>
    string Status = 3;
}

message IPRange {
    // The IP range in Classless Inter-Domain Routing (CIDR) notation.
    string CIDRIP = 1;
    // The status of the IP range, for example, "authorized".
    string Status = 2;
    // The list of tags for the IP range.
    array Tags = 3;
}

repeated IPRange IPRangeList = 1

repeated string IamRoleArnList = 1

repeated string ImportTablesCompleted = 1

repeated string ImportTablesInProgress = 1

repeated string ImportTablesNotStarted = 1

message InProgressTableRestoreQuotaExceededFault {
}

message IncompatibleOrderableOptions {
}

message InsufficientClusterCapacityFault {
}

message InsufficientS3BucketPolicyFault {
}

message InvalidClusterParameterGroupStateFault {
}

message InvalidClusterSecurityGroupStateFault {
}

message InvalidClusterSnapshotStateFault {
}

message InvalidClusterStateFault {
}

message InvalidClusterSubnetGroupStateFault {
}

message InvalidClusterSubnetStateFault {
}

message InvalidElasticIpFault {
}

message InvalidHsmClientCertificateStateFault {
}

message InvalidHsmConfigurationStateFault {
}

message InvalidRestoreFault {
}

message InvalidS3BucketNameFault {
}

message InvalidS3KeyPrefixFault {
}

message InvalidSnapshotCopyGrantStateFault {
}

message InvalidSubnet {
}

message InvalidSubscriptionStateFault {
}

message InvalidTableRestoreArgumentFault {
}

message InvalidTagFault {
}

message InvalidVPCNetworkStateFault {
}

message LimitExceededFault {
}

message LoggingStatus {
    // The name of the S3 bucket where the log files are stored.
    string BucketName = 1;
    // The message indicating that logs failed to be delivered.
    string LastFailureMessage = 2;
    // The last time when logs failed to be delivered.
    string LastFailureTime = 3;
    // The last time that logs were delivered.
    string LastSuccessfulDeliveryTime = 4;
    //  <code>true</code> if logging is on, <code>false</code> if logging is off.
    boolean LoggingEnabled = 5;
    // The prefix applied to the log file names.
    string S3KeyPrefix = 6;
}

message ModifyClusterIamRolesMessage {
    // Zero or more IAM roles to associate with the cluster. The roles must be in their Amazon Resource Name (ARN) format. You can associate up to 10 IAM roles with a single cluster in a single request.
    array AddIamRoles = 1;
    // The unique identifier of the cluster for which you want to associate or disassociate IAM roles.
    string ClusterIdentifier = 2;
    // Zero or more IAM roles in ARN format to disassociate from the cluster. You can disassociate up to 10 IAM roles from a single cluster in a single request.
    array RemoveIamRoles = 3;
}

message ModifyClusterIamRolesResult {
    Cluster Cluster = 1;
}

message ModifyClusterMessage {
    // <p>If <code>true</code>, major version upgrades will be applied automatically to the cluster during the maintenance window. </p> <p>Default: <code>false</code> </p>
    boolean AllowVersionUpgrade = 1;
    // <p>The number of days that automated snapshots are retained. If the value is 0, automated snapshots are disabled. Even if automated snapshots are disabled, you can still create manual snapshots when you want with <a>CreateClusterSnapshot</a>. </p> <p>If you decrease the automated snapshot retention period from its current value, existing automated snapshots that fall outside of the new retention period will be immediately deleted.</p> <p>Default: Uses existing setting.</p> <p>Constraints: Must be a value from 0 to 35.</p>
    integer AutomatedSnapshotRetentionPeriod = 2;
    // <p>The unique identifier of the cluster to be modified.</p> <p>Example: <code>examplecluster</code> </p>
    string ClusterIdentifier = 3;
    // <p>The name of the cluster parameter group to apply to this cluster. This change is applied only after the cluster is rebooted. To reboot a cluster use <a>RebootCluster</a>. </p> <p>Default: Uses existing setting.</p> <p>Constraints: The cluster parameter group must be in the same parameter group family that matches the cluster version.</p>
    string ClusterParameterGroupName = 4;
    // <p>A list of cluster security groups to be authorized on this cluster. This change is asynchronously applied as soon as possible.</p> <p>Security groups currently associated with the cluster, and not in the list of groups to apply, will be revoked from the cluster.</p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters or hyphens</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
    array ClusterSecurityGroups = 5;
    // <p>The new cluster type.</p> <p>When you submit your cluster resize request, your existing cluster goes into a read-only mode. After Amazon Redshift provisions a new cluster based on your resize requirements, there will be outage for a period while the old cluster is deleted and your connection is switched to the new cluster. You can use <a>DescribeResize</a> to track the progress of the resize request. </p> <p>Valid Values: <code> multi-node | single-node </code> </p>
    string ClusterType = 6;
    // <p>The new version number of the Amazon Redshift engine to upgrade to.</p> <p>For major version upgrades, if a non-default cluster parameter group is currently in use, a new cluster parameter group in the cluster parameter group family for the new version must be specified. The new cluster parameter group can be the default for that cluster parameter group family. For more information about parameters and parameter groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-parameter-groups.html">Amazon Redshift Parameter Groups</a> in the <i>Amazon Redshift Cluster Management Guide</i>.</p> <p>Example: <code>1.0</code> </p>
    string ClusterVersion = 7;
    // <p>The Elastic IP (EIP) address for the cluster.</p> <p>Constraints: The cluster must be provisioned in EC2-VPC and publicly-accessible through an Internet gateway. For more information about provisioning clusters in EC2-VPC, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html#cluster-platforms">Supported Platforms to Launch Your Cluster</a> in the Amazon Redshift Cluster Management Guide.</p>
    string ElasticIp = 8;
    // <p>An option that specifies whether to create the cluster with enhanced VPC routing enabled. To create a cluster that uses enhanced VPC routing, the cluster must be in a VPC. For more information, see <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/enhanced-vpc-routing.html">Enhanced VPC Routing</a> in the Amazon Redshift Cluster Management Guide.</p> <p>If this option is <code>true</code>, enhanced VPC routing is enabled. </p> <p>Default: false</p>
    boolean EnhancedVpcRouting = 9;
    // Specifies the name of the HSM client certificate the Amazon Redshift cluster uses to retrieve the data encryption keys stored in an HSM.
    string HsmClientCertificateIdentifier = 10;
    // Specifies the name of the HSM configuration that contains the information the Amazon Redshift cluster can use to retrieve and store keys in an HSM.
    string HsmConfigurationIdentifier = 11;
    // <p>The new password for the cluster master user. This change is asynchronously applied as soon as possible. Between the time of the request and the completion of the request, the <code>MasterUserPassword</code> element exists in the <code>PendingModifiedValues</code> element of the operation response. </p> <note> <p>Operations never return the password, so this operation provides a way to regain access to the master user account for a cluster if the password is lost.</p> </note> <p>Default: Uses existing setting.</p> <p>Constraints:</p> <ul> <li> <p>Must be between 8 and 64 characters in length.</p> </li> <li> <p>Must contain at least one uppercase letter.</p> </li> <li> <p>Must contain at least one lowercase letter.</p> </li> <li> <p>Must contain one number.</p> </li> <li> <p>Can be any printable ASCII character (ASCII code 33 to 126) except ' (single quote), " (double quote), \, /, @, or space.</p> </li> </ul>
    string MasterUserPassword = 12;
    // <p>The new identifier for the cluster.</p> <p>Constraints:</p> <ul> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens.</p> </li> <li> <p>Alphabetic characters must be lowercase.</p> </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens.</p> </li> <li> <p>Must be unique for all clusters within an AWS account.</p> </li> </ul> <p>Example: <code>examplecluster</code> </p>
    string NewClusterIdentifier = 13;
    // <p>The new node type of the cluster. If you specify a new node type, you must also specify the number of nodes parameter.</p> <p>When you submit your request to resize a cluster, Amazon Redshift sets access permissions for the cluster to read-only. After Amazon Redshift provisions a new cluster according to your resize requirements, there will be a temporary outage while the old cluster is deleted and your connection is switched to the new cluster. When the new connection is complete, the original access permissions for the cluster are restored. You can use <a>DescribeResize</a> to track the progress of the resize request. </p> <p>Valid Values: <code> ds1.xlarge</code> | <code>ds1.8xlarge</code> | <code> ds2.xlarge</code> | <code>ds2.8xlarge</code> | <code>dc1.large</code> | <code>dc1.8xlarge</code>.</p>
    string NodeType = 14;
    // <p>The new number of nodes of the cluster. If you specify a new number of nodes, you must also specify the node type parameter.</p> <p>When you submit your request to resize a cluster, Amazon Redshift sets access permissions for the cluster to read-only. After Amazon Redshift provisions a new cluster according to your resize requirements, there will be a temporary outage while the old cluster is deleted and your connection is switched to the new cluster. When the new connection is complete, the original access permissions for the cluster are restored. You can use <a>DescribeResize</a> to track the progress of the resize request. </p> <p>Valid Values: Integer greater than <code>0</code>.</p>
    integer NumberOfNodes = 15;
    // <p>The weekly time range (in UTC) during which system maintenance can occur, if necessary. If system maintenance is necessary during the window, it may result in an outage.</p> <p>This maintenance window change is made immediately. If the new maintenance window indicates the current time, there must be at least 120 minutes between the current time and end of the window in order to ensure that pending changes are applied.</p> <p>Default: Uses existing setting.</p> <p>Format: ddd:hh24:mi-ddd:hh24:mi, for example <code>wed:07:30-wed:08:00</code>.</p> <p>Valid Days: Mon | Tue | Wed | Thu | Fri | Sat | Sun</p> <p>Constraints: Must be at least 30 minutes.</p>
    string PreferredMaintenanceWindow = 16;
    // If <code>true</code>, the cluster can be accessed from a public network. Only clusters in VPCs can be set to be publicly available.
    boolean PubliclyAccessible = 17;
    // A list of virtual private cloud (VPC) security groups to be associated with the cluster.
    array VpcSecurityGroupIds = 18;
}

message ModifyClusterParameterGroupMessage {
    // The name of the parameter group to be modified.
    string ParameterGroupName = 1;
    // <p>An array of parameters to be modified. A maximum of 20 parameters can be modified in a single request.</p> <p>For each parameter to be modified, you must supply at least the parameter name and parameter value; other name-value pairs of the parameter are optional.</p> <p>For the workload management (WLM) configuration, you must supply all the name-value pairs in the wlm_json_configuration parameter.</p>
    array Parameters = 2;
}

message ModifyClusterResult {
    Cluster Cluster = 1;
}

message ModifyClusterSubnetGroupMessage {
    // The name of the subnet group to be modified.
    string ClusterSubnetGroupName = 1;
    // A text description of the subnet group to be modified.
    string Description = 2;
    // An array of VPC subnet IDs. A maximum of 20 subnets can be modified in a single request.
    array SubnetIds = 3;
}

message ModifyClusterSubnetGroupResult {
    ClusterSubnetGroup ClusterSubnetGroup = 1;
}

message ModifyEventSubscriptionMessage {
    // A Boolean value indicating if the subscription is enabled. <code>true</code> indicates the subscription is enabled
    boolean Enabled = 1;
    // <p>Specifies the Amazon Redshift event categories to be published by the event notification subscription.</p> <p>Values: Configuration, Management, Monitoring, Security</p>
    array EventCategories = 2;
    // <p>Specifies the Amazon Redshift event severity to be published by the event notification subscription.</p> <p>Values: ERROR, INFO</p>
    string Severity = 3;
    // The Amazon Resource Name (ARN) of the SNS topic to be used by the event notification subscription.
    string SnsTopicArn = 4;
    // <p>A list of one or more identifiers of Amazon Redshift source objects. All of the objects must be of the same type as was specified in the source type parameter. The event subscription will return only events generated by the specified objects. If not specified, then events are returned for all objects within the source type specified.</p> <p>Example: my-cluster-1, my-cluster-2</p> <p>Example: my-snapshot-20131010</p>
    array SourceIds = 5;
    // <p>The type of source that will be generating the events. For example, if you want to be notified of events generated by a cluster, you would set this parameter to cluster. If this value is not specified, events are returned for all Amazon Redshift objects in your AWS account. You must specify a source type in order to specify source IDs.</p> <p>Valid values: cluster, cluster-parameter-group, cluster-security-group, and cluster-snapshot.</p>
    string SourceType = 6;
    // The name of the modified Amazon Redshift event notification subscription.
    string SubscriptionName = 7;
}

message ModifyEventSubscriptionResult {
    EventSubscription EventSubscription = 1;
}

message ModifySnapshotCopyRetentionPeriodMessage {
    // <p>The unique identifier of the cluster for which you want to change the retention period for automated snapshots that are copied to a destination region.</p> <p>Constraints: Must be the valid name of an existing cluster that has cross-region snapshot copy enabled.</p>
    string ClusterIdentifier = 1;
    // <p>The number of days to retain automated snapshots in the destination region after they are copied from the source region.</p> <p>If you decrease the retention period for automated snapshots that are copied to a destination region, Amazon Redshift will delete any existing automated snapshots that were copied to the destination region and that fall outside of the new retention period.</p> <p>Constraints: Must be at least 1 and no more than 35.</p>
    integer RetentionPeriod = 2;
}

message ModifySnapshotCopyRetentionPeriodResult {
    Cluster Cluster = 1;
}

message NumberOfNodesPerClusterLimitExceededFault {
}

message NumberOfNodesQuotaExceededFault {
}

message OrderableClusterOption {
    // A list of availability zones for the orderable cluster.
    array AvailabilityZones = 1;
    // The cluster type, for example <code>multi-node</code>.
    string ClusterType = 2;
    // The version of the orderable cluster.
    string ClusterVersion = 3;
    // The node type for the orderable cluster.
    string NodeType = 4;
}

repeated OrderableClusterOption OrderableClusterOptionsList = 1

message OrderableClusterOptionsMessage {
    // A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request.
    string Marker = 1;
    // An <code>OrderableClusterOption</code> structure containing information about orderable options for the cluster.
    array OrderableClusterOptions = 2;
}

message Parameter {
    // The valid range of values for the parameter.
    string AllowedValues = 1;
    // Specifies how to apply the WLM configuration parameter. Some properties can be applied dynamically, while other properties require that any associated clusters be rebooted for the configuration changes to be applied. For more information about parameters and parameter groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-parameter-groups.html">Amazon Redshift Parameter Groups</a> in the <i>Amazon Redshift Cluster Management Guide</i>.
    ParameterApplyType ApplyType = 2;
    // The data type of the parameter.
    string DataType = 3;
    // A description of the parameter.
    string Description = 4;
    // If <code>true</code>, the parameter can be modified. Some parameters have security or operational implications that prevent them from being changed.
    boolean IsModifiable = 5;
    // The earliest engine version to which the parameter can apply.
    string MinimumEngineVersion = 6;
    // The name of the parameter.
    string ParameterName = 7;
    // The value of the parameter.
    string ParameterValue = 8;
    // The source of the parameter value, such as "engine-default" or "user".
    string Source = 9;
}

enum ParameterApplyType {
    STATIC = 0;
    DYNAMIC = 1;
}

repeated ClusterParameterGroup ParameterGroupList = 1

repeated Parameter ParametersList = 1

message PendingModifiedValues {
    // The pending or in-progress change of the automated snapshot retention period.
    integer AutomatedSnapshotRetentionPeriod = 1;
    // The pending or in-progress change of the new identifier for the cluster.
    string ClusterIdentifier = 2;
    // The pending or in-progress change of the cluster type.
    string ClusterType = 3;
    // The pending or in-progress change of the service version.
    string ClusterVersion = 4;
    // <p>An option that specifies whether to create the cluster with enhanced VPC routing enabled. To create a cluster that uses enhanced VPC routing, the cluster must be in a VPC. For more information, see <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/enhanced-vpc-routing.html">Enhanced VPC Routing</a> in the Amazon Redshift Cluster Management Guide.</p> <p>If this option is <code>true</code>, enhanced VPC routing is enabled. </p> <p>Default: false</p>
    boolean EnhancedVpcRouting = 5;
    // The pending or in-progress change of the master user password for the cluster.
    string MasterUserPassword = 6;
    // The pending or in-progress change of the cluster's node type.
    string NodeType = 7;
    // The pending or in-progress change of the number of nodes in the cluster.
    integer NumberOfNodes = 8;
    // The pending or in-progress change of the ability to connect to the cluster from the public network.
    boolean PubliclyAccessible = 9;
}

message PurchaseReservedNodeOfferingMessage {
    // <p>The number of reserved nodes that you want to purchase.</p> <p>Default: <code>1</code> </p>
    integer NodeCount = 1;
    // The unique identifier of the reserved node offering you want to purchase.
    string ReservedNodeOfferingId = 2;
}

message PurchaseReservedNodeOfferingResult {
    ReservedNode ReservedNode = 1;
}

message RebootClusterMessage {
    // The cluster identifier.
    string ClusterIdentifier = 1;
}

message RebootClusterResult {
    Cluster Cluster = 1;
}

message RecurringCharge {
    // The amount charged per the period of time specified by the recurring charge frequency.
    number RecurringChargeAmount = 1;
    // The frequency at which the recurring charge amount is applied.
    string RecurringChargeFrequency = 2;
}

repeated RecurringCharge RecurringChargeList = 1

message ReservedNode {
    // The currency code for the reserved cluster.
    string CurrencyCode = 1;
    // The duration of the node reservation in seconds.
    integer Duration = 2;
    // The fixed cost Amazon Redshift charges you for this reserved node.
    number FixedPrice = 3;
    // The number of reserved compute nodes.
    integer NodeCount = 4;
    // The node type of the reserved node.
    string NodeType = 5;
    // The anticipated utilization of the reserved node, as defined in the reserved node offering.
    string OfferingType = 6;
    // The recurring charges for the reserved node.
    array RecurringCharges = 7;
    // The unique identifier for the reservation.
    string ReservedNodeId = 8;
    // The identifier for the reserved node offering.
    string ReservedNodeOfferingId = 9;
    // The time the reservation started. You purchase a reserved node offering for a duration. This is the start time of that duration.
    string StartTime = 10;
    // <p>The state of the reserved compute node.</p> <p>Possible Values:</p> <ul> <li> <p>pending-payment-This reserved node has recently been purchased, and the sale has been approved, but payment has not yet been confirmed.</p> </li> <li> <p>active-This reserved node is owned by the caller and is available for use.</p> </li> <li> <p>payment-failed-Payment failed for the purchase attempt.</p> </li> </ul>
    string State = 11;
    // The hourly rate Amazon Redshift charges you for this reserved node.
    number UsagePrice = 12;
}

message ReservedNodeAlreadyExistsFault {
}

repeated ReservedNode ReservedNodeList = 1

message ReservedNodeNotFoundFault {
}

message ReservedNodeOffering {
    // The currency code for the compute nodes offering.
    string CurrencyCode = 1;
    // The duration, in seconds, for which the offering will reserve the node.
    integer Duration = 2;
    // The upfront fixed charge you will pay to purchase the specific reserved node offering.
    number FixedPrice = 3;
    // The node type offered by the reserved node offering.
    string NodeType = 4;
    // The anticipated utilization of the reserved node, as defined in the reserved node offering.
    string OfferingType = 5;
    // The charge to your account regardless of whether you are creating any clusters using the node offering. Recurring charges are only in effect for heavy-utilization reserved nodes.
    array RecurringCharges = 6;
    // The offering identifier.
    string ReservedNodeOfferingId = 7;
    // The rate you are charged for each hour the cluster that is using the offering is running.
    number UsagePrice = 8;
}

repeated ReservedNodeOffering ReservedNodeOfferingList = 1

message ReservedNodeOfferingNotFoundFault {
}

message ReservedNodeOfferingsMessage {
    // A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request.
    string Marker = 1;
    // A list of <code>ReservedNodeOffering</code> objects.
    array ReservedNodeOfferings = 2;
}

message ReservedNodeQuotaExceededFault {
}

message ReservedNodesMessage {
    // A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request.
    string Marker = 1;
    // The list of <code>ReservedNode</code> objects.
    array ReservedNodes = 2;
}

message ResetClusterParameterGroupMessage {
    // The name of the cluster parameter group to be reset.
    string ParameterGroupName = 1;
    // <p>An array of names of parameters to be reset. If <i>ResetAllParameters</i> option is not used, then at least one parameter name must be supplied. </p> <p>Constraints: A maximum of 20 parameters can be reset in a single request.</p>
    array Parameters = 2;
    // <p>If <code>true</code>, all parameters in the specified parameter group will be reset to their default values. </p> <p>Default: <code>true</code> </p>
    boolean ResetAllParameters = 3;
}

message ResizeNotFoundFault {
}

message ResizeProgressMessage {
    // The average rate of the resize operation over the last few minutes, measured in megabytes per second. After the resize operation completes, this value shows the average rate of the entire resize operation.
    number AvgResizeRateInMegaBytesPerSecond = 1;
    // The amount of seconds that have elapsed since the resize operation began. After the resize operation completes, this value shows the total actual time, in seconds, for the resize operation.
    integer ElapsedTimeInSeconds = 2;
    // The estimated time remaining, in seconds, until the resize operation is complete. This value is calculated based on the average resize rate and the estimated amount of data remaining to be processed. Once the resize operation is complete, this value will be 0.
    integer EstimatedTimeToCompletionInSeconds = 3;
    // <p>The names of tables that have been completely imported .</p> <p>Valid Values: List of table names.</p>
    array ImportTablesCompleted = 4;
    // <p>The names of tables that are being currently imported.</p> <p>Valid Values: List of table names.</p>
    array ImportTablesInProgress = 5;
    // <p>The names of tables that have not been yet imported.</p> <p>Valid Values: List of table names</p>
    array ImportTablesNotStarted = 6;
    // While the resize operation is in progress, this value shows the current amount of data, in megabytes, that has been processed so far. When the resize operation is complete, this value shows the total amount of data, in megabytes, on the cluster, which may be more or less than TotalResizeDataInMegaBytes (the estimated total amount of data before resize).
    integer ProgressInMegaBytes = 7;
    // <p>The status of the resize operation.</p> <p>Valid Values: <code>NONE</code> | <code>IN_PROGRESS</code> | <code>FAILED</code> | <code>SUCCEEDED</code> </p>
    string Status = 8;
    // <p>The cluster type after the resize operation is complete.</p> <p>Valid Values: <code>multi-node</code> | <code>single-node</code> </p>
    string TargetClusterType = 9;
    // The node type that the cluster will have after the resize operation is complete.
    string TargetNodeType = 10;
    // The number of nodes that the cluster will have after the resize operation is complete.
    integer TargetNumberOfNodes = 11;
    // The estimated total amount of data, in megabytes, on the cluster before the resize operation began.
    integer TotalResizeDataInMegaBytes = 12;
}

message ResourceNotFoundFault {
}

repeated string RestorableNodeTypeList = 1

message RestoreFromClusterSnapshotMessage {
    // Reserved.
    string AdditionalInfo = 1;
    // <p>If <code>true</code>, major version upgrades can be applied during the maintenance window to the Amazon Redshift engine that is running on the cluster. </p> <p>Default: <code>true</code> </p>
    boolean AllowVersionUpgrade = 2;
    // <p>The number of days that automated snapshots are retained. If the value is 0, automated snapshots are disabled. Even if automated snapshots are disabled, you can still create manual snapshots when you want with <a>CreateClusterSnapshot</a>. </p> <p>Default: The value selected for the cluster from which the snapshot was taken.</p> <p>Constraints: Must be a value from 0 to 35.</p>
    integer AutomatedSnapshotRetentionPeriod = 3;
    // <p>The Amazon EC2 Availability Zone in which to restore the cluster.</p> <p>Default: A random, system-chosen Availability Zone.</p> <p>Example: <code>us-east-1a</code> </p>
    string AvailabilityZone = 4;
    // <p>The identifier of the cluster that will be created from restoring the snapshot.</p> <p>Constraints:</p> <ul> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens.</p> </li> <li> <p>Alphabetic characters must be lowercase.</p> </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens.</p> </li> <li> <p>Must be unique for all clusters within an AWS account.</p> </li> </ul>
    string ClusterIdentifier = 5;
    // <p>The name of the parameter group to be associated with this cluster.</p> <p>Default: The default Amazon Redshift cluster parameter group. For information about the default parameter group, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-parameter-groups.html">Working with Amazon Redshift Parameter Groups</a>.</p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters or hyphens.</p> </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens.</p> </li> </ul>
    string ClusterParameterGroupName = 6;
    // <p>A list of security groups to be associated with this cluster.</p> <p>Default: The default cluster security group for Amazon Redshift.</p> <p>Cluster security groups only apply to clusters outside of VPCs.</p>
    array ClusterSecurityGroups = 7;
    // <p>The name of the subnet group where you want to cluster restored.</p> <p>A snapshot of cluster in VPC can be restored only in VPC. Therefore, you must provide subnet group name where you want the cluster restored.</p>
    string ClusterSubnetGroupName = 8;
    // The elastic IP (EIP) address for the cluster.
    string ElasticIp = 9;
    // <p>An option that specifies whether to create the cluster with enhanced VPC routing enabled. To create a cluster that uses enhanced VPC routing, the cluster must be in a VPC. For more information, see <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/enhanced-vpc-routing.html">Enhanced VPC Routing</a> in the Amazon Redshift Cluster Management Guide.</p> <p>If this option is <code>true</code>, enhanced VPC routing is enabled. </p> <p>Default: false</p>
    boolean EnhancedVpcRouting = 10;
    // Specifies the name of the HSM client certificate the Amazon Redshift cluster uses to retrieve the data encryption keys stored in an HSM.
    string HsmClientCertificateIdentifier = 11;
    // Specifies the name of the HSM configuration that contains the information the Amazon Redshift cluster can use to retrieve and store keys in an HSM.
    string HsmConfigurationIdentifier = 12;
    // <p>A list of AWS Identity and Access Management (IAM) roles that can be used by the cluster to access other AWS services. You must supply the IAM roles in their Amazon Resource Name (ARN) format. You can supply up to 10 IAM roles in a single request.</p> <p>A cluster can have up to 10 IAM roles associated at any time.</p>
    array IamRoles = 13;
    // The AWS Key Management Service (KMS) key ID of the encryption key that you want to use to encrypt data in the cluster that you restore from a shared snapshot.
    string KmsKeyId = 14;
    // <p>The node type that the restored cluster will be provisioned with.</p> <p>Default: The node type of the cluster from which the snapshot was taken. You can modify this if you are using any DS node type. In that case, you can choose to restore into another DS node type of the same size. For example, you can restore ds1.8xlarge into ds2.8xlarge, or ds2.xlarge into ds1.xlarge. If you have a DC instance type, you must restore into that same instance type and size. In other words, you can only restore a dc1.large instance type into another dc1.large instance type. For more information about node types, see <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html#rs-about-clusters-and-nodes"> About Clusters and Nodes</a> in the <i>Amazon Redshift Cluster Management Guide</i> </p>
    string NodeType = 15;
    // The AWS customer account used to create or copy the snapshot. Required if you are restoring a snapshot you do not own, optional if you own the snapshot.
    string OwnerAccount = 16;
    // <p>The port number on which the cluster accepts connections.</p> <p>Default: The same port as the original cluster.</p> <p>Constraints: Must be between <code>1115</code> and <code>65535</code>.</p>
    integer Port = 17;
    // <p>The weekly time range (in UTC) during which automated cluster maintenance can occur.</p> <p> Format: <code>ddd:hh24:mi-ddd:hh24:mi</code> </p> <p> Default: The value selected for the cluster from which the snapshot was taken. For more information about the time blocks for each region, see <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html#rs-maintenance-windows">Maintenance Windows</a> in Amazon Redshift Cluster Management Guide. </p> <p>Valid Days: Mon | Tue | Wed | Thu | Fri | Sat | Sun</p> <p>Constraints: Minimum 30-minute window.</p>
    string PreferredMaintenanceWindow = 18;
    // If <code>true</code>, the cluster can be accessed from a public network.
    boolean PubliclyAccessible = 19;
    // The name of the cluster the source snapshot was created from. This parameter is required if your IAM user has a policy containing a snapshot resource element that specifies anything other than * for the cluster name.
    string SnapshotClusterIdentifier = 20;
    // <p>The name of the snapshot from which to create the new cluster. This parameter isn't case sensitive.</p> <p>Example: <code>my-snapshot-id</code> </p>
    string SnapshotIdentifier = 21;
    // <p>A list of Virtual Private Cloud (VPC) security groups to be associated with the cluster.</p> <p>Default: The default VPC security group is associated with the cluster.</p> <p>VPC security groups only apply to clusters in VPCs.</p>
    array VpcSecurityGroupIds = 22;
}

message RestoreFromClusterSnapshotResult {
    Cluster Cluster = 1;
}

message RestoreStatus {
    // The number of megabytes per second being transferred from the backup storage. Returns the average rate for a completed backup.
    number CurrentRestoreRateInMegaBytesPerSecond = 1;
    // The amount of time an in-progress restore has been running, or the amount of time it took a completed restore to finish.
    integer ElapsedTimeInSeconds = 2;
    // The estimate of the time remaining before the restore will complete. Returns 0 for a completed restore.
    integer EstimatedTimeToCompletionInSeconds = 3;
    // The number of megabytes that have been transferred from snapshot storage.
    integer ProgressInMegaBytes = 4;
    // The size of the set of snapshot data used to restore the cluster.
    integer SnapshotSizeInMegaBytes = 5;
    // The status of the restore action. Returns starting, restoring, completed, or failed.
    string Status = 6;
}

message RestoreTableFromClusterSnapshotMessage {
    // The identifier of the Amazon Redshift cluster to restore the table to.
    string ClusterIdentifier = 1;
    // The name of the table to create as a result of the current request.
    string NewTableName = 2;
    // The identifier of the snapshot to restore the table from. This snapshot must have been created from the Amazon Redshift cluster specified by the <code>ClusterIdentifier</code> parameter.
    string SnapshotIdentifier = 3;
    // The name of the source database that contains the table to restore from.
    string SourceDatabaseName = 4;
    // The name of the source schema that contains the table to restore from. If you do not specify a <code>SourceSchemaName</code> value, the default is <code>public</code>.
    string SourceSchemaName = 5;
    // The name of the source table to restore from.
    string SourceTableName = 6;
    // The name of the database to restore the table to.
    string TargetDatabaseName = 7;
    // The name of the schema to restore the table to.
    string TargetSchemaName = 8;
}

message RestoreTableFromClusterSnapshotResult {
    TableRestoreStatus TableRestoreStatus = 1;
}

message RevokeClusterSecurityGroupIngressMessage {
    // The IP range for which to revoke access. This range must be a valid Classless Inter-Domain Routing (CIDR) block of IP addresses. If <code>CIDRIP</code> is specified, <code>EC2SecurityGroupName</code> and <code>EC2SecurityGroupOwnerId</code> cannot be provided.
    string CIDRIP = 1;
    // The name of the security Group from which to revoke the ingress rule.
    string ClusterSecurityGroupName = 2;
    // The name of the EC2 Security Group whose access is to be revoked. If <code>EC2SecurityGroupName</code> is specified, <code>EC2SecurityGroupOwnerId</code> must also be provided and <code>CIDRIP</code> cannot be provided.
    string EC2SecurityGroupName = 3;
    // <p>The AWS account number of the owner of the security group specified in the <code>EC2SecurityGroupName</code> parameter. The AWS access key ID is not an acceptable value. If <code>EC2SecurityGroupOwnerId</code> is specified, <code>EC2SecurityGroupName</code> must also be provided. and <code>CIDRIP</code> cannot be provided. </p> <p>Example: <code>111122223333</code> </p>
    string EC2SecurityGroupOwnerId = 4;
}

message RevokeClusterSecurityGroupIngressResult {
    ClusterSecurityGroup ClusterSecurityGroup = 1;
}

message RevokeSnapshotAccessMessage {
    // The identifier of the AWS customer account that can no longer restore the specified snapshot.
    string AccountWithRestoreAccess = 1;
    // The identifier of the cluster the snapshot was created from. This parameter is required if your IAM user has a policy containing a snapshot resource element that specifies anything other than * for the cluster name.
    string SnapshotClusterIdentifier = 2;
    // The identifier of the snapshot that the account can no longer access.
    string SnapshotIdentifier = 3;
}

message RevokeSnapshotAccessResult {
    Snapshot Snapshot = 1;
}

message RotateEncryptionKeyMessage {
    // <p>The unique identifier of the cluster that you want to rotate the encryption keys for.</p> <p>Constraints: Must be the name of valid cluster that has encryption enabled.</p>
    string ClusterIdentifier = 1;
}

message RotateEncryptionKeyResult {
    Cluster Cluster = 1;
}

message SNSInvalidTopicFault {
}

message SNSNoAuthorizationFault {
}

message SNSTopicArnNotFoundFault {
}

message Snapshot {
    // A list of the AWS customer accounts authorized to restore the snapshot. Returns <code>null</code> if no accounts are authorized. Visible only to the snapshot owner.
    array AccountsWithRestoreAccess = 1;
    // The size of the incremental backup.
    number ActualIncrementalBackupSizeInMegaBytes = 2;
    // The Availability Zone in which the cluster was created.
    string AvailabilityZone = 3;
    // The number of megabytes that have been transferred to the snapshot backup.
    number BackupProgressInMegaBytes = 4;
    // The time (UTC) when the cluster was originally created.
    string ClusterCreateTime = 5;
    // The identifier of the cluster for which the snapshot was taken.
    string ClusterIdentifier = 6;
    // The version ID of the Amazon Redshift engine that is running on the cluster.
    string ClusterVersion = 7;
    // The number of megabytes per second being transferred to the snapshot backup. Returns <code>0</code> for a completed backup.
    number CurrentBackupRateInMegaBytesPerSecond = 8;
    // The name of the database that was created when the cluster was created.
    string DBName = 9;
    // The amount of time an in-progress snapshot backup has been running, or the amount of time it took a completed backup to finish.
    integer ElapsedTimeInSeconds = 10;
    // If <code>true</code>, the data in the snapshot is encrypted at rest.
    boolean Encrypted = 11;
    // A boolean that indicates whether the snapshot data is encrypted using the HSM keys of the source cluster. <code>true</code> indicates that the data is encrypted using HSM keys.
    boolean EncryptedWithHSM = 12;
    // <p>An option that specifies whether to create the cluster with enhanced VPC routing enabled. To create a cluster that uses enhanced VPC routing, the cluster must be in a VPC. For more information, see <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/enhanced-vpc-routing.html">Enhanced VPC Routing</a> in the Amazon Redshift Cluster Management Guide.</p> <p>If this option is <code>true</code>, enhanced VPC routing is enabled. </p> <p>Default: false</p>
    boolean EnhancedVpcRouting = 13;
    // The estimate of the time remaining before the snapshot backup will complete. Returns <code>0</code> for a completed backup.
    integer EstimatedSecondsToCompletion = 14;
    // The AWS Key Management Service (KMS) key ID of the encryption key that was used to encrypt data in the cluster from which the snapshot was taken.
    string KmsKeyId = 15;
    // The master user name for the cluster.
    string MasterUsername = 16;
    // The node type of the nodes in the cluster.
    string NodeType = 17;
    // The number of nodes in the cluster.
    integer NumberOfNodes = 18;
    // For manual snapshots, the AWS customer account used to create or copy the snapshot. For automatic snapshots, the owner of the cluster. The owner can perform all snapshot actions, such as sharing a manual snapshot.
    string OwnerAccount = 19;
    // The port that the cluster is listening on.
    integer Port = 20;
    // The list of node types that this cluster snapshot is able to restore into.
    array RestorableNodeTypes = 21;
    // The time (UTC) when Amazon Redshift began the snapshot. A snapshot contains a copy of the cluster data as of this exact time.
    string SnapshotCreateTime = 22;
    // The snapshot identifier that is provided in the request.
    string SnapshotIdentifier = 23;
    // The snapshot type. Snapshots created using <a>CreateClusterSnapshot</a> and <a>CopyClusterSnapshot</a> will be of type "manual".
    string SnapshotType = 24;
    // The source region from which the snapshot was copied.
    string SourceRegion = 25;
    // <p>The snapshot status. The value of the status depends on the API operation used. </p> <ul> <li> <p> <a>CreateClusterSnapshot</a> and <a>CopyClusterSnapshot</a> returns status as "creating". </p> </li> <li> <p> <a>DescribeClusterSnapshots</a> returns status as "creating", "available", "final snapshot", or "failed".</p> </li> <li> <p> <a>DeleteClusterSnapshot</a> returns status as "deleted".</p> </li> </ul>
    string Status = 26;
    // The list of tags for the cluster snapshot.
    array Tags = 27;
    // The size of the complete set of backup data that would be used to restore the cluster.
    number TotalBackupSizeInMegaBytes = 28;
    // The VPC identifier of the cluster if the snapshot is from a cluster in a VPC. Otherwise, this field is not in the output.
    string VpcId = 29;
}

message SnapshotCopyAlreadyDisabledFault {
}

message SnapshotCopyAlreadyEnabledFault {
}

message SnapshotCopyDisabledFault {
}

message SnapshotCopyGrant {
    // The unique identifier of the customer master key (CMK) in AWS KMS to which Amazon Redshift is granted permission.
    string KmsKeyId = 1;
    // The name of the snapshot copy grant.
    string SnapshotCopyGrantName = 2;
    // A list of tag instances.
    array Tags = 3;
}

message SnapshotCopyGrantAlreadyExistsFault {
}

repeated SnapshotCopyGrant SnapshotCopyGrantList = 1

message SnapshotCopyGrantMessage {
    // <p>An optional parameter that specifies the starting point to return a set of response records. When the results of a <code>DescribeSnapshotCopyGrant</code> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request. </p> <p>Constraints: You can specify either the <b>SnapshotCopyGrantName</b> parameter or the <b>Marker</b> parameter, but not both. </p>
    string Marker = 1;
    // The list of <code>SnapshotCopyGrant</code> objects.
    array SnapshotCopyGrants = 2;
}

message SnapshotCopyGrantNotFoundFault {
}

message SnapshotCopyGrantQuotaExceededFault {
}

repeated Snapshot SnapshotList = 1

message SnapshotMessage {
    // A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request.
    string Marker = 1;
    // A list of <a>Snapshot</a> instances.
    array Snapshots = 2;
}

repeated string SourceIdsList = 1

message SourceNotFoundFault {
}

enum SourceType {
    CLUSTER = 0;
    CLUSTERPARAMETERGROUP = 1;
    CLUSTERSECURITYGROUP = 2;
    CLUSTERSNAPSHOT = 3;
}

message Subnet {
    AvailabilityZone SubnetAvailabilityZone = 1;
    // The identifier of the subnet.
    string SubnetIdentifier = 2;
    // The status of the subnet.
    string SubnetStatus = 3;
}

message SubnetAlreadyInUse {
}

repeated string SubnetIdentifierList = 1

repeated Subnet SubnetList = 1

message SubscriptionAlreadyExistFault {
}

message SubscriptionCategoryNotFoundFault {
}

message SubscriptionEventIdNotFoundFault {
}

message SubscriptionNotFoundFault {
}

message SubscriptionSeverityNotFoundFault {
}

message TableRestoreNotFoundFault {
}

message TableRestoreStatus {
    // The identifier of the Amazon Redshift cluster that the table is being restored to.
    string ClusterIdentifier = 1;
    // A description of the status of the table restore request. Status values include <code>SUCCEEDED</code>, <code>FAILED</code>, <code>CANCELED</code>, <code>PENDING</code>, <code>IN_PROGRESS</code>.
    string Message = 2;
    // The name of the table to create as a result of the table restore request.
    string NewTableName = 3;
    // The amount of data restored to the new table so far, in megabytes (MB).
    integer ProgressInMegaBytes = 4;
    // The time that the table restore request was made, in Universal Coordinated Time (UTC).
    string RequestTime = 5;
    // The identifier of the snapshot that the table is being restored from.
    string SnapshotIdentifier = 6;
    // The name of the source database that contains the table being restored.
    string SourceDatabaseName = 7;
    // The name of the source schema that contains the table being restored.
    string SourceSchemaName = 8;
    // The name of the source table being restored.
    string SourceTableName = 9;
    // <p>A value that describes the current state of the table restore request.</p> <p>Valid Values: <code>SUCCEEDED</code>, <code>FAILED</code>, <code>CANCELED</code>, <code>PENDING</code>, <code>IN_PROGRESS</code> </p>
    TableRestoreStatusType Status = 10;
    // The unique identifier for the table restore request.
    string TableRestoreRequestId = 11;
    // The name of the database to restore the table to.
    string TargetDatabaseName = 12;
    // The name of the schema to restore the table to.
    string TargetSchemaName = 13;
    // The total amount of data to restore to the new table, in megabytes (MB).
    integer TotalDataInMegaBytes = 14;
}

repeated TableRestoreStatus TableRestoreStatusList = 1

message TableRestoreStatusMessage {
    // A pagination token that can be used in a subsequent <a>DescribeTableRestoreStatus</a> request.
    string Marker = 1;
    // A list of status details for one or more table restore requests.
    array TableRestoreStatusDetails = 2;
}

enum TableRestoreStatusType {
    PENDING = 0;
    IN_PROGRESS = 1;
    SUCCEEDED = 2;
    FAILED = 3;
    CANCELED = 4;
}

message Tag {
    // The key, or name, for the resource tag.
    string Key = 1;
    // The value for the resource tag.
    string Value = 2;
}

repeated string TagKeyList = 1

message TagLimitExceededFault {
}

repeated Tag TagList = 1

repeated string TagValueList = 1

message TaggedResource {
    // The Amazon Resource Name (ARN) with which the tag is associated. For example, <code>arn:aws:redshift:us-east-1:123456789:cluster:t1</code>.
    string ResourceName = 1;
    // <p>The type of resource with which the tag is associated. Valid resource types are: </p> <ul> <li> <p>Cluster</p> </li> <li> <p>CIDR/IP</p> </li> <li> <p>EC2 security group</p> </li> <li> <p>Snapshot</p> </li> <li> <p>Cluster security group</p> </li> <li> <p>Subnet group</p> </li> <li> <p>HSM connection</p> </li> <li> <p>HSM certificate</p> </li> <li> <p>Parameter group</p> </li> </ul> <p>For more information about Amazon Redshift resource types and constructing ARNs, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/constructing-redshift-arn.html">Constructing an Amazon Redshift Amazon Resource Name (ARN)</a> in the Amazon Redshift Cluster Management Guide. </p>
    string ResourceType = 2;
    // The tag for the resource.
    Tag Tag = 3;
}

repeated TaggedResource TaggedResourceList = 1

message TaggedResourceListMessage {
    // A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request.
    string Marker = 1;
    // A list of tags with their associated resources.
    array TaggedResources = 2;
}

message UnauthorizedOperation {
}

message UnknownSnapshotCopyRegionFault {
}

message UnsupportedOperationFault {
}

message UnsupportedOptionFault {
}



repeated string VpcSecurityGroupIdList = 1

message VpcSecurityGroupMembership {
    // The status of the VPC security group.
    string Status = 1;
    // The identifier of the VPC security group.
    string VpcSecurityGroupId = 2;
}

repeated VpcSecurityGroupMembership VpcSecurityGroupMembershipList = 1















service AmazonRedshiftService {
    // <p>Adds an inbound (ingress) rule to an Amazon Redshift security group. Depending on whether the application accessing your cluster is running on the Internet or an Amazon EC2 instance, you can authorize inbound access to either a Classless Interdomain Routing (CIDR)/Internet Protocol (IP) range or to an Amazon EC2 security group. You can add as many as 20 ingress rules to an Amazon Redshift security group.</p> <p>If you authorize access to an Amazon EC2 security group, specify <i>EC2SecurityGroupName</i> and <i>EC2SecurityGroupOwnerId</i>. The Amazon EC2 security group and Amazon Redshift cluster must be in the same AWS region. </p> <p>If you authorize access to a CIDR/IP address range, specify <i>CIDRIP</i>. For an overview of CIDR blocks, see the Wikipedia article on <a href="http://en.wikipedia.org/wiki/Classless_Inter-Domain_Routing">Classless Inter-Domain Routing</a>. </p> <p>You must also associate the security group with a cluster so that clients running on these IP addresses or the EC2 instance are authorized to connect to the cluster. For information about managing security groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-security-groups.html">Working with Security Groups</a> in the <i>Amazon Redshift Cluster Management Guide</i>.</p>
    rpc Post(PostRequest) returns (AuthorizeClusterSecurityGroupIngressResult) {
      option (google.api.http) = {
        post: "//"
        body: "body"
      };
    }
    // <p>Authorizes the specified AWS customer account to restore the specified snapshot.</p> <p> For more information about working with snapshots, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-snapshots.html">Amazon Redshift Snapshots</a> in the <i>Amazon Redshift Cluster Management Guide</i>.</p>
    rpc Post#AuthorizeSnapshotAccess(Post#AuthorizeSnapshotAccessRequest) returns (AuthorizeSnapshotAccessResult) {
      option (google.api.http) = {
        post: "//#AuthorizeSnapshotAccess"
        body: "body"
      };
    }
    // <p>Copies the specified automated cluster snapshot to a new manual cluster snapshot. The source must be an automated snapshot and it must be in the available state.</p> <p>When you delete a cluster, Amazon Redshift deletes any automated snapshots of the cluster. Also, when the retention period of the snapshot expires, Amazon Redshift automatically deletes it. If you want to keep an automated snapshot for a longer period, you can make a manual copy of the snapshot. Manual snapshots are retained until you delete them.</p> <p> For more information about working with snapshots, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-snapshots.html">Amazon Redshift Snapshots</a> in the <i>Amazon Redshift Cluster Management Guide</i>.</p>
    rpc Post#CopyClusterSnapshot(Post#CopyClusterSnapshotRequest) returns (CopyClusterSnapshotResult) {
      option (google.api.http) = {
        post: "//#CopyClusterSnapshot"
        body: "body"
      };
    }
    // <p>Creates a new cluster.</p> <p>To create the cluster in Virtual Private Cloud (VPC), you must provide a cluster subnet group name. The cluster subnet group identifies the subnets of your VPC that Amazon Redshift uses when creating the cluster. For more information about managing clusters, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html">Amazon Redshift Clusters</a> in the <i>Amazon Redshift Cluster Management Guide</i>.</p>
    rpc Post#CreateCluster(Post#CreateClusterRequest) returns (CreateClusterResult) {
      option (google.api.http) = {
        post: "//#CreateCluster"
        body: "body"
      };
    }
    // <p>Creates an Amazon Redshift parameter group.</p> <p>Creating parameter groups is independent of creating clusters. You can associate a cluster with a parameter group when you create the cluster. You can also associate an existing cluster with a parameter group after the cluster is created by using <a>ModifyCluster</a>. </p> <p>Parameters in the parameter group define specific behavior that applies to the databases you create on the cluster. For more information about parameters and parameter groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-parameter-groups.html">Amazon Redshift Parameter Groups</a> in the <i>Amazon Redshift Cluster Management Guide</i>.</p>
    rpc Post#CreateClusterParameterGroup(Post#CreateClusterParameterGroupRequest) returns (CreateClusterParameterGroupResult) {
      option (google.api.http) = {
        post: "//#CreateClusterParameterGroup"
        body: "body"
      };
    }
    // <p>Creates a new Amazon Redshift security group. You use security groups to control access to non-VPC clusters.</p> <p> For information about managing security groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-security-groups.html">Amazon Redshift Cluster Security Groups</a> in the <i>Amazon Redshift Cluster Management Guide</i>.</p>
    rpc Post#CreateClusterSecurityGroup(Post#CreateClusterSecurityGroupRequest) returns (CreateClusterSecurityGroupResult) {
      option (google.api.http) = {
        post: "//#CreateClusterSecurityGroup"
        body: "body"
      };
    }
    // <p>Creates a manual snapshot of the specified cluster. The cluster must be in the <code>available</code> state. </p> <p> For more information about working with snapshots, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-snapshots.html">Amazon Redshift Snapshots</a> in the <i>Amazon Redshift Cluster Management Guide</i>.</p>
    rpc Post#CreateClusterSnapshot(Post#CreateClusterSnapshotRequest) returns (CreateClusterSnapshotResult) {
      option (google.api.http) = {
        post: "//#CreateClusterSnapshot"
        body: "body"
      };
    }
    // <p>Creates a new Amazon Redshift subnet group. You must provide a list of one or more subnets in your existing Amazon Virtual Private Cloud (Amazon VPC) when creating Amazon Redshift subnet group.</p> <p> For information about subnet groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-cluster-subnet-groups.html">Amazon Redshift Cluster Subnet Groups</a> in the <i>Amazon Redshift Cluster Management Guide</i>.</p>
    rpc Post#CreateClusterSubnetGroup(Post#CreateClusterSubnetGroupRequest) returns (CreateClusterSubnetGroupResult) {
      option (google.api.http) = {
        post: "//#CreateClusterSubnetGroup"
        body: "body"
      };
    }
    // <p>Creates an Amazon Redshift event notification subscription. This action requires an ARN (Amazon Resource Name) of an Amazon SNS topic created by either the Amazon Redshift console, the Amazon SNS console, or the Amazon SNS API. To obtain an ARN with Amazon SNS, you must create a topic in Amazon SNS and subscribe to the topic. The ARN is displayed in the SNS console.</p> <p>You can specify the source type, and lists of Amazon Redshift source IDs, event categories, and event severities. Notifications will be sent for all events you want that match those criteria. For example, you can specify source type = cluster, source ID = my-cluster-1 and mycluster2, event categories = Availability, Backup, and severity = ERROR. The subscription will only send notifications for those ERROR events in the Availability and Backup categories for the specified clusters.</p> <p>If you specify both the source type and source IDs, such as source type = cluster and source identifier = my-cluster-1, notifications will be sent for all the cluster events for my-cluster-1. If you specify a source type but do not specify a source identifier, you will receive notice of the events for the objects of that type in your AWS account. If you do not specify either the SourceType nor the SourceIdentifier, you will be notified of events generated from all Amazon Redshift sources belonging to your AWS account. You must specify a source type if you specify a source ID.</p>
    rpc Post#CreateEventSubscription(Post#CreateEventSubscriptionRequest) returns (CreateEventSubscriptionResult) {
      option (google.api.http) = {
        post: "//#CreateEventSubscription"
        body: "body"
      };
    }
    // <p>Creates an HSM client certificate that an Amazon Redshift cluster will use to connect to the client's HSM in order to store and retrieve the keys used to encrypt the cluster databases.</p> <p>The command returns a public key, which you must store in the HSM. In addition to creating the HSM certificate, you must create an Amazon Redshift HSM configuration that provides a cluster the information needed to store and use encryption keys in the HSM. For more information, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-HSM.html">Hardware Security Modules</a> in the Amazon Redshift Cluster Management Guide.</p>
    rpc Post#CreateHsmClientCertificate(Post#CreateHsmClientCertificateRequest) returns (CreateHsmClientCertificateResult) {
      option (google.api.http) = {
        post: "//#CreateHsmClientCertificate"
        body: "body"
      };
    }
    // <p>Creates an HSM configuration that contains the information required by an Amazon Redshift cluster to store and use database encryption keys in a Hardware Security Module (HSM). After creating the HSM configuration, you can specify it as a parameter when creating a cluster. The cluster will then store its encryption keys in the HSM.</p> <p>In addition to creating an HSM configuration, you must also create an HSM client certificate. For more information, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-HSM.html">Hardware Security Modules</a> in the Amazon Redshift Cluster Management Guide.</p>
    rpc Post#CreateHsmConfiguration(Post#CreateHsmConfigurationRequest) returns (CreateHsmConfigurationResult) {
      option (google.api.http) = {
        post: "//#CreateHsmConfiguration"
        body: "body"
      };
    }
    // <p>Creates a snapshot copy grant that permits Amazon Redshift to use a customer master key (CMK) from AWS Key Management Service (AWS KMS) to encrypt copied snapshots in a destination region.</p> <p> For more information about managing snapshot copy grants, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-db-encryption.html">Amazon Redshift Database Encryption</a> in the <i>Amazon Redshift Cluster Management Guide</i>. </p>
    rpc Post#CreateSnapshotCopyGrant(Post#CreateSnapshotCopyGrantRequest) returns (CreateSnapshotCopyGrantResult) {
      option (google.api.http) = {
        post: "//#CreateSnapshotCopyGrant"
        body: "body"
      };
    }
    // <p>Adds one or more tags to a specified resource.</p> <p>A resource can have up to 10 tags. If you try to create more than 10 tags for a resource, you will receive an error and the attempt will fail.</p> <p>If you specify a key that already exists for the resource, the value for that key will be updated with the new value.</p>
    rpc Post#CreateTags(Post#CreateTagsRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#CreateTags"
        body: "body"
      };
    }
    // <p>Deletes a previously provisioned cluster. A successful response from the web service indicates that the request was received correctly. Use <a>DescribeClusters</a> to monitor the status of the deletion. The delete operation cannot be canceled or reverted once submitted. For more information about managing clusters, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html">Amazon Redshift Clusters</a> in the <i>Amazon Redshift Cluster Management Guide</i>.</p> <p>If you want to shut down the cluster and retain it for future use, set <i>SkipFinalClusterSnapshot</i> to <code>false</code> and specify a name for <i>FinalClusterSnapshotIdentifier</i>. You can later restore this snapshot to resume using the cluster. If a final cluster snapshot is requested, the status of the cluster will be "final-snapshot" while the snapshot is being taken, then it's "deleting" once Amazon Redshift begins deleting the cluster. </p> <p> For more information about managing clusters, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html">Amazon Redshift Clusters</a> in the <i>Amazon Redshift Cluster Management Guide</i>.</p>
    rpc Post#DeleteCluster(Post#DeleteClusterRequest) returns (DeleteClusterResult) {
      option (google.api.http) = {
        post: "//#DeleteCluster"
        body: "body"
      };
    }
    // <p>Deletes a specified Amazon Redshift parameter group.</p> <note> <p>You cannot delete a parameter group if it is associated with a cluster.</p> </note>
    rpc Post#DeleteClusterParameterGroup(Post#DeleteClusterParameterGroupRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#DeleteClusterParameterGroup"
        body: "body"
      };
    }
    // <p>Deletes an Amazon Redshift security group.</p> <note> <p>You cannot delete a security group that is associated with any clusters. You cannot delete the default security group.</p> </note> <p> For information about managing security groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-security-groups.html">Amazon Redshift Cluster Security Groups</a> in the <i>Amazon Redshift Cluster Management Guide</i>.</p>
    rpc Post#DeleteClusterSecurityGroup(Post#DeleteClusterSecurityGroupRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#DeleteClusterSecurityGroup"
        body: "body"
      };
    }
    // <p>Deletes the specified manual snapshot. The snapshot must be in the <code>available</code> state, with no other users authorized to access the snapshot. </p> <p>Unlike automated snapshots, manual snapshots are retained even after you delete your cluster. Amazon Redshift does not delete your manual snapshots. You must delete manual snapshot explicitly to avoid getting charged. If other accounts are authorized to access the snapshot, you must revoke all of the authorizations before you can delete the snapshot.</p>
    rpc Post#DeleteClusterSnapshot(Post#DeleteClusterSnapshotRequest) returns (DeleteClusterSnapshotResult) {
      option (google.api.http) = {
        post: "//#DeleteClusterSnapshot"
        body: "body"
      };
    }
    // Deletes the specified cluster subnet group.
    rpc Post#DeleteClusterSubnetGroup(Post#DeleteClusterSubnetGroupRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#DeleteClusterSubnetGroup"
        body: "body"
      };
    }
    // Deletes an Amazon Redshift event notification subscription.
    rpc Post#DeleteEventSubscription(Post#DeleteEventSubscriptionRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#DeleteEventSubscription"
        body: "body"
      };
    }
    // Deletes the specified HSM client certificate.
    rpc Post#DeleteHsmClientCertificate(Post#DeleteHsmClientCertificateRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#DeleteHsmClientCertificate"
        body: "body"
      };
    }
    // Deletes the specified Amazon Redshift HSM configuration.
    rpc Post#DeleteHsmConfiguration(Post#DeleteHsmConfigurationRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#DeleteHsmConfiguration"
        body: "body"
      };
    }
    // Deletes the specified snapshot copy grant.
    rpc Post#DeleteSnapshotCopyGrant(Post#DeleteSnapshotCopyGrantRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#DeleteSnapshotCopyGrant"
        body: "body"
      };
    }
    // Deletes a tag or tags from a resource. You must provide the ARN of the resource from which you want to delete the tag or tags.
    rpc Post#DeleteTags(Post#DeleteTagsRequest) returns (google.protobuf.Empty) {
      option (google.api.http) = {
        post: "//#DeleteTags"
        body: "body"
      };
    }
    // <p>Returns a list of Amazon Redshift parameter groups, including parameter groups you created and the default parameter group. For each parameter group, the response includes the parameter group name, description, and parameter group family name. You can optionally specify a name to retrieve the description of a specific parameter group.</p> <p> For more information about parameters and parameter groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-parameter-groups.html">Amazon Redshift Parameter Groups</a> in the <i>Amazon Redshift Cluster Management Guide</i>.</p> <p>If you specify both tag keys and tag values in the same request, Amazon Redshift returns all parameter groups that match any combination of the specified keys and values. For example, if you have <code>owner</code> and <code>environment</code> for tag keys, and <code>admin</code> and <code>test</code> for tag values, all parameter groups that have any combination of those values are returned.</p> <p>If both tag keys and values are omitted from the request, parameter groups are returned regardless of whether they have tag keys or values associated with them.</p>
    rpc Post#DescribeClusterParameterGroups(Post#DescribeClusterParameterGroupsRequest) returns (ClusterParameterGroupsMessage) {
      option (google.api.http) = {
        post: "//#DescribeClusterParameterGroups"
        body: "body"
      };
    }
    // <p>Returns a detailed list of parameters contained within the specified Amazon Redshift parameter group. For each parameter the response includes information such as parameter name, description, data type, value, whether the parameter value is modifiable, and so on.</p> <p>You can specify <i>source</i> filter to retrieve parameters of only specific type. For example, to retrieve parameters that were modified by a user action such as from <a>ModifyClusterParameterGroup</a>, you can specify <i>source</i> equal to <i>user</i>.</p> <p> For more information about parameters and parameter groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-parameter-groups.html">Amazon Redshift Parameter Groups</a> in the <i>Amazon Redshift Cluster Management Guide</i>.</p>
    rpc Post#DescribeClusterParameters(Post#DescribeClusterParametersRequest) returns (ClusterParameterGroupDetails) {
      option (google.api.http) = {
        post: "//#DescribeClusterParameters"
        body: "body"
      };
    }
    // <p>Returns information about Amazon Redshift security groups. If the name of a security group is specified, the response will contain only information about only that security group.</p> <p> For information about managing security groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-security-groups.html">Amazon Redshift Cluster Security Groups</a> in the <i>Amazon Redshift Cluster Management Guide</i>.</p> <p>If you specify both tag keys and tag values in the same request, Amazon Redshift returns all security groups that match any combination of the specified keys and values. For example, if you have <code>owner</code> and <code>environment</code> for tag keys, and <code>admin</code> and <code>test</code> for tag values, all security groups that have any combination of those values are returned.</p> <p>If both tag keys and values are omitted from the request, security groups are returned regardless of whether they have tag keys or values associated with them.</p>
    rpc Post#DescribeClusterSecurityGroups(Post#DescribeClusterSecurityGroupsRequest) returns (ClusterSecurityGroupMessage) {
      option (google.api.http) = {
        post: "//#DescribeClusterSecurityGroups"
        body: "body"
      };
    }
    // <p>Returns one or more snapshot objects, which contain metadata about your cluster snapshots. By default, this operation returns information about all snapshots of all clusters that are owned by you AWS customer account. No information is returned for snapshots owned by inactive AWS customer accounts.</p> <p>If you specify both tag keys and tag values in the same request, Amazon Redshift returns all snapshots that match any combination of the specified keys and values. For example, if you have <code>owner</code> and <code>environment</code> for tag keys, and <code>admin</code> and <code>test</code> for tag values, all snapshots that have any combination of those values are returned. Only snapshots that you own are returned in the response; shared snapshots are not returned with the tag key and tag value request parameters.</p> <p>If both tag keys and values are omitted from the request, snapshots are returned regardless of whether they have tag keys or values associated with them.</p>
    rpc Post#DescribeClusterSnapshots(Post#DescribeClusterSnapshotsRequest) returns (SnapshotMessage) {
      option (google.api.http) = {
        post: "//#DescribeClusterSnapshots"
        body: "body"
      };
    }
    // <p>Returns one or more cluster subnet group objects, which contain metadata about your cluster subnet groups. By default, this operation returns information about all cluster subnet groups that are defined in you AWS account.</p> <p>If you specify both tag keys and tag values in the same request, Amazon Redshift returns all subnet groups that match any combination of the specified keys and values. For example, if you have <code>owner</code> and <code>environment</code> for tag keys, and <code>admin</code> and <code>test</code> for tag values, all subnet groups that have any combination of those values are returned.</p> <p>If both tag keys and values are omitted from the request, subnet groups are returned regardless of whether they have tag keys or values associated with them.</p>
    rpc Post#DescribeClusterSubnetGroups(Post#DescribeClusterSubnetGroupsRequest) returns (ClusterSubnetGroupMessage) {
      option (google.api.http) = {
        post: "//#DescribeClusterSubnetGroups"
        body: "body"
      };
    }
    // Returns descriptions of the available Amazon Redshift cluster versions. You can call this operation even before creating any clusters to learn more about the Amazon Redshift versions. For more information about managing clusters, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html">Amazon Redshift Clusters</a> in the <i>Amazon Redshift Cluster Management Guide</i>.
    rpc Post#DescribeClusterVersions(Post#DescribeClusterVersionsRequest) returns (ClusterVersionsMessage) {
      option (google.api.http) = {
        post: "//#DescribeClusterVersions"
        body: "body"
      };
    }
    // <p>Returns properties of provisioned clusters including general cluster properties, cluster database properties, maintenance and backup properties, and security and access properties. This operation supports pagination. For more information about managing clusters, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html">Amazon Redshift Clusters</a> in the <i>Amazon Redshift Cluster Management Guide</i>.</p> <p>If you specify both tag keys and tag values in the same request, Amazon Redshift returns all clusters that match any combination of the specified keys and values. For example, if you have <code>owner</code> and <code>environment</code> for tag keys, and <code>admin</code> and <code>test</code> for tag values, all clusters that have any combination of those values are returned.</p> <p>If both tag keys and values are omitted from the request, clusters are returned regardless of whether they have tag keys or values associated with them.</p>
    rpc Post#DescribeClusters(Post#DescribeClustersRequest) returns (ClustersMessage) {
      option (google.api.http) = {
        post: "//#DescribeClusters"
        body: "body"
      };
    }
    // <p>Returns a list of parameter settings for the specified parameter group family.</p> <p> For more information about parameters and parameter groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-parameter-groups.html">Amazon Redshift Parameter Groups</a> in the <i>Amazon Redshift Cluster Management Guide</i>.</p>
    rpc Post#DescribeDefaultClusterParameters(Post#DescribeDefaultClusterParametersRequest) returns (DescribeDefaultClusterParametersResult) {
      option (google.api.http) = {
        post: "//#DescribeDefaultClusterParameters"
        body: "body"
      };
    }
    // Displays a list of event categories for all event source types, or for a specified source type. For a list of the event categories and source types, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-event-notifications.html">Amazon Redshift Event Notifications</a>.
    rpc Post#DescribeEventCategories(Post#DescribeEventCategoriesRequest) returns (EventCategoriesMessage) {
      option (google.api.http) = {
        post: "//#DescribeEventCategories"
        body: "body"
      };
    }
    // Lists descriptions of all the Amazon Redshift event notifications subscription for a customer account. If you specify a subscription name, lists the description for that subscription.
    rpc Post#DescribeEventSubscriptions(Post#DescribeEventSubscriptionsRequest) returns (EventSubscriptionsMessage) {
      option (google.api.http) = {
        post: "//#DescribeEventSubscriptions"
        body: "body"
      };
    }
    // Returns events related to clusters, security groups, snapshots, and parameter groups for the past 14 days. Events specific to a particular cluster, security group, snapshot or parameter group can be obtained by providing the name as a parameter. By default, the past hour of events are returned.
    rpc Post#DescribeEvents(Post#DescribeEventsRequest) returns (EventsMessage) {
      option (google.api.http) = {
        post: "//#DescribeEvents"
        body: "body"
      };
    }
    // <p>Returns information about the specified HSM client certificate. If no certificate ID is specified, returns information about all the HSM certificates owned by your AWS customer account.</p> <p>If you specify both tag keys and tag values in the same request, Amazon Redshift returns all HSM client certificates that match any combination of the specified keys and values. For example, if you have <code>owner</code> and <code>environment</code> for tag keys, and <code>admin</code> and <code>test</code> for tag values, all HSM client certificates that have any combination of those values are returned.</p> <p>If both tag keys and values are omitted from the request, HSM client certificates are returned regardless of whether they have tag keys or values associated with them.</p>
    rpc Post#DescribeHsmClientCertificates(Post#DescribeHsmClientCertificatesRequest) returns (HsmClientCertificateMessage) {
      option (google.api.http) = {
        post: "//#DescribeHsmClientCertificates"
        body: "body"
      };
    }
    // <p>Returns information about the specified Amazon Redshift HSM configuration. If no configuration ID is specified, returns information about all the HSM configurations owned by your AWS customer account.</p> <p>If you specify both tag keys and tag values in the same request, Amazon Redshift returns all HSM connections that match any combination of the specified keys and values. For example, if you have <code>owner</code> and <code>environment</code> for tag keys, and <code>admin</code> and <code>test</code> for tag values, all HSM connections that have any combination of those values are returned.</p> <p>If both tag keys and values are omitted from the request, HSM connections are returned regardless of whether they have tag keys or values associated with them.</p>
    rpc Post#DescribeHsmConfigurations(Post#DescribeHsmConfigurationsRequest) returns (HsmConfigurationMessage) {
      option (google.api.http) = {
        post: "//#DescribeHsmConfigurations"
        body: "body"
      };
    }
    // Describes whether information, such as queries and connection attempts, is being logged for the specified Amazon Redshift cluster.
    rpc Post#DescribeLoggingStatus(Post#DescribeLoggingStatusRequest) returns (LoggingStatus) {
      option (google.api.http) = {
        post: "//#DescribeLoggingStatus"
        body: "body"
      };
    }
    // Returns a list of orderable cluster options. Before you create a new cluster you can use this operation to find what options are available, such as the EC2 Availability Zones (AZ) in the specific AWS region that you can specify, and the node types you can request. The node types differ by available storage, memory, CPU and price. With the cost involved you might want to obtain a list of cluster options in the specific region and specify values when creating a cluster. For more information about managing clusters, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html">Amazon Redshift Clusters</a> in the <i>Amazon Redshift Cluster Management Guide</i>.
    rpc Post#DescribeOrderableClusterOptions(Post#DescribeOrderableClusterOptionsRequest) returns (OrderableClusterOptionsMessage) {
      option (google.api.http) = {
        post: "//#DescribeOrderableClusterOptions"
        body: "body"
      };
    }
    // <p>Returns a list of the available reserved node offerings by Amazon Redshift with their descriptions including the node type, the fixed and recurring costs of reserving the node and duration the node will be reserved for you. These descriptions help you determine which reserve node offering you want to purchase. You then use the unique offering ID in you call to <a>PurchaseReservedNodeOffering</a> to reserve one or more nodes for your Amazon Redshift cluster. </p> <p> For more information about reserved node offerings, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/purchase-reserved-node-instance.html">Purchasing Reserved Nodes</a> in the <i>Amazon Redshift Cluster Management Guide</i>.</p>
    rpc Post#DescribeReservedNodeOfferings(Post#DescribeReservedNodeOfferingsRequest) returns (ReservedNodeOfferingsMessage) {
      option (google.api.http) = {
        post: "//#DescribeReservedNodeOfferings"
        body: "body"
      };
    }
    // Returns the descriptions of the reserved nodes.
    rpc Post#DescribeReservedNodes(Post#DescribeReservedNodesRequest) returns (ReservedNodesMessage) {
      option (google.api.http) = {
        post: "//#DescribeReservedNodes"
        body: "body"
      };
    }
    // <p>Returns information about the last resize operation for the specified cluster. If no resize operation has ever been initiated for the specified cluster, a <code>HTTP 404</code> error is returned. If a resize operation was initiated and completed, the status of the resize remains as <code>SUCCEEDED</code> until the next resize. </p> <p>A resize operation can be requested using <a>ModifyCluster</a> and specifying a different number or type of nodes for the cluster. </p>
    rpc Post#DescribeResize(Post#DescribeResizeRequest) returns (ResizeProgressMessage) {
      option (google.api.http) = {
        post: "//#DescribeResize"
        body: "body"
      };
    }
    // <p>Returns a list of snapshot copy grants owned by the AWS account in the destination region.</p> <p> For more information about managing snapshot copy grants, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-db-encryption.html">Amazon Redshift Database Encryption</a> in the <i>Amazon Redshift Cluster Management Guide</i>. </p>
    rpc Post#DescribeSnapshotCopyGrants(Post#DescribeSnapshotCopyGrantsRequest) returns (SnapshotCopyGrantMessage) {
      option (google.api.http) = {
        post: "//#DescribeSnapshotCopyGrants"
        body: "body"
      };
    }
    // Lists the status of one or more table restore requests made using the <a>RestoreTableFromClusterSnapshot</a> API action. If you don't specify a value for the <code>TableRestoreRequestId</code> parameter, then <code>DescribeTableRestoreStatus</code> returns the status of all table restore requests ordered by the date and time of the request in ascending order. Otherwise <code>DescribeTableRestoreStatus</code> returns the status of the table specified by <code>TableRestoreRequestId</code>.
    rpc Post#DescribeTableRestoreStatus(Post#DescribeTableRestoreStatusRequest) returns (TableRestoreStatusMessage) {
      option (google.api.http) = {
        post: "//#DescribeTableRestoreStatus"
        body: "body"
      };
    }
    // <p>Returns a list of tags. You can return tags from a specific resource by specifying an ARN, or you can return all tags for a given type of resource, such as clusters, snapshots, and so on.</p> <p>The following are limitations for <code>DescribeTags</code>: </p> <ul> <li> <p>You cannot specify an ARN and a resource-type value together in the same request.</p> </li> <li> <p>You cannot use the <code>MaxRecords</code> and <code>Marker</code> parameters together with the ARN parameter.</p> </li> <li> <p>The <code>MaxRecords</code> parameter can be a range from 10 to 50 results to return in a request.</p> </li> </ul> <p>If you specify both tag keys and tag values in the same request, Amazon Redshift returns all resources that match any combination of the specified keys and values. For example, if you have <code>owner</code> and <code>environment</code> for tag keys, and <code>admin</code> and <code>test</code> for tag values, all resources that have any combination of those values are returned.</p> <p>If both tag keys and values are omitted from the request, resources are returned regardless of whether they have tag keys or values associated with them.</p>
    rpc Post#DescribeTags(Post#DescribeTagsRequest) returns (TaggedResourceListMessage) {
      option (google.api.http) = {
        post: "//#DescribeTags"
        body: "body"
      };
    }
    // Stops logging information, such as queries and connection attempts, for the specified Amazon Redshift cluster.
    rpc Post#DisableLogging(Post#DisableLoggingRequest) returns (LoggingStatus) {
      option (google.api.http) = {
        post: "//#DisableLogging"
        body: "body"
      };
    }
    // <p>Disables the automatic copying of snapshots from one region to another region for a specified cluster.</p> <p>If your cluster and its snapshots are encrypted using a customer master key (CMK) from AWS KMS, use <a>DeleteSnapshotCopyGrant</a> to delete the grant that grants Amazon Redshift permission to the CMK in the destination region. </p>
    rpc Post#DisableSnapshotCopy(Post#DisableSnapshotCopyRequest) returns (DisableSnapshotCopyResult) {
      option (google.api.http) = {
        post: "//#DisableSnapshotCopy"
        body: "body"
      };
    }
    // Starts logging information, such as queries and connection attempts, for the specified Amazon Redshift cluster.
    rpc Post#EnableLogging(Post#EnableLoggingRequest) returns (LoggingStatus) {
      option (google.api.http) = {
        post: "//#EnableLogging"
        body: "body"
      };
    }
    // Enables the automatic copy of snapshots from one region to another region for a specified cluster.
    rpc Post#EnableSnapshotCopy(Post#EnableSnapshotCopyRequest) returns (EnableSnapshotCopyResult) {
      option (google.api.http) = {
        post: "//#EnableSnapshotCopy"
        body: "body"
      };
    }
    // <p>Returns a database user name and temporary password with temporary authorization to log in to an Amazon Redshift database. The action returns the database user name prefixed with <code>IAM:</code> if <code>AutoCreate</code> is <code>False</code> or <code>IAMA:</code> if <code>AutoCreate</code> is <code>True</code>. You can optionally specify one or more database user groups that the user will join at log in. By default, the temporary credentials expire in 900 seconds. You can optionally specify a duration between 900 seconds (15 minutes) and 3600 seconds (60 minutes). For more information, see Generating IAM Database User Credentials in the Amazon Redshift Cluster Management Guide.</p> <p>The IAM user or role that executes GetClusterCredentials must have an IAM policy attached that allows the <code>redshift:GetClusterCredentials</code> action with access to the <code>dbuser</code> resource on the cluster. The user name specified for <code>dbuser</code> in the IAM policy and the user name specified for the <code>DbUser</code> parameter must match.</p> <p>If the <code>DbGroups</code> parameter is specified, the IAM policy must allow the <code>redshift:JoinGroup</code> action with access to the listed <code>dbgroups</code>. </p> <p>In addition, if the <code>AutoCreate</code> parameter is set to <code>True</code>, then the policy must include the <code>redshift:CreateClusterUser</code> privilege.</p> <p>If the <code>DbName</code> parameter is specified, the IAM policy must allow access to the resource <code>dbname</code> for the specified database name. </p>
    rpc Post#GetClusterCredentials(Post#GetClusterCredentialsRequest) returns (ClusterCredentials) {
      option (google.api.http) = {
        post: "//#GetClusterCredentials"
        body: "body"
      };
    }
    // <p>Modifies the settings for a cluster. For example, you can add another security or parameter group, update the preferred maintenance window, or change the master user password. Resetting a cluster password or modifying the security groups associated with a cluster do not need a reboot. However, modifying a parameter group requires a reboot for parameters to take effect. For more information about managing clusters, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html">Amazon Redshift Clusters</a> in the <i>Amazon Redshift Cluster Management Guide</i>.</p> <p>You can also change node type and the number of nodes to scale up or down the cluster. When resizing a cluster, you must specify both the number of nodes and the node type even if one of the parameters does not change.</p>
    rpc Post#ModifyCluster(Post#ModifyClusterRequest) returns (ModifyClusterResult) {
      option (google.api.http) = {
        post: "//#ModifyCluster"
        body: "body"
      };
    }
    // <p>Modifies the list of AWS Identity and Access Management (IAM) roles that can be used by the cluster to access other AWS services.</p> <p>A cluster can have up to 10 IAM roles associated at any time.</p>
    rpc Post#ModifyClusterIamRoles(Post#ModifyClusterIamRolesRequest) returns (ModifyClusterIamRolesResult) {
      option (google.api.http) = {
        post: "//#ModifyClusterIamRoles"
        body: "body"
      };
    }
    // <p>Modifies the parameters of a parameter group.</p> <p> For more information about parameters and parameter groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-parameter-groups.html">Amazon Redshift Parameter Groups</a> in the <i>Amazon Redshift Cluster Management Guide</i>.</p>
    rpc Post#ModifyClusterParameterGroup(Post#ModifyClusterParameterGroupRequest) returns (ClusterParameterGroupNameMessage) {
      option (google.api.http) = {
        post: "//#ModifyClusterParameterGroup"
        body: "body"
      };
    }
    // Modifies a cluster subnet group to include the specified list of VPC subnets. The operation replaces the existing list of subnets with the new list of subnets.
    rpc Post#ModifyClusterSubnetGroup(Post#ModifyClusterSubnetGroupRequest) returns (ModifyClusterSubnetGroupResult) {
      option (google.api.http) = {
        post: "//#ModifyClusterSubnetGroup"
        body: "body"
      };
    }
    // Modifies an existing Amazon Redshift event notification subscription.
    rpc Post#ModifyEventSubscription(Post#ModifyEventSubscriptionRequest) returns (ModifyEventSubscriptionResult) {
      option (google.api.http) = {
        post: "//#ModifyEventSubscription"
        body: "body"
      };
    }
    // Modifies the number of days to retain automated snapshots in the destination region after they are copied from the source region.
    rpc Post#ModifySnapshotCopyRetentionPeriod(Post#ModifySnapshotCopyRetentionPeriodRequest) returns (ModifySnapshotCopyRetentionPeriodResult) {
      option (google.api.http) = {
        post: "//#ModifySnapshotCopyRetentionPeriod"
        body: "body"
      };
    }
    // <p>Allows you to purchase reserved nodes. Amazon Redshift offers a predefined set of reserved node offerings. You can purchase one or more of the offerings. You can call the <a>DescribeReservedNodeOfferings</a> API to obtain the available reserved node offerings. You can call this API by providing a specific reserved node offering and the number of nodes you want to reserve. </p> <p> For more information about reserved node offerings, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/purchase-reserved-node-instance.html">Purchasing Reserved Nodes</a> in the <i>Amazon Redshift Cluster Management Guide</i>.</p>
    rpc Post#PurchaseReservedNodeOffering(Post#PurchaseReservedNodeOfferingRequest) returns (PurchaseReservedNodeOfferingResult) {
      option (google.api.http) = {
        post: "//#PurchaseReservedNodeOffering"
        body: "body"
      };
    }
    // Reboots a cluster. This action is taken as soon as possible. It results in a momentary outage to the cluster, during which the cluster status is set to <code>rebooting</code>. A cluster event is created when the reboot is completed. Any pending cluster modifications (see <a>ModifyCluster</a>) are applied at this reboot. For more information about managing clusters, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html">Amazon Redshift Clusters</a> in the <i>Amazon Redshift Cluster Management Guide</i>.
    rpc Post#RebootCluster(Post#RebootClusterRequest) returns (RebootClusterResult) {
      option (google.api.http) = {
        post: "//#RebootCluster"
        body: "body"
      };
    }
    // Sets one or more parameters of the specified parameter group to their default values and sets the source values of the parameters to "engine-default". To reset the entire parameter group specify the <i>ResetAllParameters</i> parameter. For parameter changes to take effect you must reboot any associated clusters.
    rpc Post#ResetClusterParameterGroup(Post#ResetClusterParameterGroupRequest) returns (ClusterParameterGroupNameMessage) {
      option (google.api.http) = {
        post: "//#ResetClusterParameterGroup"
        body: "body"
      };
    }
    // <p>Creates a new cluster from a snapshot. By default, Amazon Redshift creates the resulting cluster with the same configuration as the original cluster from which the snapshot was created, except that the new cluster is created with the default cluster security and parameter groups. After Amazon Redshift creates the cluster, you can use the <a>ModifyCluster</a> API to associate a different security group and different parameter group with the restored cluster. If you are using a DS node type, you can also choose to change to another DS node type of the same size during restore.</p> <p>If you restore a cluster into a VPC, you must provide a cluster subnet group where you want the cluster restored.</p> <p> For more information about working with snapshots, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-snapshots.html">Amazon Redshift Snapshots</a> in the <i>Amazon Redshift Cluster Management Guide</i>.</p>
    rpc Post#RestoreFromClusterSnapshot(Post#RestoreFromClusterSnapshotRequest) returns (RestoreFromClusterSnapshotResult) {
      option (google.api.http) = {
        post: "//#RestoreFromClusterSnapshot"
        body: "body"
      };
    }
    // <p>Creates a new table from a table in an Amazon Redshift cluster snapshot. You must create the new table within the Amazon Redshift cluster that the snapshot was taken from.</p> <p>You cannot use <code>RestoreTableFromClusterSnapshot</code> to restore a table with the same name as an existing table in an Amazon Redshift cluster. That is, you cannot overwrite an existing table in a cluster with a restored table. If you want to replace your original table with a new, restored table, then rename or drop your original table before you call <code>RestoreTableFromClusterSnapshot</code>. When you have renamed your original table, then you can pass the original name of the table as the <code>NewTableName</code> parameter value in the call to <code>RestoreTableFromClusterSnapshot</code>. This way, you can replace the original table with the table created from the snapshot.</p>
    rpc Post#RestoreTableFromClusterSnapshot(Post#RestoreTableFromClusterSnapshotRequest) returns (RestoreTableFromClusterSnapshotResult) {
      option (google.api.http) = {
        post: "//#RestoreTableFromClusterSnapshot"
        body: "body"
      };
    }
    // Revokes an ingress rule in an Amazon Redshift security group for a previously authorized IP range or Amazon EC2 security group. To add an ingress rule, see <a>AuthorizeClusterSecurityGroupIngress</a>. For information about managing security groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-security-groups.html">Amazon Redshift Cluster Security Groups</a> in the <i>Amazon Redshift Cluster Management Guide</i>.
    rpc Post#RevokeClusterSecurityGroupIngress(Post#RevokeClusterSecurityGroupIngressRequest) returns (RevokeClusterSecurityGroupIngressResult) {
      option (google.api.http) = {
        post: "//#RevokeClusterSecurityGroupIngress"
        body: "body"
      };
    }
    // <p>Removes the ability of the specified AWS customer account to restore the specified snapshot. If the account is currently restoring the snapshot, the restore will run to completion.</p> <p> For more information about working with snapshots, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-snapshots.html">Amazon Redshift Snapshots</a> in the <i>Amazon Redshift Cluster Management Guide</i>.</p>
    rpc Post#RevokeSnapshotAccess(Post#RevokeSnapshotAccessRequest) returns (RevokeSnapshotAccessResult) {
      option (google.api.http) = {
        post: "//#RevokeSnapshotAccess"
        body: "body"
      };
    }
    // Rotates the encryption keys for a cluster.
    rpc Post#RotateEncryptionKey(Post#RotateEncryptionKeyRequest) returns (RotateEncryptionKeyResult) {
      option (google.api.http) = {
        post: "//#RotateEncryptionKey"
        body: "body"
      };
    }
}
