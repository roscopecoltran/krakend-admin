backends:
- group: ritekit-1-0-0
  url_pattern: /v1/emoji/auto-emojify
  method: GET
  host:
  - https://api.ritekit.com
  query_string_params:
  - text
  concurrent_calls: 1
  extra_config:
    provider: ritekit.com
    description: |-
      RiteKit API is based on REST principles.

      Authentication uses standard OAuth 2.0 process

      ##Getting started

      1. Sign up for [RiteKit](https://ritekit.com/)

      1. Go to [developer dashboard](https://ritekit.com/developer/dashboard/)

      1. Click "Create a token" button to get your **Client ID** and **Client secret**

      1. Study the documentation below (for more info on oAuth go to http://oauth.net/)

      1. When you reach your free limit of calls per month, [upgrade to paid tiers](https://ritekit.com/developer/)

      ## Options for authorizing API Calls

      #### A) Using Client ID directly

      You can directly connect to our API using your **client ID** by sending it as a GET query parameter. This option is simple (no need for oAuth) but it should be used only in case the Client ID is not exposed publicly.

      GET  https://api.ritekit.com/v1/stats/basic/php?client_id=292c6912e7710c838347ae178b4a

      #### B) Using Access token (oAuth 2.0)

      Once you have valid Access token, you can access our API by sending the token in the Authorization header bearer or as a query Access token. See the oAuth 2.0 section to learn how to get Access token and keep refreshing it.

      Authorization: Bearer xxxx

      Or

      GET  https://api.ritekit.com/v1/stats/basic/php?access_token=292c6912e7710c838347ae178b4a

      ## oAuth 2.0

      ![oauthschema](https://cdn.ritekit.com/assets/media/1/oauth-simple.png)

      ### Get Access Token

      Use your Client Credentials to get the token.

      POST   https://ritekit.com/oauth/token

                 grant_type=client_credentials
                 &client_id=CLIENT_ID
                 &client_secret=CLIENT_SECRET
                 &scope=data

      - [required] grant_type - client_credentials

      - [required] client_id (CLIENT ID)

      - [required] client_secret (CLIENT SECRET)

      - [required] (always use scope=data)

      #### Response

      {
              "access_token":292c6912e7710c838347ae178b4a,
              "token_type":"bearer",
              "expires_in": 3600,
              "refresh_token":292c6912e7710c838347ae178b4a,
          }

      ### Refresh token

      Every Access token has a lifetime. You can use Refresh token to request a new Access Token.

      POST   https://ritekit.com/oauth/token

                 grant_type=refresh_token
                 &refresh_token=REFRESH_TOKEN
                 &client_id=CLIENT_ID
                 &scope=data

      - [required] grant_type - refresh_token

      - [required] refresh_token - (REFRESH TOKEN) the refresh token you got from the expired token

      - [required] client_id (CLIENT ID)

      - [required] client_secret (CLIENT SECRET)

      - [required] scope (always use scope=data)

      #### Response

      {
              "access_token":292c6912e7710c838347ae178b4a,
              "token_type":"bearer",
              "expires_in": 3600,
              "refresh_token":292c6912e7710c838347ae178b4a,
          }
    version: 1.0.0
    slug: ritekit-1-0-0-v1-emoji-auto-emojify
    schemes:
    - https
    hosts:
    - https://api.ritekit.com
    topics:
    - social
- group: ritekit-1-0-0
  url_pattern: /v1/emoji/suggestions
  method: GET
  host:
  - https://api.ritekit.com
  query_string_params:
  - text
  concurrent_calls: 1
  extra_config:
    provider: ritekit.com
    description: |-
      RiteKit API is based on REST principles.

      Authentication uses standard OAuth 2.0 process

      ##Getting started

      1. Sign up for [RiteKit](https://ritekit.com/)

      1. Go to [developer dashboard](https://ritekit.com/developer/dashboard/)

      1. Click "Create a token" button to get your **Client ID** and **Client secret**

      1. Study the documentation below (for more info on oAuth go to http://oauth.net/)

      1. When you reach your free limit of calls per month, [upgrade to paid tiers](https://ritekit.com/developer/)

      ## Options for authorizing API Calls

      #### A) Using Client ID directly

      You can directly connect to our API using your **client ID** by sending it as a GET query parameter. This option is simple (no need for oAuth) but it should be used only in case the Client ID is not exposed publicly.

      GET  https://api.ritekit.com/v1/stats/basic/php?client_id=292c6912e7710c838347ae178b4a

      #### B) Using Access token (oAuth 2.0)

      Once you have valid Access token, you can access our API by sending the token in the Authorization header bearer or as a query Access token. See the oAuth 2.0 section to learn how to get Access token and keep refreshing it.

      Authorization: Bearer xxxx

      Or

      GET  https://api.ritekit.com/v1/stats/basic/php?access_token=292c6912e7710c838347ae178b4a

      ## oAuth 2.0

      ![oauthschema](https://cdn.ritekit.com/assets/media/1/oauth-simple.png)

      ### Get Access Token

      Use your Client Credentials to get the token.

      POST   https://ritekit.com/oauth/token

                 grant_type=client_credentials
                 &client_id=CLIENT_ID
                 &client_secret=CLIENT_SECRET
                 &scope=data

      - [required] grant_type - client_credentials

      - [required] client_id (CLIENT ID)

      - [required] client_secret (CLIENT SECRET)

      - [required] (always use scope=data)

      #### Response

      {
              "access_token":292c6912e7710c838347ae178b4a,
              "token_type":"bearer",
              "expires_in": 3600,
              "refresh_token":292c6912e7710c838347ae178b4a,
          }

      ### Refresh token

      Every Access token has a lifetime. You can use Refresh token to request a new Access Token.

      POST   https://ritekit.com/oauth/token

                 grant_type=refresh_token
                 &refresh_token=REFRESH_TOKEN
                 &client_id=CLIENT_ID
                 &scope=data

      - [required] grant_type - refresh_token

      - [required] refresh_token - (REFRESH TOKEN) the refresh token you got from the expired token

      - [required] client_id (CLIENT ID)

      - [required] client_secret (CLIENT SECRET)

      - [required] scope (always use scope=data)

      #### Response

      {
              "access_token":292c6912e7710c838347ae178b4a,
              "token_type":"bearer",
              "expires_in": 3600,
              "refresh_token":292c6912e7710c838347ae178b4a,
          }
    version: 1.0.0
    slug: ritekit-1-0-0-v1-emoji-suggestions
    schemes:
    - https
    hosts:
    - https://api.ritekit.com
    topics:
    - social
- group: ritekit-1-0-0
  url_pattern: /v1/images/logo
  method: GET
  host:
  - https://api.ritekit.com
  query_string_params:
  - domain
  concurrent_calls: 1
  extra_config:
    provider: ritekit.com
    description: |-
      RiteKit API is based on REST principles.

      Authentication uses standard OAuth 2.0 process

      ##Getting started

      1. Sign up for [RiteKit](https://ritekit.com/)

      1. Go to [developer dashboard](https://ritekit.com/developer/dashboard/)

      1. Click "Create a token" button to get your **Client ID** and **Client secret**

      1. Study the documentation below (for more info on oAuth go to http://oauth.net/)

      1. When you reach your free limit of calls per month, [upgrade to paid tiers](https://ritekit.com/developer/)

      ## Options for authorizing API Calls

      #### A) Using Client ID directly

      You can directly connect to our API using your **client ID** by sending it as a GET query parameter. This option is simple (no need for oAuth) but it should be used only in case the Client ID is not exposed publicly.

      GET  https://api.ritekit.com/v1/stats/basic/php?client_id=292c6912e7710c838347ae178b4a

      #### B) Using Access token (oAuth 2.0)

      Once you have valid Access token, you can access our API by sending the token in the Authorization header bearer or as a query Access token. See the oAuth 2.0 section to learn how to get Access token and keep refreshing it.

      Authorization: Bearer xxxx

      Or

      GET  https://api.ritekit.com/v1/stats/basic/php?access_token=292c6912e7710c838347ae178b4a

      ## oAuth 2.0

      ![oauthschema](https://cdn.ritekit.com/assets/media/1/oauth-simple.png)

      ### Get Access Token

      Use your Client Credentials to get the token.

      POST   https://ritekit.com/oauth/token

                 grant_type=client_credentials
                 &client_id=CLIENT_ID
                 &client_secret=CLIENT_SECRET
                 &scope=data

      - [required] grant_type - client_credentials

      - [required] client_id (CLIENT ID)

      - [required] client_secret (CLIENT SECRET)

      - [required] (always use scope=data)

      #### Response

      {
              "access_token":292c6912e7710c838347ae178b4a,
              "token_type":"bearer",
              "expires_in": 3600,
              "refresh_token":292c6912e7710c838347ae178b4a,
          }

      ### Refresh token

      Every Access token has a lifetime. You can use Refresh token to request a new Access Token.

      POST   https://ritekit.com/oauth/token

                 grant_type=refresh_token
                 &refresh_token=REFRESH_TOKEN
                 &client_id=CLIENT_ID
                 &scope=data

      - [required] grant_type - refresh_token

      - [required] refresh_token - (REFRESH TOKEN) the refresh token you got from the expired token

      - [required] client_id (CLIENT ID)

      - [required] client_secret (CLIENT SECRET)

      - [required] scope (always use scope=data)

      #### Response

      {
              "access_token":292c6912e7710c838347ae178b4a,
              "token_type":"bearer",
              "expires_in": 3600,
              "refresh_token":292c6912e7710c838347ae178b4a,
          }
    version: 1.0.0
    slug: ritekit-1-0-0-v1-images-logo
    schemes:
    - https
    hosts:
    - https://api.ritekit.com
    topics:
    - social
- group: ritekit-1-0-0
  url_pattern: /v1/images/quote
  method: GET
  host:
  - https://api.ritekit.com
  query_string_params:
  - author
  - authorFont
  - authorFontColor
  - backgroundColor
  - bgType
  - brandLogo
  - enableHighlight
  - fontSize
  - gradientColor1
  - gradientColor2
  - gradientType
  - highlightColor
  - quote
  - quoteFont
  - quoteFontColor
  concurrent_calls: 1
  extra_config:
    provider: ritekit.com
    description: |-
      RiteKit API is based on REST principles.

      Authentication uses standard OAuth 2.0 process

      ##Getting started

      1. Sign up for [RiteKit](https://ritekit.com/)

      1. Go to [developer dashboard](https://ritekit.com/developer/dashboard/)

      1. Click "Create a token" button to get your **Client ID** and **Client secret**

      1. Study the documentation below (for more info on oAuth go to http://oauth.net/)

      1. When you reach your free limit of calls per month, [upgrade to paid tiers](https://ritekit.com/developer/)

      ## Options for authorizing API Calls

      #### A) Using Client ID directly

      You can directly connect to our API using your **client ID** by sending it as a GET query parameter. This option is simple (no need for oAuth) but it should be used only in case the Client ID is not exposed publicly.

      GET  https://api.ritekit.com/v1/stats/basic/php?client_id=292c6912e7710c838347ae178b4a

      #### B) Using Access token (oAuth 2.0)

      Once you have valid Access token, you can access our API by sending the token in the Authorization header bearer or as a query Access token. See the oAuth 2.0 section to learn how to get Access token and keep refreshing it.

      Authorization: Bearer xxxx

      Or

      GET  https://api.ritekit.com/v1/stats/basic/php?access_token=292c6912e7710c838347ae178b4a

      ## oAuth 2.0

      ![oauthschema](https://cdn.ritekit.com/assets/media/1/oauth-simple.png)

      ### Get Access Token

      Use your Client Credentials to get the token.

      POST   https://ritekit.com/oauth/token

                 grant_type=client_credentials
                 &client_id=CLIENT_ID
                 &client_secret=CLIENT_SECRET
                 &scope=data

      - [required] grant_type - client_credentials

      - [required] client_id (CLIENT ID)

      - [required] client_secret (CLIENT SECRET)

      - [required] (always use scope=data)

      #### Response

      {
              "access_token":292c6912e7710c838347ae178b4a,
              "token_type":"bearer",
              "expires_in": 3600,
              "refresh_token":292c6912e7710c838347ae178b4a,
          }

      ### Refresh token

      Every Access token has a lifetime. You can use Refresh token to request a new Access Token.

      POST   https://ritekit.com/oauth/token

                 grant_type=refresh_token
                 &refresh_token=REFRESH_TOKEN
                 &client_id=CLIENT_ID
                 &scope=data

      - [required] grant_type - refresh_token

      - [required] refresh_token - (REFRESH TOKEN) the refresh token you got from the expired token

      - [required] client_id (CLIENT ID)

      - [required] client_secret (CLIENT SECRET)

      - [required] scope (always use scope=data)

      #### Response

      {
              "access_token":292c6912e7710c838347ae178b4a,
              "token_type":"bearer",
              "expires_in": 3600,
              "refresh_token":292c6912e7710c838347ae178b4a,
          }
    version: 1.0.0
    slug: ritekit-1-0-0-v1-images-quote
    schemes:
    - https
    hosts:
    - https://api.ritekit.com
    topics:
    - social
- group: ritekit-1-0-0
  url_pattern: /v1/influencers/hashtag/{hashtag}
  method: GET
  host:
  - https://api.ritekit.com
  url_keys:
  - hashtag
  concurrent_calls: 1
  extra_config:
    provider: ritekit.com
    description: |-
      RiteKit API is based on REST principles.

      Authentication uses standard OAuth 2.0 process

      ##Getting started

      1. Sign up for [RiteKit](https://ritekit.com/)

      1. Go to [developer dashboard](https://ritekit.com/developer/dashboard/)

      1. Click "Create a token" button to get your **Client ID** and **Client secret**

      1. Study the documentation below (for more info on oAuth go to http://oauth.net/)

      1. When you reach your free limit of calls per month, [upgrade to paid tiers](https://ritekit.com/developer/)

      ## Options for authorizing API Calls

      #### A) Using Client ID directly

      You can directly connect to our API using your **client ID** by sending it as a GET query parameter. This option is simple (no need for oAuth) but it should be used only in case the Client ID is not exposed publicly.

      GET  https://api.ritekit.com/v1/stats/basic/php?client_id=292c6912e7710c838347ae178b4a

      #### B) Using Access token (oAuth 2.0)

      Once you have valid Access token, you can access our API by sending the token in the Authorization header bearer or as a query Access token. See the oAuth 2.0 section to learn how to get Access token and keep refreshing it.

      Authorization: Bearer xxxx

      Or

      GET  https://api.ritekit.com/v1/stats/basic/php?access_token=292c6912e7710c838347ae178b4a

      ## oAuth 2.0

      ![oauthschema](https://cdn.ritekit.com/assets/media/1/oauth-simple.png)

      ### Get Access Token

      Use your Client Credentials to get the token.

      POST   https://ritekit.com/oauth/token

                 grant_type=client_credentials
                 &client_id=CLIENT_ID
                 &client_secret=CLIENT_SECRET
                 &scope=data

      - [required] grant_type - client_credentials

      - [required] client_id (CLIENT ID)

      - [required] client_secret (CLIENT SECRET)

      - [required] (always use scope=data)

      #### Response

      {
              "access_token":292c6912e7710c838347ae178b4a,
              "token_type":"bearer",
              "expires_in": 3600,
              "refresh_token":292c6912e7710c838347ae178b4a,
          }

      ### Refresh token

      Every Access token has a lifetime. You can use Refresh token to request a new Access Token.

      POST   https://ritekit.com/oauth/token

                 grant_type=refresh_token
                 &refresh_token=REFRESH_TOKEN
                 &client_id=CLIENT_ID
                 &scope=data

      - [required] grant_type - refresh_token

      - [required] refresh_token - (REFRESH TOKEN) the refresh token you got from the expired token

      - [required] client_id (CLIENT ID)

      - [required] client_secret (CLIENT SECRET)

      - [required] scope (always use scope=data)

      #### Response

      {
              "access_token":292c6912e7710c838347ae178b4a,
              "token_type":"bearer",
              "expires_in": 3600,
              "refresh_token":292c6912e7710c838347ae178b4a,
          }
    version: 1.0.0
    slug: ritekit-1-0-0-v1-influencers-hashtag-hashtag
    schemes:
    - https
    hosts:
    - https://api.ritekit.com
    topics:
    - social
- group: ritekit-1-0-0
  url_pattern: /v1/link/cta
  method: GET
  host:
  - https://api.ritekit.com
  concurrent_calls: 1
  extra_config:
    provider: ritekit.com
    description: |-
      RiteKit API is based on REST principles.

      Authentication uses standard OAuth 2.0 process

      ##Getting started

      1. Sign up for [RiteKit](https://ritekit.com/)

      1. Go to [developer dashboard](https://ritekit.com/developer/dashboard/)

      1. Click "Create a token" button to get your **Client ID** and **Client secret**

      1. Study the documentation below (for more info on oAuth go to http://oauth.net/)

      1. When you reach your free limit of calls per month, [upgrade to paid tiers](https://ritekit.com/developer/)

      ## Options for authorizing API Calls

      #### A) Using Client ID directly

      You can directly connect to our API using your **client ID** by sending it as a GET query parameter. This option is simple (no need for oAuth) but it should be used only in case the Client ID is not exposed publicly.

      GET  https://api.ritekit.com/v1/stats/basic/php?client_id=292c6912e7710c838347ae178b4a

      #### B) Using Access token (oAuth 2.0)

      Once you have valid Access token, you can access our API by sending the token in the Authorization header bearer or as a query Access token. See the oAuth 2.0 section to learn how to get Access token and keep refreshing it.

      Authorization: Bearer xxxx

      Or

      GET  https://api.ritekit.com/v1/stats/basic/php?access_token=292c6912e7710c838347ae178b4a

      ## oAuth 2.0

      ![oauthschema](https://cdn.ritekit.com/assets/media/1/oauth-simple.png)

      ### Get Access Token

      Use your Client Credentials to get the token.

      POST   https://ritekit.com/oauth/token

                 grant_type=client_credentials
                 &client_id=CLIENT_ID
                 &client_secret=CLIENT_SECRET
                 &scope=data

      - [required] grant_type - client_credentials

      - [required] client_id (CLIENT ID)

      - [required] client_secret (CLIENT SECRET)

      - [required] (always use scope=data)

      #### Response

      {
              "access_token":292c6912e7710c838347ae178b4a,
              "token_type":"bearer",
              "expires_in": 3600,
              "refresh_token":292c6912e7710c838347ae178b4a,
          }

      ### Refresh token

      Every Access token has a lifetime. You can use Refresh token to request a new Access Token.

      POST   https://ritekit.com/oauth/token

                 grant_type=refresh_token
                 &refresh_token=REFRESH_TOKEN
                 &client_id=CLIENT_ID
                 &scope=data

      - [required] grant_type - refresh_token

      - [required] refresh_token - (REFRESH TOKEN) the refresh token you got from the expired token

      - [required] client_id (CLIENT ID)

      - [required] client_secret (CLIENT SECRET)

      - [required] scope (always use scope=data)

      #### Response

      {
              "access_token":292c6912e7710c838347ae178b4a,
              "token_type":"bearer",
              "expires_in": 3600,
              "refresh_token":292c6912e7710c838347ae178b4a,
          }
    version: 1.0.0
    slug: ritekit-1-0-0-v1-link-cta
    schemes:
    - https
    hosts:
    - https://api.ritekit.com
    topics:
    - social
- group: ritekit-1-0-0
  url_pattern: /v1/link/short-link
  method: GET
  host:
  - https://api.ritekit.com
  query_string_params:
  - cta
  - url
  concurrent_calls: 1
  extra_config:
    provider: ritekit.com
    description: |-
      RiteKit API is based on REST principles.

      Authentication uses standard OAuth 2.0 process

      ##Getting started

      1. Sign up for [RiteKit](https://ritekit.com/)

      1. Go to [developer dashboard](https://ritekit.com/developer/dashboard/)

      1. Click "Create a token" button to get your **Client ID** and **Client secret**

      1. Study the documentation below (for more info on oAuth go to http://oauth.net/)

      1. When you reach your free limit of calls per month, [upgrade to paid tiers](https://ritekit.com/developer/)

      ## Options for authorizing API Calls

      #### A) Using Client ID directly

      You can directly connect to our API using your **client ID** by sending it as a GET query parameter. This option is simple (no need for oAuth) but it should be used only in case the Client ID is not exposed publicly.

      GET  https://api.ritekit.com/v1/stats/basic/php?client_id=292c6912e7710c838347ae178b4a

      #### B) Using Access token (oAuth 2.0)

      Once you have valid Access token, you can access our API by sending the token in the Authorization header bearer or as a query Access token. See the oAuth 2.0 section to learn how to get Access token and keep refreshing it.

      Authorization: Bearer xxxx

      Or

      GET  https://api.ritekit.com/v1/stats/basic/php?access_token=292c6912e7710c838347ae178b4a

      ## oAuth 2.0

      ![oauthschema](https://cdn.ritekit.com/assets/media/1/oauth-simple.png)

      ### Get Access Token

      Use your Client Credentials to get the token.

      POST   https://ritekit.com/oauth/token

                 grant_type=client_credentials
                 &client_id=CLIENT_ID
                 &client_secret=CLIENT_SECRET
                 &scope=data

      - [required] grant_type - client_credentials

      - [required] client_id (CLIENT ID)

      - [required] client_secret (CLIENT SECRET)

      - [required] (always use scope=data)

      #### Response

      {
              "access_token":292c6912e7710c838347ae178b4a,
              "token_type":"bearer",
              "expires_in": 3600,
              "refresh_token":292c6912e7710c838347ae178b4a,
          }

      ### Refresh token

      Every Access token has a lifetime. You can use Refresh token to request a new Access Token.

      POST   https://ritekit.com/oauth/token

                 grant_type=refresh_token
                 &refresh_token=REFRESH_TOKEN
                 &client_id=CLIENT_ID
                 &scope=data

      - [required] grant_type - refresh_token

      - [required] refresh_token - (REFRESH TOKEN) the refresh token you got from the expired token

      - [required] client_id (CLIENT ID)

      - [required] client_secret (CLIENT SECRET)

      - [required] scope (always use scope=data)

      #### Response

      {
              "access_token":292c6912e7710c838347ae178b4a,
              "token_type":"bearer",
              "expires_in": 3600,
              "refresh_token":292c6912e7710c838347ae178b4a,
          }
    version: 1.0.0
    slug: ritekit-1-0-0-v1-link-short-link
    schemes:
    - https
    hosts:
    - https://api.ritekit.com
    topics:
    - social
- group: ritekit-1-0-0
  url_pattern: /v1/search/trending
  method: GET
  host:
  - https://api.ritekit.com
  query_string_params:
  - green
  - latin
  concurrent_calls: 1
  extra_config:
    provider: ritekit.com
    description: |-
      RiteKit API is based on REST principles.

      Authentication uses standard OAuth 2.0 process

      ##Getting started

      1. Sign up for [RiteKit](https://ritekit.com/)

      1. Go to [developer dashboard](https://ritekit.com/developer/dashboard/)

      1. Click "Create a token" button to get your **Client ID** and **Client secret**

      1. Study the documentation below (for more info on oAuth go to http://oauth.net/)

      1. When you reach your free limit of calls per month, [upgrade to paid tiers](https://ritekit.com/developer/)

      ## Options for authorizing API Calls

      #### A) Using Client ID directly

      You can directly connect to our API using your **client ID** by sending it as a GET query parameter. This option is simple (no need for oAuth) but it should be used only in case the Client ID is not exposed publicly.

      GET  https://api.ritekit.com/v1/stats/basic/php?client_id=292c6912e7710c838347ae178b4a

      #### B) Using Access token (oAuth 2.0)

      Once you have valid Access token, you can access our API by sending the token in the Authorization header bearer or as a query Access token. See the oAuth 2.0 section to learn how to get Access token and keep refreshing it.

      Authorization: Bearer xxxx

      Or

      GET  https://api.ritekit.com/v1/stats/basic/php?access_token=292c6912e7710c838347ae178b4a

      ## oAuth 2.0

      ![oauthschema](https://cdn.ritekit.com/assets/media/1/oauth-simple.png)

      ### Get Access Token

      Use your Client Credentials to get the token.

      POST   https://ritekit.com/oauth/token

                 grant_type=client_credentials
                 &client_id=CLIENT_ID
                 &client_secret=CLIENT_SECRET
                 &scope=data

      - [required] grant_type - client_credentials

      - [required] client_id (CLIENT ID)

      - [required] client_secret (CLIENT SECRET)

      - [required] (always use scope=data)

      #### Response

      {
              "access_token":292c6912e7710c838347ae178b4a,
              "token_type":"bearer",
              "expires_in": 3600,
              "refresh_token":292c6912e7710c838347ae178b4a,
          }

      ### Refresh token

      Every Access token has a lifetime. You can use Refresh token to request a new Access Token.

      POST   https://ritekit.com/oauth/token

                 grant_type=refresh_token
                 &refresh_token=REFRESH_TOKEN
                 &client_id=CLIENT_ID
                 &scope=data

      - [required] grant_type - refresh_token

      - [required] refresh_token - (REFRESH TOKEN) the refresh token you got from the expired token

      - [required] client_id (CLIENT ID)

      - [required] client_secret (CLIENT SECRET)

      - [required] scope (always use scope=data)

      #### Response

      {
              "access_token":292c6912e7710c838347ae178b4a,
              "token_type":"bearer",
              "expires_in": 3600,
              "refresh_token":292c6912e7710c838347ae178b4a,
          }
    version: 1.0.0
    slug: ritekit-1-0-0-v1-search-trending
    schemes:
    - https
    hosts:
    - https://api.ritekit.com
    topics:
    - social
- group: ritekit-1-0-0
  url_pattern: /v1/stats/auto-hashtag?post={post}
  method: GET
  host:
  - https://api.ritekit.com
  url_keys:
  - post
  concurrent_calls: 1
  extra_config:
    provider: ritekit.com
    description: |-
      RiteKit API is based on REST principles.

      Authentication uses standard OAuth 2.0 process

      ##Getting started

      1. Sign up for [RiteKit](https://ritekit.com/)

      1. Go to [developer dashboard](https://ritekit.com/developer/dashboard/)

      1. Click "Create a token" button to get your **Client ID** and **Client secret**

      1. Study the documentation below (for more info on oAuth go to http://oauth.net/)

      1. When you reach your free limit of calls per month, [upgrade to paid tiers](https://ritekit.com/developer/)

      ## Options for authorizing API Calls

      #### A) Using Client ID directly

      You can directly connect to our API using your **client ID** by sending it as a GET query parameter. This option is simple (no need for oAuth) but it should be used only in case the Client ID is not exposed publicly.

      GET  https://api.ritekit.com/v1/stats/basic/php?client_id=292c6912e7710c838347ae178b4a

      #### B) Using Access token (oAuth 2.0)

      Once you have valid Access token, you can access our API by sending the token in the Authorization header bearer or as a query Access token. See the oAuth 2.0 section to learn how to get Access token and keep refreshing it.

      Authorization: Bearer xxxx

      Or

      GET  https://api.ritekit.com/v1/stats/basic/php?access_token=292c6912e7710c838347ae178b4a

      ## oAuth 2.0

      ![oauthschema](https://cdn.ritekit.com/assets/media/1/oauth-simple.png)

      ### Get Access Token

      Use your Client Credentials to get the token.

      POST   https://ritekit.com/oauth/token

                 grant_type=client_credentials
                 &client_id=CLIENT_ID
                 &client_secret=CLIENT_SECRET
                 &scope=data

      - [required] grant_type - client_credentials

      - [required] client_id (CLIENT ID)

      - [required] client_secret (CLIENT SECRET)

      - [required] (always use scope=data)

      #### Response

      {
              "access_token":292c6912e7710c838347ae178b4a,
              "token_type":"bearer",
              "expires_in": 3600,
              "refresh_token":292c6912e7710c838347ae178b4a,
          }

      ### Refresh token

      Every Access token has a lifetime. You can use Refresh token to request a new Access Token.

      POST   https://ritekit.com/oauth/token

                 grant_type=refresh_token
                 &refresh_token=REFRESH_TOKEN
                 &client_id=CLIENT_ID
                 &scope=data

      - [required] grant_type - refresh_token

      - [required] refresh_token - (REFRESH TOKEN) the refresh token you got from the expired token

      - [required] client_id (CLIENT ID)

      - [required] client_secret (CLIENT SECRET)

      - [required] scope (always use scope=data)

      #### Response

      {
              "access_token":292c6912e7710c838347ae178b4a,
              "token_type":"bearer",
              "expires_in": 3600,
              "refresh_token":292c6912e7710c838347ae178b4a,
          }
    version: 1.0.0
    slug: ritekit-1-0-0-v1-stats-auto-hashtag-post-post
    schemes:
    - https
    hosts:
    - https://api.ritekit.com
    topics:
    - social
- group: ritekit-1-0-0
  url_pattern: /v1/stats/hashtag-suggestions/{topic}
  method: GET
  host:
  - https://api.ritekit.com
  url_keys:
  - topic
  concurrent_calls: 1
  extra_config:
    provider: ritekit.com
    description: |-
      RiteKit API is based on REST principles.

      Authentication uses standard OAuth 2.0 process

      ##Getting started

      1. Sign up for [RiteKit](https://ritekit.com/)

      1. Go to [developer dashboard](https://ritekit.com/developer/dashboard/)

      1. Click "Create a token" button to get your **Client ID** and **Client secret**

      1. Study the documentation below (for more info on oAuth go to http://oauth.net/)

      1. When you reach your free limit of calls per month, [upgrade to paid tiers](https://ritekit.com/developer/)

      ## Options for authorizing API Calls

      #### A) Using Client ID directly

      You can directly connect to our API using your **client ID** by sending it as a GET query parameter. This option is simple (no need for oAuth) but it should be used only in case the Client ID is not exposed publicly.

      GET  https://api.ritekit.com/v1/stats/basic/php?client_id=292c6912e7710c838347ae178b4a

      #### B) Using Access token (oAuth 2.0)

      Once you have valid Access token, you can access our API by sending the token in the Authorization header bearer or as a query Access token. See the oAuth 2.0 section to learn how to get Access token and keep refreshing it.

      Authorization: Bearer xxxx

      Or

      GET  https://api.ritekit.com/v1/stats/basic/php?access_token=292c6912e7710c838347ae178b4a

      ## oAuth 2.0

      ![oauthschema](https://cdn.ritekit.com/assets/media/1/oauth-simple.png)

      ### Get Access Token

      Use your Client Credentials to get the token.

      POST   https://ritekit.com/oauth/token

                 grant_type=client_credentials
                 &client_id=CLIENT_ID
                 &client_secret=CLIENT_SECRET
                 &scope=data

      - [required] grant_type - client_credentials

      - [required] client_id (CLIENT ID)

      - [required] client_secret (CLIENT SECRET)

      - [required] (always use scope=data)

      #### Response

      {
              "access_token":292c6912e7710c838347ae178b4a,
              "token_type":"bearer",
              "expires_in": 3600,
              "refresh_token":292c6912e7710c838347ae178b4a,
          }

      ### Refresh token

      Every Access token has a lifetime. You can use Refresh token to request a new Access Token.

      POST   https://ritekit.com/oauth/token

                 grant_type=refresh_token
                 &refresh_token=REFRESH_TOKEN
                 &client_id=CLIENT_ID
                 &scope=data

      - [required] grant_type - refresh_token

      - [required] refresh_token - (REFRESH TOKEN) the refresh token you got from the expired token

      - [required] client_id (CLIENT ID)

      - [required] client_secret (CLIENT SECRET)

      - [required] scope (always use scope=data)

      #### Response

      {
              "access_token":292c6912e7710c838347ae178b4a,
              "token_type":"bearer",
              "expires_in": 3600,
              "refresh_token":292c6912e7710c838347ae178b4a,
          }
    version: 1.0.0
    slug: ritekit-1-0-0-v1-stats-hashtag-suggestions-topic
    schemes:
    - https
    hosts:
    - https://api.ritekit.com
    topics:
    - social
- group: ritekit-1-0-0
  url_pattern: /v1/stats/history/{hashtag}
  method: GET
  host:
  - https://api.ritekit.com
  url_keys:
  - hashtag
  concurrent_calls: 1
  extra_config:
    provider: ritekit.com
    description: |-
      RiteKit API is based on REST principles.

      Authentication uses standard OAuth 2.0 process

      ##Getting started

      1. Sign up for [RiteKit](https://ritekit.com/)

      1. Go to [developer dashboard](https://ritekit.com/developer/dashboard/)

      1. Click "Create a token" button to get your **Client ID** and **Client secret**

      1. Study the documentation below (for more info on oAuth go to http://oauth.net/)

      1. When you reach your free limit of calls per month, [upgrade to paid tiers](https://ritekit.com/developer/)

      ## Options for authorizing API Calls

      #### A) Using Client ID directly

      You can directly connect to our API using your **client ID** by sending it as a GET query parameter. This option is simple (no need for oAuth) but it should be used only in case the Client ID is not exposed publicly.

      GET  https://api.ritekit.com/v1/stats/basic/php?client_id=292c6912e7710c838347ae178b4a

      #### B) Using Access token (oAuth 2.0)

      Once you have valid Access token, you can access our API by sending the token in the Authorization header bearer or as a query Access token. See the oAuth 2.0 section to learn how to get Access token and keep refreshing it.

      Authorization: Bearer xxxx

      Or

      GET  https://api.ritekit.com/v1/stats/basic/php?access_token=292c6912e7710c838347ae178b4a

      ## oAuth 2.0

      ![oauthschema](https://cdn.ritekit.com/assets/media/1/oauth-simple.png)

      ### Get Access Token

      Use your Client Credentials to get the token.

      POST   https://ritekit.com/oauth/token

                 grant_type=client_credentials
                 &client_id=CLIENT_ID
                 &client_secret=CLIENT_SECRET
                 &scope=data

      - [required] grant_type - client_credentials

      - [required] client_id (CLIENT ID)

      - [required] client_secret (CLIENT SECRET)

      - [required] (always use scope=data)

      #### Response

      {
              "access_token":292c6912e7710c838347ae178b4a,
              "token_type":"bearer",
              "expires_in": 3600,
              "refresh_token":292c6912e7710c838347ae178b4a,
          }

      ### Refresh token

      Every Access token has a lifetime. You can use Refresh token to request a new Access Token.

      POST   https://ritekit.com/oauth/token

                 grant_type=refresh_token
                 &refresh_token=REFRESH_TOKEN
                 &client_id=CLIENT_ID
                 &scope=data

      - [required] grant_type - refresh_token

      - [required] refresh_token - (REFRESH TOKEN) the refresh token you got from the expired token

      - [required] client_id (CLIENT ID)

      - [required] client_secret (CLIENT SECRET)

      - [required] scope (always use scope=data)

      #### Response

      {
              "access_token":292c6912e7710c838347ae178b4a,
              "token_type":"bearer",
              "expires_in": 3600,
              "refresh_token":292c6912e7710c838347ae178b4a,
          }
    version: 1.0.0
    slug: ritekit-1-0-0-v1-stats-history-hashtag
    schemes:
    - https
    hosts:
    - https://api.ritekit.com
    topics:
    - social
- group: ritekit-1-0-0
  url_pattern: /v1/stats/multiple-hashtags
  method: GET
  host:
  - https://api.ritekit.com
  query_string_params:
  - tags
  concurrent_calls: 1
  extra_config:
    provider: ritekit.com
    description: |-
      RiteKit API is based on REST principles.

      Authentication uses standard OAuth 2.0 process

      ##Getting started

      1. Sign up for [RiteKit](https://ritekit.com/)

      1. Go to [developer dashboard](https://ritekit.com/developer/dashboard/)

      1. Click "Create a token" button to get your **Client ID** and **Client secret**

      1. Study the documentation below (for more info on oAuth go to http://oauth.net/)

      1. When you reach your free limit of calls per month, [upgrade to paid tiers](https://ritekit.com/developer/)

      ## Options for authorizing API Calls

      #### A) Using Client ID directly

      You can directly connect to our API using your **client ID** by sending it as a GET query parameter. This option is simple (no need for oAuth) but it should be used only in case the Client ID is not exposed publicly.

      GET  https://api.ritekit.com/v1/stats/basic/php?client_id=292c6912e7710c838347ae178b4a

      #### B) Using Access token (oAuth 2.0)

      Once you have valid Access token, you can access our API by sending the token in the Authorization header bearer or as a query Access token. See the oAuth 2.0 section to learn how to get Access token and keep refreshing it.

      Authorization: Bearer xxxx

      Or

      GET  https://api.ritekit.com/v1/stats/basic/php?access_token=292c6912e7710c838347ae178b4a

      ## oAuth 2.0

      ![oauthschema](https://cdn.ritekit.com/assets/media/1/oauth-simple.png)

      ### Get Access Token

      Use your Client Credentials to get the token.

      POST   https://ritekit.com/oauth/token

                 grant_type=client_credentials
                 &client_id=CLIENT_ID
                 &client_secret=CLIENT_SECRET
                 &scope=data

      - [required] grant_type - client_credentials

      - [required] client_id (CLIENT ID)

      - [required] client_secret (CLIENT SECRET)

      - [required] (always use scope=data)

      #### Response

      {
              "access_token":292c6912e7710c838347ae178b4a,
              "token_type":"bearer",
              "expires_in": 3600,
              "refresh_token":292c6912e7710c838347ae178b4a,
          }

      ### Refresh token

      Every Access token has a lifetime. You can use Refresh token to request a new Access Token.

      POST   https://ritekit.com/oauth/token

                 grant_type=refresh_token
                 &refresh_token=REFRESH_TOKEN
                 &client_id=CLIENT_ID
                 &scope=data

      - [required] grant_type - refresh_token

      - [required] refresh_token - (REFRESH TOKEN) the refresh token you got from the expired token

      - [required] client_id (CLIENT ID)

      - [required] client_secret (CLIENT SECRET)

      - [required] scope (always use scope=data)

      #### Response

      {
              "access_token":292c6912e7710c838347ae178b4a,
              "token_type":"bearer",
              "expires_in": 3600,
              "refresh_token":292c6912e7710c838347ae178b4a,
          }
    version: 1.0.0
    slug: ritekit-1-0-0-v1-stats-multiple-hashtags
    schemes:
    - https
    hosts:
    - https://api.ritekit.com
    topics:
    - social
