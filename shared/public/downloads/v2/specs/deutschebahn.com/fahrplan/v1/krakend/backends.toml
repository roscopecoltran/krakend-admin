[[backends]]
  group = "fahrplan-free-arrivalboard-id"
  slug = "fahrplan-free-v1-arrivalboard-id"
  url_pattern = "/arrivalBoard/{id}"
  method = "GET"
  encoding = "json"
  host = ["https://api.deutschebahn.com"]
  url_keys = ["id"]
  query_string_params = ["date"]
  whitelist = ["boards"]
  concurrent_calls = 0
  [backends.mapping]
    boards = "boards"
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["https://api.deutschebahn.com", "http://api.deutschebahn.com"]
    schemes = ["https", "http"]

[[backends]]
  group = "fahrplan-free-departureboard-id"
  slug = "fahrplan-free-v1-departureboard-id"
  url_pattern = "/departureBoard/{id}"
  method = "GET"
  encoding = "json"
  host = ["https://api.deutschebahn.com"]
  url_keys = ["id"]
  query_string_params = ["date"]
  whitelist = ["boards"]
  concurrent_calls = 0
  [backends.mapping]
    boards = "boards"
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["https://api.deutschebahn.com", "http://api.deutschebahn.com"]
    schemes = ["https", "http"]

[[backends]]
  group = "fahrplan-free-journeydetails-id"
  slug = "fahrplan-free-v1-journeydetails-id"
  url_pattern = "/journeyDetails/{id}"
  method = "GET"
  encoding = "json"
  host = ["https://api.deutschebahn.com"]
  url_keys = ["id"]
  whitelist = ["train_locs"]
  concurrent_calls = 0
  [backends.mapping]
    train_locs = "train_locs"
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["https://api.deutschebahn.com", "http://api.deutschebahn.com"]
    schemes = ["https", "http"]

[[backends]]
  group = "fahrplan-free-location-name"
  slug = "fahrplan-free-v1-location-name"
  url_pattern = "/location/{name}"
  method = "GET"
  encoding = "json"
  host = ["https://api.deutschebahn.com"]
  url_keys = ["name"]
  whitelist = ["locations"]
  concurrent_calls = 0
  [backends.mapping]
    locations = "locations"
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["https://api.deutschebahn.com", "http://api.deutschebahn.com"]
    schemes = ["https", "http"]
