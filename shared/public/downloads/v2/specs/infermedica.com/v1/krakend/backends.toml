[[backends]]
  group = "infermedica-v1"
  slug = "infermedica-v1-v1-conditions"
  url_pattern = "/v1/conditions"
  method = "GET"
  encoding = "json"
  host = ["https://api.infermedica.com"]
  concurrent_calls = 1
  [backends.extra_config]
    produces = ["json"]
    hosts = ["https://api.infermedica.com"]
    schemes = ["https"]

[[backends]]
  group = "infermedica-v1"
  slug = "infermedica-v1-v1-conditions-id"
  url_pattern = "/v1/conditions/{id}"
  method = "GET"
  encoding = "json"
  host = ["https://api.infermedica.com"]
  whitelist = ["acuteness", "categories", "id", "name", "prevalence", "severity", "sex_filter"]
  concurrent_calls = 1
  [backends.mapping]
    acuteness = "acuteness"
    categories = "categories"
    id = "id"
    name = "name"
    prevalence = "prevalence"
    severity = "severity"
    sex_filter = "sex_filter"
  [backends.extra_config]
    produces = ["json"]
    hosts = ["https://api.infermedica.com"]
    schemes = ["https"]

[[backends]]
  group = "infermedica-v1"
  slug = "infermedica-v1-v1-diagnosis"
  url_pattern = "/v1/diagnosis"
  method = "POST"
  encoding = "json"
  host = ["https://api.infermedica.com"]
  whitelist = ["conditions", "extras"]
  concurrent_calls = 1
  [backends.mapping]
    conditions = "conditions"
    extras = "extras"
  [backends.extra_config]
    produces = ["json"]
    hosts = ["https://api.infermedica.com"]
    schemes = ["https"]

[[backends]]
  group = "infermedica-v1"
  slug = "infermedica-v1-v1-info"
  url_pattern = "/v1/info"
  method = "GET"
  encoding = "json"
  host = ["https://api.infermedica.com"]
  whitelist = ["conditions_count", "observations_count", "updated_at"]
  concurrent_calls = 1
  [backends.mapping]
    conditions_count = "conditions_count"
    observations_count = "observations_count"
    updated_at = "updated_at"
  [backends.extra_config]
    produces = ["json"]
    hosts = ["https://api.infermedica.com"]
    schemes = ["https"]

[[backends]]
  group = "infermedica-v1"
  slug = "infermedica-v1-v1-lookup"
  url_pattern = "/v1/lookup"
  method = "GET"
  encoding = "json"
  host = ["https://api.infermedica.com"]
  whitelist = ["category", "children", "id", "name", "parent_id", "parent_relation_type", "sex_filter", "valid_for"]
  concurrent_calls = 1
  [backends.mapping]
    category = "category"
    children = "children"
    id = "id"
    name = "name"
    parent_id = "parent_id"
    parent_relation_type = "parent_relation_type"
    sex_filter = "sex_filter"
    valid_for = "valid_for"
  [backends.extra_config]
    produces = ["json"]
    hosts = ["https://api.infermedica.com"]
    schemes = ["https"]

[[backends]]
  group = "infermedica-v1"
  slug = "infermedica-v1-v1-observations"
  url_pattern = "/v1/observations"
  method = "GET"
  encoding = "json"
  host = ["https://api.infermedica.com"]
  concurrent_calls = 1
  [backends.extra_config]
    produces = ["json"]
    hosts = ["https://api.infermedica.com"]
    schemes = ["https"]

[[backends]]
  group = "infermedica-v1"
  slug = "infermedica-v1-v1-observations-id"
  url_pattern = "/v1/observations/{id}"
  method = "GET"
  encoding = "json"
  host = ["https://api.infermedica.com"]
  whitelist = ["category", "children", "id", "name", "parent_id", "parent_relation_type", "sex_filter", "valid_for"]
  concurrent_calls = 1
  [backends.mapping]
    category = "category"
    children = "children"
    id = "id"
    name = "name"
    parent_id = "parent_id"
    parent_relation_type = "parent_relation_type"
    sex_filter = "sex_filter"
    valid_for = "valid_for"
  [backends.extra_config]
    produces = ["json"]
    hosts = ["https://api.infermedica.com"]
    schemes = ["https"]

[[backends]]
  group = "infermedica-v1"
  slug = "infermedica-v1-v1-risk-factors"
  url_pattern = "/v1/risk_factors"
  method = "GET"
  encoding = "json"
  host = ["https://api.infermedica.com"]
  concurrent_calls = 1
  [backends.extra_config]
    produces = ["json"]
    hosts = ["https://api.infermedica.com"]
    schemes = ["https"]

[[backends]]
  group = "infermedica-v1"
  slug = "infermedica-v1-v1-risk-factors-id"
  url_pattern = "/v1/risk_factors/{id}"
  method = "GET"
  encoding = "json"
  host = ["https://api.infermedica.com"]
  whitelist = ["id", "name"]
  concurrent_calls = 1
  [backends.mapping]
    id = "id"
    name = "name"
  [backends.extra_config]
    produces = ["json"]
    hosts = ["https://api.infermedica.com"]
    schemes = ["https"]
