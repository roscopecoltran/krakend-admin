[[backends]]
  group = "adsense-host-v4-1"
  url_pattern = "/adsensehost/v4.1/accounts"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["etag", "items", "kind"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Generates performance reports, generates ad codes, and provides publisher management capabilities for AdSense Hosts."
    version = "v4.1"
    slug = "adsense-host-v4-1-adsensehost-v4-1-accounts"
    schemes = ["https"]
    hosts = ["https://www.googleapis.com"]
    [backends.extra_config.mapping]
      etag = "etag"
      items = "items"
      kind = "kind"

[[backends]]
  group = "adsense-host-v4-1"
  url_pattern = "/adsensehost/v4.1/accounts/{accountId}"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["id", "kind", "name", "status"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Generates performance reports, generates ad codes, and provides publisher management capabilities for AdSense Hosts."
    version = "v4.1"
    slug = "adsense-host-v4-1-adsensehost-v4-1-accounts-accountid"
    schemes = ["https"]
    hosts = ["https://www.googleapis.com"]
    [backends.extra_config.mapping]
      id = "id"
      kind = "kind"
      name = "name"
      status = "status"

[[backends]]
  group = "adsense-host-v4-1"
  url_pattern = "/adsensehost/v4.1/accounts/{accountId}/adclients"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["etag", "items", "kind", "nextPageToken"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Generates performance reports, generates ad codes, and provides publisher management capabilities for AdSense Hosts."
    version = "v4.1"
    slug = "adsense-host-v4-1-adsensehost-v4-1-accounts-accountid-adclients"
    schemes = ["https"]
    hosts = ["https://www.googleapis.com"]
    [backends.extra_config.mapping]
      etag = "etag"
      items = "items"
      kind = "kind"
      nextPageToken = "nextPageToken"

[[backends]]
  group = "adsense-host-v4-1"
  url_pattern = "/adsensehost/v4.1/accounts/{accountId}/adclients/{adClientId}"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["arcOptIn", "id", "kind", "productCode", "supportsReporting"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Generates performance reports, generates ad codes, and provides publisher management capabilities for AdSense Hosts."
    version = "v4.1"
    slug = "adsense-host-v4-1-adsensehost-v4-1-accounts-accountid-adclients-adclientid"
    schemes = ["https"]
    hosts = ["https://www.googleapis.com"]
    [backends.extra_config.mapping]
      arcOptIn = "arcOptIn"
      id = "id"
      kind = "kind"
      productCode = "productCode"
      supportsReporting = "supportsReporting"

[[backends]]
  group = "adsense-host-v4-1"
  url_pattern = "/adsensehost/v4.1/accounts/{accountId}/adclients/{adClientId}/adunits"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["etag", "items", "kind", "nextPageToken"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Generates performance reports, generates ad codes, and provides publisher management capabilities for AdSense Hosts."
    version = "v4.1"
    slug = "adsense-host-v4-1-adsensehost-v4-1-accounts-accountid-adclients-adclientid-adunits"
    schemes = ["https"]
    hosts = ["https://www.googleapis.com"]
    [backends.extra_config.mapping]
      etag = "etag"
      items = "items"
      kind = "kind"
      nextPageToken = "nextPageToken"

[[backends]]
  group = "adsense-host-v4-1"
  url_pattern = "/adsensehost/v4.1/accounts/{accountId}/adclients/{adClientId}/adunits/{adUnitId}"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["code", "contentAdsSettings", "contentAdsSettings.backupOption", "contentAdsSettings.backupOption.color", "contentAdsSettings.backupOption.url", "contentAdsSettings.size", "id", "kind", "mobileContentAdsSettings", "mobileContentAdsSettings.markupLanguage", "mobileContentAdsSettings.scriptingLanguage", "mobileContentAdsSettings.size", "name", "status"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Generates performance reports, generates ad codes, and provides publisher management capabilities for AdSense Hosts."
    version = "v4.1"
    slug = "adsense-host-v4-1-adsensehost-v4-1-accounts-accountid-adclients-adclientid-adunits-adunitid"
    schemes = ["https"]
    hosts = ["https://www.googleapis.com"]
    [backends.extra_config.mapping]
      backupOption = "backupOption"
      code = "code"
      color = "color"
      contentAdsSettings = "contentAdsSettings"
      id = "id"
      kind = "kind"
      markupLanguage = "markupLanguage"
      mobileContentAdsSettings = "mobileContentAdsSettings"
      name = "name"
      scriptingLanguage = "scriptingLanguage"
      size = "size"
      status = "status"
      url = "url"

[[backends]]
  group = "adsense-host-v4-1"
  url_pattern = "/adsensehost/v4.1/accounts/{accountId}/adclients/{adClientId}/adunits/{adUnitId}/adcode"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["adCode", "kind"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Generates performance reports, generates ad codes, and provides publisher management capabilities for AdSense Hosts."
    version = "v4.1"
    slug = "adsense-host-v4-1-adsensehost-v4-1-accounts-accountid-adclients-adclientid-adunits-adunitid-adcode"
    schemes = ["https"]
    hosts = ["https://www.googleapis.com"]
    [backends.extra_config.mapping]
      adCode = "adCode"
      kind = "kind"

[[backends]]
  group = "adsense-host-v4-1"
  url_pattern = "/adsensehost/v4.1/accounts/{accountId}/reports"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["averages", "headers", "headers.currency", "headers.name", "kind", "rows", "rows.items", "totalMatchedRows", "totals", "warnings"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Generates performance reports, generates ad codes, and provides publisher management capabilities for AdSense Hosts."
    version = "v4.1"
    slug = "adsense-host-v4-1-adsensehost-v4-1-accounts-accountid-reports"
    schemes = ["https"]
    hosts = ["https://www.googleapis.com"]
    [backends.extra_config.mapping]
      averages = "averages"
      currency = "currency"
      headers = "headers"
      items = "items"
      kind = "kind"
      name = "name"
      rows = "rows"
      totalMatchedRows = "totalMatchedRows"
      totals = "totals"
      warnings = "warnings"

[[backends]]
  group = "adsense-host-v4-1"
  url_pattern = "/adsensehost/v4.1/adclients"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["etag", "items", "kind", "nextPageToken"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Generates performance reports, generates ad codes, and provides publisher management capabilities for AdSense Hosts."
    version = "v4.1"
    slug = "adsense-host-v4-1-adsensehost-v4-1-adclients"
    schemes = ["https"]
    hosts = ["https://www.googleapis.com"]
    [backends.extra_config.mapping]
      etag = "etag"
      items = "items"
      kind = "kind"
      nextPageToken = "nextPageToken"

[[backends]]
  group = "adsense-host-v4-1"
  url_pattern = "/adsensehost/v4.1/adclients/{adClientId}"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["arcOptIn", "id", "kind", "productCode", "supportsReporting"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Generates performance reports, generates ad codes, and provides publisher management capabilities for AdSense Hosts."
    version = "v4.1"
    slug = "adsense-host-v4-1-adsensehost-v4-1-adclients-adclientid"
    schemes = ["https"]
    hosts = ["https://www.googleapis.com"]
    [backends.extra_config.mapping]
      arcOptIn = "arcOptIn"
      id = "id"
      kind = "kind"
      productCode = "productCode"
      supportsReporting = "supportsReporting"

[[backends]]
  group = "adsense-host-v4-1"
  url_pattern = "/adsensehost/v4.1/adclients/{adClientId}/customchannels"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["etag", "items", "kind", "nextPageToken"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Generates performance reports, generates ad codes, and provides publisher management capabilities for AdSense Hosts."
    version = "v4.1"
    slug = "adsense-host-v4-1-adsensehost-v4-1-adclients-adclientid-customchannels"
    schemes = ["https"]
    hosts = ["https://www.googleapis.com"]
    [backends.extra_config.mapping]
      etag = "etag"
      items = "items"
      kind = "kind"
      nextPageToken = "nextPageToken"

[[backends]]
  group = "adsense-host-v4-1"
  url_pattern = "/adsensehost/v4.1/adclients/{adClientId}/customchannels/{customChannelId}"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["code", "id", "kind", "name"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Generates performance reports, generates ad codes, and provides publisher management capabilities for AdSense Hosts."
    version = "v4.1"
    slug = "adsense-host-v4-1-adsensehost-v4-1-adclients-adclientid-customchannels-customchannelid"
    schemes = ["https"]
    hosts = ["https://www.googleapis.com"]
    [backends.extra_config.mapping]
      code = "code"
      id = "id"
      kind = "kind"
      name = "name"

[[backends]]
  group = "adsense-host-v4-1"
  url_pattern = "/adsensehost/v4.1/adclients/{adClientId}/urlchannels"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["etag", "items", "kind", "nextPageToken"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Generates performance reports, generates ad codes, and provides publisher management capabilities for AdSense Hosts."
    version = "v4.1"
    slug = "adsense-host-v4-1-adsensehost-v4-1-adclients-adclientid-urlchannels"
    schemes = ["https"]
    hosts = ["https://www.googleapis.com"]
    [backends.extra_config.mapping]
      etag = "etag"
      items = "items"
      kind = "kind"
      nextPageToken = "nextPageToken"

[[backends]]
  group = "adsense-host-v4-1"
  url_pattern = "/adsensehost/v4.1/associationsessions/start"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["accountId", "id", "kind", "productCodes", "redirectUrl", "status", "userLocale", "websiteLocale", "websiteUrl"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Generates performance reports, generates ad codes, and provides publisher management capabilities for AdSense Hosts."
    version = "v4.1"
    slug = "adsense-host-v4-1-adsensehost-v4-1-associationsessions-start"
    schemes = ["https"]
    hosts = ["https://www.googleapis.com"]
    [backends.extra_config.mapping]
      accountId = "accountId"
      id = "id"
      kind = "kind"
      productCodes = "productCodes"
      redirectUrl = "redirectUrl"
      status = "status"
      userLocale = "userLocale"
      websiteLocale = "websiteLocale"
      websiteUrl = "websiteUrl"

[[backends]]
  group = "adsense-host-v4-1"
  url_pattern = "/adsensehost/v4.1/associationsessions/verify"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["accountId", "id", "kind", "productCodes", "redirectUrl", "status", "userLocale", "websiteLocale", "websiteUrl"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Generates performance reports, generates ad codes, and provides publisher management capabilities for AdSense Hosts."
    version = "v4.1"
    slug = "adsense-host-v4-1-adsensehost-v4-1-associationsessions-verify"
    schemes = ["https"]
    hosts = ["https://www.googleapis.com"]
    [backends.extra_config.mapping]
      accountId = "accountId"
      id = "id"
      kind = "kind"
      productCodes = "productCodes"
      redirectUrl = "redirectUrl"
      status = "status"
      userLocale = "userLocale"
      websiteLocale = "websiteLocale"
      websiteUrl = "websiteUrl"

[[backends]]
  group = "adsense-host-v4-1"
  url_pattern = "/adsensehost/v4.1/reports"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["averages", "headers", "headers.currency", "headers.name", "kind", "rows", "rows.items", "totalMatchedRows", "totals", "warnings"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Generates performance reports, generates ad codes, and provides publisher management capabilities for AdSense Hosts."
    version = "v4.1"
    slug = "adsense-host-v4-1-adsensehost-v4-1-reports"
    schemes = ["https"]
    hosts = ["https://www.googleapis.com"]
    [backends.extra_config.mapping]
      averages = "averages"
      currency = "currency"
      headers = "headers"
      items = "items"
      kind = "kind"
      name = "name"
      rows = "rows"
      totalMatchedRows = "totalMatchedRows"
      totals = "totals"
      warnings = "warnings"
