[[backends]]
  group = "prediction"
  slug = "prediction-v1-3"
  url_pattern = "/hostedmodels/{hostedModelName}/predict"
  method = "POST"
  host = ["https://www.googleapis.com"]
  url_keys = ["hostedModelName"]
  whitelist = ["outputMulti", "outputValue", "outputMulti", "selfLink", "id", "outputMulti", "outputMulti", "kind", "outputLabel"]
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "prediction"
  slug = "prediction-v1-3"
  url_pattern = "/training"
  method = "POST"
  host = ["https://www.googleapis.com"]
  whitelist = ["utility", "selfLink", "modelInfo", "id", "utility", "modelInfo", "modelInfo", "modelInfo", "modelInfo", "kind", "utility", "modelInfo", "modelInfo", "trainingStatus", "modelInfo", "modelInfo", "modelInfo", "modelInfo", "modelInfo"]
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "prediction"
  slug = "prediction-v1-3"
  url_pattern = "/training/{data}"
  method = "GET"
  host = ["https://www.googleapis.com"]
  url_keys = ["data"]
  whitelist = ["utility", "selfLink", "modelInfo", "id", "utility", "modelInfo", "modelInfo", "modelInfo", "modelInfo", "kind", "utility", "modelInfo", "modelInfo", "trainingStatus", "modelInfo", "modelInfo", "modelInfo", "modelInfo", "modelInfo"]
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "prediction"
  slug = "prediction-v1-3"
  url_pattern = "/training/{data}/predict"
  method = "POST"
  host = ["https://www.googleapis.com"]
  url_keys = ["data"]
  whitelist = ["outputMulti", "outputValue", "outputMulti", "selfLink", "id", "outputMulti", "outputMulti", "kind", "outputLabel"]
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]
