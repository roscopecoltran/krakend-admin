[[backends]]
  group = "prediction-v1-2"
  url_pattern = "/prediction/v1.2/hostedmodels/{hostedModelName}/predict"
  method = "POST"
  host = ["https://www.googleapis.com"]
  whitelist = ["id", "kind", "outputLabel", "outputMulti", "outputMulti.label", "outputMulti.score", "outputValue", "selfLink"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Lets you access a cloud hosted machine learning service that makes it easy to build smart apps"
    version = "v1.2"
    slug = "prediction-v1-2-prediction-v1-2-hostedmodels-hostedmodelname-predict"
    schemes = ["https"]
    hosts = ["https://www.googleapis.com"]
    topics = ["machine-learning"]
    [backends.extra_config.mapping]
      id = "id"
      kind = "kind"
      label = "label"
      outputLabel = "outputLabel"
      outputMulti = "outputMulti"
      outputValue = "outputValue"
      score = "score"
      selfLink = "selfLink"

[[backends]]
  group = "prediction-v1-2"
  url_pattern = "/prediction/v1.2/training"
  method = "POST"
  host = ["https://www.googleapis.com"]
  whitelist = ["id", "kind", "modelInfo", "modelInfo.classificationAccuracy", "modelInfo.meanSquaredError", "modelInfo.modelType", "selfLink", "trainingStatus"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Lets you access a cloud hosted machine learning service that makes it easy to build smart apps"
    version = "v1.2"
    slug = "prediction-v1-2-prediction-v1-2-training"
    schemes = ["https"]
    hosts = ["https://www.googleapis.com"]
    topics = ["machine-learning"]
    [backends.extra_config.mapping]
      classificationAccuracy = "classificationAccuracy"
      id = "id"
      kind = "kind"
      meanSquaredError = "meanSquaredError"
      modelInfo = "modelInfo"
      modelType = "modelType"
      selfLink = "selfLink"
      trainingStatus = "trainingStatus"

[[backends]]
  group = "prediction-v1-2"
  url_pattern = "/prediction/v1.2/training/{data}"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["id", "kind", "modelInfo", "modelInfo.classificationAccuracy", "modelInfo.meanSquaredError", "modelInfo.modelType", "selfLink", "trainingStatus"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Lets you access a cloud hosted machine learning service that makes it easy to build smart apps"
    version = "v1.2"
    slug = "prediction-v1-2-prediction-v1-2-training-data"
    schemes = ["https"]
    hosts = ["https://www.googleapis.com"]
    topics = ["machine-learning"]
    [backends.extra_config.mapping]
      classificationAccuracy = "classificationAccuracy"
      id = "id"
      kind = "kind"
      meanSquaredError = "meanSquaredError"
      modelInfo = "modelInfo"
      modelType = "modelType"
      selfLink = "selfLink"
      trainingStatus = "trainingStatus"

[[backends]]
  group = "prediction-v1-2"
  url_pattern = "/prediction/v1.2/training/{data}/predict"
  method = "POST"
  host = ["https://www.googleapis.com"]
  whitelist = ["id", "kind", "outputLabel", "outputMulti", "outputMulti.label", "outputMulti.score", "outputValue", "selfLink"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Lets you access a cloud hosted machine learning service that makes it easy to build smart apps"
    version = "v1.2"
    slug = "prediction-v1-2-prediction-v1-2-training-data-predict"
    schemes = ["https"]
    hosts = ["https://www.googleapis.com"]
    topics = ["machine-learning"]
    [backends.extra_config.mapping]
      id = "id"
      kind = "kind"
      label = "label"
      outputLabel = "outputLabel"
      outputMulti = "outputMulti"
      outputValue = "outputValue"
      score = "score"
      selfLink = "selfLink"
