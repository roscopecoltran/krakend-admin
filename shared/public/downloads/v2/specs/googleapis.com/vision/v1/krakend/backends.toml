[[backends]]
  group = "google-cloud-vision"
  slug = "google-cloud-vision-v1"
  url_pattern = "/v1/images:annotate"
  method = "POST"
  host = ["https://vision.googleapis.com"]
  whitelist = ["responses"]
  [backends.extra_config]
    hosts = ["https://vision.googleapis.com"]
    schemes = ["https"]
