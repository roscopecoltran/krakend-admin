[[backends]]
  group = "google-cloud-key-management-service-kms-v1"
  url_pattern = "/v1/{name}"
  method = "GET"
  host = ["https://cloudkms.googleapis.com"]
  url_keys = ["name"]
  whitelist = ["createTime", "destroyEventTime", "destroyTime", "name", "state"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Manages encryption for your cloud services the same way you do on-premises. You can generate, use, rotate, and destroy AES256 encryption keys."
    version = "v1"
    slug = "google-cloud-key-management-service-kms-v1-v1-name"
    schemes = ["https"]
    hosts = ["https://cloudkms.googleapis.com"]
    topics = ["projects", "security"]
    [backends.extra_config.mapping]
      createTime = "createTime"
      destroyEventTime = "destroyEventTime"
      destroyTime = "destroyTime"
      name = "name"
      state = "state"

[[backends]]
  group = "google-cloud-key-management-service-kms-v1"
  url_pattern = "/v1/{name}/locations"
  method = "GET"
  host = ["https://cloudkms.googleapis.com"]
  url_keys = ["name"]
  query_string_params = ["filter", "pageSize", "pageToken"]
  whitelist = ["locations", "nextPageToken"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Manages encryption for your cloud services the same way you do on-premises. You can generate, use, rotate, and destroy AES256 encryption keys."
    version = "v1"
    slug = "google-cloud-key-management-service-kms-v1-v1-name-locations"
    schemes = ["https"]
    hosts = ["https://cloudkms.googleapis.com"]
    topics = ["projects", "security"]
    [backends.extra_config.mapping]
      locations = "locations"
      nextPageToken = "nextPageToken"

[[backends]]
  group = "google-cloud-key-management-service-kms-v1"
  url_pattern = "/v1/{name}:decrypt"
  method = "POST"
  host = ["https://cloudkms.googleapis.com"]
  url_keys = ["name"]
  whitelist = ["plaintext"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Manages encryption for your cloud services the same way you do on-premises. You can generate, use, rotate, and destroy AES256 encryption keys."
    version = "v1"
    slug = "google-cloud-key-management-service-kms-v1-v1-name-decrypt"
    schemes = ["https"]
    hosts = ["https://cloudkms.googleapis.com"]
    topics = ["projects", "security"]
    [backends.extra_config.mapping]
      plaintext = "plaintext"

[[backends]]
  group = "google-cloud-key-management-service-kms-v1"
  url_pattern = "/v1/{name}:destroy"
  method = "POST"
  host = ["https://cloudkms.googleapis.com"]
  url_keys = ["name"]
  whitelist = ["createTime", "destroyEventTime", "destroyTime", "name", "state"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Manages encryption for your cloud services the same way you do on-premises. You can generate, use, rotate, and destroy AES256 encryption keys."
    version = "v1"
    slug = "google-cloud-key-management-service-kms-v1-v1-name-destroy"
    schemes = ["https"]
    hosts = ["https://cloudkms.googleapis.com"]
    topics = ["projects", "security"]
    [backends.extra_config.mapping]
      createTime = "createTime"
      destroyEventTime = "destroyEventTime"
      destroyTime = "destroyTime"
      name = "name"
      state = "state"

[[backends]]
  group = "google-cloud-key-management-service-kms-v1"
  url_pattern = "/v1/{name}:encrypt"
  method = "POST"
  host = ["https://cloudkms.googleapis.com"]
  url_keys = ["name"]
  whitelist = ["ciphertext", "name"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Manages encryption for your cloud services the same way you do on-premises. You can generate, use, rotate, and destroy AES256 encryption keys."
    version = "v1"
    slug = "google-cloud-key-management-service-kms-v1-v1-name-encrypt"
    schemes = ["https"]
    hosts = ["https://cloudkms.googleapis.com"]
    topics = ["projects", "security"]
    [backends.extra_config.mapping]
      ciphertext = "ciphertext"
      name = "name"

[[backends]]
  group = "google-cloud-key-management-service-kms-v1"
  url_pattern = "/v1/{name}:restore"
  method = "POST"
  host = ["https://cloudkms.googleapis.com"]
  url_keys = ["name"]
  whitelist = ["createTime", "destroyEventTime", "destroyTime", "name", "state"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Manages encryption for your cloud services the same way you do on-premises. You can generate, use, rotate, and destroy AES256 encryption keys."
    version = "v1"
    slug = "google-cloud-key-management-service-kms-v1-v1-name-restore"
    schemes = ["https"]
    hosts = ["https://cloudkms.googleapis.com"]
    topics = ["projects", "security"]
    [backends.extra_config.mapping]
      createTime = "createTime"
      destroyEventTime = "destroyEventTime"
      destroyTime = "destroyTime"
      name = "name"
      state = "state"

[[backends]]
  group = "google-cloud-key-management-service-kms-v1"
  url_pattern = "/v1/{name}:updatePrimaryVersion"
  method = "POST"
  host = ["https://cloudkms.googleapis.com"]
  url_keys = ["name"]
  whitelist = ["createTime", "labels", "labels.additionalProperties", "name", "nextRotationTime", "purpose", "rotationPeriod"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Manages encryption for your cloud services the same way you do on-premises. You can generate, use, rotate, and destroy AES256 encryption keys."
    version = "v1"
    slug = "google-cloud-key-management-service-kms-v1-v1-name-updateprimaryversion"
    schemes = ["https"]
    hosts = ["https://cloudkms.googleapis.com"]
    topics = ["projects", "security"]
    [backends.extra_config.mapping]
      additionalProperties = "additionalProperties"
      createTime = "createTime"
      labels = "labels"
      name = "name"
      nextRotationTime = "nextRotationTime"
      purpose = "purpose"
      rotationPeriod = "rotationPeriod"

[[backends]]
  group = "google-cloud-key-management-service-kms-v1"
  url_pattern = "/v1/{parent}/cryptoKeyVersions"
  method = "GET"
  host = ["https://cloudkms.googleapis.com"]
  url_keys = ["parent"]
  query_string_params = ["pageSize", "pageToken"]
  whitelist = ["cryptoKeyVersions", "nextPageToken", "totalSize"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Manages encryption for your cloud services the same way you do on-premises. You can generate, use, rotate, and destroy AES256 encryption keys."
    version = "v1"
    slug = "google-cloud-key-management-service-kms-v1-v1-parent-cryptokeyversions"
    schemes = ["https"]
    hosts = ["https://cloudkms.googleapis.com"]
    topics = ["projects", "security"]
    [backends.extra_config.mapping]
      cryptoKeyVersions = "cryptoKeyVersions"
      nextPageToken = "nextPageToken"
      totalSize = "totalSize"

[[backends]]
  group = "google-cloud-key-management-service-kms-v1"
  url_pattern = "/v1/{parent}/cryptoKeys"
  method = "GET"
  host = ["https://cloudkms.googleapis.com"]
  url_keys = ["parent"]
  query_string_params = ["pageSize", "pageToken"]
  whitelist = ["cryptoKeys", "nextPageToken", "totalSize"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Manages encryption for your cloud services the same way you do on-premises. You can generate, use, rotate, and destroy AES256 encryption keys."
    version = "v1"
    slug = "google-cloud-key-management-service-kms-v1-v1-parent-cryptokeys"
    schemes = ["https"]
    hosts = ["https://cloudkms.googleapis.com"]
    topics = ["projects", "security"]
    [backends.extra_config.mapping]
      cryptoKeys = "cryptoKeys"
      nextPageToken = "nextPageToken"
      totalSize = "totalSize"

[[backends]]
  group = "google-cloud-key-management-service-kms-v1"
  url_pattern = "/v1/{parent}/keyRings"
  method = "GET"
  host = ["https://cloudkms.googleapis.com"]
  url_keys = ["parent"]
  query_string_params = ["pageSize", "pageToken"]
  whitelist = ["keyRings", "nextPageToken", "totalSize"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Manages encryption for your cloud services the same way you do on-premises. You can generate, use, rotate, and destroy AES256 encryption keys."
    version = "v1"
    slug = "google-cloud-key-management-service-kms-v1-v1-parent-keyrings"
    schemes = ["https"]
    hosts = ["https://cloudkms.googleapis.com"]
    topics = ["projects", "security"]
    [backends.extra_config.mapping]
      keyRings = "keyRings"
      nextPageToken = "nextPageToken"
      totalSize = "totalSize"

[[backends]]
  group = "google-cloud-key-management-service-kms-v1"
  url_pattern = "/v1/{resource}:getIamPolicy"
  method = "GET"
  host = ["https://cloudkms.googleapis.com"]
  url_keys = ["resource"]
  whitelist = ["auditConfigs", "bindings", "etag", "iamOwned", "version"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Manages encryption for your cloud services the same way you do on-premises. You can generate, use, rotate, and destroy AES256 encryption keys."
    version = "v1"
    slug = "google-cloud-key-management-service-kms-v1-v1-resource-getiampolicy"
    schemes = ["https"]
    hosts = ["https://cloudkms.googleapis.com"]
    topics = ["projects", "security"]
    [backends.extra_config.mapping]
      auditConfigs = "auditConfigs"
      bindings = "bindings"
      etag = "etag"
      iamOwned = "iamOwned"
      version = "version"

[[backends]]
  group = "google-cloud-key-management-service-kms-v1"
  url_pattern = "/v1/{resource}:setIamPolicy"
  method = "POST"
  host = ["https://cloudkms.googleapis.com"]
  url_keys = ["resource"]
  whitelist = ["auditConfigs", "bindings", "etag", "iamOwned", "version"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Manages encryption for your cloud services the same way you do on-premises. You can generate, use, rotate, and destroy AES256 encryption keys."
    version = "v1"
    slug = "google-cloud-key-management-service-kms-v1-v1-resource-setiampolicy"
    schemes = ["https"]
    hosts = ["https://cloudkms.googleapis.com"]
    topics = ["projects", "security"]
    [backends.extra_config.mapping]
      auditConfigs = "auditConfigs"
      bindings = "bindings"
      etag = "etag"
      iamOwned = "iamOwned"
      version = "version"

[[backends]]
  group = "google-cloud-key-management-service-kms-v1"
  url_pattern = "/v1/{resource}:testIamPermissions"
  method = "POST"
  host = ["https://cloudkms.googleapis.com"]
  url_keys = ["resource"]
  whitelist = ["permissions"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Manages encryption for your cloud services the same way you do on-premises. You can generate, use, rotate, and destroy AES256 encryption keys."
    version = "v1"
    slug = "google-cloud-key-management-service-kms-v1-v1-resource-testiampermissions"
    schemes = ["https"]
    hosts = ["https://cloudkms.googleapis.com"]
    topics = ["projects", "security"]
    [backends.extra_config.mapping]
      permissions = "permissions"
