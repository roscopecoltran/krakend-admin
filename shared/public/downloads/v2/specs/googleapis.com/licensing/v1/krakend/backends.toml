[[backends]]
  group = "enterprise-license-manager-productid-sku-skuid-user"
  slug = "enterprise-license-manager-v1-productid-sku-skuid-user"
  url_pattern = "/{productId}/sku/{skuId}/user"
  method = "POST"
  host = ["https://www.googleapis.com"]
  url_keys = ["productId", "skuId"]
  whitelist = ["etags", "kind", "productId", "productName", "selfLink", "skuId", "skuName", "userId"]
  concurrent_calls = 0
  [backends.mapping]
    etags = "etags"
    kind = "kind"
    productId = "productId"
    productName = "productName"
    selfLink = "selfLink"
    skuId = "skuId"
    skuName = "skuName"
    userId = "userId"
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "enterprise-license-manager-productid-sku-skuid-user-userid"
  slug = "enterprise-license-manager-v1-productid-sku-skuid-user-userid"
  url_pattern = "/{productId}/sku/{skuId}/user/{userId}"
  method = "GET"
  host = ["https://www.googleapis.com"]
  url_keys = ["productId", "skuId", "userId"]
  whitelist = ["etags", "kind", "productId", "productName", "selfLink", "skuId", "skuName", "userId"]
  concurrent_calls = 0
  [backends.mapping]
    etags = "etags"
    kind = "kind"
    productId = "productId"
    productName = "productName"
    selfLink = "selfLink"
    skuId = "skuId"
    skuName = "skuName"
    userId = "userId"
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "enterprise-license-manager-productid-sku-skuid-users"
  slug = "enterprise-license-manager-v1-productid-sku-skuid-users"
  url_pattern = "/{productId}/sku/{skuId}/users"
  method = "GET"
  host = ["https://www.googleapis.com"]
  url_keys = ["productId", "skuId"]
  query_string_params = ["customerId", "maxResults", "pageToken"]
  whitelist = ["etag", "items", "kind", "nextPageToken"]
  concurrent_calls = 0
  [backends.mapping]
    etag = "etag"
    items = "items"
    kind = "kind"
    nextPageToken = "nextPageToken"
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "enterprise-license-manager-productid-users"
  slug = "enterprise-license-manager-v1-productid-users"
  url_pattern = "/{productId}/users"
  method = "GET"
  host = ["https://www.googleapis.com"]
  url_keys = ["productId"]
  query_string_params = ["customerId", "maxResults", "pageToken"]
  whitelist = ["etag", "items", "kind", "nextPageToken"]
  concurrent_calls = 0
  [backends.mapping]
    etag = "etag"
    items = "items"
    kind = "kind"
    nextPageToken = "nextPageToken"
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]
