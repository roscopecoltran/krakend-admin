backends:
- group: google-cloud-runtime-configuration-v1
  url_pattern: /v1/{name}
  method: GET
  host:
  - https://runtimeconfig.googleapis.com
  url_keys:
  - name
  query_string_params:
  - filter
  - pageSize
  - pageToken
  whitelist:
  - nextPageToken
  - operations
  concurrent_calls: 1
  mapping:
    nextPageToken: nextPageToken
    operations: operations
  extra_config:
    slug: google-cloud-runtime-configuration-v1-v1-name
    hosts:
    - https://runtimeconfig.googleapis.com
    schemes:
    - https
- group: google-cloud-runtime-configuration-v1
  url_pattern: /v1/{name}:cancel
  method: POST
  host:
  - https://runtimeconfig.googleapis.com
  url_keys:
  - name
  concurrent_calls: 1
  extra_config:
    slug: google-cloud-runtime-configuration-v1-v1-name-cancel
    hosts:
    - https://runtimeconfig.googleapis.com
    schemes:
    - https
