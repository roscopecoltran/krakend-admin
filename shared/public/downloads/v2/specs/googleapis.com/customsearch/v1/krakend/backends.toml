[[backends]]
  group = "customsearch-customsearch-v1"
  slug = "customsearch-v1-customsearch-v1"
  url_pattern = "/customsearch/v1"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["items", "kind", "promotions", "queries", "searchInformation", "spelling", "url"]
  concurrent_calls = 1
  [backends.mapping]
    items = "items"
    kind = "kind"
    promotions = "promotions"
    queries = "queries"
    searchInformation = "searchInformation"
    spelling = "spelling"
    url = "url"
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]
