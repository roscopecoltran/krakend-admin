[[backends]]
  group = "customsearch-v1"
  url_pattern = "/customsearch/v1"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["items", "kind", "promotions", "queries", "queries.additionalProperties", "searchInformation", "searchInformation.formattedSearchTime", "searchInformation.formattedTotalResults", "searchInformation.searchTime", "searchInformation.totalResults", "spelling", "spelling.correctedQuery", "spelling.htmlCorrectedQuery", "url", "url.template"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Searches over a website or collection of websites"
    version = "v1"
    slug = "customsearch-v1-customsearch-v1"
    schemes = ["https"]
    hosts = ["https://www.googleapis.com"]
    topics = ["search"]
    [backends.extra_config.mapping]
      additionalProperties = "additionalProperties"
      correctedQuery = "correctedQuery"
      formattedSearchTime = "formattedSearchTime"
      formattedTotalResults = "formattedTotalResults"
      htmlCorrectedQuery = "htmlCorrectedQuery"
      items = "items"
      kind = "kind"
      promotions = "promotions"
      queries = "queries"
      searchInformation = "searchInformation"
      searchTime = "searchTime"
      spelling = "spelling"
      template = "template"
      totalResults = "totalResults"
      url = "url"
