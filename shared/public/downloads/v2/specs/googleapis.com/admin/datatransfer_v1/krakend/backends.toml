[[backends]]
  group = "admin-data-transfer"
  slug = "admin-data-transfer-datatransfer-v1"
  url_pattern = "/applications"
  method = "GET"
  host = ["https://www.googleapis.com"]
  query_string_params = ["customerId", "maxResults", "pageToken"]
  whitelist = ["nextPageToken", "kind", "applications", "etag"]
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "admin-data-transfer"
  slug = "admin-data-transfer-datatransfer-v1"
  url_pattern = "/applications/{applicationId}"
  method = "GET"
  host = ["https://www.googleapis.com"]
  url_keys = ["applicationId"]
  whitelist = ["id", "kind", "transferParams", "etag", "name"]
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "admin-data-transfer"
  slug = "admin-data-transfer-datatransfer-v1"
  url_pattern = "/transfers"
  method = "GET"
  host = ["https://www.googleapis.com"]
  query_string_params = ["customerId", "maxResults", "newOwnerUserId", "oldOwnerUserId", "pageToken", "status"]
  whitelist = ["etag", "kind", "nextPageToken", "dataTransfers"]
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "admin-data-transfer"
  slug = "admin-data-transfer-datatransfer-v1"
  url_pattern = "/transfers/{dataTransferId}"
  method = "GET"
  host = ["https://www.googleapis.com"]
  url_keys = ["dataTransferId"]
  whitelist = ["etag", "requestTime", "newOwnerUserId", "oldOwnerUserId", "kind", "id", "overallTransferStatusCode", "applicationDataTransfers"]
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]
