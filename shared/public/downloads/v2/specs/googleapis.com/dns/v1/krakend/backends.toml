[[backends]]
  group = "google-cloud-dns-project"
  slug = "google-cloud-dns-v1-project"
  url_pattern = "/{project}"
  method = "GET"
  host = ["https://www.googleapis.com"]
  url_keys = ["project"]
  whitelist = ["kind", "number", "id"]
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "google-cloud-dns-project-managedzones"
  slug = "google-cloud-dns-v1-project-managedzones"
  url_pattern = "/{project}/managedZones"
  method = "GET"
  host = ["https://www.googleapis.com"]
  url_keys = ["project"]
  query_string_params = ["dnsName", "maxResults", "pageToken"]
  whitelist = ["nextPageToken", "managedZones", "kind"]
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "google-cloud-dns-project-managedzones-managedzone"
  slug = "google-cloud-dns-v1-project-managedzones-managedzone"
  url_pattern = "/{project}/managedZones/{managedZone}"
  method = "GET"
  host = ["https://www.googleapis.com"]
  url_keys = ["project", "managedZone"]
  whitelist = ["name", "creationTime", "dnsName", "id", "nameServerSet", "nameServers", "kind", "description"]
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "google-cloud-dns-project-managedzones-managedzone-changes"
  slug = "google-cloud-dns-v1-project-managedzones-managedzone-changes"
  url_pattern = "/{project}/managedZones/{managedZone}/changes"
  method = "GET"
  host = ["https://www.googleapis.com"]
  url_keys = ["project", "managedZone"]
  query_string_params = ["maxResults", "pageToken", "sortBy", "sortOrder"]
  whitelist = ["changes", "nextPageToken", "kind"]
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "google-cloud-dns-project-managedzones-managedzone-changes-changeid"
  slug = "google-cloud-dns-v1-project-managedzones-managedzone-changes-changeid"
  url_pattern = "/{project}/managedZones/{managedZone}/changes/{changeId}"
  method = "GET"
  host = ["https://www.googleapis.com"]
  url_keys = ["project", "managedZone", "changeId"]
  whitelist = ["status", "deletions", "additions", "startTime", "kind", "id"]
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "google-cloud-dns-project-managedzones-managedzone-rrsets"
  slug = "google-cloud-dns-v1-project-managedzones-managedzone-rrsets"
  url_pattern = "/{project}/managedZones/{managedZone}/rrsets"
  method = "GET"
  host = ["https://www.googleapis.com"]
  url_keys = ["project", "managedZone"]
  query_string_params = ["maxResults", "name", "pageToken", "type"]
  whitelist = ["kind", "rrsets", "nextPageToken"]
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]
