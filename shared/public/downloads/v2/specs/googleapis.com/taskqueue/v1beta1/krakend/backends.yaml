backends:
- group: taskqueue-project-taskqueues-taskqueue
  slug: taskqueue-v1beta1-project-taskqueues-taskqueue
  url_pattern: /{project}/taskqueues/{taskqueue}
  method: GET
  host:
  - https://www.googleapis.com
  url_keys:
  - project
  - taskqueue
  query_string_params:
  - getStats
  whitelist:
  - acl
  - id
  - kind
  - maxLeases
  - stats
  concurrent_calls: 0
  mapping:
    acl: acl
    id: id
    kind: kind
    maxLeases: maxLeases
    stats: stats
  extra_config:
    hosts:
    - https://www.googleapis.com
    schemes:
    - https
- group: taskqueue-project-taskqueues-taskqueue-tasks
  slug: taskqueue-v1beta1-project-taskqueues-taskqueue-tasks
  url_pattern: /{project}/taskqueues/{taskqueue}/tasks
  method: GET
  host:
  - https://www.googleapis.com
  url_keys:
  - project
  - taskqueue
  whitelist:
  - items
  - kind
  concurrent_calls: 0
  mapping:
    items: items
    kind: kind
  extra_config:
    hosts:
    - https://www.googleapis.com
    schemes:
    - https
- group: taskqueue-project-taskqueues-taskqueue-tasks-lease
  slug: taskqueue-v1beta1-project-taskqueues-taskqueue-tasks-lease
  url_pattern: /{project}/taskqueues/{taskqueue}/tasks/lease
  method: POST
  host:
  - https://www.googleapis.com
  url_keys:
  - project
  - taskqueue
  query_string_params:
  - leaseSecs
  - numTasks
  whitelist:
  - items
  - kind
  concurrent_calls: 0
  mapping:
    items: items
    kind: kind
  extra_config:
    hosts:
    - https://www.googleapis.com
    schemes:
    - https
- group: taskqueue-project-taskqueues-taskqueue-tasks-task
  slug: taskqueue-v1beta1-project-taskqueues-taskqueue-tasks-task
  url_pattern: /{project}/taskqueues/{taskqueue}/tasks/{task}
  method: GET
  host:
  - https://www.googleapis.com
  url_keys:
  - project
  - task
  - taskqueue
  whitelist:
  - enqueueTimestamp
  - id
  - kind
  - leaseTimestamp
  - payloadBase64
  - queueName
  concurrent_calls: 0
  mapping:
    enqueueTimestamp: enqueueTimestamp
    id: id
    kind: kind
    leaseTimestamp: leaseTimestamp
    payloadBase64: payloadBase64
    queueName: queueName
  extra_config:
    hosts:
    - https://www.googleapis.com
    schemes:
    - https
