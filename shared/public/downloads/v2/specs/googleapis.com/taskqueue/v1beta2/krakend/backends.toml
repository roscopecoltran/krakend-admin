[[backends]]
  group = "taskqueue-v1beta2"
  slug = "taskqueue-v1beta2-taskqueue-v1beta2-projects-project-taskqueues-taskqueue"
  url_pattern = "/taskqueue/v1beta2/projects/{project}/taskqueues/{taskqueue}"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["acl", "id", "kind", "maxLeases", "stats"]
  concurrent_calls = 1
  [backends.mapping]
    acl = "acl"
    id = "id"
    kind = "kind"
    maxLeases = "maxLeases"
    stats = "stats"
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "taskqueue-v1beta2"
  slug = "taskqueue-v1beta2-taskqueue-v1beta2-projects-project-taskqueues-taskqueue-tasks"
  url_pattern = "/taskqueue/v1beta2/projects/{project}/taskqueues/{taskqueue}/tasks"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["items", "kind"]
  concurrent_calls = 1
  [backends.mapping]
    items = "items"
    kind = "kind"
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "taskqueue-v1beta2"
  slug = "taskqueue-v1beta2-taskqueue-v1beta2-projects-project-taskqueues-taskqueue-tasks-lease"
  url_pattern = "/taskqueue/v1beta2/projects/{project}/taskqueues/{taskqueue}/tasks/lease"
  method = "POST"
  host = ["https://www.googleapis.com"]
  whitelist = ["items", "kind"]
  concurrent_calls = 1
  [backends.mapping]
    items = "items"
    kind = "kind"
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "taskqueue-v1beta2"
  slug = "taskqueue-v1beta2-taskqueue-v1beta2-projects-project-taskqueues-taskqueue-tasks-task"
  url_pattern = "/taskqueue/v1beta2/projects/{project}/taskqueues/{taskqueue}/tasks/{task}"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["enqueueTimestamp", "id", "kind", "leaseTimestamp", "payloadBase64", "queueName", "retry_count", "tag"]
  concurrent_calls = 1
  [backends.mapping]
    enqueueTimestamp = "enqueueTimestamp"
    id = "id"
    kind = "kind"
    leaseTimestamp = "leaseTimestamp"
    payloadBase64 = "payloadBase64"
    queueName = "queueName"
    retry_count = "retry_count"
    tag = "tag"
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]
