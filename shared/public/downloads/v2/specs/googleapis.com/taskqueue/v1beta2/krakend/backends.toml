[[backends]]
  group = "taskqueue-project-taskqueues-taskqueue"
  slug = "taskqueue-v1beta2-project-taskqueues-taskqueue"
  url_pattern = "/{project}/taskqueues/{taskqueue}"
  method = "GET"
  host = ["https://www.googleapis.com"]
  url_keys = ["project", "taskqueue"]
  query_string_params = ["getStats"]
  whitelist = ["id", "acl", "stats", "kind", "maxLeases"]
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "taskqueue-project-taskqueues-taskqueue-tasks"
  slug = "taskqueue-v1beta2-project-taskqueues-taskqueue-tasks"
  url_pattern = "/{project}/taskqueues/{taskqueue}/tasks"
  method = "GET"
  host = ["https://www.googleapis.com"]
  url_keys = ["project", "taskqueue"]
  whitelist = ["items", "kind"]
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "taskqueue-project-taskqueues-taskqueue-tasks-lease"
  slug = "taskqueue-v1beta2-project-taskqueues-taskqueue-tasks-lease"
  url_pattern = "/{project}/taskqueues/{taskqueue}/tasks/lease"
  method = "POST"
  host = ["https://www.googleapis.com"]
  url_keys = ["project", "taskqueue"]
  query_string_params = ["numTasks", "leaseSecs", "groupByTag", "tag"]
  whitelist = ["items", "kind"]
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "taskqueue-project-taskqueues-taskqueue-tasks-task"
  slug = "taskqueue-v1beta2-project-taskqueues-taskqueue-tasks-task"
  url_pattern = "/{project}/taskqueues/{taskqueue}/tasks/{task}"
  method = "GET"
  host = ["https://www.googleapis.com"]
  url_keys = ["project", "taskqueue", "task"]
  whitelist = ["id", "queueName", "enqueueTimestamp", "retry_count", "leaseTimestamp", "tag", "kind", "payloadBase64"]
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]
