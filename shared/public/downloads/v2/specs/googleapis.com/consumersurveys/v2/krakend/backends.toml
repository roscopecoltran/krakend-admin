[[backends]]
  group = "consumer-surveys-mobileapppanels"
  slug = "consumer-surveys-v2-mobileapppanels"
  url_pattern = "/mobileAppPanels"
  method = "GET"
  host = ["https://www.googleapis.com"]
  query_string_params = ["maxResults", "startIndex", "token"]
  whitelist = ["requestId", "resources"]
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "consumer-surveys-mobileapppanels-panelid"
  slug = "consumer-surveys-v2-mobileapppanels-panelid"
  url_pattern = "/mobileAppPanels/{panelId}"
  method = "GET"
  host = ["https://www.googleapis.com"]
  url_keys = ["panelId"]
  whitelist = ["mobileAppPanelId", "country", "isPublicPanel", "owners", "name", "language"]
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "consumer-surveys-surveys"
  slug = "consumer-surveys-v2-surveys"
  url_pattern = "/surveys"
  method = "GET"
  host = ["https://www.googleapis.com"]
  query_string_params = ["maxResults", "startIndex", "token"]
  whitelist = ["requestId", "resources"]
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "consumer-surveys-surveys-resourceid-start"
  slug = "consumer-surveys-v2-surveys-resourceid-start"
  url_pattern = "/surveys/{resourceId}/start"
  method = "POST"
  host = ["https://www.googleapis.com"]
  url_keys = ["resourceId"]
  whitelist = ["requestId"]
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "consumer-surveys-surveys-resourceid-stop"
  slug = "consumer-surveys-v2-surveys-resourceid-stop"
  url_pattern = "/surveys/{resourceId}/stop"
  method = "POST"
  host = ["https://www.googleapis.com"]
  url_keys = ["resourceId"]
  whitelist = ["requestId"]
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "consumer-surveys-surveys-surveyurlid"
  slug = "consumer-surveys-v2-surveys-surveyurlid"
  url_pattern = "/surveys/{surveyUrlId}"
  method = "GET"
  host = ["https://www.googleapis.com"]
  url_keys = ["surveyUrlId"]
  whitelist = ["owners", "wantedResponseCount", "customerData", "title", "state", "questions", "description", "surveyUrlId"]
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "consumer-surveys-surveys-surveyurlid-results"
  slug = "consumer-surveys-v2-surveys-surveyurlid-results"
  url_pattern = "/surveys/{surveyUrlId}/results"
  method = "GET"
  host = ["https://www.googleapis.com"]
  url_keys = ["surveyUrlId"]
  whitelist = ["status", "surveyUrlId"]
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]
