[[backends]]
  group = "google-play-game-services-publishing-v1configuration"
  url_pattern = "/games/v1configuration/achievements/{achievementId}"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["achievementType", "id", "initialState", "kind", "stepsToUnlock", "token"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "The Publishing API for Google Play Game Services."
    version = "v1configuration"
    slug = "google-play-game-services-publishing-v1configuration-games-v1configuration-achievements-achievementid"
    schemes = ["https"]
    hosts = ["https://www.googleapis.com"]
    [backends.extra_config.mapping]
      achievementType = "achievementType"
      id = "id"
      initialState = "initialState"
      kind = "kind"
      stepsToUnlock = "stepsToUnlock"
      token = "token"

[[backends]]
  group = "google-play-game-services-publishing-v1configuration"
  url_pattern = "/games/v1configuration/applications/{applicationId}/achievements"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["items", "kind", "nextPageToken"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "The Publishing API for Google Play Game Services."
    version = "v1configuration"
    slug = "google-play-game-services-publishing-v1configuration-games-v1configuration-applications-applicationid-achievements"
    schemes = ["https"]
    hosts = ["https://www.googleapis.com"]
    [backends.extra_config.mapping]
      items = "items"
      kind = "kind"
      nextPageToken = "nextPageToken"

[[backends]]
  group = "google-play-game-services-publishing-v1configuration"
  url_pattern = "/games/v1configuration/applications/{applicationId}/leaderboards"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["items", "kind", "nextPageToken"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "The Publishing API for Google Play Game Services."
    version = "v1configuration"
    slug = "google-play-game-services-publishing-v1configuration-games-v1configuration-applications-applicationid-leaderboards"
    schemes = ["https"]
    hosts = ["https://www.googleapis.com"]
    [backends.extra_config.mapping]
      items = "items"
      kind = "kind"
      nextPageToken = "nextPageToken"

[[backends]]
  group = "google-play-game-services-publishing-v1configuration"
  url_pattern = "/games/v1configuration/images/{resourceId}/imageType/{imageType}"
  method = "POST"
  host = ["https://www.googleapis.com"]
  whitelist = ["imageType", "kind", "resourceId", "url"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "The Publishing API for Google Play Game Services."
    version = "v1configuration"
    slug = "google-play-game-services-publishing-v1configuration-games-v1configuration-images-resourceid-imagetype-imagetype"
    schemes = ["https"]
    hosts = ["https://www.googleapis.com"]
    [backends.extra_config.mapping]
      imageType = "imageType"
      kind = "kind"
      resourceId = "resourceId"
      url = "url"

[[backends]]
  group = "google-play-game-services-publishing-v1configuration"
  url_pattern = "/games/v1configuration/leaderboards/{leaderboardId}"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["id", "kind", "scoreMax", "scoreMin", "scoreOrder", "token"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "The Publishing API for Google Play Game Services."
    version = "v1configuration"
    slug = "google-play-game-services-publishing-v1configuration-games-v1configuration-leaderboards-leaderboardid"
    schemes = ["https"]
    hosts = ["https://www.googleapis.com"]
    [backends.extra_config.mapping]
      id = "id"
      kind = "kind"
      scoreMax = "scoreMax"
      scoreMin = "scoreMin"
      scoreOrder = "scoreOrder"
      token = "token"
