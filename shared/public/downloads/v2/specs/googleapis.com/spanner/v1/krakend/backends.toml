[[backends]]
  group = "cloud-spanner-v1"
  url_pattern = "/v1/{database}/ddl"
  method = "GET"
  host = ["https://spanner.googleapis.com"]
  url_keys = ["database"]
  whitelist = ["statements"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Cloud Spanner is a managed, mission-critical, globally consistent and scalable relational database service."
    version = "v1"
    slug = "cloud-spanner-v1-v1-database-ddl"
    schemes = ["https"]
    hosts = ["https://spanner.googleapis.com"]
    topics = ["projects"]
    [backends.extra_config.mapping]
      statements = "statements"

[[backends]]
  group = "cloud-spanner-v1"
  url_pattern = "/v1/{database}/sessions"
  method = "GET"
  host = ["https://spanner.googleapis.com"]
  url_keys = ["database"]
  query_string_params = ["filter", "pageSize", "pageToken"]
  whitelist = ["nextPageToken", "sessions"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Cloud Spanner is a managed, mission-critical, globally consistent and scalable relational database service."
    version = "v1"
    slug = "cloud-spanner-v1-v1-database-sessions"
    schemes = ["https"]
    hosts = ["https://spanner.googleapis.com"]
    topics = ["projects"]
    [backends.extra_config.mapping]
      nextPageToken = "nextPageToken"
      sessions = "sessions"

[[backends]]
  group = "cloud-spanner-v1"
  url_pattern = "/v1/{name}"
  method = "GET"
  host = ["https://spanner.googleapis.com"]
  url_keys = ["name"]
  whitelist = ["approximateLastUseTime", "createTime", "labels", "labels.additionalProperties", "name"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Cloud Spanner is a managed, mission-critical, globally consistent and scalable relational database service."
    version = "v1"
    slug = "cloud-spanner-v1-v1-name"
    schemes = ["https"]
    hosts = ["https://spanner.googleapis.com"]
    topics = ["projects"]
    [backends.extra_config.mapping]
      additionalProperties = "additionalProperties"
      approximateLastUseTime = "approximateLastUseTime"
      createTime = "createTime"
      labels = "labels"
      name = "name"

[[backends]]
  group = "cloud-spanner-v1"
  url_pattern = "/v1/{name}:cancel"
  method = "POST"
  host = ["https://spanner.googleapis.com"]
  url_keys = ["name"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Cloud Spanner is a managed, mission-critical, globally consistent and scalable relational database service."
    version = "v1"
    slug = "cloud-spanner-v1-v1-name-cancel"
    schemes = ["https"]
    hosts = ["https://spanner.googleapis.com"]
    topics = ["projects"]

[[backends]]
  group = "cloud-spanner-v1"
  url_pattern = "/v1/{parent}/databases"
  method = "GET"
  host = ["https://spanner.googleapis.com"]
  url_keys = ["parent"]
  query_string_params = ["pageSize", "pageToken"]
  whitelist = ["databases", "nextPageToken"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Cloud Spanner is a managed, mission-critical, globally consistent and scalable relational database service."
    version = "v1"
    slug = "cloud-spanner-v1-v1-parent-databases"
    schemes = ["https"]
    hosts = ["https://spanner.googleapis.com"]
    topics = ["projects"]
    [backends.extra_config.mapping]
      databases = "databases"
      nextPageToken = "nextPageToken"

[[backends]]
  group = "cloud-spanner-v1"
  url_pattern = "/v1/{parent}/instanceConfigs"
  method = "GET"
  host = ["https://spanner.googleapis.com"]
  url_keys = ["parent"]
  query_string_params = ["pageSize", "pageToken"]
  whitelist = ["instanceConfigs", "nextPageToken"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Cloud Spanner is a managed, mission-critical, globally consistent and scalable relational database service."
    version = "v1"
    slug = "cloud-spanner-v1-v1-parent-instanceconfigs"
    schemes = ["https"]
    hosts = ["https://spanner.googleapis.com"]
    topics = ["projects"]
    [backends.extra_config.mapping]
      instanceConfigs = "instanceConfigs"
      nextPageToken = "nextPageToken"

[[backends]]
  group = "cloud-spanner-v1"
  url_pattern = "/v1/{parent}/instances"
  method = "GET"
  host = ["https://spanner.googleapis.com"]
  url_keys = ["parent"]
  query_string_params = ["filter", "pageSize", "pageToken"]
  whitelist = ["instances", "nextPageToken"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Cloud Spanner is a managed, mission-critical, globally consistent and scalable relational database service."
    version = "v1"
    slug = "cloud-spanner-v1-v1-parent-instances"
    schemes = ["https"]
    hosts = ["https://spanner.googleapis.com"]
    topics = ["projects"]
    [backends.extra_config.mapping]
      instances = "instances"
      nextPageToken = "nextPageToken"

[[backends]]
  group = "cloud-spanner-v1"
  url_pattern = "/v1/{resource}:getIamPolicy"
  method = "POST"
  host = ["https://spanner.googleapis.com"]
  url_keys = ["resource"]
  whitelist = ["bindings", "etag", "version"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Cloud Spanner is a managed, mission-critical, globally consistent and scalable relational database service."
    version = "v1"
    slug = "cloud-spanner-v1-v1-resource-getiampolicy"
    schemes = ["https"]
    hosts = ["https://spanner.googleapis.com"]
    topics = ["projects"]
    [backends.extra_config.mapping]
      bindings = "bindings"
      etag = "etag"
      version = "version"

[[backends]]
  group = "cloud-spanner-v1"
  url_pattern = "/v1/{resource}:setIamPolicy"
  method = "POST"
  host = ["https://spanner.googleapis.com"]
  url_keys = ["resource"]
  whitelist = ["bindings", "etag", "version"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Cloud Spanner is a managed, mission-critical, globally consistent and scalable relational database service."
    version = "v1"
    slug = "cloud-spanner-v1-v1-resource-setiampolicy"
    schemes = ["https"]
    hosts = ["https://spanner.googleapis.com"]
    topics = ["projects"]
    [backends.extra_config.mapping]
      bindings = "bindings"
      etag = "etag"
      version = "version"

[[backends]]
  group = "cloud-spanner-v1"
  url_pattern = "/v1/{resource}:testIamPermissions"
  method = "POST"
  host = ["https://spanner.googleapis.com"]
  url_keys = ["resource"]
  whitelist = ["permissions"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Cloud Spanner is a managed, mission-critical, globally consistent and scalable relational database service."
    version = "v1"
    slug = "cloud-spanner-v1-v1-resource-testiampermissions"
    schemes = ["https"]
    hosts = ["https://spanner.googleapis.com"]
    topics = ["projects"]
    [backends.extra_config.mapping]
      permissions = "permissions"

[[backends]]
  group = "cloud-spanner-v1"
  url_pattern = "/v1/{session}:beginTransaction"
  method = "POST"
  host = ["https://spanner.googleapis.com"]
  url_keys = ["session"]
  whitelist = ["id", "readTimestamp"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Cloud Spanner is a managed, mission-critical, globally consistent and scalable relational database service."
    version = "v1"
    slug = "cloud-spanner-v1-v1-session-begintransaction"
    schemes = ["https"]
    hosts = ["https://spanner.googleapis.com"]
    topics = ["projects"]
    [backends.extra_config.mapping]
      id = "id"
      readTimestamp = "readTimestamp"

[[backends]]
  group = "cloud-spanner-v1"
  url_pattern = "/v1/{session}:commit"
  method = "POST"
  host = ["https://spanner.googleapis.com"]
  url_keys = ["session"]
  whitelist = ["commitTimestamp"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Cloud Spanner is a managed, mission-critical, globally consistent and scalable relational database service."
    version = "v1"
    slug = "cloud-spanner-v1-v1-session-commit"
    schemes = ["https"]
    hosts = ["https://spanner.googleapis.com"]
    topics = ["projects"]
    [backends.extra_config.mapping]
      commitTimestamp = "commitTimestamp"

[[backends]]
  group = "cloud-spanner-v1"
  url_pattern = "/v1/{session}:executeSql"
  method = "POST"
  host = ["https://spanner.googleapis.com"]
  url_keys = ["session"]
  whitelist = ["rows"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Cloud Spanner is a managed, mission-critical, globally consistent and scalable relational database service."
    version = "v1"
    slug = "cloud-spanner-v1-v1-session-executesql"
    schemes = ["https"]
    hosts = ["https://spanner.googleapis.com"]
    topics = ["projects"]
    [backends.extra_config.mapping]
      rows = "rows"

[[backends]]
  group = "cloud-spanner-v1"
  url_pattern = "/v1/{session}:executeStreamingSql"
  method = "POST"
  host = ["https://spanner.googleapis.com"]
  url_keys = ["session"]
  whitelist = ["chunkedValue", "resumeToken", "values"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Cloud Spanner is a managed, mission-critical, globally consistent and scalable relational database service."
    version = "v1"
    slug = "cloud-spanner-v1-v1-session-executestreamingsql"
    schemes = ["https"]
    hosts = ["https://spanner.googleapis.com"]
    topics = ["projects"]
    [backends.extra_config.mapping]
      chunkedValue = "chunkedValue"
      resumeToken = "resumeToken"
      values = "values"

[[backends]]
  group = "cloud-spanner-v1"
  url_pattern = "/v1/{session}:read"
  method = "POST"
  host = ["https://spanner.googleapis.com"]
  url_keys = ["session"]
  whitelist = ["rows"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Cloud Spanner is a managed, mission-critical, globally consistent and scalable relational database service."
    version = "v1"
    slug = "cloud-spanner-v1-v1-session-read"
    schemes = ["https"]
    hosts = ["https://spanner.googleapis.com"]
    topics = ["projects"]
    [backends.extra_config.mapping]
      rows = "rows"

[[backends]]
  group = "cloud-spanner-v1"
  url_pattern = "/v1/{session}:rollback"
  method = "POST"
  host = ["https://spanner.googleapis.com"]
  url_keys = ["session"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Cloud Spanner is a managed, mission-critical, globally consistent and scalable relational database service."
    version = "v1"
    slug = "cloud-spanner-v1-v1-session-rollback"
    schemes = ["https"]
    hosts = ["https://spanner.googleapis.com"]
    topics = ["projects"]

[[backends]]
  group = "cloud-spanner-v1"
  url_pattern = "/v1/{session}:streamingRead"
  method = "POST"
  host = ["https://spanner.googleapis.com"]
  url_keys = ["session"]
  whitelist = ["chunkedValue", "resumeToken", "values"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "googleapis.com"
    description = "Cloud Spanner is a managed, mission-critical, globally consistent and scalable relational database service."
    version = "v1"
    slug = "cloud-spanner-v1-v1-session-streamingread"
    schemes = ["https"]
    hosts = ["https://spanner.googleapis.com"]
    topics = ["projects"]
    [backends.extra_config.mapping]
      chunkedValue = "chunkedValue"
      resumeToken = "resumeToken"
      values = "values"
