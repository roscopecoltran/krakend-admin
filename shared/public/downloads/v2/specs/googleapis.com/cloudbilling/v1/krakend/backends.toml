[[backends]]
  group = "google-cloud-billing"
  slug = "google-cloud-billing-v1"
  url_pattern = "/v1/billingAccounts"
  method = "GET"
  host = ["https://cloudbilling.googleapis.com"]
  query_string_params = ["pageSize", "pageToken"]
  whitelist = ["nextPageToken", "billingAccounts"]
  [backends.extra_config]
    hosts = ["https://cloudbilling.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "google-cloud-billing"
  slug = "google-cloud-billing-v1"
  url_pattern = "/v1/services"
  method = "GET"
  host = ["https://cloudbilling.googleapis.com"]
  query_string_params = ["pageSize", "pageToken"]
  whitelist = ["services", "nextPageToken"]
  [backends.extra_config]
    hosts = ["https://cloudbilling.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "google-cloud-billing"
  slug = "google-cloud-billing-v1"
  url_pattern = "/v1/{name}"
  method = "GET"
  host = ["https://cloudbilling.googleapis.com"]
  url_keys = ["name"]
  whitelist = ["name", "displayName", "open"]
  [backends.extra_config]
    hosts = ["https://cloudbilling.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "google-cloud-billing"
  slug = "google-cloud-billing-v1"
  url_pattern = "/v1/{name}/billingInfo"
  method = "GET"
  host = ["https://cloudbilling.googleapis.com"]
  url_keys = ["name"]
  whitelist = ["billingEnabled", "name", "projectId", "billingAccountName"]
  [backends.extra_config]
    hosts = ["https://cloudbilling.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "google-cloud-billing"
  slug = "google-cloud-billing-v1"
  url_pattern = "/v1/{name}/projects"
  method = "GET"
  host = ["https://cloudbilling.googleapis.com"]
  url_keys = ["name"]
  query_string_params = ["pageSize", "pageToken"]
  whitelist = ["projectBillingInfo", "nextPageToken"]
  [backends.extra_config]
    hosts = ["https://cloudbilling.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "google-cloud-billing"
  slug = "google-cloud-billing-v1"
  url_pattern = "/v1/{parent}/skus"
  method = "GET"
  host = ["https://cloudbilling.googleapis.com"]
  url_keys = ["parent"]
  query_string_params = ["currencyCode", "endTime", "pageSize", "pageToken", "startTime"]
  whitelist = ["skus", "nextPageToken"]
  [backends.extra_config]
    hosts = ["https://cloudbilling.googleapis.com"]
    schemes = ["https"]
