[[backends]]
  group = "url-shortener-urlshortener-v1-url"
  slug = "url-shortener-v1-urlshortener-v1-url"
  url_pattern = "/urlshortener/v1/url"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["created", "id", "kind", "longUrl", "status"]
  concurrent_calls = 1
  [backends.mapping]
    created = "created"
    id = "id"
    kind = "kind"
    longUrl = "longUrl"
    status = "status"
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]

[[backends]]
  group = "url-shortener-urlshortener-v1-url-history"
  slug = "url-shortener-v1-urlshortener-v1-url-history"
  url_pattern = "/urlshortener/v1/url/history"
  method = "GET"
  host = ["https://www.googleapis.com"]
  whitelist = ["items", "itemsPerPage", "kind", "nextPageToken", "totalItems"]
  concurrent_calls = 1
  [backends.mapping]
    items = "items"
    itemsPerPage = "itemsPerPage"
    kind = "kind"
    nextPageToken = "nextPageToken"
    totalItems = "totalItems"
  [backends.extra_config]
    hosts = ["https://www.googleapis.com"]
    schemes = ["https"]
