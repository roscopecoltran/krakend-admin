backends:
- group: google-apps-script-execution
  slug: google-apps-script-execution-v1
  url_pattern: /v1/scripts/{scriptId}:run
  method: POST
  host:
  - https://script.googleapis.com
  url_keys:
  - scriptId
  whitelist:
  - metadata
  - done
  - response
  mapping: {}
  extra_config:
    hosts:
    - https://script.googleapis.com
    schemes:
    - https
