backends:
- group: bigquery-data-transfer-v1
  url_pattern: /v1/{name}
  method: GET
  host:
  - https://bigquerydatatransfer.googleapis.com
  url_keys:
  - name
  whitelist:
  - authorizationType
  - clientId
  - dataRefreshType
  - dataSourceId
  - defaultDataRefreshWindowDays
  - defaultSchedule
  - description
  - displayName
  - helpUrl
  - manualRunsDisabled
  - minimumScheduleInterval
  - name
  - parameters
  - scopes
  - supportsCustomSchedule
  - supportsMultipleTransfers
  - transferType
  - updateDeadlineSeconds
  concurrent_calls: 1
  extra_config:
    provider: googleapis.com
    description: Transfers data from partner SaaS applications to Google BigQuery
      on a scheduled, managed basis.
    version: v1
    slug: bigquery-data-transfer-v1-v1-name
    schemes:
    - https
    mapping:
      authorizationType: authorizationType
      clientId: clientId
      dataRefreshType: dataRefreshType
      dataSourceId: dataSourceId
      defaultDataRefreshWindowDays: defaultDataRefreshWindowDays
      defaultSchedule: defaultSchedule
      description: description
      displayName: displayName
      helpUrl: helpUrl
      manualRunsDisabled: manualRunsDisabled
      minimumScheduleInterval: minimumScheduleInterval
      name: name
      parameters: parameters
      scopes: scopes
      supportsCustomSchedule: supportsCustomSchedule
      supportsMultipleTransfers: supportsMultipleTransfers
      transferType: transferType
      updateDeadlineSeconds: updateDeadlineSeconds
    hosts:
    - https://bigquerydatatransfer.googleapis.com
    topics:
    - projects
- group: bigquery-data-transfer-v1
  url_pattern: /v1/{name}/locations
  method: GET
  host:
  - https://bigquerydatatransfer.googleapis.com
  url_keys:
  - name
  query_string_params:
  - filter
  - pageSize
  - pageToken
  whitelist:
  - locations
  - nextPageToken
  concurrent_calls: 1
  extra_config:
    provider: googleapis.com
    description: Transfers data from partner SaaS applications to Google BigQuery
      on a scheduled, managed basis.
    version: v1
    slug: bigquery-data-transfer-v1-v1-name-locations
    schemes:
    - https
    mapping:
      locations: locations
      nextPageToken: nextPageToken
    hosts:
    - https://bigquerydatatransfer.googleapis.com
    topics:
    - projects
- group: bigquery-data-transfer-v1
  url_pattern: /v1/{name}:checkValidCreds
  method: POST
  host:
  - https://bigquerydatatransfer.googleapis.com
  url_keys:
  - name
  whitelist:
  - hasValidCreds
  concurrent_calls: 1
  extra_config:
    provider: googleapis.com
    description: Transfers data from partner SaaS applications to Google BigQuery
      on a scheduled, managed basis.
    version: v1
    slug: bigquery-data-transfer-v1-v1-name-checkvalidcreds
    schemes:
    - https
    mapping:
      hasValidCreds: hasValidCreds
    hosts:
    - https://bigquerydatatransfer.googleapis.com
    topics:
    - projects
- group: bigquery-data-transfer-v1
  url_pattern: /v1/{parent}/dataSources
  method: GET
  host:
  - https://bigquerydatatransfer.googleapis.com
  url_keys:
  - parent
  query_string_params:
  - pageSize
  - pageToken
  whitelist:
  - dataSources
  - nextPageToken
  concurrent_calls: 1
  extra_config:
    provider: googleapis.com
    description: Transfers data from partner SaaS applications to Google BigQuery
      on a scheduled, managed basis.
    version: v1
    slug: bigquery-data-transfer-v1-v1-parent-datasources
    schemes:
    - https
    mapping:
      dataSources: dataSources
      nextPageToken: nextPageToken
    hosts:
    - https://bigquerydatatransfer.googleapis.com
    topics:
    - projects
- group: bigquery-data-transfer-v1
  url_pattern: /v1/{parent}/runs
  method: GET
  host:
  - https://bigquerydatatransfer.googleapis.com
  url_keys:
  - parent
  query_string_params:
  - pageSize
  - pageToken
  - runAttempt
  - states
  whitelist:
  - nextPageToken
  - transferRuns
  concurrent_calls: 1
  extra_config:
    provider: googleapis.com
    description: Transfers data from partner SaaS applications to Google BigQuery
      on a scheduled, managed basis.
    version: v1
    slug: bigquery-data-transfer-v1-v1-parent-runs
    schemes:
    - https
    mapping:
      nextPageToken: nextPageToken
      transferRuns: transferRuns
    hosts:
    - https://bigquerydatatransfer.googleapis.com
    topics:
    - projects
- group: bigquery-data-transfer-v1
  url_pattern: /v1/{parent}/transferConfigs
  method: GET
  host:
  - https://bigquerydatatransfer.googleapis.com
  url_keys:
  - parent
  query_string_params:
  - dataSourceIds
  - pageSize
  - pageToken
  whitelist:
  - nextPageToken
  - transferConfigs
  concurrent_calls: 1
  extra_config:
    provider: googleapis.com
    description: Transfers data from partner SaaS applications to Google BigQuery
      on a scheduled, managed basis.
    version: v1
    slug: bigquery-data-transfer-v1-v1-parent-transferconfigs
    schemes:
    - https
    mapping:
      nextPageToken: nextPageToken
      transferConfigs: transferConfigs
    hosts:
    - https://bigquerydatatransfer.googleapis.com
    topics:
    - projects
- group: bigquery-data-transfer-v1
  url_pattern: /v1/{parent}/transferLogs
  method: GET
  host:
  - https://bigquerydatatransfer.googleapis.com
  url_keys:
  - parent
  query_string_params:
  - messageTypes
  - pageSize
  - pageToken
  whitelist:
  - nextPageToken
  - transferMessages
  concurrent_calls: 1
  extra_config:
    provider: googleapis.com
    description: Transfers data from partner SaaS applications to Google BigQuery
      on a scheduled, managed basis.
    version: v1
    slug: bigquery-data-transfer-v1-v1-parent-transferlogs
    schemes:
    - https
    mapping:
      nextPageToken: nextPageToken
      transferMessages: transferMessages
    hosts:
    - https://bigquerydatatransfer.googleapis.com
    topics:
    - projects
- group: bigquery-data-transfer-v1
  url_pattern: /v1/{parent}:scheduleRuns
  method: POST
  host:
  - https://bigquerydatatransfer.googleapis.com
  url_keys:
  - parent
  whitelist:
  - runs
  concurrent_calls: 1
  extra_config:
    provider: googleapis.com
    description: Transfers data from partner SaaS applications to Google BigQuery
      on a scheduled, managed basis.
    version: v1
    slug: bigquery-data-transfer-v1-v1-parent-scheduleruns
    schemes:
    - https
    mapping:
      runs: runs
    hosts:
    - https://bigquerydatatransfer.googleapis.com
    topics:
    - projects
