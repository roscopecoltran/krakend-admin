[[backends]]
  group = "discovery-market-research-0-1"
  url_pattern = "/api/contracts/"
  method = "GET"
  host = ["http://discovery-prod.app.cloud.gov"]
  query_string_params = ["direction", "duns", "naics", "page", "sort"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "cloud.gov"
    description = "<p>This API drives the <a href=\"https://discovery.gsa.gov\">Discovery Market Research Tool</a>.\nIt contains information on the vendors that are part of the OASIS and OASIS Small Business contracting vehicles, such as their contracting history, their elligibility for contract awards, and their small business designations.\nTo learn more about the tool, please visit <a href=\"https://discovery.gsa.gov\">Discovery</a> or see the README on our <a href=\"https://github.com/18F/mirage\">GitHub repository</a>.</p>\n<p><strong>Please note that the base path for this API is <code>https://api.data.gov/gsa/discovery/</code></strong></p>\n<p>It requires an API key, obtainable at <a href=\"http://api.data.gov/\">api.data.gov</a>.\nIt must be passed in the <code>api_key</code> parameter with each request.</p>"
    version = "0.1"
    slug = "discovery-market-research-0-1-api-contracts"
    schemes = ["http"]
    hosts = ["http://discovery-prod.app.cloud.gov"]
    topics = ["contracts", "open-data"]

[[backends]]
  group = "discovery-market-research-0-1"
  url_pattern = "/api/metadata/"
  method = "GET"
  host = ["http://discovery-prod.app.cloud.gov"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "cloud.gov"
    description = "<p>This API drives the <a href=\"https://discovery.gsa.gov\">Discovery Market Research Tool</a>.\nIt contains information on the vendors that are part of the OASIS and OASIS Small Business contracting vehicles, such as their contracting history, their elligibility for contract awards, and their small business designations.\nTo learn more about the tool, please visit <a href=\"https://discovery.gsa.gov\">Discovery</a> or see the README on our <a href=\"https://github.com/18F/mirage\">GitHub repository</a>.</p>\n<p><strong>Please note that the base path for this API is <code>https://api.data.gov/gsa/discovery/</code></strong></p>\n<p>It requires an API key, obtainable at <a href=\"http://api.data.gov/\">api.data.gov</a>.\nIt must be passed in the <code>api_key</code> parameter with each request.</p>"
    version = "0.1"
    slug = "discovery-market-research-0-1-api-metadata"
    schemes = ["http"]
    hosts = ["http://discovery-prod.app.cloud.gov"]
    topics = ["metadata", "open-data"]

[[backends]]
  group = "discovery-market-research-0-1"
  url_pattern = "/api/naics/"
  method = "GET"
  host = ["http://discovery-prod.app.cloud.gov"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "cloud.gov"
    description = "<p>This API drives the <a href=\"https://discovery.gsa.gov\">Discovery Market Research Tool</a>.\nIt contains information on the vendors that are part of the OASIS and OASIS Small Business contracting vehicles, such as their contracting history, their elligibility for contract awards, and their small business designations.\nTo learn more about the tool, please visit <a href=\"https://discovery.gsa.gov\">Discovery</a> or see the README on our <a href=\"https://github.com/18F/mirage\">GitHub repository</a>.</p>\n<p><strong>Please note that the base path for this API is <code>https://api.data.gov/gsa/discovery/</code></strong></p>\n<p>It requires an API key, obtainable at <a href=\"http://api.data.gov/\">api.data.gov</a>.\nIt must be passed in the <code>api_key</code> parameter with each request.</p>"
    version = "0.1"
    slug = "discovery-market-research-0-1-api-naics"
    schemes = ["http"]
    hosts = ["http://discovery-prod.app.cloud.gov"]
    topics = ["naics", "open-data"]

[[backends]]
  group = "discovery-market-research-0-1"
  url_pattern = "/api/vendor/{duns}"
  method = "GET"
  host = ["http://discovery-prod.app.cloud.gov"]
  url_keys = ["duns"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "cloud.gov"
    description = "<p>This API drives the <a href=\"https://discovery.gsa.gov\">Discovery Market Research Tool</a>.\nIt contains information on the vendors that are part of the OASIS and OASIS Small Business contracting vehicles, such as their contracting history, their elligibility for contract awards, and their small business designations.\nTo learn more about the tool, please visit <a href=\"https://discovery.gsa.gov\">Discovery</a> or see the README on our <a href=\"https://github.com/18F/mirage\">GitHub repository</a>.</p>\n<p><strong>Please note that the base path for this API is <code>https://api.data.gov/gsa/discovery/</code></strong></p>\n<p>It requires an API key, obtainable at <a href=\"http://api.data.gov/\">api.data.gov</a>.\nIt must be passed in the <code>api_key</code> parameter with each request.</p>"
    version = "0.1"
    slug = "discovery-market-research-0-1-api-vendor-duns"
    schemes = ["http"]
    hosts = ["http://discovery-prod.app.cloud.gov"]
    topics = ["open-data", "vendor"]

[[backends]]
  group = "discovery-market-research-0-1"
  url_pattern = "/api/vendors/"
  method = "GET"
  host = ["http://discovery-prod.app.cloud.gov"]
  query_string_params = ["naics", "setasides", "vehicle"]
  concurrent_calls = 1
  [backends.extra_config]
    provider = "cloud.gov"
    description = "<p>This API drives the <a href=\"https://discovery.gsa.gov\">Discovery Market Research Tool</a>.\nIt contains information on the vendors that are part of the OASIS and OASIS Small Business contracting vehicles, such as their contracting history, their elligibility for contract awards, and their small business designations.\nTo learn more about the tool, please visit <a href=\"https://discovery.gsa.gov\">Discovery</a> or see the README on our <a href=\"https://github.com/18F/mirage\">GitHub repository</a>.</p>\n<p><strong>Please note that the base path for this API is <code>https://api.data.gov/gsa/discovery/</code></strong></p>\n<p>It requires an API key, obtainable at <a href=\"http://api.data.gov/\">api.data.gov</a>.\nIt must be passed in the <code>api_key</code> parameter with each request.</p>"
    version = "0.1"
    slug = "discovery-market-research-0-1-api-vendors"
    schemes = ["http"]
    hosts = ["http://discovery-prod.app.cloud.gov"]
    topics = ["open-data", "vendors"]
