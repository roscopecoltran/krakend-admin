backends:
- group: scim-n-a
  url_pattern: /identity/v1/Groups
  method: GET
  encoding: json
  host:
  - https://api.citrixonline.com
  whitelist:
  - resources
  - totalResults
  concurrent_calls: 1
  extra_config:
    provider: citrixonline.com
    description: The SCIM API lets you manage users in your organization. You can
      then automate the provisioning of product licenses for these users, and they
      can use your company's Single Sign-On solution through an Identity Provider.
    version: N/A
    slug: scim-n-a-identity-v1-groups
    schemes:
    - https
    consumes:
    - json
    produces:
    - json
    mapping:
      resources: resources
      totalResults: totalResults
    hosts:
    - https://api.citrixonline.com
- group: scim-n-a
  url_pattern: /identity/v1/Groups/{groupKey}
  method: GET
  encoding: json
  host:
  - https://api.citrixonline.com
  whitelist:
  - displayName
  - id
  - members
  concurrent_calls: 1
  extra_config:
    provider: citrixonline.com
    description: The SCIM API lets you manage users in your organization. You can
      then automate the provisioning of product licenses for these users, and they
      can use your company's Single Sign-On solution through an Identity Provider.
    version: N/A
    slug: scim-n-a-identity-v1-groups-groupkey
    schemes:
    - https
    consumes:
    - json
    produces:
    - json
    mapping:
      displayName: displayName
      id: id
      members: members
    hosts:
    - https://api.citrixonline.com
- group: scim-n-a
  url_pattern: /identity/v1/Schemas/Users
  method: GET
  encoding: json
  host:
  - https://api.citrixonline.com
  whitelist:
  - attributes
  - description
  - endpoint
  - id
  - name
  - schema
  concurrent_calls: 1
  extra_config:
    provider: citrixonline.com
    description: The SCIM API lets you manage users in your organization. You can
      then automate the provisioning of product licenses for these users, and they
      can use your company's Single Sign-On solution through an Identity Provider.
    version: N/A
    slug: scim-n-a-identity-v1-schemas-users
    schemes:
    - https
    consumes:
    - json
    produces:
    - json
    mapping:
      attributes: attributes
      description: description
      endpoint: endpoint
      id: id
      name: name
      schema: schema
    hosts:
    - https://api.citrixonline.com
- group: scim-n-a
  url_pattern: /identity/v1/ServiceProviderConfigs
  method: GET
  encoding: json
  host:
  - https://api.citrixonline.com
  whitelist:
  - documentationUrl
  concurrent_calls: 1
  extra_config:
    provider: citrixonline.com
    description: The SCIM API lets you manage users in your organization. You can
      then automate the provisioning of product licenses for these users, and they
      can use your company's Single Sign-On solution through an Identity Provider.
    version: N/A
    slug: scim-n-a-identity-v1-serviceproviderconfigs
    schemes:
    - https
    consumes:
    - json
    produces:
    - json
    mapping:
      documentationUrl: documentationUrl
    hosts:
    - https://api.citrixonline.com
- group: scim-n-a
  url_pattern: /identity/v1/Users
  method: GET
  encoding: json
  host:
  - https://api.citrixonline.com
  whitelist:
  - resources
  - totalResults
  concurrent_calls: 1
  extra_config:
    provider: citrixonline.com
    description: The SCIM API lets you manage users in your organization. You can
      then automate the provisioning of product licenses for these users, and they
      can use your company's Single Sign-On solution through an Identity Provider.
    version: N/A
    slug: scim-n-a-identity-v1-users
    schemes:
    - https
    consumes:
    - json
    produces:
    - json
    mapping:
      resources: resources
      totalResults: totalResults
    hosts:
    - https://api.citrixonline.com
- group: scim-n-a
  url_pattern: /identity/v1/Users/me
  method: GET
  encoding: json
  host:
  - https://api.citrixonline.com
  whitelist:
  - displayName
  - id
  - locale
  - timezone
  - userName
  concurrent_calls: 1
  extra_config:
    provider: citrixonline.com
    description: The SCIM API lets you manage users in your organization. You can
      then automate the provisioning of product licenses for these users, and they
      can use your company's Single Sign-On solution through an Identity Provider.
    version: N/A
    slug: scim-n-a-identity-v1-users-me
    schemes:
    - https
    consumes:
    - json
    produces:
    - json
    mapping:
      displayName: displayName
      id: id
      locale: locale
      timezone: timezone
      userName: userName
    hosts:
    - https://api.citrixonline.com
- group: scim-n-a
  url_pattern: /identity/v1/Users/{userKey}
  method: GET
  encoding: json
  host:
  - https://api.citrixonline.com
  whitelist:
  - displayName
  - id
  - locale
  - timezone
  - userName
  concurrent_calls: 1
  extra_config:
    provider: citrixonline.com
    description: The SCIM API lets you manage users in your organization. You can
      then automate the provisioning of product licenses for these users, and they
      can use your company's Single Sign-On solution through an Identity Provider.
    version: N/A
    slug: scim-n-a-identity-v1-users-userkey
    schemes:
    - https
    consumes:
    - json
    produces:
    - json
    mapping:
      displayName: displayName
      id: id
      locale: locale
      timezone: timezone
      userName: userName
    hosts:
    - https://api.citrixonline.com
