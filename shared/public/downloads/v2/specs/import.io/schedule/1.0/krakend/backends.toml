[[backends]]
  group = "import-io-extractor"
  slug = "import-io-1-0-extractor"
  url_pattern = "/extractor"
  method = "GET"
  encoding = "json"
  host = ["https://schedule.import.io"]
  whitelist = ["extractorId", "intervalData", "ownerId", "interval", "startTimestamp", "nextRunAt"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json;charset=UTF-8"]
    hosts = ["https://schedule.import.io"]
    schemes = ["https"]

[[backends]]
  group = "import-io-extractor-extractorid"
  slug = "import-io-1-0-extractor-extractorid"
  url_pattern = "/extractor/{extractorId}/"
  method = "GET"
  encoding = "json"
  host = ["https://schedule.import.io"]
  url_keys = ["extractorId"]
  whitelist = ["ownerId", "interval", "intervalData", "startTimestamp", "nextRunAt", "extractorId"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json;charset=UTF-8"]
    hosts = ["https://schedule.import.io"]
    schemes = ["https"]
