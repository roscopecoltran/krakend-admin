[[backends]]
  group = "incidents-1-0"
  url_pattern = "/Incidents.svc/BusIncidents"
  method = "GET"
  host = ["https://api.wmata.com"]
  concurrent_calls = 1
  [backends.extra_config]
    slug = "incidents-1-0-incidents-svc-busincidents"
    hosts = ["http://api.wmata.com", "https://api.wmata.com"]
    topics = ["transport"]
    schemes = ["http", "https"]

[[backends]]
  group = "incidents-1-0"
  url_pattern = "/Incidents.svc/ElevatorIncidents"
  method = "GET"
  host = ["https://api.wmata.com"]
  concurrent_calls = 1
  [backends.extra_config]
    slug = "incidents-1-0-incidents-svc-elevatorincidents"
    hosts = ["http://api.wmata.com", "https://api.wmata.com"]
    topics = ["transport"]
    schemes = ["http", "https"]

[[backends]]
  group = "incidents-1-0"
  url_pattern = "/Incidents.svc/Incidents"
  method = "GET"
  host = ["https://api.wmata.com"]
  concurrent_calls = 1
  [backends.extra_config]
    slug = "incidents-1-0-incidents-svc-incidents"
    hosts = ["http://api.wmata.com", "https://api.wmata.com"]
    topics = ["transport"]
    schemes = ["http", "https"]

[[backends]]
  group = "incidents-1-0"
  url_pattern = "/Incidents.svc/json/BusIncidents"
  method = "GET"
  host = ["https://api.wmata.com"]
  concurrent_calls = 1
  [backends.extra_config]
    slug = "incidents-1-0-incidents-svc-json-busincidents"
    hosts = ["http://api.wmata.com", "https://api.wmata.com"]
    topics = ["transport"]
    schemes = ["http", "https"]

[[backends]]
  group = "incidents-1-0"
  url_pattern = "/Incidents.svc/json/ElevatorIncidents"
  method = "GET"
  host = ["https://api.wmata.com"]
  concurrent_calls = 1
  [backends.extra_config]
    slug = "incidents-1-0-incidents-svc-json-elevatorincidents"
    hosts = ["http://api.wmata.com", "https://api.wmata.com"]
    topics = ["transport"]
    schemes = ["http", "https"]

[[backends]]
  group = "incidents-1-0"
  url_pattern = "/Incidents.svc/json/Incidents"
  method = "GET"
  host = ["https://api.wmata.com"]
  concurrent_calls = 1
  [backends.extra_config]
    slug = "incidents-1-0-incidents-svc-json-incidents"
    hosts = ["http://api.wmata.com", "https://api.wmata.com"]
    topics = ["transport"]
    schemes = ["http", "https"]
