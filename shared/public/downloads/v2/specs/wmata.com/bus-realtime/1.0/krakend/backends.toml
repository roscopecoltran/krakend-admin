[[backends]]
  group = "real-time-bus-predictions-predictions"
  slug = "real-time-bus-predictions-1-0-predictions"
  url_pattern = "/Predictions"
  method = "GET"
  host = ["https://api.wmata.com"]
  query_string_params = ["StopID"]
  concurrent_calls = 0
  [backends.extra_config]
    hosts = ["http://api.wmata.com", "https://api.wmata.com"]
    schemes = ["http", "https"]

[[backends]]
  group = "real-time-bus-predictions-json-jpredictions"
  slug = "real-time-bus-predictions-1-0-json-jpredictions"
  url_pattern = "/json/jPredictions"
  method = "GET"
  host = ["https://api.wmata.com"]
  query_string_params = ["StopID"]
  concurrent_calls = 0
  [backends.extra_config]
    hosts = ["http://api.wmata.com", "https://api.wmata.com"]
    schemes = ["http", "https"]
