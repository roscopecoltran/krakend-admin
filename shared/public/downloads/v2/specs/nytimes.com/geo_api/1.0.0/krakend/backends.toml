[[backends]]
  group = "geographic-query"
  slug = "geographic-1-0-0-query"
  url_pattern = "/query"
  method = "JSON"
  encoding = "json"
  host = ["https://api.nytimes.com"]
  concurrent_calls = 0
  [backends.extra_config]
    produces = ["json"]
    hosts = ["http://api.nytimes.com", "https://api.nytimes.com"]
    schemes = ["http", "https"]
