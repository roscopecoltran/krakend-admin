[[backends]]
  group = "domainsdb-info-search"
  slug = "domainsdb-info-1-0-0-search"
  url_pattern = "/search"
  method = "GET"
  encoding = "json"
  host = ["https://api.domainsdb.info"]
  query_string_params = ["query", "tld"]
  whitelist = ["domains", "time", "total"]
  concurrent_calls = 1
  [backends.mapping]
    domains = "domains"
    time = "time"
    total = "total"
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["https://api.domainsdb.info"]
    schemes = ["https"]
