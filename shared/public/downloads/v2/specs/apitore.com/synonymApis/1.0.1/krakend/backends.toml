[[backends]]
  group = "synonym-apis-api-9-word2vec-neologd-jawiki-distance"
  slug = "synonym-apis-1-0-1-api-9-word2vec-neologd-jawiki-distance"
  url_pattern = "/api/9/word2vec-neologd-jawiki/distance"
  method = "GET"
  encoding = "json"
  host = ["https://api.apitore.com"]
  whitelist = ["distances", "endTime", "input", "log", "num", "processTime", "startTime"]
  concurrent_calls = 1
  [backends.mapping]
    distances = "distances"
    endTime = "endTime"
    input = "input"
    log = "log"
    num = "num"
    processTime = "processTime"
    startTime = "startTime"
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["https://api.apitore.com"]
    schemes = ["https"]
