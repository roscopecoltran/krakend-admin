[[backends]]
  group = "synonym-apis-word2vec-neologd-jawiki-distance"
  slug = "synonym-apis-1-0-1-word2vec-neologd-jawiki-distance"
  url_pattern = "/word2vec-neologd-jawiki/distance"
  method = "GET"
  encoding = "json"
  host = ["https://api.apitore.com"]
  query_string_params = ["access_token", "word", "num"]
  whitelist = ["log", "startTime", "processTime", "endTime", "input", "distances", "num"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["https://api.apitore.com"]
    schemes = ["https"]
