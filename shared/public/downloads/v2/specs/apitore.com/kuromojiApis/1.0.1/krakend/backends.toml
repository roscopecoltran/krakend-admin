[[backends]]
  group = "kuromoji-apis-1-0-1"
  slug = "kuromoji-apis-1-0-1-api-7-kuromoji-ipadic-neologd-tokenize"
  url_pattern = "/api/7/kuromoji-ipadic-neologd/tokenize"
  method = "GET"
  encoding = "json"
  host = ["https://api.apitore.com"]
  whitelist = ["endTime", "log", "processTime", "startTime", "tokens"]
  concurrent_calls = 1
  [backends.mapping]
    endTime = "endTime"
    log = "log"
    processTime = "processTime"
    startTime = "startTime"
    tokens = "tokens"
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["https://api.apitore.com"]
    schemes = ["https"]

[[backends]]
  group = "kuromoji-apis-1-0-1"
  slug = "kuromoji-apis-1-0-1-api-7-kuromoji-ipadic-tokenize"
  url_pattern = "/api/7/kuromoji-ipadic/tokenize"
  method = "GET"
  encoding = "json"
  host = ["https://api.apitore.com"]
  whitelist = ["endTime", "log", "processTime", "startTime", "tokens"]
  concurrent_calls = 1
  [backends.mapping]
    endTime = "endTime"
    log = "log"
    processTime = "processTime"
    startTime = "startTime"
    tokens = "tokens"
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["https://api.apitore.com"]
    schemes = ["https"]
