[[backends]]
  group = "kuromoji-apis-kuromoji-ipadic-neologd-tokenize"
  slug = "kuromoji-apis-1-0-1-kuromoji-ipadic-neologd-tokenize"
  url_pattern = "/kuromoji-ipadic-neologd/tokenize"
  method = "GET"
  encoding = "json"
  host = ["https://api.apitore.com"]
  query_string_params = ["access_token", "text"]
  whitelist = ["endTime", "log", "processTime", "startTime", "tokens"]
  concurrent_calls = 0
  [backends.mapping]
    endTime = "endTime"
    log = "log"
    processTime = "processTime"
    startTime = "startTime"
    tokens = "tokens"
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["https://api.apitore.com"]
    schemes = ["https"]

[[backends]]
  group = "kuromoji-apis-kuromoji-ipadic-tokenize"
  slug = "kuromoji-apis-1-0-1-kuromoji-ipadic-tokenize"
  url_pattern = "/kuromoji-ipadic/tokenize"
  method = "GET"
  encoding = "json"
  host = ["https://api.apitore.com"]
  query_string_params = ["access_token", "text"]
  whitelist = ["endTime", "log", "processTime", "startTime", "tokens"]
  concurrent_calls = 0
  [backends.mapping]
    endTime = "endTime"
    log = "log"
    processTime = "processTime"
    startTime = "startTime"
    tokens = "tokens"
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["https://api.apitore.com"]
    schemes = ["https"]
