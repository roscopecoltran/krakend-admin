[[backends]]
  group = "design-feeds-apis-feeds-design"
  slug = "design-feeds-apis-0-0-1-feeds-design"
  url_pattern = "/feeds/design"
  method = "GET"
  encoding = "json"
  host = ["https://api.apitore.com"]
  query_string_params = ["access_token", "page"]
  whitelist = ["startTime", "log", "endTime", "num", "entries", "processTime", "lastUpdatedAt", "page"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["https://api.apitore.com"]
    schemes = ["https"]
