[[backends]]
  group = "design-feeds-apis"
  slug = "design-feeds-apis-0-0-1"
  url_pattern = "/feeds/design"
  method = "GET"
  encoding = "json"
  host = ["https://api.apitore.com"]
  query_string_params = ["access_token", "page"]
  whitelist = ["page", "startTime", "entries", "num", "endTime", "lastUpdatedAt", "processTime", "log"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["https://api.apitore.com"]
    schemes = ["https"]
