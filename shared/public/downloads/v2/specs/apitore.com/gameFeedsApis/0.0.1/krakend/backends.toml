[[backends]]
  group = "game-feeds-apis-feeds-game"
  slug = "game-feeds-apis-0-0-1-feeds-game"
  url_pattern = "/feeds/game"
  method = "GET"
  encoding = "json"
  host = ["https://api.apitore.com"]
  query_string_params = ["access_token", "page"]
  whitelist = ["page", "startTime", "entries", "lastUpdatedAt", "num", "processTime", "endTime", "log"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["https://api.apitore.com"]
    schemes = ["https"]
