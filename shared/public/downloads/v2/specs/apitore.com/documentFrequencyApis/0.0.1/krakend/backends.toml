[[backends]]
  group = "document-frequency-apis-documentfrequency-get"
  slug = "document-frequency-apis-0-0-1-documentfrequency-get"
  url_pattern = "/documentfrequency/get"
  method = "GET"
  encoding = "json"
  host = ["https://api.apitore.com"]
  query_string_params = ["access_token", "word"]
  whitelist = ["log", "endTime", "startTime", "processTime", "word", "documentFrequency"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["https://api.apitore.com"]
    schemes = ["https"]
