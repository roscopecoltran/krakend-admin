[[backends]]
  group = "sentence-similarity-sentence-similarity-eval"
  slug = "sentence-similarity-0-0-1-sentence-similarity-eval"
  url_pattern = "/sentence-similarity/eval"
  method = "POST"
  encoding = "json"
  host = ["https://api.apitore.com"]
  query_string_params = ["access_token"]
  whitelist = ["log", "endTime", "processTime", "text1", "startTime", "similarity", "text2"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["https://api.apitore.com"]
    schemes = ["https"]
