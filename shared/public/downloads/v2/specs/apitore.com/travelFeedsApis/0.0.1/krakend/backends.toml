[[backends]]
  group = "travel-feeds-apis"
  slug = "travel-feeds-apis-0-0-1"
  url_pattern = "/feeds/travel"
  method = "GET"
  encoding = "json"
  host = ["https://api.apitore.com"]
  query_string_params = ["access_token", "page"]
  whitelist = ["endTime", "entries", "page", "log", "processTime", "lastUpdatedAt", "num", "startTime"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["https://api.apitore.com"]
    schemes = ["https"]
