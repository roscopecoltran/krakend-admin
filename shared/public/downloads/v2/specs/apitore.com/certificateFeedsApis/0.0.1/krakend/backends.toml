[[backends]]
  group = "certificate-feeds-apis"
  slug = "certificate-feeds-apis-0-0-1"
  url_pattern = "/feeds/certificate"
  method = "GET"
  encoding = "json"
  host = ["https://api.apitore.com"]
  query_string_params = ["access_token", "page"]
  whitelist = ["num", "page", "startTime", "log", "endTime", "entries", "lastUpdatedAt", "processTime"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["https://api.apitore.com"]
    schemes = ["https"]
