[[backends]]
  group = "certificate-feeds-apis-feeds-certificate"
  slug = "certificate-feeds-apis-0-0-1-feeds-certificate"
  url_pattern = "/feeds/certificate"
  method = "GET"
  encoding = "json"
  host = ["https://api.apitore.com"]
  query_string_params = ["access_token", "page"]
  whitelist = ["num", "processTime", "endTime", "entries", "page", "lastUpdatedAt", "log", "startTime"]
  [backends.extra_config]
    consumes = ["json"]
    produces = ["json"]
    hosts = ["https://api.apitore.com"]
    schemes = ["https"]
